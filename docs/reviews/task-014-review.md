# Ревью TASK-014 — раунд 1

| Поле | Значение |
| :--- | :--- |
| Артефакт | `docs/TASK.md` |
| Tree fingerprint | sha256 `0b1b65601c3405cd11570b2e18a5dfa6469cf3777267dfc8756ff763563d1b87` |
| Дата | 2026-08-12 |
| Ревьюер | Task Reviewer Agent (роль read-only; файл записан оркестратором) |
| Ветка | `fix/l-13-l-14-completeness-claims` |
| Статус | APPROVED WITH COMMENTS — 0 BLOCKING · 9 MAJOR · 6 MINOR |

## Исполнение скрипт-контракта чеклиста

| Пункт | Состояние |
| :--- | :--- |
| Register scan (§6) | Прочитан результат прогона оркестратора `.agent/tmp/task-014-register-scan.txt`: 0 warn / 5 info, exit 0; все 9 детекторов `live`, мёртвых нет; 203 предложения, max 32 при пределе 35; 206 ячеек, шире 116 при пределе 120 |
| References resolver (§7) | NOT RUN на момент раунда 1 — вывод в бриф не передан. Ручной спот-чек координат выполнен чтением: все резолвятся |

**Закрыто оркестратором после раунда.** `check_positional_refs.py --all docs/TASK.md` →
`OK: 5 of 5 reference(s) in 1 document(s) resolve against the working tree`. Вывод сохранён в
`.agent/tmp/task-014-refs.txt` и передаётся в бриф раунда 2.

## Общая оценка

Три самых нагруженных утверждения спецификации подтверждены по источникам.

1. **Потеря пункта в строке дорожной карты — правда.** `DPAL-OPTIONS-2026-07-31.md:206-218`
   называет четыре строго обязательных предмета, третий — ревизия SSRF-гейта под новый вход.
   `ROADMAP.md:327` его не содержит; `ADR-003` D3 (`:159-176`) держит его наравне с общим
   лимитером. Внесение как R-10/R-11 — восстановление объёма, не расширение.
2. **R-23 обоснован.** Поле `Delivered` в `ARCHITECTURE.md:6` заканчивается на T-012 при сданном
   2026-08-10 T-013; §3 несёт баннер «T-013 is DESIGNED, not built»; `:50-52` обещает
   «a 14th tool» при 20 живых; `functional-architecture.md:151` и `ROADMAP.md:220` объявляют
   Streamable HTTP содержимым M6.
3. **Предупреждения меты точны.** Файлов `ADR-009…ADR-012` не существует; шапка `DPAL-OPTIONS`
   рассинхронизирована с §5; оба отзыва прежней редакции на местах.

Границы с T-015 и T-016 удержаны. Ловушка `costOf: () => ({credits: 0})` у `dune` в объём не
втянута.

Чего документу не хватает: матрица не прослеживает. У RTM нет колонки источника и колонки
критерия, и 11 требований из 23 не имеют ни одного критерия приёмки.

## BLOCKING

Не найдено.

## MAJOR

**MAJOR-1 — RTM не прослеживает ни к источнику, ни к приёмке.** Без критериев остаются R-2, R-4,
R-8, R-9, R-12, R-13, R-14, R-15, R-17, R-19, R-21 и под-требование R-10.2.

**MAJOR-2 — два детектора рисков не детектируют свои риски.** RISK-1 отслеживается через AC-8,
который проходит одинаково при хуке до кеша и после него. RISK-5 отслеживается через AC-14,
который проверяет текст отказа, но не код статуса.

**MAJOR-3 — три предмета, назначенных на T-014 источником, в RTM отсутствуют.** `ADR-003`
§«Проба принципала» (`:389-392`) оставляет на T-014 время жизни и вытеснение сессий и поведение
при конкурентных запросах внутри одной сессии. Третий предмет — представление отказа на
транспорте.

**MAJOR-4 — «20 способностей» неверно: их 26.** В `packages/core/src/capability-manifest.ts`
объявлено 26 строк. AC-13 в нынешней формулировке проходит на 20 из 26. Источник ошибки —
`ARCHITECTURE.md:10`, документ, который спецификация сама объявляет отставшим. 20 — число тулов, и
в AC-7 и AC-11 оно применено верно.

**MAJOR-5 — OQ-13 (TLS) уже отвечен `ADR-003` D1** (`:88-92`): TLS терминируется прокси, движок не
несёт сертификатов. Тот же абзац прочитан как решение для loopback и CORS и как открытый вопрос
для TLS.

**MAJOR-6 — OQ-6 (общий кеш между арендаторами) закрыт в источниках.** `ADR-003` D4 (`:218-224`):
ключ кеша не сегментируется по клиенту, и это не меняется. §«Чего не будет» (`:331`) перечисляет
сегментацию кеша среди вычеркнутого.

**MAJOR-7 — OQ-8 отвечен наполовину.** `ADR-003` Q3 (`:359`, `:384-387`) показывает механизм
пер-принципального инвентаря, D5 (`:240-242`) задаёт фазу 0: один токен, безлимитная квота, все
тулы разрешены. Значит сужение сегодня пустое и замороженный снимок не ломается.

**MAJOR-8 — передача ловушки `dune costOf` в T-015 нигде не зафиксирована.** Ни §2.2
спецификации, ни `ROADMAP.md:328` её не называют. Единственный носитель — незакрытый чекбокс
`dune-tasks.md:277`.

**MAJOR-9 — AC-17 требует править подписанные ADR.** Греп по `docs/` ловит исторические
утверждения в `ADR-003:66-67`, `ADR-001:224-226` и `DPAL-OPTIONS` §2.3.

## MINOR

**MINOR-1.** OQ-1: `ADR-003` упоминает профиль дважды (`:342`, `:414`), не один раз.

**MINOR-2.** AC-15 называет `pnpm gate --task T-014`; канон — `pnpm --filter
@onchain-intel/mcp-server gate --task <id>`.

**MINOR-3.** R-17.3 «расход записан» не называет леджер.

**MINOR-4.** UC-3 не имеет предусловий и альтернативных сценариев. Пропущенная альтернатива не
декоративна: после R-7 локальный процесс и сетевой сервер над одним `DATA_DIR` делят корзину.

**MINOR-5.** R-23.4 не называет координаты. Найдены `interfaces.md:584`,
`system-architecture-call-budget.md:659`, `ARCHITECTURE.md:119`, `ARCHITECTURE.md:10`.

**MINOR-6.** Пять `info` регистра оставлены без письменного решения.

## Проверка по чеклисту

| Раздел | Итог |
| :--- | :--- |
| 1. Соответствие задаче | Пять предметов вехи покрыты; L-15 внесён; незапрошенного объёма нет |
| 2. Полнота | UC-1 и UC-2 полны; UC-3 неполон по структуре; критерии покрывают 12 требований из 23 |
| 3. Совместимость | Термины проекта соблюдены; OQ-10 действительно открыт |
| 4. Непротиворечивость | Внутренних противоречий нет. Коллизия идентификаторов: OQ-9 ссылается на проектный канон R-23, тогда как R-23 документа — документный долг |
| 5. Нефункциональные | Безопасность покрыта широко, но без критериев у R-12 и R-15; пределов сессий и конкурентности нет |
| 6. Регистр | 0 warn, мёртвых детекторов нет |
| 7. Ссылки | NOT RUN в раунде 1; закрыто оркестратором после раунда |

## Рекомендация

Артефакт годен как основание и ещё не является контрактом приёмки. Возврат аналитику одним раундом
вместе с ответами владельца на OQ-1…OQ-4.

1. Закрыть MAJOR-1…MAJOR-4.
2. Снять OQ-13 и OQ-6 в решения, переформулировать OQ-8 — очередь владельца сокращается с 18
   вопросов до 15.
3. Внести MAJOR-8 строкой в §2.2 и сузить AC-17.
4. MINOR — по ходу той же правки.

---

# Ревью TASK-014 — раунд 2

| Поле | Значение |
| :--- | :--- |
| Tree fingerprint | sha256 `6a3b2cb49ed12c2f234ff11f17f33c9bf9f315aec2b447c9a0594775b457653b` |
| Дата | 2026-08-12 |
| Предмет | закрытие 9 MAJOR и 6 MINOR раунда 1 плюс дефекты, внесённые правкой |
| Статус | APPROVED WITH COMMENTS — 0 BLOCKING · 3 MAJOR · 7 MINOR |

## Скрипт-контракт

| Пункт | Состояние |
| :--- | :--- |
| Register scan | 0 warn / 8 info, exit 0; девять детекторов `live` |
| References resolver | `OK: 21 of 21 reference(s) resolve` — пункт §7, оставшийся NOT RUN в раунде 1 |

Резолвер не подтвердил ни одной координаты по существу: у корпуса нет референтов. По §7 это не
дефект. Координаты проверены чтением — 19 совпали.

## Факт «26» подтверждён

`Object.keys(capabilityManifests)` даёт 26 ключей; `toolSpecs` — 20 тулов. Оба числа §0 верны.
Побочно найден долг кода: докстринг `capability-manifest.ts:117` и `:236-240` говорит 20 строк,
тогда как `:95` и таблица того же файла говорят 26. Внесено как R-23.7.

## Закрытие раунда 1

Все девять MAJOR и шесть MINOR закрыты. Два закрыты формально и породили новые замечания: сужение
AC-17 обнажило пять шестых R-23; §8 ответила на пять находок регистра из восьми.

## MAJOR раунда 2

**MAJOR-R2-1 — AC-32 запрещал форму отказа, которой пользуется движок и которую предписывает
протокол.** `toCallToolResult` (`registry.ts:205`) рендерит каждый неуспешный исход как
`{ isError: true, content: [...] }` — успешный ответ с текстом внутри. Гейт по букве прежнего
AC-32 краснел бы на нетронутом дереве, а RISK-5 висел на нём единственным детектором.
**Закрыто:** R-26 разведён на протокольный отказ и отказ исполнения тула; AC-32 проверяет первый,
новый AC-33 запрещает обратное — рендерить провал с `isError: false`.

**MAJOR-R2-2 — сужение AC-17 оставило R-23 с критерием на два под-пункта из шести.**
**Закрыто:** добавлен AC-17b на три числа и на поле `Delivered`.

**MAJOR-R2-3 — решённые умолчания периметра не имели критерия, и два разных механизма назывались
одними словами.** `enableDnsRebindingProtection` — входящая опция транспорта; AC-22 относился к
исходящему SSRF-гейту. **Закрыто:** добавлены AC-34…AC-37 на привязку, CORS, сертификаты и входящую
защиту; R-12.3 и R-10.2 переименованы в «входящая» и «на исходящем вызове».

## MINOR раунда 2

Семь замечаний, все закрыты: молчаливая перенумерация вопросов названа в преамбуле §5; AC-30 и
AC-31 переписаны на наблюдаемый исход; UC-3 получил AC-4 и AC-21; AC-7 и AC-11 больше не зашивают
счёт; R-23.6 помечен историческими записями; источник R-20 указывает и на OQ-13; §8 дополнена и
освобождена от фантомной строки.

## Состояние после правок

| Проверка | Значение |
| :--- | :--- |
| Регистр | 0 warn / 6 info, exit 0 |
| Ссылки | 22 из 22 резолвятся |
| Якорей контракта | 6 из 6 |
| Требований | 26, у каждого критерий |
| Критериев | 38; AC-16 объявлен гейтом границы фазы |

## Рекомендация

Раунда 3 правка не требует: она вносится аналитиком и проверяется на фазе архитектуры.
