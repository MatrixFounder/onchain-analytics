# Ревью плана TASK-015 — раунд 1

| Поле | Значение |
| :--- | :--- |
| Дата | 2026-08-26 |
| Ревьюер | Plan Reviewer (VDD, четыре линзы + состязательная проверка) |
| Артефакт | `docs/PLAN.md` + 32 файла задач `docs/tasks/task-015-01…015-32` |
| Предмет | TASK-015: тарификация фазы 0, суточный гейт Blockscout, переезд таблиц движка (WI-62) |
| Линзы | rtm (прослеживаемость требований), carry (поглощение перенесённых замечаний), ops (операции на dev-VM), exec (исполнимость кодом) |
| Находок после дедупликации | 16 из 21 (пять пар описывали один дефект) |
| Статус | **НА ДОРАБОТКУ — 1 BLOCKING, 6 MAJOR, 9 MINOR** |

## Оговорка об исполнении

| Инструмент | Исход | Основание |
| :--- | :--- | :--- |
| `pnpm lint && format:check && typecheck && test` | PASS, 2555 тестов (core 1637/94, mcp-server 918/82) | измерено до ревью; ревью суть не меняло — правок кода в фазе планирования нет |
| `pnpm --filter @onchain-intel/mcp-server gate --task T-015` | **NOT RUN** | кода T-015 не существует; живой гейт спроектирован задачей 015-29 и исполняется в фазе разработки |
| Операции на dev-VM (`ssh vm`) | **NOT RUN** | `ssh vm` отвечает таймаутом устойчиво; числа строк в таблицах старого контейнера не измерены |
| Регистр Index-Mode | не мерился | 602 warn — унаследованная база, вклад T-015 нулевой; правкой ревью не является |

Все находки ниже получены чтением артефактов плана против исходного кода, миграций, экспортов воркфлоу и принятой спецификации. Ни одна не опирается на прогон, помеченный NOT RUN.

## Общая оценка

План поглотил все тринадцать перенесённых обязательств (двенадцать MAJOR и остаток архитектора по `errorClass`) — «потеряно» ноль. Указание ревью о сведении MAJOR-A, MAJOR-B, MAJOR-C и MAJOR-9 в один рабочий элемент исполнено буквально: единственным владельцем транзакционной границы объявлена задача 015-10, и это записано в стратегии декомпозиции и в риске PR-1. Декомпозиция на 32 задачи в 8 этапах разумна, критический путь назван, риски плана измеримы, деструктивная операция 015-27 сохраняет все три предусловия.

Дефекты сосредоточены в трёх местах. Первое — одна правка кода (`STATE_TABLES`), на которой стоят обе оси леджера, не принадлежит ни одной задаче, а пять файлов адресуют её друг другу по кругу; это единственное блокирующее замечание. Второе — стык живого гейта с осью хранения: задача 015-29 читает леджер из SQLite-файла, а прогон приёмки объявлен на профиле, где состояние живёт в Postgres. Третье — операционный контур dev-VM: перечень писателей переносимых таблиц неполон, а необратимое удаление гасит единственный регулярный отчёт `onchain-verify`, чего погасившая его задача не замечает.

Семь из шестнадцати находок — расхождения текста между двумя задачами плана, правимые одной-двумя строками. Пересмотра принятой спецификации не требует ни одна.

---

## BLOCKING

### B-1. Расширение `STATE_TABLES` именем `client_usage` не принадлежит ни одной задаче

*(линзы rtm и exec — одна находка; линза exec оценила её как MAJOR, обоснование расхождения ниже)*

**Утверждение.** `packages/core/src/pg/state-client.ts:87-99` несёт ровно двенадцать имён, `client_usage` среди них нет. Правку адресуют друг другу пять файлов: `task-015-02:193-196`, `task-015-06:229-231` и `task-015-07:43-44` называют владельцем задачу 015-03; `task-015-04:244-246` — задачи 015-02 и 015-03; `task-015-10:247-248` — задачу 015-07. Задача 015-03 в разделе «Изменения в существующих файлах» перечисляет только `sql/migrations/004_t015_billing.sql`, `packages/core/test/ddl-dialect-parity.test.ts` и `docs/architectures/deployment.md`, а в Примечаниях (`:229-230`) отказывается прямо: «Эта задача файлов в `packages/*/src` не меняет». Прогон по всем 32 файлам задач: ни одна не называет `packages/core/src/pg/state-client.ts` в перечне изменяемых файлов.

Следствие проверено по коду. `EngineTable = (typeof STATE_TABLES)[number]` (`packages/mcp-server/src/engine/pg-engine-store.ts:23`) не примет литерал `'client_usage'` — ошибка компиляции; `qualify()` (`:48`) отвергнет обращение `UnknownEngineTableError`. Объявленное самой 015-07 предусловие («после того, как задача 015-03 внесла имя») не выполняется никогда, то есть ось Postgres леджера неисполнима, а с ней R-1, R-7.3, AC-4, AC-13, AC-15. На оси SQLite из того же перечня строится гейт неквалифицированной ссылки (`packages/core/src/sqlite/state-client.ts:37`), поэтому TC-UNIT-10 задачи 015-06 не может покраснеть на мутации.

Две обязательные правки тестов тоже не закреплены: `packages/core/test/pg-store-parity.test.ts:622` утверждает «STATE_TABLES names exactly the tables migration 002 creates» и читает только `sql/migrations/002_t014_network_profile.sql`, тогда как `client_usage` создаёт файл 004 — а `task-015-07:209` объявляет этот тест остающимся зелёным, чего быть не может.

**Координата.** `docs/tasks/task-015-03-pg-migration-billing.md:229-230` против `task-015-02:193-196`, `task-015-04:244-246`, `task-015-06:229-231`, `task-015-07:43-44`, `task-015-10:247-248`; код — `packages/core/src/pg/state-client.ts:87`, `packages/core/test/pg-store-parity.test.ts:622`, `packages/core/test/sqlite-state-client.test.ts:57`.

**Предложение.** Назначить владельца одним местом. Дописать в «Изменения в существующих файлах» задачи 015-03 (либо 015-02, которая уже правит `packages/core/src/cache/ddl.ts` и уже является предусловием 015-06 и 015-21): (а) тринадцатое имя `client_usage` в `STATE_TABLES`; (б) `packages/core/test/sqlite-state-client.test.ts:57` — 12 → 13; (в) `packages/core/test/pg-store-parity.test.ts:622` — сверка перечня читает объединение `CREATE TABLE` миграций 002 и 004, а не только 002. Снять из Примечаний 015-03 фразу «файлов в `packages/*/src` не меняет» и исправить ошибочную адресацию в `task-015-04:244-246` и `task-015-10:247-248`, чтобы все пять файлов называли одного владельца. Привести `task-015-07:209` в согласие с правкой теста.

**Основание тяжести.** Линза exec понизила ту же находку до MAJOR доводом «отсутствие имени даёт ошибку компиляции у первого же писателя, обнаруживается мгновенно и правится одной строкой». Довод верен для разработчика, читающего код, и неверен для передачи в разработку: перечень изменяемых файлов — машиночитаемый контракт распределения работ, а здесь он образует кольцо из пяти файлов, ни один из которых работу не берёт. Сверх того ложным по построению становится утверждение зелёного теста (`pg-store-parity.test.ts:622`), а задача 015-07 обещает обратное. Блокирует до правки текста задач; после правки — работа на одну строку кода и два теста.

---

## MAJOR

### M-1. Живой гейт 015-29 читает леджер из SQLite-файла, а прогон приёмки объявлен на профиле `network` с осью Postgres

*(линзы rtm и exec — одна находка)*

**Утверждение.** Все три новых кейса задачи 015-29 читают строки через `better-sqlite3` из каталога фазы: «Прочитать `client_usage` из `dataDir`» (`:85`), «Прочитать `usage` из `dataDir`: `calls_made` для blockscout равен N» (`:124`), с основанием «Ось SQLite обеих фаз держит состояние в `DATA_DIR/cache.sqlite3`» (`:74-75`). Примечания той же задачи (`:235-238`) объявляют прогон приёмки с `ONCHAIN_EVAL_HTTP_PROFILE=network`, потому что профиль `network` называет AC-28b (`docs/TASK.md:710`). По коду `packages/mcp-server/src/profile.ts:59-60`: `network` = `{ transport: 'http', storage: 'postgres' }`, `network-sqlite` = `{ transport: 'http', storage: 'sqlite' }`; `packages/core/src/pg/stores.ts:29-52` показывает, что по оси `profile.storage` выбираются **оба** хранилища — и cache, и budget. На профиле `network` и `client_usage`, и `usage.calls_made` уходят в Postgres, а `DATA_DIR/cache.sqlite3` остаётся пустым: TC-E2E-02, TC-E2E-03 и TC-E2E-04 прочитают ноль строк и подтвердят его.

Задача 015-30 усиливает расхождение: её четыре SQL-замера идут по `onchain.usage` (`:69-77`), то есть по другой оси, чем кейс, который эти вызовы делает. `ONCHAIN_STATE_PG_URL` в перечне ключей, задаваемых процессу фазы (`015-29:41-42`, `:151-154`), не назван — там только `ONCHAIN_META_NAMESPACE` и `BLOCKSCOUT_DAILY_CALL_CAP`; `packages/mcp-server/eval/README.md:42-43` прямо говорит, что пара `network` требует поднятого Postgres. Подутверждение «прогон не стартует» ослаблено проверкой: `startHttpServer` подставляет `...process.env` (`eval/run.mjs:509`), поэтому DSN может прийти из окружения оператора — на вердикт это не влияет, читатель леджера всё равно смотрит не туда. R-13 остаётся без исполнимого пути, AC-28b, AC-41 и AC-26 в объявленном виде непроверяемы.

**Координата.** `docs/tasks/task-015-29-live-gate-t015.md:74-75, :85, :124, :151-154, :235-238`; `docs/tasks/task-015-30-gate-verification-run.md:69-77`; `packages/mcp-server/src/profile.ts:59-60`, `packages/core/src/pg/stores.ts:29-52`, `packages/mcp-server/eval/README.md:42-43`.

**Предложение.** Записать ось прогона приёмки одним решением. Вариант (а): фаза идёт под `network` — в раздел «Что фаза HTTP-набора получает в окружении» добавляется `ONCHAIN_STATE_PG_URL` с указанием контейнера задачи 015-25, предусловие «контейнер доступен», а кейсы получают читателя `client_usage` поверх `pg`. Вариант (б): фаза остаётся на умолчании `network-sqlite` (`eval/run.mjs:439`) — тогда расхождение с буквой AC-28b записывается абзацем «отклонение и его основание», как это уже сделано для R-8.14 в 015-23, а замеры 015-30 переводятся на ту же ось. Смешанный вариант (кейс — SQLite, замер — Postgres) исключить.

### M-2. Документная половина MAJOR-B, MAJOR-C и MAJOR-D не имеет задачи: `system-architecture.md` не правит ни одна из 32

**Утверждение.** Прогон по всем 32 файлам задач (блок «Изменения в существующих файлах») даёт множество правимых документов: `data-model.md`, `deployment.md`, `open-questions.md`, `ARCHITECTURE.md`, `BACKLOG.md`, `PROD-RUNBOOK.md`, `TASK.md`, `KNOWN_ISSUES.md`, `PLAN.md`, `docs/methods/task-015-blockscout-daily-gate-verification.md`. Файла `docs/architectures/system-architecture.md` в нём нет — включая задачу 015-31, специально заведённую под исправления корпуса архитектуры, чей перечень ограничен четырьмя другими файлами. После T-015 корпус сохраняет ровно те три текста, которые ревью назвало дефектными: `:4246` «Folded into the existing `withTrace` closure» (координата MAJOR-B), `:3988-3989` «PLUS crediting `priceRaw` back … in the same transaction as the state transition» без оговорки о непустом `RETURNING` (координата MAJOR-C), `:3984` контракт `reserve()` без поля класса отказа и `:4116` `reserved.errorClass` (координаты MAJOR-D и остатка архитектора). Раунд 2 предписывал правку текста прямо: «§3.5.3 переписать так, чтобы входом была пара `(outcome.ok, rowId)`» (review-round2:159-161), «В §3.5.1 и §4.6.1 сформулировать то же ограничение» (review-round2:186-189). Планом взята только поведенческая половина; §4.6.1 тоже не правится ни одной задачей. Стандарт двойной: для MAJOR-A с координатой в `data-model.md` план правку корпуса завёл (015-10, §4.6.5).

**Координата.** `docs/architectures/system-architecture.md:3984, :3988-3989, :4116, :4246` против перечня правимых файлов `docs/tasks/task-015-31-architecture-corpus-corrections.md`.

**Предложение.** Дать §3.5.1–§3.5.3 владельца тем же способом, каким план уже дал его `data-model.md` §4.6.5: 015-04 правит контракт `:3984` и докстринг `refund` `:3988-3989`, 015-09 правит псевдокод `:4116` на `refusalClass`, 015-10 правит §3.5.3 `:4246` на «тело обёртки после вычисления `outcome`». Каждой добавить критерий приёмки вида «текст корпуса называет то же размещение и то же имя поля, что и код». Равноценная альтернатива — расширить перечень файлов 015-31 на `system-architecture.md` теми же тремя пунктами.

### M-3. Удаление 015-27 гасит живой ежедневный отчёт `onchain-verify`: его запрос читает `onchain.diagnostics` в СТАРОМ контейнере

**Утверждение.** Узел `Verify query` воркфлоу `onchain-verify` работает на credential «Supabase DB» (`n8n-workflows/exported/onchain-verify.json:58`) и одним оператором читает и снапшоттерские таблицы, и переезжающую: `(SELECT max(ts) FROM onchain.diagnostics WHERE event = 'alert.delivered') AS alert_last_ts` (`:44`). Это пульс канала оповещений WI-64, а пишется он узлом `Write delivery` того же воркфлоу (`:161`) и узлом `onchain-error-alert` (`onchain-error-alert.json:160`) на credential «Onchain engine state». Задача 015-22 перецеливает «Onchain engine state» на новый контейнер, задача 015-27 удаляет `onchain.diagnostics` в старом (`:49`). После 015-22 запись идёт в новый контейнер, чтение остаётся в старом — возраст пульса перестаёт обновляться, отчёт ежедневно даёт ложное «нет подтверждённой доставки» и постоянное предупреждение (класс L-3); после 015-27 `Verify query` падает на несуществующем отношении, и суточного отчёта не остаётся вовсе. Единственное упоминание `onchain-verify` во всех 32 файлах задач стоит в разделе «что задача не делает» задачи 015-22 (`:88-89`). Посылка унаследована из архитектуры: `deployment.md:757` утверждает, что оба воркфлоу остаются на «Supabase DB» без изменений, что неверно по факту их узлов `Write delivery`. Класс — L-21: единственный регулярный пульс канала оповещений гасится необратимой операцией.

**Координата.** `n8n-workflows/exported/onchain-verify.json:44` и `:58` против `docs/tasks/task-015-27-old-container-decommission.md:49` и `docs/tasks/task-015-22-move-credentials.md:88-89`; посылка — `docs/architectures/deployment.md:757`.

**Предложение.** Завести в этапе 6 задачу-владельца правки `onchain-verify`, исполняемую после 015-22 и до 015-27: разделить `Verify query` на два узла — снапшоттерский на «Supabase DB» и пульсовый (`max(ts) FROM onchain.diagnostics`) на «Onchain engine state», потому что после переезда ни одна credential не видит обе таблицы. Добавить в 015-27 предусловие «отчёт `onchain-verify` снят на новой топологии и содержит строку пульса» и тест-кейс, проверяющий **доставленный** отчёт (L-4: доказательство — пришедшее сообщение, а не верный запрос). В 015-22 заменить формулировку «не правит `onchain-verify`» на именованное исключение: credential «Supabase DB» не перецеливается, но запрос, читающий переезжающую таблицу, правится. Правка `deployment.md:757` — тем же владельцем.

### M-4. Окно «без писателя» задачи 015-23 покрывает только сетевой профиль; в переезжающую `onchain.diagnostics` всё это время пишет n8n

**Утверждение.** Шаг 3 задачи 015-23 останавливает сетевой профиль и меряет постусловие как `SELECT count(*) FROM pg_stat_activity WHERE usename = '<роль состояния>'` (`:56-58`), а единственный названный второй писатель — снапшоттер, объявленный непричастным, потому что пишет `assets`/`metrics`/`snapshots` (`:60-62`). При этом `onchain.diagnostics` и `onchain.retention_runs` входят в одиннадцать переносимых таблиц (`:89-91`), и в `diagnostics` пишут живые узлы `Write delivery` воркфлоу `onchain-error-alert` и `onchain-verify` от той же роли состояния. Соединения транзиентны, поэтому нулевой `count` в момент замера ничего не гарантирует минутой позже; постусловие «в старый контейнер не пишет никто» сформулировано как абсолютное и не держится. Следствие для гейта 015-24, снимающего старую сторону до переноса (`:224`): доставка в Telegram внутри окна даёт по `diagnostics` расхождение чисел, а правило блокировки (`:186-189`) останавливает шаг 10 — гейт выдаёт расхождение собственного производства.

Проверкой ослаблены две части исходного утверждения. Отличить такое расхождение можно: лишняя строка несёт `event='alert.delivered'` и `ts` внутри окна переезда, и спот-чек с границами времени её называет. Семь узлов `onchain-retention` в окне не участвуют — воркфлоу не установлен (`deployment.md:79`), живых узлов два, окно измеряется минутами против суточного расписания. Гейт при расхождении отказывает безопасно.

**Координата.** `docs/tasks/task-015-23-row-transfer.md:56-62` против `n8n-workflows/exported/onchain-error-alert.json:152` и `n8n-workflows/exported/onchain-verify.json:161`.

**Предложение.** Расширить шаг 3 с «остановлен сетевой профиль» до «остановлены все писатели переносимых таблиц»: деактивировать `onchain-verify` и `onchain-error-alert` на время окна (это наши `onchain-*` воркфлоу) либо, если канал оповещений гасить нельзя, записать это именованной уступкой и исключить `diagnostics`/`retention_runs` из первой и второй проверок 015-24 с названной причиной — по образцу уже сделанного для `provider_buckets`. Постусловие шага 3 дополнить замером «нет соединений роли состояния И ни один из двух воркфлоу не активен». В 015-24 добавить отрицательный тест-кейс: строка `alert.delivered`, записанная в окне, обязана быть отличима от дублирования `access_audit`.

### M-5. Миграция 005 нового контейнера не несёт `usage.calls_made`, а адресат миграции 004 назван двумя задачами по-разному

**Утверждение.** Задача 015-21 задаёт форму файла нового контейнера как «паттерн `sql/migrations/002_t014_network_profile.sql` с тремя отличиями» (`:48-54`), и три отличия — один параметр вместо двух, тринадцатая таблица `client_usage`, суперпользователь применения. Колонки `usage.calls_made` среди них нет; в файле 002 её нет тоже — `sql/migrations/002_t014_network_profile.sql:63-70` объявляет `onchain.usage (provider, day, credits_used, updated_at)`, а `calls_made` есть только у `usage_window` (`:72-81`). Нет там и ограничения формата на `credits_balance_raw`, вводимого MINOR-7 раунда 2. Обе вещи объявлены только файлом 004, а его адресат расходится: `task-015-03:43-46` — «новый (`postgres:16`) — да, существующий (`supabase-db`) — нет», тогда как `task-015-21:78` — «`004_t015_billing.sql` → контейнер с двенадцатью», то есть `supabase-db`, куда решение MAJOR-F применять запрещает.

При объявленной форме 005 постусловие `task-015-21:81` («применение 004 после 005 ничего не меняет») и TC-OPS-09 (`:198-200`) ложны: `ALTER TABLE onchain.usage ADD COLUMN IF NOT EXISTS calls_made` колонку добавит. Оператор задачи 015-14 (`INSERT INTO onchain.usage (provider, day, credits_used, calls_made, updated_at)`) на новом контейнере упадёт с `column "calls_made" does not exist`. Verify-гейт 015-24 этого не поймает: спот-чек `:112` покрывает у `usage` только `credits_used`, перечня колонок не сверяет.

**Координата.** `docs/tasks/task-015-21-container-migration-grants.md:48-54, :78, :81, :198-200` против `docs/tasks/task-015-03-pg-migration-billing.md:43-46` и `sql/migrations/002_t014_network_profile.sql:63-70`.

**Предложение.** Назвать 005 полным, а не паттерном: добавить четвёртое и пятое отличия — `usage.calls_made BIGINT NOT NULL DEFAULT 0` с `CHECK (calls_made >= 0)` внутри `CREATE TABLE onchain.usage` и именованное ограничение формата на `access_profiles.credits_balance_raw`. Тогда TC-OPS-09 становится верным утверждением. Одновременно снять расхождение адресата 004: в 015-03 записать «005 создаёт всё сразу; 004 — аддитивный путь для базы, уже несущей двенадцать таблиц, и в T-015 не применяется» — либо свести 005 к последовательности 002+004 и удалить дублирующее объявление. В verify-гейт 015-24 добавить пятую проверку: перечень колонок `usage` на новом контейнере содержит `calls_made`.

### M-6. AC-25 измеряется двумя несовместимыми способами, и вариант 015-15 невыполним

**Утверждение.** Задача 015-13 определяет различимость двух отказов явно: «Различимость определена как непересечение по РАЗЛИЧАЮЩИМ подстрокам, а не по всем. Оба текста несут имя провайдера, и полное непересечение недостижимо» (`:136-139`), с перечнем четырёх подстрок — `daily call ceiling reached`, `throttle: rejected`, `rate limit`, `bucket`. Задача 015-15 требует противоположного: TC-UNIT-07 (`:120`) — «тексты не имеют общей подстроки длиной от восьми знаков», и это же вынесено в критерий приёмки (`:136`). На фактических текстах требование невыполнимо: `packages/core/src/net/rate-limit.ts:274` даёт `throttle: rejected for provider "${providerId}": ${reason}`, а текст суточного отказа (`task-015-14:109`) — `daily call ceiling reached for provider=<id>: <N> of <M> calls already made today (day starts <ts>)`. Общие подстроки `for provider` (12 знаков) и имя провайдера `blockscout` (10 знаков) неизбежны при любой корректной реализации, поскольку 015-13 сам требует, чтобы отказ называл провайдера. Задача не сдаётся по собственному критерию приёмки. Формулировки о восьми знаках `docs/TASK.md:706` (AC-25) не содержит — это дефект плана, а не спор со спецификацией.

**Координата.** `docs/tasks/task-015-15-blockscout-call-gate-wiring.md:120, :136` против `docs/tasks/task-015-13-call-gate-contract-stub.md:136-139`.

**Предложение.** Привести TC-UNIT-07 и критерий AC-25 задачи 015-15 к формулировке владельца контракта: проверять четыре именованные подстроки — `daily call ceiling reached` встречается только в тексте гейта, `throttle: rejected` только в тексте бакета, `rate limit` и `bucket` отсутствуют в тексте гейта. Такой тест красный на подмене класса отказа (мутация, которую 015-15 и требует) и зелёный на верной реализации, тогда как порог «восемь знаков» слеп к предмету и красен всегда. В обеих задачах сослаться на один абзац, чтобы третья формулировка не появилась.

---

## MINOR

### m-1. Задача 015-08 пишет `errorClass` там, где владелец контракта объявил `refusalClass`

*(линзы rtm и exec — одна находка)*

**Утверждение.** Задача 015-04 объявляет неуспешную ветвь как `{ ok: false; reason: string; refusalClass: BillingRefusalClass }` (`:96-98`) и прямо разбирает конфликт: «Псевдокод архитектуры пишет `reserved.errorClass`… и то же написание повторяет задача 015-08. Побеждает написание этой задачи» (`:239-241`). Задача 015-08 сохраняет старое имя в четырёх нормативных местах: таблица решения ветви конфликта (`:90`), абзац класса отказа (`:110-112`), ожидаемый результат TC-UNIT-03 (`:142`), Примечания (`:188-189`). Читатели того же поля — 015-09 (`:107, :113, :189, :223`) и 015-11 (`:35, :41, :159, :183`) — пишут `refusalClass`. Это тот самый остаток архитектора, перенесённый в план наполовину; вред ограничен ошибкой компиляции у разработчика 015-08.

**Координата.** `docs/tasks/task-015-08-replay-window.md:90, :110-112, :142, :188-189` против `docs/tasks/task-015-04-billing-store-stub.md:96-98, :239-242`.

**Предложение.** Заменить `errorClass` на `refusalClass` во всех четырёх местах задачи 015-08 и снять из `015-04:239-242` фразу о конкурирующем написании — после правки конкурента не остаётся.

### m-2. Обратная зависимость этапа 5 от этапа 6 не объявлена: 015-19 и графа «Обнаруживается» риска PR-7

*(линзы rtm и exec — одна находка)*

**Утверждение.** `docs/PLAN.md:505` (PR-7) утверждает: «этап 6 зависит только от 015-02 и 015-03». Зависимость идёт и в обратную сторону: 015-18 стоит на этапе 5 с зависимостью «015-10, 015-21» (`PLAN.md:178`), что задача подтверждает основанием «воркфлоу пишет в базу нового контейнера. До переезда леджера там нет» (`task-015-18:308-309`). При недоступной dev-VM этап 5 не продолжается целиком. Задача 015-19 несёт ту же зависимость необъявленной: её TC-INT-01…TC-INT-04 (`:186-194`) удаляют и читают строки `onchain.client_usage` на живом инстансе, тогда как по решению MAJOR-F леджер существует только на новом контейнере, а credential перецеливается задачей 015-22; объявлена единственная зависимость 015-03 (`PLAN.md:184`, `task-015-19:215-216`). Асимметрия читается как разрешение прогнать интеграционные проверки против старого контейнера, где отношения не существует.

**Координата.** `docs/PLAN.md:505` и `docs/PLAN.md:184`; `docs/tasks/task-015-19-client-usage-retention.md:186-194, :215-216` против `docs/PLAN.md:178` и `docs/tasks/task-015-18-reconcile-workflow.md:308-309`.

**Предложение.** Переписать графу «Обнаруживается» у PR-7 на измеренную: «этап 6 зависит от 015-02 и 015-03; обратно на этап 6 опираются 015-18 и 015-19 — при недоступной VM продолжаются 015-01…015-17 и 015-31». Добавить в `PLAN.md` и в Примечания 015-19 зависимость интеграционной половины от 015-21/015-22 тем же текстом, что уже стоит у 015-18, оставив правку JSON в репозитории зависящей только от 015-03.

### m-3. Заголовок этапа 6 объявляет порядок, которому перечень не следует, а операция 015-22 названа аддитивной

*(линзы rtm и ops — одна находка)*

**Утверждение.** `docs/PLAN.md:193` утверждает: «Порядок задач — порядок шагов UC-6. Пометка деструктивности стоит на каждой задаче», а `PLAN.md:24-25` объявляет порядком именно перечень. Фактический перечень (`:205-245`) идёт 015-20 (шаг 1), 015-21 (шаг 2), 015-22 (шаг 6, часть), 015-23 (шаги 3–4), 015-24 (шаг 5), 015-25 (шаги 6–7) — шаг 6 предшествует шагам 3, 4 и 5, и по одному из прочтений credential перецеливается на пустой контейнер до переноса строк (механизм, порождающий расхождение из M-4). Пометка деструктивности стоит не «на каждой задаче», а на 015-27. Задача 015-22 записывает: «правка credential аддитивна и явного подтверждения владельца не требует» (`:148-149`) — операция ничего не добавляет, а меняет адресата живых записей.

Проверкой смягчено: граф зависимостей непротиворечив (015-22 зависит только от 015-21, а 015-25 — и от 015-22, и от 015-24), необратимая 015-27 сохраняет все три предусловия, из «девяти живых узлов» семь принадлежат неустановленному `onchain-retention` (`deployment.md:79`), правка credential обратима вводом прежнего DSN и в перечень деструктивных операций skill `vm-deploy` §5 не входит.

**Координата.** `docs/PLAN.md:193` против `docs/PLAN.md:205-215` и `docs/tasks/task-015-22-move-credentials.md:148-149`.

**Предложение.** Либо переставить 015-22 после 015-24 и до 015-25, добавив ей зависимость от 015-24, либо смягчить строку 193 до «порядок задач следует шагам UC-6, кроме 015-22: credential заводится раньше, потому что зависит только от 015-21», и привести второе предложение строки к факту («пометка деструктивности стоит на 015-27»). В Примечаниях 015-22 заменить квалификацию «аддитивна» на «правка живой конфигурации с признаком отката»: прежние хост, порт и роль записываются в рантбук до правки, и называется окно, в котором пульс WI-64 пишется в одну базу, а читается из другой.

### m-4. Перечень зависимостей 015-09 не называет 015-05, хотя точка перехвата читает прайс-лист

**Утверждение.** Задача 015-09 определяет цену до вызова обработчика (`:137-144`, ключ `definition.capability ?? definition.name`) и закрепляет это критерием приёмки (`:224`); `reserve()` принимает `priceRaw` (`015-04:60`). Функцию поиска цены экспортирует задача 015-05 (`packages/mcp-server/src/billing/price-list.ts`, `:27-33`), и `015-09:165` её признаёт, но перечни зависимостей — `PLAN.md:118` и Примечания `:228-229` — называют только 015-04, 015-06, 015-07. Порядок этапов случайно спасает исполнение (015-05 стоит в этапе 1 и зависимостей не имеет), объявленная зависимость при этом неполна.

**Координата.** `docs/PLAN.md:118` и `docs/tasks/task-015-09-interception-point-reserve.md:228-229`.

**Предложение.** Добавить 015-05 в перечень зависимостей 015-09 в обоих местах с пометкой «источник `priceRaw` на границе тула».

### m-5. Деструктивная 015-27 удаляет тринадцать таблиц, артефакт отката 015-26 покрывает двенадцать

**Утверждение.** Задача 015-27 объявляет тринадцать операторов `DROP TABLE IF EXISTS`, и `onchain.client_usage` стоит в перечне десятым (`:49`). Задача 015-26 перечисляет в дампе двенадцать таблиц (`:44-49`) и обосновывает это таблицей множеств: «таблицы в артефакте отката | 12 | всё, что удаляет задача 015-27» (`:58-60`) — утверждение неверно по факту чужого перечня. Посылка «в старом контейнере `client_usage` не заводилась» держится адресным предусловием (015-03 запрещает применение миграции 004 к Supabase, риск-регистр называет сценарий поимённо, `PLAN.md:498`), а не замером: dev-VM недоступна. Предусловия 015-27 (`:29-34`) проверки «`client_usage` в старом контейнере отсутствует» не содержат; при соблюдённой посылке `DROP IF EXISTS` — no-op.

**Координата.** `docs/tasks/task-015-27-old-container-decommission.md:49` против `docs/tasks/task-015-26-rollback-artifact.md:58-60`.

**Предложение.** Свести два перечня к одному факту: добавить `--table=onchain.client_usage` в команду 015-26 (отсутствующая таблица дампу не мешает) и привести формулировку `:60` в согласие с фактическим перечнем DROP. Дополнительно — пятая строка предусловий 015-27: замер перечня таблиц старого контейнера перед транзакцией, ненулевое число строк `client_usage` останавливает задачу и возвращает её к 015-26.

### m-6. Новый контейнер поднимается без именованного тома и политики перезапуска

**Утверждение.** Задача 015-20 описывает операцию четырьмя шагами (`:40-50`) и пятью критериями приёмки (`:97-101`), не называя ни именованного тома под `PGDATA`, ни `--restart`, ни резервного копирования; `deployment.md:760-775` их тоже не называет. Оба `pg_dump` этапа снимаются со старого контейнера (`015-23:105`, `015-26:44`). После 015-27 единственная копия `api_tokens.token_hash`, `access_profiles.credits_balance_raw` и леджера живёт в новом контейнере, а артефакт отката хранится 30 дней, содержит состояние до переезда и `client_usage` не содержит.

Проверкой тяжесть снижена: ссылка на канон «All state in a single DATA_DIR» относится к каталогу состояния движка, а не к файловой системе контейнера Postgres; ни одно требование R-8.1…R-8.17 и ни один критерий приёмки тома, политики перезапуска или регулярной копии не требуют — находка расширяет принятый объём. Речь о dev/test-VM, токены дев-контура переиздаются.

**Координата.** `docs/tasks/task-015-20-dedicated-container.md:40-50, :97-101`.

**Предложение.** Дополнить шаг 2 и запись рантбука именованным томом и политикой перезапуска, добавив критерий «после `docker restart` контейнера данные и порт на месте». Обязательство регулярной копии нового контейнера — строкой в «Обязательства, не закрытые планом» (`PLAN.md:521`): до её появления переезд оставляет единственную копию денежных данных.

### m-7. Шаг 8 UC-6 (перемер AC-17/AC-17b на новом контейнере после переезда) не имеет задачи

**Утверждение.** UC-6 шаг 8 предписывает измерить два постусловия на новом контейнере после шагов 6–7 (`docs/TASK.md:585-591`). План относит оба критерия к задаче 015-21 (`PLAN.md:361`), где они снимаются на шаге 2 — тринадцать таблиц ещё пусты, писатель ещё указывает на старый контейнер (`task-015-21:169-173`). Задачи под шаг 8 в перечне этапа 6 нет, отклонение не записано, при том что для аналогичного случая (R-8.14 против шага 5) план записывать отклонение обязывает. Задача 015-28 переводит `SEC-2` в `fixed`, цитируя этот замер (`:84-86`).

Вывод о безопасности при этом не ослаблен: `has_table_privilege` меряет привилегию, а не данные, и между 015-21 и шагом 8 ни одна задача грантов не меняет (015-23 копирует строки суперпользователем, 015-22 заводит credential на уже выданную роль), поэтому результат на пустой и заполненной схеме тождественен.

**Координата.** `docs/PLAN.md:361` против `docs/TASK.md:585-591`.

**Предложение.** Добавить перемер шага 8 в 015-25 или в 015-28: тот же запрос `may_select` по всем ролям нового контейнера после переноса строк и запуска профиля, с перечнем существующих ролей на момент замера; его вывод — то, что цитирует 015-28. В таблице покрытия критериев AC-17/AC-17b указать обе задачи, как уже сделано для критериев с двумя владельцами. Шаг 9 закрепить строкой в 015-25, а не обоснованием в тексте.

### m-8. Объявленная сигнатура `createCallGate` расходится с собственным тест-кейсом и критерием приёмки

**Утверждение.** Задача 015-13 объявляет `createCallGate(deps: { budgetStore: BudgetStore; dailyCallCeilingOverride?: number }): { ensureCallBudget(provider: string, now: () => number): Promise<void> }` (`:93-100`) — провайдер приходит только в `ensureCallBudget`. Та же задача требует отказа от конструктора: `:111-114`, TC-UNIT-04 (`:155-157`) и критерий приёмки (`:189`) — «`createCallGate` отказывается конструироваться для провайдера, объявившего `'none'`». Формулировки расходятся: отказ «для провайдера» на конструкции против пер-вызовного параметра. Реализация при этом возможна без изменения сигнатуры — модуль объявлен как `packages/core/src/adapters/blockscout/call-gate.ts`, привязан к одному провайдеру по построению, регистрация доступна из модульной области, как у процитированного прецедента `NansenBudgetGateDeps` (`packages/core/src/adapters/nansen/budget-gate.ts:232`).

**Координата.** `docs/tasks/task-015-13-call-gate-contract-stub.md:93-100` против `:155-157` и `:189`.

**Предложение.** Записать носителя явно одной строкой: либо конструктор принимает провайдера (`createCallGate({ provider, budgetStore, registrations?, dailyCallCeilingOverride? })` с `ensureCallBudget(now)`), либо в тексте `:111-114` фиксируется, что провайдер берётся из регистрации модуля, а TC-UNIT-04 формулируется против неё. Свойство «отказ при старте, а не на первом запросе», обеспеченное гейтом `assertValidAdapterRegistrations` задачи 015-12, сохраняется в обоих вариантах.

### m-9. Число существующих транспортных кейсов в 015-29 захардкожено и измерено неверно

**Утверждение.** Регрессионный перечень `task-015-29:208` называет «Четыре существующих транспортных кейса зелёные». Измерено исполнением: `TRANSPORT_CASES.length === 5` — `http-auth-rejected`, `http-perimeter-rejected`, `http-shared-limiter-rate`, `http-shared-limiter`, `http-success`; шестое вхождение строки — комментарий `cases/index.mjs:30`. Перечень собирается загрузкой каталога (`cases/index.mjs:128`), поэтому следующий кейс сдвинет и это число. CLAUDE.md §Working discipline запрещает жёсткие счётчики; верная форма в той же задаче уже есть — TC-UNIT-01 (`:199-200`) сформулирован относительно («`TRANSPORT_CASES` вырос на три»).

**Координата.** `docs/tasks/task-015-29-live-gate-t015.md:208`.

**Предложение.** Заменить строку на «Все существующие транспортные кейсы зелёные (перечень берётся из `TRANSPORT_CASES`, не из этого файла)» и применить ту же относительную форму к числам в регрессионных перечнях остальных задач этапа.

---

## Поглощение перенесённых замечаний

Двенадцать MAJOR и остаток архитектора по `errorClass`. Метка «потеряно» — ноль.

| Перенесённое замечание | Задача-владелец | Состояние поглощения | Остаток |
| :--- | :--- | :--- | :--- |
| MAJOR-A (раунд 2): фоновая сверка не возвращает баланс | 015-10 | Поглощено полностью. Переход и возврат исполняются одним оператором; строка с NULL в `access_profile_id` возврата не порождает; правка `data-model.md` §4.6.5 закреплена | — |
| MAJOR-B (раунд 2): `settle`/`refund` под `withTrace`, на профиле local строки не закрываются | 015-10 | Поглощено по поведению: вызов из тела обёртки после вычисления `outcome`, завершение при отсутствующем `ctx.requestTrace`, отказ не отменяет ответ | Текст корпуса `system-architecture.md:4246` не правит никто → **M-2** |
| MAJOR-C (раунд 2): повторный `refund()` кредитует безусловно | 015-10 | Поглощено по поведению: кредитование только при непустом `RETURNING` условного `UPDATE`; критерий «повторный `refund` меняет баланс один раз» | Текст `:3988-3989` и §4.6.1 не правит никто → **M-2** |
| MAJOR-9 (раунд 1): ответ пришёл после того, как сверка вернула резерв | 015-10 | Поглощено полностью. Поздний исход по закрытой строке её не переписывает; сведено с A/B/C в один рабочий элемент, как предписало ревью | — |
| MAJOR-D (раунд 2): контракт `reserve()` без носителя класса отказа | 015-04 (объявление), 015-09 (чтение) | Поглощено по контракту: обязательное поле `refusalClass`, оба класса возвращаются значением; критерии приёмки в обеих задачах | 015-08 сохраняет `errorClass` в четырёх местах → **m-1**; псевдокод `:4116` не правится → **M-2** |
| Остаток архитектора: `reserve()` не объявляет поле, которое читает его же псевдокод | 015-04 | Поглощено: поле объявлено там, где его читает псевдокод; конфликт написаний назван и разрешён | Тот же остаток → **m-1**, **M-2** |
| MAJOR-E (раунд 2): ретенция `client_usage` без job'а, окна и параметра | 015-19 | Поглощено полностью: четвёртая работа `client_usage.purge`, четвёртая строка таблицы окон (пол, максимум, умолчание 1095 дней) | Зависимость от переезда не объявлена → **m-2** |
| MAJOR-F (раунд 2): дом `client_usage` между выкаткой и переездом | 015-03 (решение), 015-21 (повтор предусловием) | Поглощено: леджер живёт только на новом контейнере, миграция к Supabase не применяется, решение вынесено в стратегию декомпозиции и в риск PR-3 | Адресат миграции 004 назван двумя задачами по-разному → **M-5** |
| MAJOR-2 (раунд 1): методика проверки гейта R-12/AC-21/UC-7 до первого живого вызова | 015-01 | Поглощено полностью. Заведён этап 0 «Обязательства до кода»; артефакт `docs/methods/task-015-blockscout-daily-gate-verification.md`, признак различения известных дефектов, отличимость проверочных вызовов, возврат порога; риск PR-5 ссылается на него | — |
| MAJOR-5 (раунд 1): credential переезда | 015-22 | Поглощено: оба воркфлоу указывают на роль состояния нового контейнера, `deployment.md` §10.8 приведён в соответствие | Формулировки «не правит `onchain-verify`» и «аддитивна» неточны → **M-3**, **m-3** |
| MAJOR-6 (раунд 1): порядок удаления против проверки аутентификации AC-44 | 015-27 | Поглощено: удаление стоит после проверки аутентификации 015-25, зависимости 015-24/015-25/015-26 сохранены, риск PR-2 назван | Удаление гасит `onchain-verify` — писатель не учтён → **M-3** |
| MAJOR-7 (раунд 1): объём артефакта отката, `pg_dump --schema` захватывает контур снапшоттера | 015-26 | Поглощено: дамп ограничен перечнем таблиц движка, `assets`/`metrics`/`snapshots` не захватываются | Перечень расходится с 015-27 на `client_usage` → **m-5** |
| MAJOR-10 (раунд 1): R-13, живой гейт не спроектирован | 015-29 (проектирование), 015-30 (прогон) | Поглощено: три кейса, отдельная задача прогона, четыре SQL-замера, бесплатные провайдеры, кредитов Nansen не расходует | Ось чтения леджера противоречит объявленному профилю приёмки → **M-1** |

MINOR обоих раундов поглощены таблицей `PLAN.md:414-433` — четырнадцать строк, каждая с владельцем; выборочная сверка по 015-12, 015-13, 015-23, 015-24 и 015-31 расхождений не дала.

---

## Итог

### Блокирует фазу разработки

1. **B-1** — владелец правки `packages/core/src/pg/state-client.ts` (тринадцатое имя `STATE_TABLES`) не назначен; пять файлов задач адресуют её друг другу по кругу, а два зелёных теста, которые она ломает, не закреплены ни за кем. До правки текста задач задачи 015-06, 015-07 и 015-10 не стартуют, а ось Postgres леджера неисполнима.

### Переносится в фазу разработки

1. **M-1** — ось чтения леджера в 015-29 против профиля приёмки AC-28b; смешанная ось замеров 015-30.
2. **M-2** — документная половина MAJOR-B, MAJOR-C и MAJOR-D: `system-architecture.md` §3.5.1–§3.5.3 и `data-model.md` §4.6.1 не правит ни одна задача.
3. **M-3** — удаление 015-27 гасит `onchain-verify`; правка запроса `alert_last_ts` не имеет владельца.
4. **M-4** — окно «без писателя» 015-23 не покрывает узлы `Write delivery` двух живых воркфлоу.
5. **M-5** — миграция 005 без `usage.calls_made` и ограничения формата; расхождение адресата миграции 004.
6. **M-6** — AC-25 измеряется двумя несовместимыми способами; критерий 015-15 невыполним на фактических текстах.
7. **m-1**…**m-9** — девять расхождений текста и неполных объявлений: `errorClass` в 015-08; зависимость 015-19 и графа PR-7; порядок этапа 6 и квалификация 015-22; зависимость 015-09 от 015-05; перечни 015-26 против 015-27; том и политика перезапуска нового контейнера; шаг 8 UC-6; сигнатура `createCallGate`; захардкоженное число транспортных кейсов.

Статус: **НА ДОРАБОТКУ**. После правки B-1 (текст задач: один владелец, два теста, четыре обратные ссылки) статус переходит в APPROVED WITH COMMENTS — остальные шестнадцать замечаний исполнимы внутри фазы разработки их задачами-владельцами.