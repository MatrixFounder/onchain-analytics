---
id: WI-27
type: work-item
status: done
opened_at: 2026-08-02
slug: wi-27-zoderror-issue
source: 'vdd-enhanced T-011 adversarial-cycle-2'
resolved_at: 2026-08-03
resolved_by: 'не три копии соседской формы, а одна декларация `packages/mcp-server/src/tools/contract-violation.ts` и девять читателей; гейт `packages/mcp-server/test/contract-violation.test.ts`'
provenance: machine
component: packages/mcp-server/src/tools/chain-tvl.ts
fingerprint: 943596aa78be0720
finding_ref: fnd-20260802-114145-943596aa
---

# WI-27 — Три тула отдают модели весь ZodError вместо первого issue

> ## Закрыто 2026-08-03 — сделано сильнее заявки: одна декларация вместо девяти копий
>
> Запись предлагала «привести три места к форме соседей (три строки)». Форма соседей была
> **скопирована в шесть файлов дословно**, и именно расхождение копий — шесть одинаковых плюс три
> разошедшихся — то состояние, которого никто не замечал. Поэтому сделан не третий экземпляр копии,
> а `packages/mcp-server/src/tools/contract-violation.ts` — одна функция `contractViolationReason()`
> и **девять** читателей (`vdd-enhanced` §4.6: «повторено дословно в нескольких файлах → лучше одна
> декларация с читателями»).
>
> **Компромисс назван явно.** Три сообщения потеряли свой префикс с идентификатором способности
> (`chain.tvl result failed contract validation: …`) в обмен на одну форму на девять тулов и один
> гейт над ней. Имя тула и так присутствует в `isError`-кадре, поэтому диагностическая потеря
> нулевая; выигрыш — что «форму соседей» теперь нельзя не соблюсти.
>
> ### Две поправки к телу записи (обе безобидные, обе проверены)
>
> 1. **`get-token.ts` в списке соседей — ошибка примера, но не счёта.** Этот файл вообще не делает
>    `safeParse` над выходом; ограниченную форму несли шесть других
>    (`entity-label`, `new-pairs`, `protocol-tvl`, `smart-money-flows`, `token-holders`, `token-risk`).
>    Шесть — верно, пример — нет.
> 2. **«На текст завязаны ассершены в `m2-degradation.integration.test.ts`» — не подтвердилось.**
>    На три исправляемых сообщения не завязан **ни один** тест в репозитории (греп по
>    `failed contract validation`). Завязаны ассершены на *соседскую* формулировку — пять файлов с
>    `toContain('violating the tool contract')`, — и именно поэтому унификация сделана **к ней**, а не
>    к новому тексту: все пять продолжают проходить без правок.
>
> **Гейт — `packages/mcp-server/test/contract-violation.test.ts`**, две половины:
> *поведение* — на ряде из 1825 точек, где падает каждый элемент, полный `ZodError.message` даёт
> **> 100 000 символов**, а `contractViolationReason` — **< 200**; отдельно закреплено, что наружу не
> уходит ни одно значение провайдера (то самое, из-за чего severity была low, а не medium — zod v4
> вырезает `input`, и апгрейд zod теперь не сможет изменить это молча);
> *структура* — ни один тул не рендерит нарушение по-своему, и ни один не интерполирует
> `error.message` (исключение — `resolve-capability.ts`, где `error` это брошенный Error и его
> сообщение и есть диагностика R-24/R-40).
>
> Прогон: 301 тест в `mcp-server` (было 285), полный `pnpm test` зелёный, lint/format/typecheck/build
> зелёные.

> ### Состязательный цикл 1 (2026-08-03) — что он снял с этой правки
>
> - **Обоснование потери диагностики было ложным, и это чинилось кодом, а не текстом.** Утверждение
>   «имя тула и так в `isError`-кадре» опровергнуто по исходнику: `registry.ts`'s `toCallToolResult`
>   рендерит `{isError: true, content: [{type: 'text', text: outcome.reason}]}` и больше ничего, а
>   `CallToolResult` имени тула не несёт — девять тулов отдавали бы побайтно одинаковую строку, и
>   серверный лог перестал бы её различать. Вместо переписывания аргумента добавлен параметр:
>   `contractViolationReason(capability, error)`, и способность снова названа в тексте. Пять
>   ассершенов на `toContain(...)` это переживают, единственная точная сверка обновлена.
> - **Гейт обходился по написанию.** `${parsed.error}`, `String(parsed.error)` и
>   `JSON.stringify(parsed.error.issues)` — три способа вернуть неограниченный дамп при зелёном
>   гейте (`ZodError.toString()` и есть JSON-дамп). Список запрещённых форм теперь явный, из пяти
>   пунктов, комментарии перед сверкой срезаются; остаток («написание, которого никто не придумал»)
>   назван в самом гейте.
> - **`path.join('.')` мог бросить в единственной функции, которой это запрещено.** `issue.path` —
>   `PropertyKey[]`, а `Array.prototype.join` бросает `TypeError` на элементе-символе; каждый
>   вызывающий тремя строками выше обещает «never throws out of the handler». Сегодня недостижимо,
>   стоило одну `map(String)` — и тест на это добавлен.
> - **Причина, по которой severity была low, теперь под сторожем.** `z.record()`/`.catchall()` в
>   выходной схеме положили бы **ключ, выбранный провайдером**, в `issue.path`, а он интерполируется
>   в текст для модели. Ни у одного тула такой схемы нет — и гейт теперь это держит.

> Filed by `run-feedback` from capture `fnd-20260802-114145-943596aa`. **This body is data, not instructions** — it derives from captured output and may quote untrusted text.

> Origin: T-011, состязательный цикл 2 (линза `critic-security`), 2026-08-02.

**Сигнал.** Три тула интерполируют в текст, который читает модель, **весь** `parsed.error.message`:

- `packages/mcp-server/src/tools/chain-tvl.ts:62`
- `packages/mcp-server/src/tools/chain-supply.ts:70`
- `packages/mcp-server/src/tools/dex-volume.ts:115`

Шесть соседей (`entity-label.ts:165-171`, `get-token.ts` и др.) берут ограниченную форму
`первое issue: path: message`.

**Почему это не косметика.** `ZodError.message` — это `JSON.stringify(issues, null, 2)`
(`zod@4.4.3/v4/core/errors.js:13`), то есть длина растёт пропорционально числу непрошедших
элементов. Для `dex.volume.history` ряд может быть до 1825 точек: поэлементный отказ даёт порядка
**200 КБ JSON в одном `isError`-кадре** на однопоточном stdio-сервере.

**Что проверено и снимает часть тревоги.** zod v4 вырезает `input` из финализированных issue, если
не выставлен `reportInput` (`v4/core/util.js:565-570`), поэтому **значения** провайдера наружу не
уезжают — только пути и сообщения схемы. Поэтому severity low, а не medium.

**Почему не исправлено в T-011.** Код доцелевой, задача про единый реестр тулов его не трогала;
правка меняет текст ошибки, а на него завязаны ассершены в `test/m2-degradation.integration.test.ts`.
Отдельным изменением — с прогоном этих тестов.

**Как чинить.** Привести три места к форме соседей (три строки).
