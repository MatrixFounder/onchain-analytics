# Backlog — onchain-intel

Тонкий бэклог для work-item'ов из retro/run-feedback (см. `.agent/skills/run-feedback`).
Крупные фазы живут в [ROADMAP](onchain-analytics/ROADMAP.md); сюда попадают
инженерные улучшения и полировка, не тянущие на roadmap-строку.

Дефекты идут **не сюда**, а в [KNOWN_ISSUES.md](KNOWN_ISSUES.md) + `docs/issues/`. Разница та же,
что в `run-feedback`: **defect** — воспроизводимое неверное поведение; **work-item** — улучшение или
сигнал без сломанного контракта.

---

## Правила / Conventions

> Этот файл — **индекс**, как и `KNOWN_ISSUES.md`. Тело каждой записи лежит в
> [`docs/backlog/`](backlog/), здесь только строка-указатель. Правило появилось после того, как одна
> запись выросла до **7 849 символов** в одном буллете списка — её нельзя было ни прочитать, ни
> отдиффать, ни закрыть по частям.

<!-- contract:work-items -->

**Файл записи** — `docs/backlog/wi-N-краткий-слаг.md`, YAML-frontmatter, затем H1 и тело:

```yaml
---
id: WI-11 # WI-<n>, сквозная нумерация, следующий = max+1
type: work-item # всегда этот литерал
status: open # open · done · dropped
opened_at: 2026-07-28 # ISO-дата первой записи
slug: wi-11-hot-lru-bounded-by-entry-count # = имя файла без .md
effort: S # S · M · L (опционально)
value: 'одной строкой, что это даёт' # опционально
source: TASK-007 adversarial cycle 3 # опционально: откуда пришло
# resolved_at / resolved_by — только при status: done
---
```

**Строка индекса** (новые сверху; ID по возрастанию даты заведения):

```
- **WI-N** [заголовок](backlog/slug.md) — effort `S`, opened YYYY-MM-DD
```

Закрытая запись **сохраняет файл** и переезжает в раздел «Закрытые» с `resolved_at`/`resolved_by`.
Ничего не удаляется: закрытая запись — это ответ на вопрос, который кто-то ещё задаст.

> ### Где закрываются процессные записи
>
> Часть work-item'ов — уроки о том, **как** работает пайплайн, а не о коде этого проекта. Их
> «место жительства» — скиллы и промпты, а те лежат в **отдельном репозитории**
> `agentic-development` (здесь подключён симлинками `System/` и `.agent/skills/` и внесён в
> `.gitignore`). Такие записи закрываются правкой ТАМ и несут `resolved_by: framework edit
> (agentic-development)` — искать `developer-guidelines` или `vdd-enhanced.md` внутри этого
> репозитория бессмысленно, их здесь нет. Радиус у таких правок шире проекта: фреймворк делят
> четыре репозитория, поэтому правка сопровождается мета-аудитом
> (`skill-self-improvement-verificator` → `docs/reviews/framework-audit-*.md` в том репозитории).
>
> **Отправить правку — не то же самое, что закрыть запись.** Владелец фреймворка может её принять,
> переписать или отклонить, и запись должна отражать то, что **осталось в файлах**, а не то, что было
> предложено. Проверять по факту (`git diff` в том репозитории), а не по памяти о собственном
> предложении. Из пяти правок 2026-07-28 одна была отклонена целиком, у одной откачен тезис, три
> переписаны — реестр, не сверенный с результатом, соврал бы про все пять (ср. **RF-2**: «evidence
> records drifted from the shipped commit»).
>
> Два повода для отказа, оба реальные: **урок, привязанный к языку/инструменту/раскладке репозитория**
> (скиллы грузятся в каждой задаче на любом стеке — в чужом он либо игнорируется, либо исполняется
> буквально и даёт неверный результат), и **изменение поведения, поданное как рекомендация** —
> у него планка обоснования выше, и его надо сверять с уже действующими обязательными правилами.

> ### Про `run_feedback.py file --as work-item`
>
> **С 2026-07-30 (правка фреймворка по WI-23) скрипт ведёт двухуровневый бэклог сам:** пишет файл
> записи `docs/backlog/<slug>.md` с телом **дословно** и **одну** строку-указатель после якоря
> `<!-- feedback:discovered-issues -->`, лочкстепом с откатом, выделяя `WI-<n>` = max+1 по каталогу
> записей. Разносить тело руками больше не нужно. Якорь трогать по-прежнему нельзя — на него
> завязаны `file` и `doctor`. Всегда сначала `--dry-run`: он показывает ID, путь записи и точную
> строку индекса.

---

## Discovered issues / work-items

<!-- feedback:discovered-issues -->
- **WI-52** [Нет признаков риска протокола: инциденты безопасности, активность разработчиков, инвесторы](backlog/wi-52-no-protocol-risk-signals.md) — effort `L`, opened 2026-08-10, **done 2026-08-12**: вариант 1 — способность `protocol.incidents` и тул `onchain_protocol_incidents` из ленты `/hacks` УЖЕ подключённого DeFiLlama. Предусловие записи («пока WI-49/50/51 открыты, слой не на что положить») выполнено — все три закрыты 2026-08-11, — а оценка `L` завышена по той же причине, что у WI-49: «нужен новый класс провайдера» неверно, лента лежит у того же вендора, что и TVL. Живьём: 621 запись 2016-06-17…2026-08-09 (свежайшей 2.5 суток, 219 за год, 99 за 90 суток — лента живая, не архив), $16.9 млрд; склейка точная — `defillamaId`↔`id` 353 из 357, `parentProtocolId`↔`parentProtocol` 97 из 97. **Главный риск был не в соединении, а в пустом ответе:** «инцидентов нет» — это то, что модель перескажет как «протокол безопасен», поэтому четыре смысла пустоты разведены полями и по кейсу на каждый: неизвестный слаг → `resolved: false` (утверждение не делается); нечитаемая лента → БРОСАЕТ, а не отдаёт пустой список; неатрибутируемые записи (264 из 621 — биржи, мосты, люди) → `unattributedRecords`; и только четвёртый случай — измерение. Возраст ленты несёт `feedThroughTs`: WI-52 требует, чтобы неончейновый признак не наследовал свежесть TVL рядом. **Варианты 2 и 3 (активность разработчиков, раунды) НЕ закрыты** и названы необслуживаемыми в описании тула — обязательство варианта 4 landing варианта 1 не снимает
- **WI-51** [Нет способностей про активность сети: ни активных адресов, ни транзакций, ни газа](backlog/wi-51-no-chain-activity-capabilities.md) — effort `L`, opened 2026-08-10, **done 2026-08-11** (частично, и граница названа): способности `gas.price` и `chain.transactions` + тулы `onchain_gas_price` / `onchain_chain_transactions`. **Оценка L оказалась завышена ровно там, где запись велела проверить:** «нужен новый класс провайдера» неверно — газ отдаёт нода через УЖЕ курированные `chain.rpcHosts` (33 хоста на 18 сетях, нулевой новый egress), транзакции отдаёт `/api/v2/stats` через уже разрешённый `mcp.blockscout.com`. Замер также поправил моё же утверждение в диалоге: одобренных RPC-хостов не два, а 33 — два в `providers.config.ts` это SSRF-запись адаптера, а не транспорт. `gas.price` намеренно на ДВУХ адаптерах (`rpc-evm` -> `blockscout`): L-6 был способностью с одним адаптером и умер вместе с решением вендора об аутентификации, и строить следующую так же значило заработать тот же отказ заново. TTL разведены замером, а не вкусом: 30 с у газа (вендор переставляет метку примерно раз в минуту) против 600 с у транзакций (суточный агрегат не сдвинулся за 45 с, в которые `total_blocks` вырос на 10). **АКТИВНЫЕ АДРЕСА НЕ ЗАКРЫТЫ и закрыты не будут этим источником:** вендор публикует только `total_addresses` — кумулятив с генезиса, а не активность. Подставить его в поле `activeAddresses` было бы ровно тем подлогом, который запись запрещает своими словами, и 704 млн неотличимы downstream от настоящего числа. Пробел назван в описании тула, в схеме и в eval-кейсе (проверка на ОТСУТСТВИЕ поля). Живьём: `gas.price` ✅ и `chain.transactions` ✅ на ethereum и base
- **WI-50** [Нет истории TVL ни у сети, ни у протокола: вопросы «как изменилось» нерешаемы](backlog/wi-50-no-historical-tvl-for-chain-or-protocol.md) — effort `M`, opened 2026-08-10, **done 2026-08-11**: `chain.tvl.history` и `protocol.tvl.history` с тем же контрактом ряда, что у `dex_volume`, и тем же КОДОМ — оконная арифметика вынесена в `daily-series.ts`, где уже находили L-1/L-4/L-5, и вторая реализация заработала бы их заново. Сверх записи оба несут готовый `change` по концам окна (`Acceptance` спрашивал «одним вызовом», ряд из 90 точек формально отвечал, но заставлял считать). **Вариант 2 сужен замером:** его предусловие перевернулось (L-7 починили уходом ОТ документа на протокол, а вариант 2 требует туда вернуться), из топ-30 по TVL 10 не влезают в лимит, худший `gate` — 65.96 МиБ; лимит маршрута поднят до 32 МиБ, чем покрыты все DeFi-протоколы топ-30, а `gate` и `mexc` (биржевые трекеры) падают громко — названный остаток. **И он не покупал того, ради чего записан:** сценарий 6 требует истории по многим протоколам сразу и нерешаем при любом лимите — он закрыт в WI-49 через ранжирование из общих документов, так что скрининговый вопрос и точечный разведены по цене намеренно
- **WI-49** [Нет перечисления и ранжирования протоколов: топ-N недостижим, полнота недоказуема](backlog/wi-49-no-protocol-enumeration-or-ranking.md) — effort `M`, opened 2026-08-10, **done 2026-08-11**: способность `protocol.list` + тул `onchain_list_protocols` — популяция протоколов на сети, ранжирование по TVL и по росту за 1/7/30 дней, `minTvlUsd` до ранжирования. Живьём: `base` — 836 совпадений, топ-5 по TVL за 825 мс. **Оценка M была завышена ровно по той причине, которую запись требовала проверить пробой:** `/protocols` уже лежит в общем кэше для `protocol.tvl`, так что популяция не стоит нового запроса. Второй документ всё же нужен — 30-дневного изменения в `/protocols` нет, оно берётся из `/lite/protocols2` (`tvl` и `tvlPrevMonth` из ОДНОЙ строки, чтобы обе половины процента были одним наблюдением); соединение по `defillamaId` ↔ `id` покрыло 7843 из 7843. Фильтр по сети работает по нашему слагу через карту из L-10 — сравнение строкой дало бы пустой список на 43 сетях, а это убедительнее любого неверного числа
- **WI-48** [Таблица тулов в ROADMAP не проверяется гейтом, на который сама ссылается](backlog/wi-48-roadmap-tool-table-not-gated-only-tool-mention-is.md) — effort `S`, opened 2026-08-10, **done 2026-08-10**: закрыта **вариантом 1** (построчная проверка), и разрыв оказался шире записи — **7 устаревших утверждений в 4 документах**, все зелёные. Правое число ≠ правый список, и обе половины отказали независимо: `.AGENTS.md` нёс все 14 имён под словом «thirteen», а C4-диаграмма — «14 tools» над перечислением из 13. Поэтому гейта два: 7 новых паттернов числа (включая словесное и **кириллицу** — JS `\w` не матчит «Четырнадцать») и отдельная проверка перечислений по трём областям. `interfaces.md:505` («13 tools minus 2») НЕ тронут — он верно описывает дерево без 14-го тула, а `docs-counts` с первого дня запрещает себе править историю. Побочно: устарело не только число — довод про необязательность `title?` («4 из 13 несут, 9 не несут») опровергнут замером, `title` несут **все 14**. 7 положительных контролей, три из них воспроизводят настоящий дрейф; переформулировка якоря валит гейт, а не проходит молча. Исходный сигнал: раздел «Текущий состав тулов» объявляет себя разделом о сегодняшнем дне и называет `tool-inventory-docs` своим гейтом, но тот требует лишь, чтобы документ **упомянул** имя тула. 013-8 удовлетворил это одной строкой в **плановом** списке M2, и таблица четверо суток утверждала «Тринадцать тулов» при 14 живых — сьют зелёный. Тот же класс, что `eval-capability-coverage` в самой 013-8: сторож зелен над дырой, ради которой построен. Образец правильного гейта рядом — `readme-tool-table.test.ts` проверяет содержимое строк, а не упоминание. Либо распространить построчную проверку (включая **словесное** число, именно оно разошлось), либо снять из раздела фразу «Проверяется гейтом»
- **WI-47** [`search_path` не доживает до запроса через пулер, и `pg-history` молча выпадает из слияния](backlog/wi-47-pg-history-search-path-lost-through-the-pooler.md) — effort `S`, opened 2026-08-09, **done 2026-08-09**: исправлено квалификацией `FROM onchain.snapshots`; на живом пулере оба ряда читаются по 100 строк, до правки — `relation "snapshots" does not exist`. Добавлен `SchemaResolvingPool`, резолвящий имя как настоящий сервер; мутация со снятием схемы валит именно его. Исходный сигнал: найдено ЖИВЫМ вызовом приёмки 013-10. Оба вызова вернулись одним источником с `missingSources: pg-history: database unavailable`, при том что база здорова, а леджер несёт 341 строку на каждую из восьми метрик. Замер: хостом `:5432` владеет `supabase-pooler` (у `supabase-db` проброса нет), пулер **подменяет** `search_path` на `cvj, public, extensions`, поэтому SELECT адаптера к неквалифицированному `snapshots` не резолвится, а `onchain.snapshots` читается — 3039 строк. Ломается допущение нашего кода (стартовый параметр соединения не обязан пережить пулер), а не конфигурация оператора. Правка — квалифицировать таблицу схемой; тесты идут на мокнутом `PoolCtor` и подмену схемы не видят, поэтому гейт зелен над этой дырой. **Пункт 4 закрыт 2026-08-10:** `PgServerRejectedError` отделяет «сервер ответил ошибкой» от «сервера нет» — наружу `database reachable, request rejected (SQLSTATE 42P01, ERROR)`, остаток сохраняет `unavailable`. Дискриминатор (`severity` + SQLSTATE-образный `code`) выведен пробой пяти живых отказов, а не из доков: один `code` классифицировал бы `EPIPE` как ответ сервера, а `28P01`/`XX000` показали, что класс обязан утверждать «сервер ответил», а не «запрос неверен». Текст сервера наружу не идёт — он цитирует имя пользователя из DSN; проверка формы полей живёт в конструкторе. Побочная находка той же пробы: мёртвый порт приходит `AggregateError`-ом с пустым `message`, и stderr писал строку, обрывающуюся на двоеточии
- **WI-46** [Цитирование basename'ом делает `--targets-changed` инертным, а `.AGENTS.md` — вне области миграции](backlog/wi-46-basename-citations-make-targets-changed-inert.md) — effort `M`, status `open`, opened 2026-08-07: замерено — при изменённом `registry.ts` режим даёт `NOTHING CHECKED`; одна repo-relative ссылка включает его и сразу находит 9 ошибок. `registry.ts` матчит 3 файла, `index.ts` — 15. Живой корпус: 34 ошибки на 348 ссылок; 8 устаревших координат в `packages/core/.AGENTS.md` вне области WI-45
- **WI-44** [`pnpm typecheck` резолвит core в собранный `dist`, поэтому межпакетная поломка типов ему не видна](backlog/wi-44-typecheck-reads-stale-core-dist-so-cross-package-type-breakage-is-invisible.md) — effort `S`, status `open`, opened 2026-08-06: замерено на 013-3 — `adapterId: string→number` в core даёт `typecheck` EXIT 0 и `build` EXIT 2; набор гейтов ловит, но не тот гейт и не тот, чей зелёный читают
- **WI-42** [Докстринг таблицы манифеста называет 012-5 будущим: направление чтения ttlFor() описано обратно](backlog/wi-42-manifest-table-docstring-calls-012-5-future.md) — effort `S`, status `open`, opened 2026-08-06
- **WI-19** [vdd-multi: контракт вывода отчёта не масштабируется](backlog/wi-19-vdd-multi-report-routing-does-not-scale.md) — opened 2026-07-29, effort S: inline-отчёт по умолчанию нечитаем на реальном объёме находок; **изменение поведения общего фреймворка — на рассмотрение владельцу**, не готовая правка
- **WI-18** [Мутационное тестирование живёт вне репозитория](backlog/wi-18-mutation-testing-lives-outside-the-repo.md) — opened 2026-07-29, effort M: единственная механическая проверка качества тестов существует только как одноразовый скрипт вне репозитория — и однажды соврала в свою пользу, посчитав непримененную мутацию пойманной

## Закрытые

- **WI-53** [amountNum читается как величина с применёнными децималами, хотя контракт этого не обещает](backlog/wi-53-amountnum-reads-as-decimals-applied.md) — effort `S`, opened 2026-08-10, **done 2026-08-11**: вариант 1 — единицы названы в самом описании `onchain_wallet_balances` (`amountRaw` точное целое строкой, `amountNum` lossy-удобство В ТОЙ ЖЕ наименьшей единице, децималы НЕ применены, делить на `10^decimals`). Это адресует настоящую причину: докстринг в `types/wallet.ts` до MCP-клиента не доходит, а описание — единственный контракт, который тот читает. Вариант 2 не брался (цена не в переименовании, а в гейтах имён и синхронизации с `rpc-solana`), вариант 3 отклонён записью заранее — канон держит одну точную форму и одну lossy-проекцию. Замороженный снапшот `tools/list` покраснел на изменении описания и перегенерирован, то есть контракт тула под гейтом, а не на словах
- **WI-55** [Побочные эффекты вне рабочего дерева — отдельное решение, а не деталь реализации](backlog/wi-55-effects-outside-the-working-tree-are-a-decision-not-a-detail.md) — effort `S`, opened 2026-08-11, **dropped 2026-08-11**: решением владельца — урок уже зафиксирован в памяти проекта (`m5-background-jobs-need-explicit-approval-and-live-only-in-n8n`) в действующей здесь форме: автоматизация только в песочнице n8n, host OS не трогаем, согласование **до** установки. Правка общего артефакта фреймворка не преследуется намеренно: наблюдение одно и в одном месте, а правка в четырёх репозиториях была бы обобщением по одному случаю; обобщённая формулировка сохранена в теле и готова к подъёму при повторе. Породившее обстоятельство устранено в тот же день — launchd-агент выгружен, plist и лог удалены, `launchctl list` пуст
- **WI-54** [task_id_tool отвергает грамматику ID, которую разрешает протокол архивирования](backlog/wi-54-task-id-tool-refuses-the-grammar-the-archive-protocol-allows.md) — effort `S`, opened 2026-08-11, **dropped 2026-08-11**: решением владельца — **делать ничего не надо**, `013a` вынужденная разовая мера, а не начало соглашения: суффикс понадобился один раз, чтобы служебная задача не отняла номер `014`, зарезервированный в ROADMAP под T-014. Обход отработал штатно — Option B из `skill-archive-task` предусмотрен ровно для этого, конфликт проверен вручную (`task-013a-*`/`plan-013a-*` пусты, подзадачи `task-013-<цифры>-*` не задеты: их грамматика требует цифру после номера, буква с ней не пересекается). Варианты 1–2 в теле измерены и ждут, если внеплановых задач станет много
- **WI-45** [Живой корпус не несёт референтов, и свои workflow'ы его не проверяют](backlog/wi-45-living-corpus-carries-no-referents.md) — effort `M`, opened 2026-08-07, **done 2026-08-07**: корпус мигрирован — `OK: 184 of 184`, exit 0, `0 unresolvable`, покрытие 12 → 145 референтов; аудит [docs/reviews/wi-45-reference-drift-audit.md](reviews/wi-45-reference-drift-audit.md). **Все три предпосылки записи перемерены и оказались неверны.** Съехавших **53 из 170**, а не 26 из 142 — метод сильнее исходного (`git blame` строки-цитаты → версия цели на том коммите → текст ТОГДА против ТЕПЕРЬ), 40 из 53 в `registry.ts`, выросшем до 1560 строк за 013-2/3/4, сдвиги до **+610**. Критерий 3 был **уже исполнен**: `.agent/workflows/` — реальный каталог, но **все 23 файла симлинки** (как и 47 скиллов), и семь шагов приехали с TASK 104 `73a1e4d`; проверено прогоном — вставка 5 строк в `registry.ts` без правки `docs/` даёт `--targets-changed` exit 1, `--fix` чинит 13 ссылок. Ошибка в записи родилась оттого, что `grep -r` **не ходит по симлинкам**, а каталог из них целиком. По дороге найден дефект самого резолвера — **соседняя координата читалась как референт** (`` `a.ts:N`, `b.ts:M` ``), то есть 12 из 12 referent-несущих ссылок падали ложно; правка ушла в `agentic-development@c8fbe1e` (v3.28.1, `REFERENT_IS_A_COORDINATE`, +6 тестов, мутация валит 5 из 6, мета-аудит `docs/reviews/framework-audit-20260807-referent-vs-coordinate.md`), радиус шире репозитория: в собственных docs фреймворка **48** «референтов» были координатами, а его гейт шёл с 51 ошибкой, из них 44 — этот дефект. **Семантика (§5) сознательно не закрыта**: 4 координаты цитируют докстринг, который `013-1` переписал с «обязательство T-013» на «исполнено» — координата исправлена, проза нет, референт выбран говорящим, чтобы расхождение видел читатель
- **WI-43** [Строчные ссылки в docs/ протухают молча: ни один гейт не резолвит path:line](backlog/wi-43-line-anchored-citations-in-docs-decay-silently.md) — effort `M`, status `done`, opened 2026-08-06 · **done 2026-08-07** (перенос в agentic-development, TASK 103 `ed2af74` + TASK 104 `73a1e4d`): механизм приехал сюда симлинком и проверен прогоном; миграция корпуса — WI-45
- **WI-41** [Фаза, правящая машинно-проверяемые документы, не заканчивается прогоном проверок: сьют дважды краснел без строки кода](backlog/wi-41-no-regression-gate-at-the-architecture-phase-boundary.md) — effort `S`, opened 2026-08-06, **done 2026-08-06**: `resolved_by: framework edit (agentic-development, рабочее дерево, не закоммичено)` — взят **вариант 2** (решение владельца), потому что оба условия записи измерены и развернулись в его пользу: полный сьют здесь **4.8 с** (332 теста), а «список проверок, читающих документы» из варианта 1 **механически не выводится** — широкая проба даёт 30 из 35 тест-файлов, и отделить документ от пиннутого JSON-фикстура под `docs/onchain-analytics/raw/` можно только суждением; такой список молча расходился бы с действительностью, то есть тот же класс, что RF-6 и WI-38. Гейт поставлен на **обе** границы, не только Architecture → Planning: 6 тест-файлов пиннят `docs/tasks` / `docs/PLAN` / `docs/TASK`, значит Планирование в том же классе. **Вариант 3 теперь противоречит контракту**, а не просто не рекомендуется: после WI-40 §2.4 запрещает выдавать роли без средства исполнения поручение, требующее исполнения, а `artifact-management` — TIER 0 и грузится ревьюерам. Шаг `Phase-boundary gate` дописан в конец `01-start-feature` (6), `vdd-01-start-feature` (6), `02-plan-implementation` (4), `vdd-02-plan` (4): критерий класса, гейт формулировкой «the full regression suite passes» **без имени команды** (образец `vdd-enhanced` §3 шаг 2), фиксация результата в `skill-session-state`, ветка IF RED и приписка, что обязанность оркестратора, не ревьюера. Покрыты `/vdd`, `/start-feature`, `/plan`, `base-stub-first`, `light`. **Acceptance разряжается прогоном, не правкой** — гейта на исполнение шага нет, остаток того же класса, что у WI-40 и WI-38
- **WI-40** [Read-only ролям поручают команды, которые они не могут выполнить: правило сбора доказательств адресовано только фазе 4](backlog/wi-40-read-only-roles-briefed-with-commands-they-cannot-run.md) — effort `S`, opened 2026-08-05, **done 2026-08-06**: `resolved_by: framework edit (agentic-development, рабочее дерево, не закоммичено)` — взят **вариант 3 + 1**. Вариант 3 в одиночку измерен и оказался не «слабее», а **инертным**: `skill-parallel-orchestration` отсутствует во всех таблицах загрузки `skill-phase-context` (ни TIER 0/1/2), промпт оркестратора ссылается на §2.2, а не §2.4, скилл называют **5 из 23** workflow и все пять — фаза 4+; в четырёх файлах фаз 1–3 по `evidence`/`read-only`/`NOT RUN` — **0 вхождений**. Прецедент WI-38 перемерян: Retro 18/23, `skill-archive-task` 8/23, `skill-update-memory` 1/23 — правило не связывает workflow, который его не называет. Сделано: §2.4 получил блок области («любой спавн роли без средства исполнения — один ревьюер или двенадцать критиков, любая фаза») + строку в уже существовавшей таблице «Readers of this contract»; двухчастная ссылка вписана в **четыре** точки спавна (`01-start-feature` шаги 4/5, `vdd-01-start-feature` 4/5, `02-plan-implementation` 3, `vdd-02-plan` 3), для гейта архитектуры — «выложить дифф в файл, передать путь». Правка доехала сюда: все четыре workflow и каталог скилла в этом репо — симлинки (проверено чтением по локальным путям), предупреждение WI-38 про реальный каталог относится к `03-develop-single-task.md`. **Acceptance разряжается прогоном, не правкой**: критерий записи — «читается один бриф»; гейта на это не заведено, остаток того же класса, что WI-38 оставил за собой
- **WI-38** [Статус-маркеры в архитектуре не сверяет ни один гейт](backlog/wi-38-no-gate-reads-architecture-status-markers.md) — effort `M`, opened 2026-08-05, **dropped 2026-08-05**: `resolved_by: agentic-development, backlog/wi-16-state-claims-carry-no-required-referent.md` — закрыто как ПЕРЕНОС, не как исправление: правка фреймворка не приехала, поэтому `dropped`, а не `done`. Варианты 1 и 2 отклонены (зависят от языка документа, против инварианта L1); взят свод по идентификатору задачи — определение в одном скилле, проводка ссылкой в девять workflow — плюс правило авторинга. Замер, критерий отбора и пересчёт несходящегося счёта — в записи
- **WI-39** [`skill-*` ссылки во фреймворке не резолвятся в каталог скилла](backlog/wi-39-framework-skill-references-do-not-resolve.md) — opened 2026-08-05, **done 2026-08-05**: `resolved_by: framework edit (agentic-development, рабочее дерево, не закоммичено)` — **108 вхождений 19 имён в 25 файлах** приведены к именам каталогов, плюс гейт `check_skill_refs.py` + `tests/test_skill_refs.py` (11 кейсов) в `CURATED_UNITTEST_MODULES`; мутация (возврат одного `skill-task-review-checklist`) даёт `exit=1` с файлом, строкой и верным написанием. **Знаменатель был неверен трижды** (2 → 10 → 19): каждый раз список имён выводился из подмножества ФАЙЛОВ, а не из файловой системы — `System/Agents/*` ссылались ещё на девять скиллов, которых не было в списке. Полным перечисление сделала смена вопроса: не «какие имена выглядят неправильными» (суждение), а «для каких `skill-X` существует каталог `.agent/skills/X/`» (решаемо файловой системой) — ровно это и спрашивает гейт. **Шесть ссылок не тронуты и это проверено**: три предложения в ROADMAP создать скилл, имя CI-джоба, выдуманный скилл в примере аудита и настоящий скилл вне `.agent/skills/` — слепая замена испортила бы все шесть. Правились ССЫЛКИ, не каталоги: `.agent/skills/*` в четырёх репо симлинки, 176 корректно-префиксных ссылок не тронуты. Гейты фреймворка: `run_tests.py` 319 OK, `pytest` 387 + 74 subtests
- **WI-33** [Зависание `task-reviewer` на 600 с осталось без объяснения](backlog/wi-33-task-reviewer-stall-unexplained.md) — opened 2026-08-03, **done 2026-08-05**: `resolved_by: 5 прогонов read-only ролей после правки` — 3 × `task-reviewer` (321 / 298 / 246 с) + `plan-reviewer` (358 с) + `architecture-reviewer` (395 с), все завершились, максимум **395 с против 600 с** обоих случаев прогона 2026-08-02. **Решающее — не длительность, а что каждый доложил:** все пять вернули явный `NOT RUN (<причина>)` с именем пропущенного скрипта, то есть роль дошла до предписания, распознала, что исполнить не может, и пошла дальше — это отличает «развилка `skill-session-state` §3 сработала» от «повода не возникло». Соседние роли взяты в замер потому, что правка `agentic-development@75f624b` одинакова у всех трёх враппер-ревьюеров. **Методическая оговорка:** из промптов пришлось убрать строку «you are read-only, эта инструкция к тебе не относится» — с ней чистый прогон доказывал бы работу инструкции промпта, а не правки фреймворка. НЕ закрыта вторая гипотеза записи (обрыв на стороне рантайма) — пятью успешными прогонами она и не опровергается; смежна с WI-7. Попутно: `plan-reviewer` не резолвит `skill-plan-review-checklist` — каталог называется `plan-review-checklist`, то же расхождение у `task-review-checklist`; дефект фреймворка, здесь только зафиксирован
- **WI-37** [Дедлайн вызова объявлен на 20 способностях, исполняется на 4](backlog/wi-37-call-deadline-declared-but-unenforced-on-ten-adapters.md) — opened 2026-08-05, **done 2026-08-05**: вариант 1 с гейтом — дедлайн протянут в `coingecko`, `dexscreener`, `defillama`, `rpc-evm`, `rpc-solana`, `platform-explorer`, `blockchain-info`, `pg-history`; замер после: **10 адаптеров из 12, 20 способностей из 20**, оба числа пересчитываются прогоном (TC-F5-GATE по исходникам, `deadline-uptake.test.ts` по поведению). **Отступление в пользу записи:** двум заглушкам параметр НЕ дописан — они не тратят времени вовсе (E-DASH = 0), а параметр-декорация купил бы счётчик записью ответа в исходник; исключение **выводится** по импорту транспортного модуля, поэтому обещанный записью «список исключений» не понадобился и гейт остался выразимым. Три адаптера потребовали большего, чем шаблон: `rpc-evm`/`rpc-solana` прерывают перебор эндпоинтов на `DeadlineExceededError` (истёкшее СВОЁ время верно для всех оставшихся), `defillama` ограничивает **ожидание** разделяемого документа, а не саму загрузку (иначе дедлайн одного вызывающего обрывал бы загрузку другого — форма H-1, закрытая WI-12), `pg-history` сужает границу запроса. Огибающие двух `*.history` выросли до 140 000 — рост измеренного числа здесь и есть признак того, что измеряемое обзавелось границей
- **WI-36** [Два адаптера расплющивают типизированную ошибку транспорта в обычный `Error`](backlog/wi-36-adapter-wrappers-flatten-typed-errors.md) — opened 2026-08-04, **done 2026-08-05**: вариант 1 — `PASS_THROUGH_TRANSPORT_ERRORS` объявлен в `net/safe-fetch.ts` рядом с классами, адаптер спрашивает `isPassThroughTransportError()`, но не решает: безопасность — свойство КЛАССА (каждый редактирует свой контекст в конструкторе). Враппер и его причина (M-7, `?apikey=` в URL) целы. Гейт сильнее заявки: помимо проверки редакции на каждом классе он требует, чтобы **каждый экспортируемый Error-класс** был либо в списке, либо в явно пустом `DELIBERATELY_WRAPPED` — падает на молчании, которым класс и терялся четыре задачи. **TC-INT-11 разделился на два по собственному правилу**: он позеленел сам по себе, расплющивающих адаптеров не осталось, поэтому 11a гонит настоящий `blockscout` и утверждает новый факт, а 11b прижимает дизъюнкт фейком. Дизъюнкт остался — двенадцать адаптеров против двух. Латентный второй экземпляр не успел стать действующим: `blockchain-info` взял дедлайн тем же коммитом. Тот же класс найден слоем ниже в `pg/read-client.ts`
- **WI-35** [У пути чтения Postgres ограничено подключение, но не запрос](backlog/wi-35-pg-read-path-has-no-query-timeout.md) — opened 2026-08-04, **done 2026-08-05**: **вариант 3, а не рекомендованный 1** — `statement_timeout` исполняет СЕРВЕР, а отказ, названный в самой записи («сервер, переставший отвечать после установления соединения»), им не покрывается; Acceptance п. 1 говорит то же операционально — мокнутый `PoolCtor` без клиентской границы просто повиснет. Два числа: `statement_timeout = 5_000` (снимает запрос, освобождает соединение) и `DEFAULT_QUERY_TOTAL_TIMEOUT_MS = 20_000` (останавливает ожидание) — второе **сумма**, оно обязано превышать вложенные 10 000 + 5 000, иначе три диагностируемых отказа схлопываются в один. Число выведено, а не выбрано: `EXPLAIN (ANALYZE, BUFFERS)` на dev-VM дал **0.87 мс** и **0.23 мс**. Свой `query_timeout` у `pg` сознательно не выставлен — второй клиентский таймер без дискриминатора. Новый `PgQueryTimeoutError`; **в его же `catch` нашёлся дефект WI-36 одним слоем ниже** — санитизация расплющивала собственный типизированный отказ
- **WI-34** [`pg-history` объявляет `rateLimit`, которого не применяет ни одна строка кода](backlog/wi-34-pg-history-ratelimit-declared-not-enforced.md) — opened 2026-08-04, **done 2026-08-05**: варианты 3 + 1 в порядке рекомендации — гейт на **регистрантов** в `throttle-seam.test.ts` (первая популяция определялась импортом, поэтому адаптер, не импортирующий лимитер, в неё не входил вовсе) плюс применение лимитера вместе с дедлайном WI-37. **Исключение для двух заглушек выводится, а не перечисляется**, вопреки предложенному записью списку: `fetch()`, не делающий ничего кроме `throw NotImplementedInM1Error`, трафика не порождает — и заглушка, обзаведшаяся настоящим `fetch()`, входит в популяцию тем же коммитом, тогда как список промолчал бы. Проверено мутацией (снятие `await throttle(…)` у `coingecko`). Замечено при закрытии: `max: 3` у пула ограничивает **параллелизм**, а не темп, то есть пул никогда не был тем ограничителем, которого якобы хватало; два теста покраснели по таймауту в момент приезда лимитера — дефект WI-26 по расписанию

- **WI-29** [Критики-субагенты встают на 600 с: скилл требует сканер, роль без Bash](backlog/wi-29-600-bash.md) — opened 2026-08-02, **done 2026-08-03**: `resolved_by: framework edit (agentic-development, рабочее дерево, не закоммичено)` — предложенная в записи развилка **уже стояла** в `skill-adversarial-security` §3 с 2026-06-10 (`e9a2360`) и стойла не предотвратила; настоящий дефект — контракт execution-evidence, объявленный в `skill-parallel-orchestration` §7 универсальным, но записанный только внутри `vdd-multi` Step 1.0, тогда как `/vdd` фаза 4 идёт в `vdd-adversarial`, где не было ни одной из двух половин. Починено в **шести** местах, из них самое неприятное — три рукописных `.claude/agents/critic-*`, которым не хватало строки про read-only, **которую их же сгенерированные копии несут** с Task 081. Попутно снят `security-auditor`'s «mock results if the environment restricts execution» — инструкция выдумать гейт безопасности. Половина про `task-reviewer` **не закрыта** и заведена отдельно как [WI-33](backlog/wi-33-task-reviewer-stall-unexplained.md): механизм критика к ней не подходит. Состязательный цикл нашёл в самой правке семь дефектов, главный — **`NOT RUN` удовлетворял любую планку выхода**, то есть громкое стойло менялось на тихий непроверенный проход; и пункт 8 был **вставлен выше пункта 7** при трёх утверждениях «дописан в конец». Всё исправлено, знаменатель вырос с 6 мест до 10. Мета-аудит `docs/reviews/framework-audit-20260803-wi29-execution-evidence.md`, релиз **v3.22.1**; gates того репозитория (список CI ∪ локальные) — 390 pytest + 35 subtests, 275 `run_tests.py`, 45/45 скиллов, prompt-refs/security-lint/workflow-smoke зелёные. Первый проход мерил более узким вызовом (343) — это WI-31 на самом себе, исправлено
- **WI-28** [Колонки TTL и кредитов в двух README не сверяются с кодом](backlog/wi-28-ttl-readme.md) — opened 2026-08-02, **done 2026-08-03**: гейт `readme-tool-table.test.ts` — при закрытии выяснилось, что таблиц с этим фактом **пять**, а не одна (состязательный цикл нашёл пятую — `system-architecture.md` §3.2, ту самую, которую `ttl.ts` называет своим источником, и она **уже** отставала на шесть способностей): секция «Caching» несёт те же TTL ключом по способности и цитирует цены в колонке «Почему». TTL сверяется с `ttlFor()` (экспортирован из core ради гейта), кредиты **выводятся** через `costOf()` адаптера на трёх формах аргументов (поэтому `0/5/100` вычислено, а не переписано), плюс построчная сверка английской и русской версий. Расхождений на сегодня нет — как и сказано в записи; проверено тремя мутациями. Попутно удалён замороженный счётчик `# vitest, 527 tests` (на момент удаления 1190, на момент цикла 2 уже 1196 — ровно поэтому удалён, а не поправлен) — того же класса
- **WI-27** [Три тула отдают модели весь ZodError вместо первого issue](backlog/wi-27-zoderror-issue.md) — opened 2026-08-02, **done 2026-08-03**: сделано сильнее заявки — не третья копия соседской формы, а одна декларация `tools/contract-violation.ts` и **девять** читателей (шесть копий были дословными, три разошлись — это и есть состояние, которого не замечали). Две поправки к телу: `get-token.ts` соседом не был (он не валидирует выход; шесть — верно, пример — нет), и завязанных на три сообщения ассершенов **не существовало** — завязаны на соседскую формулировку, поэтому унифицировано к ней. Гейт: 1825 непрошедших элементов дают >100 000 символов против <200, плюс структурная проверка, что никто не рендерит по-своему (пять запрещённых написаний — состязательный цикл нашёл три обхода). Он же вернул в сообщение **имя способности**: обоснование «тул и так назван в isError-кадре» опровергнуто по `registry.ts` — кадр несёт только `reason`
- **WI-26** [Тесты blockscout делят продовый лимитер, поэтому вердикт прогона зависит от загрузки машины](backlog/wi-26-tests-share-the-production-throttle-singleton.md) — opened 2026-08-01, **done 2026-08-03**: вариант 2 на **виртуальных часах** (арифметика бакета сохранена, сна нет). Запись назвала один файл — замер показал **четыре** ждущих: 46.8 с из 48.5 с суммарного времени `core` было сном лимитера. Причина глубже заявленной: у **шести адаптеров из двенадцати** сеама не было вовсе, тест не мог инжектнуть лимитер даже при желании — сеам добавлен всем шести. Гейт `throttle-seam.test.ts` требует сеам от любого адаптера, который **импортирует** лимитер, и запрещает называть синглтон вне строки резолва — состязательный цикл обошёл первую редакцию дважды (переименованием привязки и обходом одного вызова из трёх), обе дыры закрыты и перепроверены мутацией. `blockscout.transport` 23 517 → 65 мс, полный `pnpm test` 26.8 → 3.3 с, три подряд прогона идентичны, под нагрузкой 8 ядер вердикт тот же. Поправка к механизму записи: vitest изолирует по файлу, поэтому протечка внутрифайловая
- **WI-30** [Валидатор RTM опирается на язык документа, а пропуск шага не событие](backlog/wi-30-rtm-validator-assumes-latin-and-skip-is-silent.md) — opened 2026-08-02, **done 2026-08-02**: `resolved_in_repo: agentic-development@14799d3 + a20670d` (TASK 095) — вариант 1: `documentation-standards` §4.3 (третья ступень адресации — якорь) + §4.4 реестр; привязок в валидаторе оказалось **три**, а не одна (`r['ID']` в `--mode plan` держал режим мёртвым даже после починки колонок), сведены в общий `locate_rtm()`; тот же класс закрыт ещё в трёх скриптах. При проверке разошлось двое: `14799d3` внёс регрессию в `calculate_wsjf.py` (починена в `a20670d`, вскрыв второй дефект — непозитивный Job Size молча считался как 1), и «0 из 1105» оказалось напечатанным, а не посчитанным — настоящий итог **1102**, теперь его печатает `compat_diff.py`, прогнан на закрывающем коммите. Вторая половина («пропуск шага = событие») **не сделана** и отложена явно в спеку 095 §7.2; локально `task-010` валидатор по-прежнему не проходит — архив не ретрофитим
- **WI-32** [Правки оркестратора устойчиво применяются наполовину](backlog/wi-32-orchestrator-fixes-are-half-applied.md) — opened 2026-08-02, **done 2026-08-02**: `resolved_in_repo: agentic-development@14799d3` (TASK 095) — `vdd-enhanced` §4 пункты 6–7 (искать все места ДО правки; докладывать «N из M найденных»; N < M законно, молчание нет), дописаны в конец ради сохранения ordinals, на которые ссылается спека 095; сверх заявки Cycle Brief стал артефактом в `vdd-adversarial` шаг 2a — **отсутствие блока само является находкой**, а не молчаливым пропуском
- **WI-31** [Гейт проверяется более узким вызовом, чем запускает CI](backlog/wi-31-gate-verified-with-narrower-invocation-than-ci.md) — opened 2026-08-02, **done 2026-08-02**: `resolved_in_repo: agentic-development@14799d3` (TASK 095) — ни один из четырёх предложенных вариантов не взят: `core-principles` не тронут (TIER 0, аудитория шире нужного), правило легло в `developer-guidelines` — уточнение §5.1 «сужай то, что ПИШЕТ; воспроизводи вызов CI для того, что ВЫНОСИТ ВЕРДИКТ» + новая §6.3 на все три режима; предел объявлен явно — переносимо механизуется только режим трубы, остальные два остаются дисциплиной
- **WI-25** [Позиционные ссылки проверены до собственных правок — документ процитировал текст, который сам удалил](backlog/wi-25-positional-refs-verified-before-own-edits.md) — opened 2026-07-31, **done 2026-07-31**: `resolved_in_repo: agentic-development@cf13d8e` (v3.21.11) — вариант 2: `documentation-standards` §4.1 (позиционные против именных ссылок, без привязки к стеку) + пункт в `code-review-checklist` §3, явно закреплённый **за ревьюером**, потому что проверка автора проходит по построению; вариант 3 (обязательный гейт) рассмотрен и отклонён — он падал бы на законных цитатах прежнего состояния
- **WI-24** [Архитектурный документ показывает удалённое поле chains как действующий механизм](backlog/wi-24-system-architecture-drifts-from-code.md) — opened 2026-07-31, **done 2026-07-31**: вариант 1 по решению владельца (рекомендован был 2) + половина варианта 3 — копия таблицы маршрутов удалена в пользу счётчика и образцов, `chainSupport`/`CapabilityRoute` приведены к коду, гейт `docs-counts.test.ts` сверяет число маршрутов и **запрещает** строку `chains: ['`; при закрытии выяснилось, что копий **две**, а расхождений **пять**, а не четыре
- **WI-22** [Задачник `dune-tasks.md` опровергнут зондом дважды; A-4 идёт из него же](backlog/wi-22-dune-tasks-source-doc-refuted-twice.md) — opened 2026-07-29, **done 2026-07-30**: предупреждение вписано в САМ задачник — опровергнутое утверждение зачёркнуто на месте, блок «ВЫПОЛНЕНО» для A-3, таблица обеих ошибок прямо над A-4; риск снимется только выполнением A-4 с живым зондом
- **WI-23** [Путь `work-item` в `run-feedback` схлопывает двухуровневый бэклог](backlog/wi-23-run-feedback-work-item-path-flattens-two-level-backlogs.md) — opened 2026-07-29, **done 2026-07-30**: `resolved_by: framework edit (agentic-development TASK 091)` — вариант 1 (лочкстеп: файл записи + строка-указатель, ID `WI-<n>`, откат, засев бэклога) плюс вариант 2 как гарантия на `backlog_layout: "flat"` (отказ вместо схлопывания); конфиг здесь править не понадобилось — новые ключи аддитивны и по умолчанию совпадают с этой раскладкой
- **WI-21** [Индекс ARCHITECTURE расходится со своими же разделами](backlog/wi-21-architecture-index-drifts-from-its-sections.md) — opened 2026-07-29, **done 2026-07-29**: `test/docs-counts.test.ts` (5 проверок, счётчики против кода + каждый тул назван в §5) и `test/ttl-coverage.test.ts`; попутно исправлено 8 мест в 5 файлах и слит дважды написанный абзац в `eval/README.md`
- **WI-20** [Три независимых инвентаря тулов, последний падает только на `smoke:dist`](backlog/wi-20-three-tool-inventory-lists.md) — opened 2026-07-29, **done 2026-07-29**: вариант 3 — каждый сторож называет остальные в тексте падения; при закрытии выяснилось, что списков **четыре**, а не три; **частично пересмотрено 2026-08-01 (T-011)** — каналы сохранены, избыточность сжата в три намеренных сторожа, несущий из них рукописная нижняя граница `toolSpecs.length`
- **WI-2** [Пересмотреть пин typescript ^6.0.3](backlog/wi-2-typescript-pin-revisit-when-tsup-supports-ts7-dts.md) — opened 2026-07-22, **done 2026-07-28**: пин держит `typescript-eslint` (peer `<6.1.0`), а не tsup; диапазон сужен `^6.0.3` → `~6.0.3` по границе peer, триггер пересмотра переписан

- **WI-10** [mcp-server тесты резолвят `@onchain-intel/core` в `dist`, поэтому правки в `src` невидимы до `pnpm build`](backlog/wi-10-mcp-server-tests-resolve-core-to-dist.md) — opened 2026-07-27, **done 2026-07-28**: `vitest.config.ts` алиасит core на `src`, spawn-сьют идёт через `tsx --tsconfig`; оба механизма закрыты guard-тестами, сьют не зависит от сборки
- **WI-9** [«Один коммит на задачу» не оговаривает задачи с пересекающимися файлами](backlog/wi-9-one-commit-per-task-vs-overlapping-files.md) — opened 2026-07-26, **dropped 2026-07-28**: правило отклонено владельцем фреймворка — посылка про красные промежуточные коммиты ложна в Stub-First, а триггер срабатывал бы на каждом плане
- **WI-8** [R-47 carry-over: `rpc-solana` теряет точность на балансе выше 2^53](backlog/wi-8-r47-carryover-rpc-solana-exact-lamports.md) — opened 2026-07-24, **done 2026-07-28**: точные цифры снимаются с самого парсера (`parseGetBalance`), `amountRaw` больше не отказывает выше 2^53
- **WI-7** [Устойчивость субагентов к обрыву: инкрементальная запись и resume](backlog/wi-7-subagent-stall-resilience.md) — opened 2026-07-24, **done 2026-07-28**: `08_developer` Step 2b, `04_architect` перед Step 3, `01_orchestrator` §7 случай 15; владелец уточнил, что приоритизация не переставляет фазы Stub-First
- **WI-6** [Правки, применённые оркестратором, нуждаются в собственном ревью](backlog/wi-6-orchestrator-applied-fixes-need-their-own-review-pass.md) — opened 2026-07-24, **done 2026-07-28**: `vdd-enhanced.md` §4 пункты 4–5 (переревью + вердикт WARNING вместо PASS); тезис «независимые критики лучше самокритики» откачен как неподкреплённый
- **WI-5** [Проверять зависимости тестов, прежде чем удалять «мёртвый код»](backlog/wi-5-check-test-deps-before-removing-dead-code.md) — opened 2026-07-24, **done 2026-07-28**: `code-review-checklist` §2; владелец заменил греп по `test/` на греп по всему репозиторию — раскладка тестов зависит от языка
- **WI-4** [Развести две причины `markUnreconciled`, чтобы cooldown не ломал UC-6](backlog/wi-4-split-markunreconciled-two-causes.md) — opened 2026-07-24, **done 2026-07-28**: формулировка перенесена в докстринг `needsResync()`; сама правка осознанно не делается
- **WI-3** [Триаж непроверенных кандидатов цикла 3 M1 (10 MINOR + 4 bikeshed)](backlog/wi-3-triage-m1-cycle3-unverified-candidates.md) — opened 2026-07-23, **done**: триаж проведён 2026-07-23, все три отложенных в M2 пункта проверены 2026-07-28
- **WI-1** [Расширение гейта форматтера требует проверки блэст-радиуса](backlog/wi-1-formatter-gate-blast-radius-guard.md) — opened 2026-07-22, **done 2026-07-28**: `developer-guidelines` §5.1 Blast Radius — владелец обобщил до любой команды массовой перезаписи и перевёл правку ignore-конфига в эскалацию
- **WI-15** [Цена `MAX_DAYS` в контексте модели](backlog/wi-15-max-days-context-cost-and-includeseries-key.md) — opened 2026-07-28, **by-design 2026-07-28**: владелец оставил 1825; второй ключ кэша исправлен
- **WI-17** [Тест-долг: четыре теста зелены по неверной причине](backlog/wi-17-test-quality-debt-green-for-the-wrong-reason.md) — opened 2026-07-28, **done 2026-07-28**: T-1 переписан, T-2 переименован, T-4 усилен, D-2 закрыт записью фикстуры
- **WI-14** [Переразбор на каждое окно и zod на попаданиях в кэш](backlog/wi-14-per-window-reparse-and-zod-on-cache-hits.md) — opened 2026-07-28, **dropped 2026-07-28**: замерено — 112 мкс и 232 мкс против оценок 0.4–0.8 мс и 1–3 мс; правка не окупается
- **WI-13** [В кэш кладётся 74% документа, которые мы контрактно не читаем](backlog/wi-13-project-document-before-caching.md) — opened 2026-07-28, **done 2026-07-28**: документ проецируется на семь читаемых полей до кэширования
- **WI-12** [Вытеснение незавершённых записей ломает singleflight на широких прогонах](backlog/wi-12-in-flight-eviction-breaks-singleflight.md) — opened 2026-07-28, **done 2026-07-28**: флаг `settled`, вытесняются только завершённые записи
- **WI-11** [Горячий LRU ограничен числом записей, а записи выросли ~в 475 раз](backlog/wi-11-hot-lru-bounded-by-entry-count.md) — opened 2026-07-28, **done 2026-07-28**: второй бюджет в 16 МБ сериализованных байт + `ttlAutopurge`
- **WI-16** [Сверить эхо-поле `chain` у вендора по всем покрытым сетям](backlog/wi-16-defillama-chain-echo-probe.md) — opened 2026-07-28, **done 2026-07-28**: зонд по 274 сетям, 0 расхождений, evidence запиннена в provenance
