{
  "$comment": [
    "Probe inputs for the live eval, as DATA — adding a chain here is a config edit, never a code change.",
    "Only the FREE (keyless) capability surface is covered: chain.tvl, protocol.tvl, token.price/metadata,",
    "pairs.active, wallet.balances.native. Nansen-backed tools (entity.labels, smart-money.flows, token.risk)",
    "are excluded on purpose — they spend credits.",
    "",
    "A field left absent means 'no probe curated' → that case is reported as `no-probe`, never as a failure.",
    "That distinction matters: an eval that scores its own missing test data as a provider defect is lying.",
    "",
    "wallet: the EVM zero address is used deliberately as a universal probe. It is valid on every EVM chain,",
    "needs no per-chain curation, and cannot rot. The check asserts a well-formed balance with the right",
    "symbol/decimals — NOT balance > 0 — because zero is a legitimate answer and asserting otherwise would",
    "make the eval fail on a correct response.",
    "",
    "token: a curated contract per chain. An earlier design discovered the address live from pairs.active",
    "instead; the real payload settled it — active_pairs returns baseTokenSymbol and never a contract address,",
    "so that chain does not exist. Cross-provider agreement is checked a different way (see checks.mjs",
    "crossChecks): registry-vs-provider, chain echo, and registry nativeSymbol vs what the RPC reports.",
    "",
    "Slugs and addresses here are VERIFIED AGAINST THE LIVE PROVIDERS, not recalled: a wrong one shows up",
    "as a provider error and would blame the server for a defect in the test data.",
    "",
    "referenceSources (TASK-009): a source the ENGINE does not use, fetched directly by the eval to check an",
    "answer against something with no shared cause. Adding one is a config edit here — run.mjs reads url/parse",
    "generically. Rules: https only; the host lives in this reviewed file, never computed at run time; and a",
    "reference that fails to answer yields `no-probe`, never a provider failure, because it is OUR test",
    "apparatus and not the vendor under test.",
    "",
    "linkProbes (WI-65): hosts used to measure OUR OWN egress while the run happens, by TCP connect only.",
    "Data here for the same reason referenceSources are: the host is chosen in a reviewed file, never at",
    "run time. They must be OUTSIDE the engine — a host under test cannot be the control for the test —",
    "and outside the eval's own reference sources, so a slow mempool.space cannot read as a slow link.",
    "The verdict decides whether a run's vendor rows may be used to SET an acknowledgement bound",
    "(owner decision 2026-08-24); it never suppresses a failure."
  ],
  "linkProbes": {
    "hosts": ["www.cloudflare.com", "api.github.com", "www.google.com"],
    "intervalMs": 30000,
    "why": "Three unrelated networks (Cloudflare anycast, GitHub, Google), none of them an engine adapter and none of them a referenceSource. A TCP connect asks nothing of the application behind them, spends no quota and returns no data, so a probe every 30s for the length of a run is polite on hosts that owe us nothing. Three rather than one because the signal that matters is a UNIFORM floor across unrelated paths — one host slowing down is that host's problem, three at once is ours.",
    "verifiedAt": "2026-08-24",
    "verifiedNote": "TCP connect 7-8ms median on a healthy link; the stall of the same day measured 215-502ms on five unrelated hosts, which is the two-state separation the 100ms bound sits between"
  },
  "referenceSources": {
    "btcTipHeight": {
      "url": "https://mempool.space/api/blocks/tip/height",
      "parse": "integer",
      "why": "Independent of blockchain-info in vendor, infrastructure and funding. It is the only value in the BTC supply answer a second source can genuinely contradict: re-deriving emission from the halving schedule agrees with blockchain.info by construction, because the vendor computes it the same way. mempool.space is deliberately NOT an engine adapter — a source we answer from cannot be the check on that answer.",
      "verifiedAt": "2026-07-29",
      "verifiedNote": "HTTP 200, content-type text/plain, body '960103'; 8 rapid sequential probes all 200, no RateLimit-* header of any kind"
    }
  },
  "crossChecks": {
    "supplyVsConsensus": {
      "reference": "btcTipHeight",
      "maxDeltaBlocks": 6,
      "why": "The delta is counted in BLOCKS OF SUBSIDY, never in percent: one block is 0.000016% of supply, so an off-by-one and a full day of staleness (144 blocks, 0.0023%) both round to zero on any percentage scale a human would pick. 6 blocks is about an hour. MEASURED skew across three samples on 2026-07-29 was -2..0 blocks — blockchain.info's /stats is an aggregate with its own refresh cadence, and a block can be found between the two calls; the tool's 600s TTL can add one more. The BOUND itself is DEFENSIVE, not measured, and must not be cited as a vendor ceiling — it lives here as data so it can be retuned without a release."
    }
  },
  "chains": {
    "ethereum": {
      "family": "evm",
      "why": "reference EVM: the only profile with every free capability at once",
      "token": "0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2",
      "tokenNote": "WETH",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "aave",
      "pool": "0x88e6A0c2dDD26FEEb64F039a2c41296FcB3f5640",
      "poolNote": "WETH/USDC on Uniswap v3 — fee() answers 500 (5 bps) — the highest-liquidity pool of this row's own `token` ($105,452,090 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "solana": {
      "family": "svm",
      "why": "the only non-EVM family with wallet balances — a different adapter (rpc-solana)",
      "token": "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",
      "tokenNote": "USDC",
      "wallet": "11111111111111111111111111111111",
      "walletNote": "System Program — always exists, balance may be 0",
      "protocolSlug": "raydium",
      "pool": "9d9mb8kooFfaD3SctgZtkxQypkshx6ezhbKio89ixyy2",
      "poolNote": "TRUMP/USDC on Meteora DLMM — no fee(), and not an evm chain either — the highest-liquidity pool of this row's own `token` ($29,648,935 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "base": {
      "family": "evm",
      "token": "0x4200000000000000000000000000000000000006",
      "tokenNote": "WETH (OP-stack predeploy)",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "aave",
      "pool": "0x6c561B446416E1A00E8E93E221854d6eA4171372",
      "poolNote": "WETH/USDC on Uniswap v3 — fee() answers 3000 (30 bps) — the highest-liquidity pool of this row's own `token` ($112,041,158 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "arbitrum": {
      "family": "evm",
      "token": "0x82aF49447D8a07e3bd95BD0d56f35241523fBab1",
      "tokenNote": "WETH",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "aave",
      "pool": "0x641C00A822e8b671738d32a431a4Fb6074E5c79d",
      "poolNote": "WETH/USD₮0 on Uniswap — the highest-liquidity pool of this row's own `token` ($7,106,459 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "polygon": {
      "family": "evm",
      "why": "non-ETH native symbol (POL) — catches symbol/decimals assumptions",
      "token": "0x0d500B1d8E8eF31E21C99d1Db9A6444d3ADf1270",
      "tokenNote": "WMATIC/WPOL",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "aave",
      "pool": "0x9B08288C3Be4F62bbf8d1C20Ac9C5e6f9467d8B7",
      "poolNote": "WPOL/USDT0 on Uniswap — the highest-liquidity pool of this row's own `token` ($1,303,799 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "bsc": {
      "family": "evm",
      "token": "0xbb4CdB9CBd36B01bD1cBaEBF2De08d9173bc095c",
      "tokenNote": "WBNB",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "pancakeswap",
      "pool": "0x16b9a82891338f9bA80E2D6970FddA79D1eb0daE",
      "poolNote": "WBNB/USDT on PancakeSwap v2 — fee() reverts, which is the measured \"no tier\" case — the highest-liquidity pool of this row's own `token` ($90,539,330 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "avalanche": {
      "family": "evm",
      "token": "0xB31f66AA3C1e785363F0875A1B74E27b85FD66c7",
      "tokenNote": "WAVAX",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "aave",
      "pool": "0x8aC5707f8D4BDe1d771d34c7AfD81c3922b73379",
      "poolNote": "WAVAX/USDC on Pharaoh DLMM — fee() reverts — the highest-liquidity pool of this row's own `token` ($8,659,306 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "berachain": {
      "family": "evm",
      "why": "serves pairs.active/pool.info WITHOUT the Nansen capabilities — a profile ethereum/solana hide",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "kodiak",
      "token": "0x6969696969696969696969696969696969696969",
      "tokenNote": "WBERA",
      "pool": "0xCda0ca7C3a609773067261D86E817bf777a2870d",
      "poolNote": "WBERA/WETH on Kodiak v3 — implements fee(), but the chain carries no curated rpcHosts — the highest-liquidity pool of this row's own `token` ($288,587 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "gnosis": {
      "family": "evm",
      "why": "wallet balances but no pairs.active and no paid capabilities — the plain long-tail EVM profile. Re-verified 2026-08-20 (task 014-32a): the vendor answers HTTP 400 for this chain segment, so `pairs.active` stays uncovered and the claim holds under the per-chain witness that replaced the three-entry list.",
      "token": "0xe91D153E0b41518A2Ce8Dd3D7944Fa863463a97d",
      "tokenNote": "WXDAI",
      "wallet": "0x0000000000000000000000000000000000000000",
      "protocolSlug": "aave"
    },
    "tron": {
      "family": "other",
      "why": "non-EVM, non-SVM, NO wallet balances — the case that must report `unsupported` for a wallet. CORRECTED 2026-08-20 (task 014-32a): the old wording said `token+tvl only`, and the per-chain witness refutes it — the vendor routes `tron` and echoes it, so this row now carries `vendors.dexscreener` and DECLARES `pairs.active`. The probe still earns its place: the unsupported case it exists for is the wallet one, which is unchanged.",
      "protocolSlug": "justlend",
      "token": "TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t",
      "tokenNote": "USDT TRC20 — non-EVM address format, which is the point of probing it",
      "pool": "TFGDbUyP8xez44C76fin3bn3Ss6jugoUwJ",
      "poolNote": "WTRX/USDT on SunSwap — the highest-liquidity pool of this row's own `token` ($94,371,657 TVL), measured 2026-08-21 via GET /token-pairs/v1/{chainId}/{token}; re-measure rather than invent if it thins out"
    },
    "bitcoin": {
      "family": "other",
      "why": "minimal non-EVM profile; no token contracts at all",
      "protocolSlug": "babylon-protocol",
      "protocolNote": "verified live 2026-07-27; the slug is NOT 'babylon'"
    },
    "zcash": {
      "family": "other",
      "why": "this project's privacy focus; the narrowest profile in the registry — chain.tvl + protocol.tvl only"
    }
  }
}
