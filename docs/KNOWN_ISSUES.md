# Known Issues & Tech Debt

**Purpose:** Track recurring bugs, architectural limitations, and sensitive areas to avoid
repeating mistakes.

This file is a **thin index**. Each issue lives in its own file under [`docs/issues/`](issues/);
the lines below are one-per-issue pointers grouped by category. Read the linked file for the full
symptom, workaround, and cross-links.

---

## Rules / Conventions

> The index below is **hand-maintained** — there is no generator. When you add, resolve, or
> re-categorize an issue you MUST edit **both** the per-issue file *and* the matching line here.
> These rules keep that hand-editing consistent.

<!-- contract:defects -->

**Per-issue file** — `docs/issues/<slug>.md`, YAML frontmatter then an H1 title and body:

```yaml
---
id: L-1                  # <PREFIX>-<n>, unique (see prefix→category table)
type: known-issue        # always this literal
status: open             # see status vocab below
opened_at: 2026-01-01    # ISO date first recorded (git-truthful)
category: logic          # see prefix→category table
severity: SEV-2          # OPTIONAL — omit when not meaningfully rankable
slug: l-1-short-kebab-title   # filename stem: a slugified, human-readable id+title (normalize symbols, e.g. ≠ → "not")
# component: transcript-fetcher   # OPTIONAL automation keys, appended AFTER slug —
# fingerprint: 614ee37f7fb28554   # see "Automation extension keys" below
# evidence_paths:
#   - path/to/artifact
# auto_fixable: true
# finding_ref: fnd-20260713-081500-614ee37f
# resolved_at: 2026-02-01   # add ONLY when status: fixed
# resolved_by: TASK 042     # add ONLY when status: fixed
---
```

**ID prefix → category.** Define prefixes as the project needs them; **add a row here** whenever you
introduce a new prefix. A common starter set (extend/replace freely):

| Prefix  | Category      | Scope |
|---------|---------------|-------|
| `L-N`   | `logic`       | Logic / correctness defects and edge cases. |
| `P-N`   | `performance` | Performance, algorithmic, or resource issues. |
| `SEC-N` | `security`    | Security / auth / injection / secrets. |
| `Q-N`   | `quality`     | Quality, UX, or robustness nits. |
| `DF-N`  | `dogfood`     | Found while dogfooding the product itself. |
| `RF-N`  | `workflow-docs` | Run-feedback filings: defects in workflow/task docs and pipeline tooling. |

**Status vocabulary:** `open` · `fixed` · `documented` (accepted; guidance written) ·
`by-design` (intended trade-off, not a defect) · `mitigated` · `wontfix`.

A `fixed` issue **keeps its file** and adds `resolved_at` / `resolved_by` + a resolution
blockquote; it is never deleted.

**Severity vocabulary (optional):** `SEV-2` (blocks a workflow / real impact) ·
`SEV-3` (degraded / annoying) · `SEV-4` (minor) · `LOW`. Omit for pure documented constraints.

**Index line format** (severity clause omitted when the file has no `severity`):

```
- **<ID>** [<title>](issues/<slug>.md) — severity `<SEV>`, status `<status>`, opened <YYYY-MM-DD>
```

**Automation extension keys (optional).** Automated tools append machine-oriented keys AFTER
`slug` — `component`, `fingerprint`, `evidence_paths`, `auto_fixable`, `finding_ref` (written by
the `run-feedback` skill's filing step; consumed by the `/heal-issues` harness, which selects
ONLY issues carrying an explicit `auto_fixable: true`). Automation STATE (attempt counters,
journals) lives outside the ledger under `.agent/feedback/`. Per-project ledgers may carry local
read-side extensions (e.g. `status: handled`, `severity: MED`); readers MUST tolerate them, while
new writes stick to the vocabularies above. Automated `resolved_by` values use the token
`heal-issues (verified-gone <ts>)` / `heal-issues run <ts>`.

**Adding a new issue:** ① pick the next `<PREFIX>-<n>`; ② create `docs/issues/<slug>.md` with the
frontmatter above (body preserved verbatim — never drop a clause); ③ add one line under the matching
`## <category>` heading below, in ID order. Add the category heading if it is the first of its kind.

---

## dogfood

- **DF-1** [Nansen `POST /api/v1/smart-money/netflow` silently returned zero rows for a real, well-known token — two request-construction defects, both fixed](issues/df-1-nansen-smart-money-netflow-empty-for-base-pair-tokens.md) — severity `SEV-3`, status `fixed`, opened 2026-07-24

## tooling

- **RF-6** [validate.py читает только первую таблицу RTM под якорем, поэтому многоэпиковый TASK гейтится частично и рапортует успех](issues/rf-6-validate-py-reads-only-the-first-rtm-table-under-the-anchor.md) — severity `SEV-2`, status `fixed`, opened 2026-08-05, **fixed 2026-08-06**: `resolved_by: framework edit (agentic-development, рабочее дерево, не закоммичено)` — **предложенный в записи fix path взят НЕ был**: «разбирать все таблицы блока» изобретает требования из подсекции `### N.N Details by ID` (так устроены `task-096`, `task-085`, `task-101`), а «отказывать при N таблицах» роняет те же три сданные задачи — оба варианта дают гейт, не проходящий на своём корпусе. Взято правило по ряду заголовков: первая таблица задаёт форму, следующие с тем же заголовком — та же RTM, чужие пропускаются и **называются** в выводе. **Область дефекта оказалась шире замера записи**: не одна T-013, а **24 артефакта из 1127 в 5 проектах**, все — вверх (локально `task-012` 3 → 26, `task-011` 7 → 23). Штатный `compat_diff.py` этого не видит — он сравнивает вердикты (`0 of 1127 changed verdict`), поэтому рядом дописан свод по счёту. Гейты: набор скилла 47 → 57, мутация роняет 6 кейсов, `run_tests.py` 319 OK, `pytest` 572 passed. Смежное и НЕ исправленное: `Found N` считает строки, а не идентификаторы (пустой ID в строках-продолжениях) — поведение дособственное, покрытия не затрагивает


## workflow-docs

- **RF-1** [task-001-3 acceptance snippets not runnable (pnpm 11 '--' forwarding; macOS lacks timeout)](issues/rf-1-task-001-3-acceptance-snippets-not-runnable-pnpm-11-forwarding-macos-lacks-timeout.md) — severity `SEV-4`, status `fixed`, opened 2026-07-22
- **RF-2** [M2's own evidence records describe an earlier tree than the one that shipped](issues/rf-2-m2-evidence-records-drifted-from-the-shipped-commit.md) — severity `SEV-3`, status `fixed`, opened 2026-07-25
- **RF-3** [pnpm lint and format:check were left red on main by two merged commits, blocking the next task's regression exit](issues/rf-3-pnpm-lint-and-format-check-were-left-red-on-main-by-two-merged-commits-blocking-the-next-task-s-regression-exit.md) — severity `SEV-3`, status `fixed`, opened 2026-07-27
- **RF-4** [the smoke:dist CI gate still asserts 8 tools, so it has been red on main since TASK-006](issues/rf-4-smoke-dist-ci-gate-still-asserts-8-tools-so-it-has-been-red-on-main-since-task-006.md) — severity `SEV-2`, status `fixed`, opened 2026-07-28
- **RF-5** [the live eval derives chains from the registry but capabilities from a hand-written list, so dex.volume.history ships untested](issues/rf-5-live-eval-capability-axis-is-hand-written-so-dex-volume-history-ships-untested.md) — severity `SEV-3`, status `fixed`, opened 2026-07-28
- **RF-7** [мутационный протокол и read-only роаст идут по одному дереву одновременно, и ревьюер меряет состояние, которого не было](issues/rf-7-mutation-protocol-and-read-only-roast-run-concurrently-so-the-reviewer-measures-a-tree-that-never-shipped.md) — severity `SEV-3`, status `fixed`, opened 2026-08-06, **fixed 2026-08-11**: `resolved_by: agentic-development TASK 105 (v3.29.0), варианты 1+3` — правило порядка и отпечаток лежат в **новой §2.4.1** `skill-parallel-orchestration` и ещё в 29 местах, её читающих. **Отклонение от варианта 3 в исходной формулировке**: у критика в `tools:` нет Bash, поэтому сверять он не может — обязанность разделена, **роль цитирует** выданное значение, **вызывающий сверяет** до и после, и сверка тем самым привязана к тому, что роль действительно видела. Приёмка: `tests/test_frozen_tree_contract.py`, 7 тестов — строка отпечатка во всех 9 брифах, инструкция «процитируй» во всех 20 определениях ролей, перечисление носителей контракта **с диска** (место, добавленное позже, падает, а не остаётся непокрытым); три мутации выполнены, каждая роняет свой assert. **Вариант 2 (изоляция в worktree) не строился** — запись предлагает его только если сериализация станет дорогой, а эта цена не измерена. Прогоны: pytest 435 passed, `generate_wrappers.py --check` без дрейфа, `check_loop_contract.py` 25 loops / 0 errors. Исходный симптом: `code-reviewer` 013-3 поймал прогон `1 failed | 336 passed` и `35 ++++` вместо `38 +` — это была мутация оркестратора, а не флейк; §2.4 обязывает спавнера настреливать `NOT RUN (no Bash)`-команды критика и тем порождает конфликт, не говоря о сериализации
- **RF-8** [The live eval never read the repo-root .env, so a correctly configured secret reported as missing](issues/rf-8-the-live-eval-never-read-the-repo-root-env-so-a-correctly-configured-secret-reported-as-missing.md) — severity `SEV-2`, opened 2026-08-11, **fixed 2026-08-11**: раннер сам грузит `<repoRoot>/.env` перед spawn'ом, ребёнок наследует. НЕ починено переносом cwd в корень: пробовал, `--import tsx` резолвится только из `packages/mcp-server`, и обмен получался «пропавший секрет на сервер, который не стартует». Отсутствующий `.env` остался нефатальным, как в `loadEnv`
- **RF-9** [The eval reports a transport timeout as rate-limited and names an unrelated knob](issues/rf-9-the-eval-reports-a-transport-timeout-as-rate-limited-and-names-an-unrelated-knob.md) — severity `SEV-3`, opened 2026-08-11, **fixed 2026-08-11**: `RETRIABLE` разделён на два предиката. Ретраить стоит оба класса, но после исчерпания попыток они значат разное: 429/503 — вендор отказался обслуживать (ничего не измерено, `rate-limited` честен), а таймаут после трёх попыток — эндпоинт не ответил вовсе, то есть измерение, и плохое. Таймаут остаётся `error`. Третье следствие исправилось вместе с первыми двумя: `rate-limited` не входит в `FAILING` гейта, поэтому открытый отказ вендора нельзя было ПРИЗНАТЬ в файле, созданном для называния отказов вендора — теперь можно. Совет в отчёте больше не называет ручку CoinGecko для чужого таймаута
- **RF-10** [the gate's acknowledgement is a per-row boolean, so an INTERMITTENT vendor failure can never be acknowledged](issues/rf-10-the-gate-acknowledgement-is-a-per-row-boolean-so-an-intermittent-vendor-failure-can-never-be-acknowledged.md) — severity `SEV-2`, status `fixed`, opened 2026-08-20, **fixed 2026-08-22** задачей 014-43: подтверждение называет МНОЖЕСТВО рядов и ГРАНИЦУ на число одновременно упавших, плюс срок пересмотра (не более 90 дней, проверяется). Граница поглощает кочевание и блокирует рост: она равна числу рядов, измеренных упавшими при заведении записи, а граница ≥ размера множества отвергается — её нельзя превысить, то есть она приняла бы полный отказ. Срок заменяет признак «ряд прошёл», поэтому запись переживает восстановление вендора; история прогонов отвергнута как признак — L-12 фиксирует, что зелёный ряд значит «кеш был тёплым», и K зелёных прогонов за вечер не K независимых проб. Счёт против границы печатается на каждом прогоне (M6). Побочно: `issue` теперь сверяется с этим файлом, леджер пишет причину отказа, а не только ключ, и закрыт остаток RF-9 — `rate-limited` считается НЕ ПРОВЕРЕННЫМ. У гейта впервые появились тесты (14 кейсов, реальный скрипт подпроцессом). Проверено на том самом дефекте: `base/chain.transactions` подтверждён, пока проходит
- **RF-12** [инструмент архивации читает буквенно-цифровой под-ID как родительский архив, поэтому отказывает задаче в её собственном номере](issues/rf-12-the-archive-tool-reads-alphanumeric-sub-ids-as-parents-so-it-refuses-a-tasks-own-number.md) — severity `SEV-2`, status `open`, opened 2026-08-25. `SUBTASK_FILENAME_RE` требует чисто числовой под-ID, а планировщик T-014 завёл `task-014-32a`…`32e`; пять под-задач читаются как родительские архивы номера 014, и архивация TASK-014 получила `conflict` с подсказкой **015** — номером следующей вехи. Защитил только `--no-correction`. Однострочной правки нет: поведение закреплено собственным тестом фреймворка (`task-012-3d-viewer.md` обязан читаться как родитель), то есть это столкновение двух соглашений. Три варианта с ценой каждого записаны в файле; решение — за владельцем фреймворка, дефект живёт в `agentic-development`, а не здесь. Обход: архивация проведена по Option B вручную
- **RF-13** [гейт «выведенный из сьюты тест обязан иметь исполнителя» красен в законном промежутке между анализом и планированием](issues/rf-13-the-out-of-suite-gate-is-red-in-the-legitimate-window-between-analysis-and-planning.md) — severity `SEV-3`, status `fixed`, opened 2026-08-25, **исправлено 2026-08-25**: гейт требует существования `docs/tasks/task-<веха>-<n>-*.md`, которых до планирования нет. Его докстринг утверждал обратное («keeps working on T-015 with no edit») — утверждение выведено рассуждением, а не прогоном в том состоянии, о котором говорит, и потому пережило четыре круга ревью T-014; гейт заведён 2026-08-14, уже внутри разработки, и ротации не видел ни разу. Ослабить проверку нельзя: она существует ради вакуумного прохода на пустой области. Введён дискриминатор `planned` по наличию `docs/PLAN.md` — артефакта самого пайплайна, ротируемого в lockstep; в неспланированном режиме гейт утверждает обратное («`files` пуст», «`runnerFile === null`»), то есть не отключается. Мутацией доказано: с подложенным `docs/PLAN.md` гейт снова краснеет
- **RF-14** [два стартовых теста импортируют весь граф сервера внутри дефолтного бюджета 5 с, и один раз это кончилось таймаутом](issues/rf-14-two-startup-tests-import-the-whole-server-graph-inside-a-5s-default-budget.md) — severity `SEV-3`, status `open`, opened 2026-08-26. `TC-INT-01` и `TC-INT-05` — единственные два теста репозитория, вызывающие `await import('../src/index.js')` внутри тела теста, то есть внутри дефолтного `testTimeout` vitest в 5000 мс (своего в конфигурации нет). Наблюдено один раз: оба упали с `Test timed out in 5000ms` при `transform 59.34 s / import 99.86 s` против нормальных `12.02 / 41.38`; в обычном прогоне каждый занимает 394 мс, запас двенадцатикратный. **Причина не установлена.** Первая редакция объясняла падение нагрузкой машины — объяснение опровергнуто: восемь busy-loop'ов, оставшихся работать из-за неудачного `kill $LOADPIDS` (`$(jobs -p)` в подоболочке пуст), держали 100 % CPU три часа при `load average` около 15, и все прогоны сьюта этого периода, включая гейты этапа 1 T-015, были зелёными. Верным остаётся то, что бюджет 5000 мс — дефолт vitest, покрывающий трансформацию всего графа сервера, взятый наощупь; на CI кэш холодный всегда. Запись открыта не ради причины, а ради следствия: гейт, краснеющий по неназванной причине, перезапускают вместо того, чтобы читать — цена RF-3 и RF-11
- **RF-15** [у проверщика позиционных ссылок есть нужный режим и нет базовой линии, поэтому новая поломка неотличима от четырёхсот старых](issues/rf-15-positional-reference-checker-has-the-right-mode-and-no-baseline.md) — severity `SEV-3`, status `open`, opened 2026-08-28. `check_positional_refs.py --targets-changed` выбирает документы, цитирующие изменённые файлы, — тот самый класс, который создаёт вставка строк в исходник. Режим не подключён ни к одному гейту и как гейт не работает: измерено 2026-08-28 — 406 ошибок на всём `docs` в рабочем дереве против 423 на точке ветвления `09a0e7a`, 354 ошибки на 84 документах в области `--targets-changed`. Долг унаследован, а не создан вехой: ссылок за T-015 прибавилось около тысячи, а `REFERENT_MOVED` упал со 126 до 86 поштучными ремонтами. Наблюдённый случай: вставка докстринга на 14 строк в задаче 015-13 сдвинула объявление `checkAndReserve(` с 63 на 77, и цитата `docs/architectures/interfaces.md:185` стала неоднозначной — документ в область задачи не входил и никем не открывался, нашёл его отчёт исполнителя, а не гейт
- **RF-16** [суточный гейт вызовов пишет `usage.calls_made`, и этот момент никто не докладывает через `onVendorSpend`](issues/rf-16-daily-call-gates-usage-calls-made-write-has-no-vendor-spend-reporter.md) — severity `SEV-3`, status `open`, opened 2026-08-28. Задача 015-14 подключила `blockscout/call-gate.ts` к `checkAndReserve`; каждый допущенный вызов двигает `usage.calls_made`, а канала доклада у этого измерения нет: `VendorChargeRecord.calls` документирован как вклад в `usage_window.calls_made`, `onVendorSpend` спускается per-REQUEST через `fetch()`, а сигнатура `ensureCallBudget(now)` его не несёт. Кредитное измерение не затронуто — `cost` литеральный `0`. Гейт `TC-GATE-01` получил узкое исключение, и при приёмке этапа 4 у него нашлись две дыры, обе измерены: исключение стояло на имени файла, и второй вызов на 500 кредитов оставлял гейт зелёным на 1699 из 1699; сам гейт искал докладчика по сырому тексту, поэтому удовлетворялся упоминанием в комментарии — дефект задачи 014-30, на дереве им пользовался ровно один файл (`raw=1, code=0`). Обе закрыты: исключение переведено на предикат, проверка докладчика — на строки кода
- **RF-11** [the format gate lived only in CI, so it turned main red six times in thirteen days](issues/rf-11-the-format-gate-lived-only-in-ci-so-it-turned-main-red-six-times-in-thirteen-days.md) — severity `SEV-3`, status `fixed`, opened 2026-08-24, **исправлено 2026-08-24**: `prettier --check` переехал на СТАВЯЩИЕСЯ блобы — в тот же pre-commit хук, что и провенанс RF-2, восемь кейсов на временном репозитории. Класс назвала ещё RF-3 месяцем раньше и закрылась без него («это та часть, которую правка конфига не предотвращает») — тринадцать дней и семь прогонов ушли на то, что было записано и не сделано. Цена шире собственного красного: `format:check` — второй шаг из восьми, поэтому все семь падений останавливались ДО typecheck, test, build и smoke:dist, и о самом коде CI две недели не сообщал ничего. Проверено после правки: остальные шесть шагов проходят без изменений

## logic

- **L-1** [a paid Nansen call whose result `normalize()` rejects is never cached, so every retry pays again](issues/l-1-nansen-no-negative-caching-paid-call-discarded-on-empty-result.md) — severity `SEV-2`, status `fixed`, opened 2026-07-25
- **L-2** [the snapshotter drops a metric silently: `dropped[]` is computed and then discarded](issues/l-2-snapshotter-drops-a-metric-silently-dropped-array-never-leaves-the-node.md) — severity `SEV-3`, status `fixed`, opened 2026-07-27
- **L-3** [`onchain-verify`'s staleness detector is permanently red by construction, and names no metric](issues/l-3-verify-staleness-detector-is-permanently-red-and-names-no-metric.md) — severity `SEV-2`, status `fixed`, opened 2026-07-27
- **L-4** [Telegram entity parsing 400s on snake_case metric names, so the alert is never delivered](issues/l-4-telegram-entity-parsing-400s-on-snake-case-metric-names-no-alert-delivered.md) — severity `SEV-2`, status `fixed`, opened 2026-07-27
- **L-5** [onchain_dex_volume reports gapDays: 0 for a window with no data at all — the empty-chart case breaks its own invariant](issues/l-5-dex-volume-empty-chart-reports-zero-gapdays-breaking-its-own-invariant.md) — severity `SEV-3`, status `fixed`, opened 2026-07-28
- **L-6** [token.holders is advertised on ~30 chains and fails on all of them (blockscout HTTP 403); the live eval was already red](issues/l-6-token-holders-advertised-everywhere-blockscout-403-everywhere.md) — severity `SEV-2`, opened 2026-08-10, **fixed 2026-08-11**: причина 403 снята замером, а не догадкой — вендор закрыл бесключевой доступ и читает ключ ТОЛЬКО из заголовка `Blockscout-MCP-Pro-Api-Key`. Замер каналов: заведомо неверный ключ в заголовке -> 401 (увиден, отвергнут), тот же ключ в `?apikey=` -> побайтово тот же бесключевой 403 (не увиден). Ключ переехал в заголовок; побочно исчезло единственное в репозитории место, где секрет ехал в URL. `isAvailable()` теперь требует ключ — иначе реестр продолжал бы обещать `token.holders` на 39 сетях, где не отвечает ни одна, то есть дефект «объявлено матрицей, не обслуживается нигде» вернулся бы со стороны вендора. **Пункт 1 fix-path (маршрутизация на Nansen) НЕ сделан и не забыт:** платный фолбэк — решение о бюджете, а не о проводке, и запись сама это оговаривает. Живьём: `token.holders` ✅ 2424 мс на ethereum. Порождены [L-11](issues/l-11-the-blockscout-degrade-set-enumerated-401-402-429-the-vendor-answers-403-so-the-anticipated-branch-never-ran.md), [L-12](issues/l-12-blockscout-facade-does-not-answer-token-holders-on-base-within-30s-ethereum-is-marginal-at-our-5s-ceiling.md), [RF-8](issues/rf-8-the-live-eval-never-read-the-repo-root-env-so-a-correctly-configured-secret-reported-as-missing.md), [RF-9](issues/rf-9-the-eval-reports-a-transport-timeout-as-rate-limited-and-names-an-unrelated-knob.md)
- **L-7** [The 10 MiB safeFetch cap refuses exactly the largest multichain protocols, by as little as 2 KB](issues/l-7-safefetch-10mib-cap-refuses-the-largest-protocols.md) — severity `SEV-2`, status `fixed`, opened 2026-08-10, **fixed 2026-08-11**: `protocol.tvl` читает общий каталог `GET /protocols` (8.14 МиБ на все 8 009 протоколов, раз в окно TTL) вместо документа на каждый вызов — в живом гейте первый вызов 479 мс, следующие десять сетей по 16–24 мс. **Таблица в записи меряла не ту величину:** её 2–16 КБ превышения сняты с `Content-Length` HEAD, то есть со СЖАТОГО размера, а лимит применяется к РАСПАКОВАННЫМ байтам — живой замер через наш же `safeFetch` дал 28 914 177 байт (27.57 МиБ) для `/protocol/aave-v3`, превышение ~18 МиБ, растущее с каждым днём истории; вариант «поднять константу» закрыт замером, а обходной путь `aave` вместо `aave-v3` был на 98 % лимита. Правило разрешения слага снято с вендора, а не выведено: прямая строка выигрывает у одноимённого родителя (`/protocol/beanstalk` → `0` своей строки, не 3.2 M суммы), родитель адресуется `parentProtocolSlug` (`/protocol/ether.fi` → 200, `/protocol/ether-fi` → 400). **Названный остаток:** 38 родителей из 802 объявляют `tokensExcludedFromParent` (сумма детей завышает на +9.5 % на `ether.fi`) и отвечаются агрегатом самого вендора; девять из десяти крупнейших таких документов < 1.7 МиБ, а `curve-finance` (27.77 МиБ) по-прежнему отказывается по лимиту — теперь это узкий случай с внятным сообщением, а не поведение по умолчанию
- **L-8** [Nansen ownershipPercentage 0 reaches the client as a real share for a 185.8M USDC holder](issues/l-8-ownership-percentage-zero-passes-through-as-a-real-share.md) — severity `SEV-3`, status `fixed`, opened 2026-08-10, **fixed 2026-08-11**: поле опускается на ПРОТИВОРЕЧИИ (нулевая доля при положительном балансе), а не на нуле — пылевой держатель законно округляется в ноль; остальная строка сохраняется, потому что отказ правилен для целого ответа (DF-1, L-1), но не для одного поля. **Дефект оказался старше и шире записи:** правка покраснила золотой `TC-CONTRACT-01`, и замер живой фикстуры 2026-07-24 по Ethereum USDC дал **10 строк из 10** с нулевой долей при положительных балансах, крупнейший 4 534 414 876 USDC ($4.53B) — значит неверное значение ехало с M2, а не появилось на Base. Сьют молчал по построению: эталон **копировал `row.ownership_percentage` из той же фикстуры**, то есть утверждал «мы воспроизводим вендора» — ровно то, чего анти-коррупционный слой обещать не должен. Эталон переписан на правило, добавлен контроль в обе стороны, счётчик опущенных пишется в stderr (L-2)
- **L-9** [A protocol that is not deployed on a chain is indistinguishable from a provider outage](issues/l-9-not-deployed-on-chain-indistinguishable-from-provider-outage.md) — severity `SEV-3`, status `fixed`, opened 2026-08-10, **fixed 2026-08-11**: «протокола здесь нет» стало успешным ответом `deployed: false` / `tvlUsd: 0` (живая проверка: `aave @ bitcoin` вместо `capability unavailable`). **Состояний оказалось три, а не два:** 41 строка каталога из 6 917 объявляет сеть, для которой публикуются только корзины `-staking`/`-borrowed`/`-pool2` — там `tvlUsd: null` при `deployed: true`, потому что ноль заявил бы измерение, которого никто не делал (L-2). Второй слой закрыт вместе с первым: ответ несёт `deployments` (весь набор сетей нашими слагами, по убыванию TVL) и `unmappedDeployments` — 19 названных сетей и 4 неопознанных вместо 6.8 % TVL на неразрешимом остатке. Список сетей берётся из общего каталога, а не из документа протокола: собственный документ родителя отвечает `chains: []` (измерено на `uniswap`/`aave`/`raydium`)
- **L-10** [Two DeFiLlama chain vocabularies: 43 of 458 chains answer a confident 'not deployed'](issues/l-10-two-defillama-chain-vocabularies-43-of-458-chains-answer-a-confident-not-deployed.md) — severity `SEV-2`, status `fixed`, opened 2026-08-11, **fixed 2026-08-11**: обе вендорские схемы именования зарегистрированы в поиске сетей адаптера через СГЕНЕРИРОВАННУЮ из вендорских же колонок идентичности карту (`chain-aliases.ts`, образец `dex-chains.ts`: улика закоммичена, запинена в provenance, есть тест регенерации); `cmcId` как ключ не используется вовсе (Terra и Flare делят 4172), `gecko_id` — только когда уникален с обеих сторон (Bitcoincash и smartBCH делят bitcoin-cash). **Первая попытка чинила половину:** карта исправила обратный поиск, а `deployed` продолжал спрашивать новым именем в накопителе по старым — те же четыре нуля живьём; накопитель переведён на наш канонический слаг. **Дефект был создан фиксом L-9:** до него несовпадение бросало ошибку — неверно, но громко, — а трёхсостоянийный контракт придал промаху поиска форму истинного утверждения. Оба гейта были зелёными: 1298 тестов не проверяли ни одной сети с расходящимся именем, а живой eval прогонял `bsc` и `gnosis` и показывал ✅, потому что кейс проверял непротиворечивость трёх состояний, но не их возможность. Добавлены регрессия (с удалённой картой падают ровно 4 теста из 1310) и арифметическая проверка в сьюте и в живом гейте: при нуле неназванных сетей сумма по сетям обязана давать 100% от `totalTvlUsd` — ловит и потерянную сеть (~98%), и посчитанную дважды (ровно 200%)
- **L-11** [The Blockscout degrade set enumerated 401/402/429; the vendor answers 403, so the anticipated branch never ran](issues/l-11-the-blockscout-degrade-set-enumerated-401-402-429-the-vendor-answers-403-so-the-anticipated-branch-never-ran.md) — severity `SEV-2`, opened 2026-08-11, **fixed 2026-08-11**: 403 добавлен в `DEGRADE_STATUSES`, петля статусов в транспортном сьюте расширена до `[401, 402, 403, 429, 500]`. Урок шире номера: комментарий над списком ПРЕДСКАЗЫВАЛ эту ветку («иначе обнаружим в проде в день окончания grace-периода») — предсказание сбылось, а список всё равно был неверен, потому что перечислял придуманные номера. Заготовленная ветка на не том номере — это незаготовленная ветка
- **L-12** [Blockscout facade does not answer token.holders on base within 30s; ethereum is marginal at our 5s ceiling](issues/l-12-blockscout-facade-does-not-answer-token-holders-on-base-within-30s-ethereum-is-marginal-at-our-5s-ceiling.md) — severity `SEV-3`, status `open`, opened 2026-08-11. **Пункт 2 fix-path (пер-чейновые публичные инстансы вторым адаптером) ЗАКРЫТ замером 2026-08-11, а не отложен:** маршрут жив — все 50 инстансов отдали keyless `/api/v2/stats` за 312–1340 мс, — но holders на них тормозит ровно там же: base и polygon не отвечают за 15 с ни через инстанс, ни через фасад, а прогрев ОБЩИЙ (arbitrum ответил фасаду за 2.05 с сразу после прогрева по инстансу). Значит индекс холдеров у вендора один, и второй адаптер купил бы те же два таймаута с другого хоста — за 24 сторонних домена в SSRF-allowlist и на маршруте, который вендор уже перевёл в «API Archive». Побочно вскрыт словарь-двойник (класс L-10): `chains[id]` и `chains_metadata[].explorers[]` расходятся на 7 сетях из 50, и по первому полю Gnosis и Scroll уехали бы на Etherscan-клоны. **Пункт 1 (перезамерить в ДРУГОЙ день) остаётся:** все три выборки сделаны 2026-08-11 в течение нескольких часов — устойчивость отказа между днями не измерена. Улики: `blockscout-instance-config-2026-08-11.json`, `blockscout-per-instance-probe-2026-08-11.json`
- **L-13** [`change` is computed on an unvalidated endpoint, so a one-day vendor artifact becomes the reported trend](issues/l-13-change-is-computed-on-an-unvalidated-endpoint-so-a-one-day-vendor-artifact-becomes-the-reported-trend.md) — severity `SEV-2`, status `fixed`, opened 2026-08-11, **fixed 2026-08-12**: `change` теперь несёт `endpointContext` — то же окно, измеренное к предыдущей точке и к медиане хвоста без конечной точки, обе от той же базы, что заголовок (−16.43% / −3.04% / −4.31% на окне, породившем запись). **Порог НЕ введён, и это измерено, а не выбрано:** артефакт (≈13%) меньше обычного дневного шума своей же сети (berachain p90 = 17.5%), а у последней точки нет следующего дня, чтобы увидеть возврат, — вердикт был бы утверждением, которого данные не поддерживают. Живой гейт 96 → **119 ok**, `pass`. Тот же гейт и **поймал**, как правка ломает все сети: обязательное поле в общей схеме против устаревшего `dist`, при 1380 зелёных юнит-тестах — шов WI-44. Состязательное ревью в 4 линзы: 14 претензий, **12 опровергнуто**, 2 уцелевшие — пробелы покрытия в `median()` (чётная ветвь и числовая сортировка), обе закрыты тестами с проверенной мутацией; отгруженная арифметика в обоих случаях была верна
- **L-14** [`truncated` reports only the losses that happen inside our process, so a vendor-capped page is marked complete](issues/l-14-truncated-reports-only-losses-inside-our-process-not-the-vendor-page-cap.md) — severity `SEV-2`, status `fixed`, opened 2026-08-11, **fixed 2026-08-12**: полная вендорская страница отчитывается как усечение — ТРЕТЬЕЙ причиной, отдельно от двух наших, с числом слотов, ушедших чужим сетям; сворачивать её в `cutByLimit` нельзя, это совет увеличить `limit`, который не может сработать. `VENDOR_PAGE_SIZE = 30` **измерен**: семь запросов дали ровно 30, а `q=zzzqqxunlikely9` дал 0 — только второй зонд доказывает, что 30 это потолок, а не набивка. **Улика лежала в репозитории с M1:** обе фикстуры — полные страницы, `ethereum.json` содержит 2 строки ethereum из 30, и тест утверждал над ней `truncated: {pairs: false}` с комментарием «страница ЕСТЬ полный ответ». Защита от всегда-истинности: 29 строк → `false`, 30 → `true`
- **L-15** [`pool.info` is advertised by the capability manifest and no tool serves it](issues/l-15-pool-info-is-advertised-by-the-capability-manifest-and-no-tool-serves-it.md) — severity `SEV-3`, status `fixed`, opened 2026-08-11, **fixed 2026-08-21** (TASK 014-32c): `onchain_pool_info` ships, и AC-29 сравнивает манифест с набором тулов гейтом
- **L-16** [`createServer` with the HTTP principal resolver throws, so the network profile cannot create a session](issues/l-16-createserver-with-the-http-principal-resolver-throws-so-the-network-profile-cannot-create-a-session.md) — severity `SEV-2`, status `fixed`, opened 2026-08-17, **fixed 2026-08-17**: место конструирования сессии больше не ВЫЗЫВАЕТ резолвер — без резолвера `STDIO_PRINCIPAL`, с резолвером новый `UNRESOLVED_PRINCIPAL` (наименьшая привилегия словаря и идентификатор, называющий сам себя), потому что `STDIO_PRINCIPAL` это `admin` и заглушка, протёкшая в решение, была бы эскалацией привилегий в одежде умолчания. Вычислявшееся значение было мёртвым в обе стороны: обёртка `defineTool` перезаписывает принципала на каждом запросе. Ни один тест не краснел, потому что сочетание «`createServer` + http-резолвер» не покрыто ничем: сквозная HTTP-сьюта резолвер не передаёт, сьюта перехвата регистрирует зонд на собранном вручную `McpServer`. Регрессию держит `e2e.request-trace.test.ts` — первая сьюта, проводящая `createServer` через HTTP-транспорт с резолвером
- **L-17** [the `network` profile kept its cache and credit ledger in a local file](issues/l-17-the-network-profile-kept-its-cache-and-credit-ledger-in-a-local-file.md) — severity `SEV-2`, status `fixed`, opened 2026-08-18, **fixed 2026-08-18**: `index.ts` брал `createCacheStore` и `createBudgetStore` безусловно — пару SQLite, — а `profile.storage` решал ровно одну вещь: какой `StateClient` открывают репозитории личности. Миграция 002 создала `cache_entries` и `usage` в схеме `onchain`, и обе оставались пустыми: два процесса `network` держали по СВОЕМУ файлу и, значит, по полному суточному потолку Nansen — та же форма, что дефект лимитера R-7, этажом ниже и в деньгах. Ни один тест не краснел: сторы проверяются напрямую, а единственная сьюта, собирающая настоящий runtime, подставляет свои фабрики нарочно (иначе писала бы в `DATA_DIR` разработчика), поэтому ветка выбора фабрики — единственная, которую не исполняет никто. Отсрочка, записанная в docstring («не подключается этой задачей, блокирует 014-09»), пережила снятие блокера
- **L-18** [the registry marks 62 DexScreener-served chains as unsupported, and the live gate counts each refusal as a pass](issues/l-18-the-registry-marks-62-dexscreener-served-chains-as-unsupported.md) — severity `SEV-2`, status `fixed`, opened 2026-08-18, **fixed 2026-08-20**: колонка `vendors.dexscreener` производится генератором с закоммиченной уликой, а не спот-чеком из трёх записей — 3 → 49, покрытие `pairs.active` 3 → 49 сетей. Зонд задаёт ДВА вопроса: маршрутизируемость (`200` против `400`) и эхо (отдаёт ли вендор этот идентификатор как `pair.chainId`); один статус читал бы пустой ответ как покрытие — класс L-10 внутри исправления L-18. Замер 2026-08-20: 858 кандидатов, два круга, ноль неотвеченных; 65 сетей маршрутизируемы, 49 подтвердили эхо, 16 держат `null` и отказывают формулировкой «нет подтверждённого вендорского идентификатора», а не «вендор не обслуживает эту сеть». Значение колонки — ОТВЕТИВШИЙ КАНДИДАТ: четыре сети отвечают 400 на слаг и 200 на вендорский id
- **L-19** [`pairs.active` answers an empty page wherever the native-symbol search does not return that chain](issues/l-19-pairs-active-answers-an-empty-page-wherever-the-native-symbol-search-does-not-return-that-chain.md) — severity `SEV-2`, status `fixed`, opened 2026-08-20, **fixed 2026-08-21** (TASK 014-32c): замер по всем 49 сетям — прежняя стратегия пуста на 22, а не на 4; запрос переведён на вендорский идентификатор сети с добором обёрнутым тикером, 49/49
- **L-20** [Blockscout answers HTTP 500 for the `base` stats document, on the facade and on the public instance alike](issues/l-20-blockscout-answers-http-500-for-the-base-stats-document-on-both-routes.md) — severity `SEV-3`, status `open`, opened 2026-08-20
- **L-21** [nine consecutive Telegram alerts failed over five days, and nothing reported it](issues/l-21-nine-consecutive-telegram-alerts-failed-over-five-days-and-nothing-reported-it.md) — severity `SEV-4`, status `documented`, opened 2026-08-24, **НЕ ошибка**: причина установлена владельцем 2026-08-24 — у ноутбука с VM не было связи. Улики записи с этим согласуются: ECONNRESET на установке TLS — это отсутствие маршрута, а исполнение 45219, прочитанное здесь как потерянный инцидент, было падением снапшоттера на HTTP-ноде, то есть тем же обрывом. Запись сохранена ради одного абзаца, верного независимо от причины: у падающего обработчика ошибок нет своего читателя, поэтому молчание канала неотличимо от тишины — пульс заведён как WI-64 и закрыт 2026-08-24 — подтверждённая доставка пишет `alert.delivered` в `onchain.diagnostics`, `onchain-verify` читает возраст ежедневно
- **L-22** [`pairs.active` loses `berachain` and `tron` under gate load, while the vendor still serves both](issues/l-22-pairs-active-loses-two-chains-under-gate-load-while-the-vendor-still-serves-them.md) — severity `SEV-3`, status `fixed`, opened 2026-08-24, **исправлено 2026-08-24** задачей 014-33: причина оказалась арифметической, а не нагрузочной. Дедлайн способности равнялся потолку ОДНОГО хопа (оба 15_000), а маршрут с 014-32c делает два — поэтому второй запрос не мог начаться, если первый занял больше мгновения. Держалось на здоровом вендоре и без всякой нагрузки. Способность переехала в ярус ~30 с, а поиск получил свой потолок хопа 12_000: две ноги плюс ожидания корзины дают ~26 с. Наблюдаемое доказательство — сменился класс отказа: `capability unavailable` на 12004 мс вместо `capability deadline exceeded` на 15010 мс, то есть «вендор не ответил» вместо «у нас кончилось время». Сам вендор в этот день деградировал, и ни одно число под это состояние не подгонялось
- **L-23** [DexScreener перестал отвечать целиком и увёл с собой три способности](issues/l-23-dexscreener-stopped-answering-entirely-and-took-three-capabilities-with-it.md) — severity `SEV-2`, status `open`, opened 2026-08-24. Гейт упал со `ok 147 · error 2` до `ok 126 · error 24` за один прогон: падают все три способности вендора — `pairs.active` 8 из 9, каждый ряд `pool.info` и `token.pools`. Изолировано замером: в ту же минуту с той же машины DefiLlama, CoinGecko и mempool.space отвечают за доли секунды, а DexScreener не отвечает за 15 с. Деградация шла весь день: утром 1.3–2.5 с, днём 6.8–7.2 с при большинстве проб без ответа за 40 с, вечером ни одного ответа. Кода это не касается, и подтвердить отказ НЕЛЬЗЯ по замыслу: файл подтверждений отвергает границу, равную размеру множества, потому что непревосходимая граница принимает полный отказ, ради видимости которого и существует (RF-10)
- **L-24** [DeFiLlama's two per-call history routes answer erratically, and only those two](issues/l-24-defillamas-two-per-call-history-routes-answer-erratically.md) — severity `SEV-3`, status `open`, opened 2026-08-24
- **L-25** [a wide sweep makes DeFiLlama rows fail that answer fine on their own](issues/l-25-a-wide-sweep-makes-defillama-rows-fail-that-answer-fine-alone.md) — severity `SEV-4`, status `documented`, opened 2026-08-24, **опровергнута своим же экспериментом в тот же день**: три плеча (обход в одиночку / при брошенных загрузках / снова в одиночку) дали 11 из 11 ok каждое, а следом полный прогон гейта взял все 11 строк за 89–1730 мс. Ошибка была в рассуждении: три замера, на которые опиралась запись, взяты в РАЗНОЕ время — это «упало тогда, прошло потом», а не «падает в обходе, проходит вне». Нагрузка эксперимента при этом не состоялась (в полёте остался 1 документ из 4), так что отрицательный результат узкий: теснота проверена только при здоровом вендоре. Сохранена ради урока — эксперимент про переходный процесс ставят ВО ВРЕМЯ него
- **L-26** [`token.price` on tron exceeded its deadline once, with the vendor measured healthy](issues/l-26-token-price-on-tron-exceeded-its-deadline-once-with-the-vendor-healthy.md) — severity `SEV-4`, status `open`, opened 2026-08-24
- **L-27** [the FIRST caller of a shared document pays a deadline the document does not cost](issues/l-27-the-first-caller-of-a-shared-document-pays-a-deadline-the-document-does-not-cost.md) — severity `SEV-3`, status `open`, opened 2026-08-24

## security

- **SEC-1** [the daily credit cap bounds damage per day, not per minute: there is no velocity guard](issues/sec-1-nansen-daily-cap-does-not-bound-a-burst-no-velocity-guard.md) — severity `SEV-2`, status `fixed`, opened 2026-07-25
- **SEC-2** [a stock Supabase role holds `pg_read_all_data`, so every engine table is readable by it](issues/sec-2-a-stock-supabase-role-holds-pg-read-all-data-so-the-engine-tables-are-readable-by-it.md) — severity `SEV-2`, status `documented`, opened 2026-08-22, **принято для dev-VM 2026-08-23** решением владельца: на управляемом кластере Supabase постусловие §10.4.2 недостижимо — платформа поставляет суперпользователя, роль владельца и зарезервированную read-only роль, отзыв закрыт `supautils`, RLS не связывает (`rolbypassrls` у обоих членов `pg_read_all_data`). Держится граница, которая несёт риск: `authenticator`, единственная достижимая снаружи роль, не читает ни одной из двенадцати таблиц. Решение НЕ переносится на другой хост; для отдельной машины владелец намерен ставить выделенный Postgres — WI-62

## quality

- **Q-1** [under a persistent reconcile degrade, the nansen stderr line repeats per call](issues/q-1-nansen-degrade-stderr-repeats-per-call.md) — severity `SEV-4`, status `by-design`, opened 2026-07-24
- **Q-2** [`NANSEN_DAILY_CREDIT_CAP` is optional with no default, so a stock install has no self-imposed ceiling](issues/q-2-nansen-daily-credit-cap-has-no-default.md) — severity `SEV-3`, status `fixed`, opened 2026-07-24
- **Q-3** [the 0-credit `entity.labels` query tier is structurally unrefusable by a credit-denominated gate](issues/q-3-nansen-zero-credit-entity-labels-tier-is-unrefusable-by-the-gate.md) — severity `SEV-3`, status `fixed`, opened 2026-07-25
- **Q-4** [`token.risk` pays 1cr per call for `/tgm/token-information`, whose body is never read](issues/q-4-nansen-token-information-subcall-paid-but-never-consumed.md) — severity `SEV-3`, status `fixed`, opened 2026-07-25
- **Q-5** [a literal NUL byte in registry-core.ts makes every repo-wide grep gate skip the SSRF-allowlist module silently](issues/q-5-a-literal-nul-byte-in-registry-core-ts-makes-every-repo-wide-grep-gate-skip-the-ssrf-allowlist-module-silently.md) — severity `SEV-2`, status `fixed`, opened 2026-07-27
- **Q-6** [The self-imposed budget refusal names the ceiling where its own vendor branch names the remainder](issues/q-6-self-imposed-budget-refusal-names-the-ceiling-not-the-remainder.md) — severity `SEV-3`, status `fixed`, opened 2026-08-10, **fixed 2026-08-11**: самоналоженная ветка печатает `need <cost>, remaining <n> of <ceiling>` — обе половины одного отказа отвечают на один вопрос и стали сравнимы. Взят `effectiveCeiling`, а не `capNow`: в этой ветке они равны по построению, но `capNow` типизирован `number | undefined`, и заменённая строка интерполировала его сырым — то есть исходный код отрендерил бы `allows undefined`, что вскрыл typecheck, когда потребовалась арифметика. `getUsage()` best-effort и не может заменить собой отказ (SQLITE_BUSY превратил бы бюджетный отказ в ошибку хранилища); запасной текст строго не хуже прежнего. Оба теста теперь ТРЕБУЮТ остатка, а `of <ceiling>` держит потолок на виду — падают и на откате к «только потолок», и на потере потолка
- **Q-7** [totals.h24 is the previous whole day, not the last series point, and the last point is partial](issues/q-7-dex-volume-h24-is-the-previous-whole-day-not-the-last-series-point.md) — severity `SEV-4`, opened 2026-08-10, **fixed 2026-08-11**: `totals.asOfTs` называет день, который покрывает `h24`, а точка текущего дня несёт `partial: true`. Смещение подтверждено живьём на ethereum/base/solana — `total24h` равен ПРЕДПОСЛЕДНЕЙ точке. `asOfTs` **верифицируется, а не предполагается**: ставится, только если `h24` действительно совпал с последним полным днём (допуск покрывает округление вендора — solana разошлась на 4 единицы из 1.58 млрд), иначе `null`, что означает «сверку подтвердить не удалось», а не «тоталов нет». Захардкодить «предпоследняя точка» было бы верно на всех наших фикстурах и неверно в день смены агрегата — ровно то, что `Do-not` записи запрещает. `partial` живёт в dex-volume, а не в общем shaper: он осмыслен для НАКАПЛИВАЕМОЙ величины, тогда как у TVL сегодняшний снимок полон в момент съёма. Точка не удаляется (`Do-not`). Проверка выравнивания добавлена в живой eval — только там она и проверяема, потому что смещение принадлежит вендору
- **Q-8** [onchain_new_pairs returns long-established pools, and its name is what a client selects on](issues/q-8-new-pairs-returns-established-pools.md) — severity `SEV-3`, opened 2026-08-10, **fixed 2026-08-11**: вариант 1 — `onchain_new_pairs` → `onchain_active_pairs`, способность `pairs.new` → `pairs.active`, описание прямо говорит, что «что запустилось недавно» движок не отвечает ни на одной сети. **Вариант 2 закрыт замером, как запись и требовала:** `/latest/dex/search` — релевантностный поиск без семантики свежести; свежайшая строка в выдаче 154.7 суток, `pairCreatedAt` отсутствует у 6 строк из 30, а на `sort=`/`sortBy=`/`rankBy=` вендор отдаёт побайтово ту же страницу (игнорирует неизвестные параметры). Никакой over-fetch этого не исправляет; `token-profiles` — промо-лента без пулов. Переименование: 145 ссылок в 41 живом файле и ЧЕТЫРЕ написания (`pairs.new` / `onchain_new_pairs` / `new-pairs` / `new_pairs`); четвёртое нашёл гейт C4-диаграммы, а не шаблон — знаменатель проверен пофайлово. Архив (`docs/tasks`, `plans`, `reviews`, `version-history`, `*.evidence.md`) не тронут: там имя — запись о том, как тул назывался тогда
- **Q-9** [On a merged response the top-level source names one provider while its own points name another](issues/q-9-dash-history-top-level-source-contradicts-its-own-points.md) — severity `SEV-4`, opened 2026-08-10, **fixed 2026-08-11**: корневой скаляр `source` заменён на `sources[]`, выведенный ИЗ ТОЧЕК, поэтому противоречить им он не может по построению. Реестровое `CapabilityResolution.sources` переслать было НЕЛЬЗЯ: оно несёт идентификаторы адаптеров (`pg-history`), а точки — метки провенанса (`derived`), и пересылка дала бы второе противоречие вместо починки первого. `derived` сохраняется отдельно — его схлопывание в имя вендора прячет залеченную метрику (L-2). Провенанс кэша не потерян: он и так публикуется в `_meta.cache`, а скаляр был его копией под именем, которое на всех соседних тулах значит другое
- **Q-10** [onchain_new_pairs silently drops vendor rows that fail validation; the count goes only to stderr](issues/q-10-new-pairs-silently-drops-vendor-rows-that-fail-validation.md) — severity `SEV-3`, opened 2026-08-10, **fixed 2026-08-11**: адаптер отдаёт страницу `{pools, truncated}`, и `truncated: {pairs, reason}` доходит до вызывающего — тем же контрактом, что у `onchain_dex_volume`. Две причины названы РАЗДЕЛЬНО, потому что для потребителя они не равнозначны: срез по `limit` возвращается увеличением `limit`, отброшенная строка не возвращается ничем. Строка в stderr сохранена — это канал оператора, а правка добавляет канал вызывающего (`Fix path` требует именно этого). Поле сразу нашло дефект: солановая фикстура всё это время резалась (15 строк на сети, отдавалось 10), и сказать об этом было нечем
