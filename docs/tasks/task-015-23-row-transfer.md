# Задача 015.23: остановка сетевого профиля и перенос строк одиннадцати таблиц `[VM]`

## Связь со сценариями

- UC-6 — таблицы движка переезжают в выделенный контейнер Postgres на dev-VM, шаги 3 и 4

<!-- contract:goal -->

## Цель задачи

Перенести строки таблиц движка в новый контейнер в окне, когда в старый контейнер не пишет никто.

**Why окно без писателя, а не сверка постфактум.** Идемпотентность по ключу UNIQUE дедуплицирует
двух писателей только внутри одной базы (`DB-SCHEMA-CONCEPT` §6). Здесь баз две: строка, записанная
в старую базу после начала копии, в новой не появится.

**Why задача помечена `[VM]`.** Каждый её шаг — операция на dev-VM по skill `vm-deploy`. Кода она не
меняет.

<!-- contract:changes -->

## Описание изменений

### Изменения в существующих файлах

**Файл `docs/onchain-analytics/PROD-RUNBOOK.md`:**

- Раздел с процедурой шагов 3 и 4: остановка профиля, порядок таблиц, команда конвейера, ветвь
  повтора для `access_audit`

**Файл `docs/architectures/deployment.md`:**

- §10.9.4 — абзац отклонения от буквы R-8.14 (MINOR-9 раунда 2)
- §10.9.7 — ссылка на этот абзац

Оба документа ведутся по-английски; правки вносятся на языке своего корпуса (ARCHITECTURE §7.3).

### Предусловия

| Предусловие                                                   | Как проверяется                            |
| :------------------------------------------------------------ | :----------------------------------------- |
| Задача 015-21 применена: тринадцать таблиц существуют пустыми | SQL-замер на новом контейнере              |
| `ssh vm` отвечает                                             | прогон команды                             |
| Оба контейнера подняты, `pg_isready` отвечает на обоих портах | задача 015-20                              |
| Числа строк старого контейнера не измерены                    | снимаются здесь и сверяются задачей 015-24 |

**Why числа снимаются в момент переезда.** На момент планирования dev-VM отвечала таймаутом
устойчиво (`docs/TASK.md` §0). План, опирающийся на сегодняшние числа, сверял бы копию с числом,
которого никто не измерял.

### Шаг 3 — остановлены ВСЕ писатели переносимых таблиц

Писателей переносимых таблиц **четверо**, и останавливаются все четыре.

| Писатель                                    | Что пишет                                        | Как останавливается                           |
| :------------------------------------------ | :----------------------------------------------- | :-------------------------------------------- |
| сетевой профиль движка                      | все одиннадцать таблиц                           | процесс останавливается                       |
| `onchain-verify`, узел `Write delivery`     | `onchain.diagnostics`                            | воркфлоу деактивируется                       |
| `onchain-error-alert`, узел записи доставки | `onchain.diagnostics`                            | воркфлоу деактивируется — ДОПУЩЕНИЕ, см. ниже |
| `onchain-retention`, шесть узлов Postgres   | `retention_runs`, `diagnostics`, `request_trace` | воркфлоу деактивируется                       |

Постусловие — две величины, обе снимаются до первой команды копии:

1. `SELECT count(*) FROM pg_stat_activity WHERE usename = '<роль состояния>'` в старом контейнере
   даёт ноль
2. ни один из **трёх** воркфлоу не активен на инстансе n8n

**Why постусловие меряется, а не предполагается.** Остановленный процесс и закрытое соединение —
разные события. Пул мог оставить соединение, которое ещё пишет.

**Why нулевой `count` сам по себе окна не даёт.** Соединения узла n8n транзиентны: ноль в момент
замера не запрещает записи минутой позже. Постусловие «в старый контейнер не пишет никто» держится
только вместе со вторым замером.

**Why три воркфлоу названы поимённо.** Все три ходят под одним credential `Onchain engine state`
(n8n id `MjsP4aIFWZd25tik`, замер 2026-08-31) — то есть от роли состояния старого контейнера.
`onchain-verify` и `onchain-error-alert` пишут `alert.delivered` в `onchain.diagnostics`
(`n8n-workflows/exported/onchain-verify.json:161`, `onchain-error-alert.json:160`);
`onchain-retention` вставляет в `retention_runs` и `diagnostics` и **удаляет** из `diagnostics` и
`request_trace`. Все три таблицы входят в одиннадцать переносимых.

**Why их деактивация разрешена.** Все три — наши `onchain-*` воркфлоу (CLAUDE.n8n.md). Чужие
воркфлоу инстанса не трогаются; `onchain-snapshotter` остаётся активным (R-8.3).

**Why credential один, а воркфлоу три.** Это упрощает 015-22: правится одна запись credential, и
все три писателя переезжают вместе. Обратная сторона названа там же — правка до переноса строк
перевела бы писателей раньше снимка.

### Рычаг остановки третьего писателя — НЕИЗМЕРЕННОЕ ДОПУЩЕНИЕ

Для `onchain-verify` деактивация — измеренный рычаг: воркфлоу запускается Schedule-триггером, и
неактивный триггер не срабатывает.

Для `onchain-error-alert` то же не измерено. Он вызывается не своим триггером, а ПО ССЫЛКЕ
`settings.errorWorkflow` из исполнений других воркфлоу. Единственный измеренный проектом факт об этом
пути — про режим исполнения, а не про признак активности (`CLAUDE.n8n.md` §«Executing a workflow from
an agent»; `docs/onchain-analytics/PROD-RUNBOOK.md:180-182`).

| Поле                    | Значение                                                        |
| :---------------------- | :-------------------------------------------------------------- |
| Допущение               | снятый признак активности гасит и вызов по ссылке               |
| Почему не измерено      | dev-VM остановлена; измерение требует живого инстанса n8n       |
| Способ проверки         | одноразовый воркфлоу-зонд, состав ниже                          |
| Когда проверять         | первым шагом задачи, после `ssh vm` и до первой команды копии   |
| Последствие, если ложно | запись в СТАРЫЙ контейнер после снимка → `less_on_new` у 015-24 |
| Класс отказа            | безопасный: гейт блокирует этап, строку называет TC-NEG-05      |
| Что делать, если ложно  | повторить копию `onchain.diagnostics` через `TRUNCATE`          |

**Состав зонда** — по рецепту `CLAUDE.n8n.md` §«Executing a workflow from an agent»: одноразовый
воркфлоу с Schedule-триггером на дату, которая не наступит; `settings.errorWorkflow` указывает на
`onchain-error-alert`; один узел, падающий на `http://127.0.0.1:9/`. Зонд активируется, запускается в
режиме `production`, после чего проверяется, появилась ли новая строка `alert.delivered`. Зонд
удаляется тем же шагом.

**Отклонение от рецепта: триггер Webhook вместо Schedule (замер 2026-08-31).** Рецепт запускает зонд
через `mcp__n8n-builtin__execute_workflow`; в этой сессии оба MCP-сервера n8n не поднялись
(`CONNECT_TIMEOUT`), а публичный API n8n эндпоинта запуска не имеет. Schedule-триггер на дату,
которая не наступит, при этом нечем дёрнуть. Замена — Webhook-триггер: активированный воркфлоу даёт
production-URL, и обычный `GET` по нему порождает исполнение в режиме `webhook`, то есть
**производственном**, а не `manual`. Ровно это и требуется: `errorWorkflow` не срабатывает на
`manual` (замер WI-64), а на `webhook` — срабатывает.

**Контрольный прогон обязателен, и он идёт ПЕРВЫМ — при ВКЛЮЧЁННОМ `onchain-error-alert`.**

| Прогон                             | Что доказывает                                            |
| :--------------------------------- | :-------------------------------------------------------- |
| контрольный, обработчик активен    | зонд действительно доходит до `errorWorkflow` и до записи |
| основной, обработчик деактивирован | снятый признак активности гасит вызов по ссылке           |

**Why без контроля основной прогон ничего не значит.** «Новой строки не появилось» имеет два
объяснения: рычаг сработал, либо зонд никогда не доходил до обработчика — опечатка в
`settings.errorWorkflow`, неверный режим исполнения, узел, который не падает. Один прогон их не
различает. Это тот же приём, которым в проекте доказывается чувствительность теста: сначала
зелёное на неизменённом, потом мутация.

**Замер контрольного прогона, 2026-08-31 11:41 UTC:** исполнение зонда `46804`, статус `error`,
режим `webhook`; `alert.delivered` в `onchain.diagnostics` старого контейнера 5 → 6, новая строка
`01M1BT05WBKA485P54Y5GYJRPJ` с `detail_json.about = "zz-t015-pr9-probe-DELETE-ME"`. Рычаг зонда
доказан; попутно подтверждено, что канал оповещений жив непосредственно перед его выключением.

**Why строка появляется только при принятом Telegram.** Узел записи доставки стоит **после** узла
Telegram (WI-64), поэтому контрольный прогон обязательно отправляет одно сообщение в целевой чат.
Это не побочный эффект замера, а его условие: измеряется доставка, а не намерение доставить.

**Последствие ложного допущения — полностью.** Отказ любого воркфлоу инстанса внутри окна запишет
`alert.delivered` в старый контейнер после снятия снимка. Verify-гейт задачи 015-24 увидит это как
`less_on_new` по `onchain.diagnostics`. Восстановление — повтор копии этой таблицы через `TRUNCATE`
плюс полный повтор в одной транзакции, тот же порядок, что уже объявлен для `access_audit`.

**Why допущение записано, а не заменено гарантией.** Гарантии на неизмеренном рычаге не бывает, а
запись «вопрос решён» на её месте — единственная форма, которая не даст исполнителю проверить.
Прецедент — измерение WI-64, где ручной прогон казался доказательством пути оповещения и им не был.

**Why этап на этом не останавливается.** Отказ наблюдаем и обратим: verify-гейт стоит между копией и
любым удалением, а повторная копия одной таблицы дешевле, чем ожидание доступной VM.

### Уступка на время окна: канал оповещений молчит

Пока `onchain-error-alert` деактивирован, отказ любого воркфлоу инстанса не доходит до Telegram.

| Величина                          | Значение                                                                                                         |
| :-------------------------------- | :--------------------------------------------------------------------------------------------------------------- |
| Что открывает окно                | деактивация трёх воркфлоу, эта задача                                                                            |
| Что закрывает окно                | реактивация всех трёх, задача 015-33                                                                             |
| Ожидаемая длительность            | 12–22 часа — сумма оценок задач 015-24, 015-22, 015-25 и 015-33 (`docs/PLAN.md` § Оценка этапа)                  |
| Объявленная граница               | 24 часа от записанного начала окна                                                                               |
| Что делать при достижении границы | реактивировать `onchain-error-alert` досрочно, не дожидаясь задачи 015-33, и записать факт досрочной реактивации |

**Why не «минутами».** Минутами измеряется копирование строк шага 4. Молчание канала длится от этой
задачи до 015-33, а между ними по перечню этапа 6 стоят verify-гейт, перевод credential и запуск
профиля. Названная минутами величина — это чужая величина, поставленная на место своей.

**Why граница названа числом.** `onchain-error-alert` — терминальный читатель всех сигналов здоровья
инстанса, и цена его молчания уже оплачена проектом: L-21 — девять недоставленных оповещений за пять
дней, замеченных случайно. Срок, который не назван, нельзя превысить наблюдаемо, а PR-8 плана
объявляет обнаружением именно превышение.

**Why досрочная реактивация возможна только после задачи 015-22.** `onchain-error-alert` не читает
`onchain.diagnostics` — он в неё только пишет
(`n8n-workflows/exported/onchain-error-alert.json` — узел `Write delivery`), поэтому разделения
запроса задачи 015-33 ему не требуется. Но до перевода credential задачей 015-22 его запись идёт в
СТАРЫЙ контейнер, то есть ровно в отказ, описанный допущением выше. Досрочная реактивация после
015-22 пишет в новый контейнер — в целевое состояние, а не мимо него.

Начало и конец окна записываются в лог инсталляции (015-28), а реактивация обоих воркфлоу
принадлежит задаче 015-33.

**Why снапшоттер при этом не останавливается.** Он пишет `assets`/`metrics`/`snapshots` в старом
контейнере и в переезде не участвует (R-8.3). Его credential «Supabase DB» не правится.

**`onchain-retention` В ПЕРЕЧНЕ ЕСТЬ — прежняя посылка отменена замером (2026-08-31).** Задача
писала: «воркфлоу не установлен (`deployment.md:79`), и писателем он сегодня не является». Оба
утверждения не выдержали проверки, и по разным причинам.

| Утверждение          | Что показал замер                                                                                                                                                               |
| :------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| «не установлен»      | установлен задачей 015-19 на этапе 5 — то есть ПОСЛЕ того, как этот текст был написан; на инстансе активен                                                                      |
| «`deployment.md:79`» | строка говорит не об этом: в таблице «Properties that follow the storage axis» она означает, что на оси SQLite работ ретенции НЕТ. Про инсталляцию на n8n она не говорит ничего |

**Why две ошибки, а не одна.** Вывод устарел (на момент планирования воркфлоу действительно не был
установлен), а процитированное основание было неверным **с самого начала**: цитата указывала на
ячейку соседней оси. Класс — `RF-15`: координата разошлась с тем, что по ней читают.

**Почему это не косметика.** Триггер `Daily` работы — `{"interval": [{}]}`, то есть 00:00 UTC.
Окно объявлено на 12–22 часа, поэтому суточный запуск попадает в него при любом старте. Работа не
только пишет, но и **удаляет** строки `diagnostics` и `request_trace` — то есть внутри окна она
изменила бы старый контейнер после снятия снимка, и verify-гейт 015-24 увидел бы расхождение
одновременно в обе стороны: `more_on_old` по `retention_runs` и `less_on_old` по вычищенным строкам.

### Шаг 4 — порядок переноса

Двенадцать таблиц из тринадцати несут шаг переноса; `provider_buckets` исключена (R-8.11). Строки
несут одиннадцать из двенадцати: `client_usage` создаётся пустой и источника для копирования не
имеет (`deployment.md` §10.9.2).

| #   | Таблица            | Строки    | Ссылается на                       | Столбец времени |
| :-- | :----------------- | :-------- | :--------------------------------- | :-------------- |
| 1   | `providers`        | да        | —                                  | нет             |
| 2   | `users`            | да        | —                                  | `created_at`    |
| 3   | `access_profiles`  | не копир. | —                                  | `created_at`    |
| 4   | `api_tokens`       | да        | `users:125`, `access_profiles:126` | `created_at`    |
| 5   | `access_audit`     | да        | `users:147`                        | `ts`            |
| 6   | `cache_entries`    | да        | `providers:54`                     | `created_at`    |
| 7   | `usage`            | да        | `providers:64`                     | `updated_at`    |
| 8   | `usage_window`     | да        | `providers:73`                     | `updated_at`    |
| 9   | `request_trace`    | да        | `providers:192`                    | `received_at`   |
| 10  | `diagnostics`      | да        | —                                  | `ts`            |
| 11  | `retention_runs`   | да        | —                                  | `started_at`    |
| 12  | `client_usage`     | нет       | —                                  | `reserved_at`   |
| —   | `provider_buckets` | исключена | `providers:165`                    | `updated_at`    |

Номера строк — `sql/migrations/002_t014_network_profile.sql`.

**Why порядок такой.** Внешний ключ отвергает строку, чья цель ещё не перенесена. `users` и
`access_profiles` идут до `api_tokens`; `providers` — до `cache_entries`, `usage`, `usage_window` и
`request_trace`.

**Why у `providers` нет столбца времени.** Таблица объявлена тремя колонками
(`sql/migrations/002_t014_network_profile.sql:46`). Вторая проверка verify-гейта на ней неприменима,
и это её вход, а не находка.

**Why `provider_buckets` исключена.** Состояние лимитера эфемерно и доливается заново. Цена названа:
`tokens` может быть отрицательным, и потеря строки прощает накопленный долг (`deployment.md` §10.9.5).

**Why `client_usage` всё равно стоит в перечне.** У неё есть шаг переноса — и он пуст. Таблица без
шага выпала бы из счёта тринадцати.

### `access_profiles` НЕ копируется — её строка приходит из DDL (замер 2026-08-31)

Единственная строка таблицы — `01JPHASE00000000000000000A` / `phase0-unlimited` — не пользовательские
данные, а посев самой миграции: `sql/migrations/002_t014_network_profile.sql:287` в старом
контейнере и `sql/migrations/005_wi62_dedicated_container.sql:357` в новом. Задача 015-21 применила
005, поэтому строка на целевой стороне уже стоит.

| Проверено                       | Результат                                                   |
| :------------------------------ | :---------------------------------------------------------- |
| Идентификатор на обеих сторонах | совпадает                                                   |
| Все двенадцать колонок побайтно | совпадают                                                   |
| `created_at` / `updated_at`     | `0` и `0` на обеих — константы миграции, не показания часов |

**Why копия её сломала бы.** `pg_dump --data-only` даёт `COPY`, а `id` — первичный ключ. Под
`ON_ERROR_STOP=1` (см. ниже) дубликат ключа роняет шаг, и при последовательном конвейере — весь
перенос на третьей таблице из двенадцати.

**Why это не то же самое, что `client_usage`.** У `client_usage` источника нет; у `access_profiles`
источник есть, но целевая строка уже создана DDL и **тождественна** ему. Шаг переноса исполняется как
сверка, а не как копия: равенство подтверждается, строка не пишется.

**Why сверка, а не «пропустить молча».** Если строки разойдутся (кто-то правил профиль в старом
контейнере), пропуск унесёт расхождение в новый контейнер незамеченным, а verify-гейт 015-24 сверяет
ЧИСЛА строк — `1` и `1` он примет. Здесь числа совпадают при любом содержимом, поэтому равенство
проверяется значениями.

**Why это находка, а не вход задачи.** Строка таблицы переноса говорила «Строки: да», то есть
предполагала таблицу пустой на целевой стороне. Ни одно предусловие задачи этого не проверяло:
«тринадцать таблиц существуют пустыми» проверялось как существование, а не как пустота.

### Механика — конвейер через stdin

```
ssh vm 'docker exec -i supabase-db pg_dump -U postgres -d postgres \
  --schema=onchain --table=onchain.<name> --data-only --format=plain' \
  | ssh vm 'docker exec -i <новый контейнер> psql -qU postgres -d postgres -v ON_ERROR_STOP=1'
```

**Why конвейер, а не файл внутри контейнера.** `-f /tmp/…` читает файловую систему контейнера и
исполняет устаревшую копию (skill `vm-deploy` §4).

**Why по одной таблице, а не одним дампом схемы.** Порядок задают внешние ключи, а `--schema=onchain`
захватил бы три таблицы снапшоттера, которые остаются на месте (R-8.3).

**Why `ON_ERROR_STOP=1`.** Без него отказ на одной таблице оставляет частичную копию и продолжает
следующие.

### `api_tokens` переносится вместе с остальными

**Why прежняя посылка отменена, а не поправлена молча.** Решение владельца 2026-08-25 (R-8.11)
отменило посылку документа о том, что перенести токены нельзя.

**Why перенос возможен.** `token_hash` — обычная колонка `TEXT`, копируется и спот-чекается
байт-в-байт, как любая точная колонка.

**Why перец не переносится.** `ONCHAIN_TOKEN_HASH_SALT` — не колонка, а секрет в `.env` сервера
(`sql/migrations/003_seed_engine_admin.sql:24-26`, «WHY THE PEPPER IS NOT A PARAMETER»). Переезд
контейнера его не трогает.

**Why это проверяемо.** Запрос уже выпущенным токеном после переключения писателя аутентифицируется
без переиздания (AC-44, задача 015-25).

### `access_audit` — копия одноразовая

**Why не `ON CONFLICT DO NOTHING`.** Таблица не несёт естественного ключа дедупликации по замыслу
(`sql/migrations/003_seed_engine_admin.sql:190`, «`access_audit` has no natural UNIQUE key by
design»). Цели у `ON CONFLICT` нет, поэтому и оговорки нет.

**Координата поправлена (2026-08-31).** Задача цитировала `003_seed_engine_admin.sql:157-158`;
комментарий стоит на строке `190`. Строки 157–158 — про четыре проверки токена. Класс `RF-15`.

**Продолжение «повтор копии удваивает каждую строку» ОТМЕНЕНО замером (2026-08-31).** Удвоение
не наступает: у таблицы есть первичный ключ `access_audit_pkey` на `id`, и повтор конвейера
отвергается целиком.

```
ERROR:  duplicate key value violates unique constraint "access_audit_pkey"
DETAIL:  Key (id)=(01M0NFF8921AJMN6GTN038USER) already exists.
код возврата: 3
```

| Величина                   | Замер                                      |
| :------------------------- | :----------------------------------------- |
| Строк до повтора           | 2                                          |
| Строк после отказа         | 2                                          |
| `md5` содержимого до/после | совпадает и равен исходному на старой базе |

**Why отказ атомарен.** `pg_dump --data-only` даёт одну команду `COPY` на таблицу, а `COPY` —
один оператор: он либо принят целиком, либо отвергнут целиком. Полусостояния не остаётся.

**Why суррогатный ключ и естественный — не одно и то же.** Комментарий `003:190` отрицает ключ
**дедупликации** (пары «источник + момент», по которой строку можно опознать повторно) — и это
верно. Первичный ключ на ULID при этом есть, и от повторной вставки той же строки защищает именно
он. Задача сложила два разных утверждения в одно и получила несуществующую угрозу.

**Почему `TRUNCATE` всё равно нужен, но по другой причине.** Не «чтобы не удвоить» — удвоить
нельзя, — а чтобы убрать строки, лежавшие в целевой таблице ДО копии. Ровно этот случай здесь и
наступил: две строки зонда. Без `TRUNCATE` повтор просто не проходит, и таблица остаётся с чужим
содержимым.

Ветвь повтора — `TRUNCATE` и полная копия в одной транзакции:

```sql
BEGIN;
TRUNCATE onchain.access_audit;
-- повторить копию данных этой одной таблицы
COMMIT;
```

**Why ветвь требует явного подтверждения владельца.** `TRUNCATE` входит в перечень операций, которые
skill `vm-deploy` §5 запрещает без него.

**Ветвь повтора обязательна с самого начала, а не по условию (записано 2026-08-28).** На новом
контейнере в `onchain.access_audit` уже лежат ДВЕ строки — `user.create` и `token.issue` с
идентификаторами `01TESTPROBE…`. Их посеяла миграция `003_seed_engine_admin.sql`, запущенная при
ремонте записи `L-28` для проверки шестой охраны: подготовительная вставка не-админа не прошла
(не заполнила `status`), охрана поэтому не сработала, и файл отработал до конца. Строки `users` и
`api_tokens` того прогона удалены; строки `access_audit` удалить нельзя — таблицу защищает правило
`access_audit_no_delete` формы `DO INSTEAD NOTHING` плюс триггер `access_audit_no_update`, то есть
append-only канон работает как задумано.

Следствие для этой задачи: без `TRUNCATE` копия даст на новом контейнере на две строки больше
источника, и verify-гейт 015-24 назовёт это `more_on_new` — материализованной угрозой дублирования.
`TRUNCATE` входит в перечень операций, требующих явного подтверждения владельца (skill `vm-deploy`
§5), и подтверждение теперь нужно не по ветке отказа, а на первом же прогоне.

**Why ветвь применяется только на новом контейнере.** В старом контейнере строки — источник, и до
задачи 015-27 они остаются нетронутыми.

**Why исходные идентификаторы не регенерируются.** Идентичность строки несёт только её ULID.
Новый идентификатор разорвал бы ссылку `access_audit.target_id` на строку токена.

### MINOR-9 раунда 2 — отклонение от буквы R-8.14 записывается

| Редакция                              | Что говорит                                                       |
| :------------------------------------ | :---------------------------------------------------------------- |
| Текст R-8.14                          | verify-гейт стоит сразу после запуска профиля на новом контейнере |
| UC-6 шаг 5                            | verify снимается, пока профиль ещё остановлен                     |
| Архитектура (`deployment.md` §10.9.4) | следует UC-6                                                      |

**Why архитектура следует UC-6.** Первая и вторая проверки сравнивают две базы. Писатель, работающий
во время сверки, делает числа несравнимыми: расхождение читалось бы как недокопированное.

**Why отклонение записывается, а не исполняется молча.** Две редакции одного требования, из которых
исполняется одна, — это решение, а не деталь. Форма записи уже применена к R-8.10 в записи `WI-62`.

**Why ссылка ставится из §10.9.7.** Раздел удаления — единственное место, где порядок шагов
становится необратимым, и его читатель обязан видеть, какой редакции следовал verify.

### Что задача не делает

- не удаляет ни одной строки и ни одной таблицы в старом контейнере (задача 015-27)
- не снимает verify-гейт (задача 015-24)
- не переключает `ONCHAIN_STATE_PG_URL` и не запускает профиль (задача 015-25)
- не снимает артефакт отката (задача 015-26)
- не переносит `provider_buckets`

<!-- contract:tests -->

## Тест-кейсы

### SQL-замеры на VM

1. **TC-VM-01:** все писатели остановлены до первого переноса
   - Входные данные: `pg_stat_activity` старого контейнера под ролью состояния; признак активности
     обоих воркфлоу на инстансе n8n
   - Ожидаемый результат: ноль строк; оба воркфлоу неактивны; оба замера сняты до первой команды
     копии
   - Падает при мутации: замер соединений снят, признак активности воркфлоу не снят
2. **TC-VM-01a:** допущение о рычаге `onchain-error-alert` проверено зондом
   - Входные данные: одноразовый воркфлоу-зонд по рецепту из раздела допущения, прогон в режиме
     `production` при деактивированном `onchain-error-alert`
   - Ожидаемый результат: новой строки `alert.delivered` в `onchain.diagnostics` старого контейнера
     не появилось; зонд удалён после прогона
   - Если строка появилась: допущение ложно — записать это, довести копию `onchain.diagnostics` до
     согласованности через `TRUNCATE` плюс полный повтор и предупредить задачу 015-24 о причине
     расхождения
3. **TC-VM-02:** порядок по внешним ключам соблюдён
   - Входные данные: перенос `api_tokens` при пустых `users`
   - Ожидаемый результат: Postgres отвергает вставку по внешнему ключу; порядок из таблицы выше
     этого случая не создаёт
4. **TC-VM-03:** число строк снято по каждой из одиннадцати таблиц на обеих сторонах
   - Ожидаемый результат: пары чисел записаны; сверка — задача 015-24
5. **TC-VM-04:** идентификаторы сохранены
   - Входные данные: N случайных `id` из каждой таблицы старого контейнера
   - Ожидаемый результат: те же значения найдены в новом контейнере
6. **TC-VM-05:** `token_hash` совпадает байт-в-байт
   - Ожидаемый результат: равенство строк на выборке `api_tokens`; длина 64 сохранена
7. **TC-VM-06:** `provider_buckets` в новом контейнере пуста
   - Ожидаемый результат: ноль строк; таблица существует
8. **TC-VM-07:** `client_usage` в новом контейнере пуста
   - Ожидаемый результат: ноль строк; шаг переноса исполнен и не нашёл источника
9. **TC-VM-08:** повтор копии `access_audit` без `TRUNCATE` ОТВЕРГАЕТСЯ, а не удваивает строки
   - Ожидаемый результат по замыслу задачи: число больше ожидаемого, ветвь «больше ожидаемого»
     verify-гейта
   - **Измерено 2026-08-31: результат другой.** Повтор отвергнут первичным ключом
     `access_audit_pkey`, код возврата 3, число строк и `md5` содержимого не изменились. Ветвь
     «больше ожидаемого» плоским повтором конвейера недостижима
   - Что это меняет: `TRUNCATE` нужен не против удвоения, а против строк, лежавших в целевой
     таблице до копии — см. §«`access_audit` — копия одноразовая»
10. **TC-VM-09:** ноль сирот по `api_tokens.id` после копии

- Входные данные: явный `SELECT` по `request_trace.principal_id` и `access_audit.target_id`
- Ожидаемый результат: ноль по обеим строкам результата

### Регрессионные тесты

- Кода задача не меняет. `pnpm test` и `pnpm typecheck` прогоняются как контроль неизменности.

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] Все четыре писателя переносимых таблиц остановлены до первого переноса строки: сетевой
      профиль, `onchain-verify`, `onchain-error-alert`, `onchain-retention` — окно без записи в
      старый контейнер
- [ ] Постусловие снято двумя замерами: ноль соединений роли состояния И ни один из трёх воркфлоу
      не активен
- [ ] Рычаг зонда доказан контрольным прогоном при ВКЛЮЧЁННОМ `onchain-error-alert` до основного
      прогона; оба исхода записаны числами
- [ ] Молчание канала оповещений на время окна записано уступкой с началом и концом окна;
      объявленная граница — 24 часа, ожидаемая длительность 12–22 часа названа суммой оценок
- [ ] Допущение о рычаге остановки `onchain-error-alert` записано как неизмеренное, проверено зондом
      до первой команды копии и его исход записан; при ложном допущении копия `onchain.diagnostics`
      повторена через `TRUNCATE`
- [ ] Строки переносятся для двенадцати таблиц из тринадцати; `provider_buckets` исключена,
      `api_tokens` переносится вместе с остальными; перец остаётся в `.env` сервера и контейнером не
      переносится
- [ ] Порядок переноса соблюдает внешние ключи: `users` и `access_profiles` до `api_tokens`,
      `providers` до `cache_entries`, `usage` и `usage_window`
- [ ] Исходные идентификаторы сохраняются без регенерации
- [ ] Копия `access_audit` одноразовая; повторный прогон идёт через `TRUNCATE` плюс полный повтор в
      одной транзакции
- [ ] `access_profiles` не копируется: её строка посеяна DDL на обеих сторонах и сверена по
      значениям, а не по числу строк
- [ ] `onchain-retention` внесён в список реактивации задачи 015-33 вместе с двумя остальными
- [ ] MINOR-9 — отклонение архитектуры от буквы R-8.14 записано: verify-гейт стоит до запуска
      профиля, и причина названа
- [ ] Перенос идёт конвейером через stdin в `docker exec -i`, без файла внутри контейнера
- [ ] UC-6 шаги 3 и 4 исполнены и записаны

## Примечания

Задача обратима: строки в старом контейнере остаются на месте до 015-27, единственной необратимой
операции этапа.

`request_trace` ссылается на `providers` только колонкой `vendor_provider`
(`sql/migrations/002_t014_network_profile.sql:192`), и она допускает пустое значение. Порядок всё
равно ставит `providers` первой: непустое значение отвергается без цели.

Реактивация обоих воркфлоу принадлежит задаче 015-33: до неё запрос `Verify query` читает
`onchain.diagnostics` в старом контейнере, а писатель уже переведён.

Четыре проверки verify-гейта названы задачей 015-24. Числа строк и границы времени, снятые здесь, —
её вход.

Риск задачи назван планом: числа строк не измерены, потому что dev-VM была недоступна. Снимает риск
verify-гейт, а не оценка.
