# Задача 014.34: сдача этапа

## Связь со сценариями

- UC-1 — n8n вызывает способность по сети
- UC-2 — две сессии делят вендорскую квоту
- UC-3 — оператор запускает локально

<!-- contract:goal -->

## Цель задачи

Свести этап: прогнать полную сьюту и статические гейты, проверить снятие документного долга и
записать, какие критерии закрыты чем.

<!-- contract:changes -->

## Описание изменений

### Изменения в существующих файлах

**Файл `docs/ARCHITECTURE.md`:**

- Поле `Delivered` дополняется T-014 по факту сдачи

**Файл `docs/onchain-analytics/ROADMAP.md`:**

- Строка T-014 получает дату сдачи

### Проверка документного долга R-23

Долг снят в фазе архитектуры. Задача проверяет, что он остался снятым.

| Координата                                             | Что проверяется                                                            |
| :----------------------------------------------------- | :------------------------------------------------------------------------- |
| `ARCHITECTURE.md`                                      | отсутствие строк `manifest of 20 rows`, `a 14th tool`, `thirteen tools`    |
| `ARCHITECTURE.md` поле `Delivered`                     | называет T-013 до сдачи и T-014 после                                      |
| `ARCHITECTURE.md`, `docs/architectures/`, `ROADMAP.md` | Streamable HTTP не объявлен содержимым M6                                  |
| `capability-manifest.ts`                               | докстринг и таблица называют одно число (R-23.7)                           |
| `interfaces.md`, `system-architecture.md`              | рядом с историческими записями о 13 тулах стоит действующее число (R-23.6) |
| `deployment.md` §10.3, `security.md` §7.5.4            | рядом с замером «двенадцать ключей сегодня» стоит действующее число        |

Строка про ключи окружения добавлена при выполнении задачи 014-40. Задача внесла в `EnvSchema`
десять ключей.

С этого момента оба замера описывают прошлое: «declares **twelve** keys today» (§10.3, 2026-08-13) и
«12 keys are declared today» (§7.5.4, 2026-08-12). Слово «today» в обоих говорит обратное.

Замер не переписывается: рядом с ним ставится действующее число, как того требует R-23.6.

**Why исторические записи не переписываются.** Запись эпохи фиксирует, что было верно тогда.
Требование — чтобы рядом стояло действующее число, а не чтобы прошлое было переписано.

### Интервал заглушки закрыт с обеих сторон

Задача 014-32b заводит в `packages/mcp-server/eval/capabilities.mjs` две временные записи
`CAPABILITY_EXCLUSIONS` — `pool.info` и `token.pools`, — каждая с причиной, называющей снимающую
задачу. Снимают их 014-32c и 014-32d.

**Проверка:** после отгрузки обеих логик ни одного из двух ключей в `CAPABILITY_EXCLUSIONS` нет, и
обе способности присутствуют в `CAPABILITY_TOOLS` через свои файлы кейсов.

**Why проверка стоит здесь.** Запись в `CAPABILITY_EXCLUSIONS` делает способность «учтённой» для
`packages/mcp-server/test/eval-capability-coverage.test.ts:48`, `const accounted = new Set<string>([`
без единого кейса ⇒ забытая запись маскирует «тул зарегистрирован и не проверяется ничем». Это
запись M6 памяти: новый законный ответ обязан иметь владельца снятия и внешнюю сверку, иначе
временная запись становится постоянной молча.

### Гейты

- `pnpm test` — полная сьюта; поднятой базы не требует
- **интеграционный набор против живого Postgres** — прогон на dev-VM, результат записан
- `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm format`
- `pnpm --filter @onchain-intel/mcp-server gate --task T-014`
- `scan_register.py` на `docs/TASK.md`, `docs/PLAN.md` и правленых разделах архитектуры

### Интеграционный набор: единственное место, где он прогоняется

Пять задач выводят свои сквозные тесты из прогона сьюты, потому что тем нужна живая база. Пометка
выводит тест из CI и **не называет, где он тогда исполняется**.

**Интеграционный набор прогоняется здесь:** 014-03, 014-18, 014-35, 014-37, 014-39.

**Why эта строка написана так однообразно.** На ней стоит гейт
`packages/mcp-server/test/out-of-suite-tests-have-a-runner.test.ts`. Он сверяет перечень с теми
задачами, которые объявили исключение, и падает в обе стороны: на выведенном из прогона тесте без
исполнителя и на исполнителе, названном для задачи без таких тестов.

**Предложение не подлежит переписыванию ради стиля.** Гейт, ключащийся на прозу, ломается от её
правки, и поломка выглядит как «гейт прошёл».

**Why гейт заведён здесь.** Без него пять задач сдают критерии AC-4, AC-5, AC-41, AC-46 и
постусловия грантов тестами, которые не запускает никто. Это форма утверждения о полноте без
прогона за ним — класс L-13 и L-14, и на этом этапе он воспроизвёлся бы пятикратно.

**Why на dev-VM, а не в CI.** R-21 запрещает сеть в CI, и это ограничение не пересматривается.
Постгрес живёт на dev-VM (`vm-deploy`), туда же применяются миграции этапа.

**Прогон записывается.** Дата, хост, число прошедших и упавших тестов — форма DB-SCHEMA §6, та же,
что у переездов хоста. Заблокированный прогон означает, что этап не сдан.

<!-- contract:tests -->

## Тест-кейсы

### Сквозные тесты

1. **TC-E2E-01:** полная сьюта зелёная; четыре статических гейта чисты (AC-16)
   - Входные данные: прогон без поднятой базы и без сети
   - Ожидаемый результат: зелёная сьюта; ни один тест не потребовал Postgres
     1a. **TC-E2E-01a:** интеграционный набор пяти задач зелёный против живого Postgres
   - Входные данные: dev-VM, схема `onchain` после миграций этапа
   - Ожидаемый результат: тесты 014-03, 014-18, 014-35, 014-37 и 014-39 прошли; дата, хост и счёт
     прошедших и упавших записаны
2. **TC-E2E-02:** живые документы не описывают Streamable HTTP как содержимое M6 (AC-17)
3. **TC-E2E-03:** три строки отсутствуют, поле `Delivered` называет T-013 (AC-17b)
4. **TC-E2E-04:** рядом с исторической записью о тринадцати тулах стоит действующее число (R-23.6)
   - Входные данные: вхождения `13 tools` в `docs/architectures/interfaces.md` и
     `docs/architectures/system-architecture.md` (замер 2026-08-19: три — `interfaces.md:595`,
     `interfaces.md:611`, `system-architecture.md:2738`); действующее число тулов читается как длина
     массива `tools` в `packages/mcp-server/tool-inventory.json`
   - Ожидаемый результат: раздел каждого вхождения называет прочитанное число; текст исторической
     записи не изменён
5. **TC-E2E-05:** докстринг и таблица `packages/core/src/capability-manifest.ts` называют одно число
   (R-23.7)
   - Входные данные: вхождения форм `<число> rows`, `<число> of <число> capabilities` и
     `All <число> routed capabilities` в докстринге файла и в комментариях его таблицы; действующее
     число читается как длина `Object.keys(capabilityManifests)`
   - **Третья форма добавлена 2026-08-19.** Прежние две не ловили строку
     `packages/core/src/capability-manifest.ts:210`, `All 23 routed capabilities`, и она устарела на
     три незамеченной. Гейт, не видящий одну из форм, — это гейт, который зелен над устаревшим числом
   - Ожидаемый результат: каждое найденное число равно прочитанному; числа строк манифеста в тексте
     теста нет

### Регрессионные тесты

- Снимок `tools/list` изменён на два тула — `onchain_pool_info` и `onchain_token_pools` — и на шесть
  необязательных полей `Pool` внутри `onchain_active_pairs`. Всё это один коммит задачи 014-32b, и
  правка обоснована в нём

<!-- contract:acceptance -->

## Критерии приёмки

- [ ] AC-16: сьюта зелёная, `lint`, `typecheck`, `build`, `format` чисты
- [ ] Интеграционный набор пяти задач прогнан против живого Postgres, результат записан
- [ ] AC-17: греп по трём документам чист
- [ ] AC-17b: греп по четырём строкам чист
- [ ] R-23.6: рядом с каждой исторической записью о тринадцати тулах стоит действующее число, текст
      записи не переписан
- [ ] R-23.7: докстринг и таблица `capability-manifest.ts` называют одно число, равное длине
      `Object.keys(capabilityManifests)`
- [ ] Обе временные записи `CAPABILITY_EXCLUSIONS` сняты, обе способности покрыты файлами кейсов
- [ ] Живой гейт пройден, строка в леджере есть
- [ ] Каждый из 53 критериев сопоставлен с закрывшей его задачей

## Примечания

Сборка `dist/` обязана быть свежей до прогона живого гейта. Причина — WI-44: сьюта импортирует
`core` из исходников, а гейт поднимает сервер, резолвящий `core` из собранного `dist/`. На T-013 это
стоило 23 сломанных сетей при 1380 зелёных тестах.
