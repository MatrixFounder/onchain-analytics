# Ревью плана TASK-014 — граница «планирование → исполнение»

> Персистентная запись раундов ревью плана. Раунд без текста предыдущего раунда
> структурно не может закрыть собственный цикл: рецензент не может подтвердить «находка N закрыта»,
> не читая находку N. Файл заведён по требованию раунда 4, пункт 10.

## Раунд 3 — 2026-08-19, вердикт REJECTED

4 BLOCKING, 11 MAJOR, 7 MINOR. Текст отчёта в репозитории не сохранён; единственный уцелевший след
нумерации — `docs/backlog/wi-61-curated-registry-row-mechanism.md`, поле `source`. Это и есть дефект,
ради которого заведён этот файл.

Предмет: расщепление задачи 014-32 на 014-32a/b/c и внесение R-33 и R-34 в спецификацию.

Блокирующие по существу, четыре:

- Stub-First нарушен для двух новых тулов
- подпункт R-33.5 не прослеживался ни в изменение, ни в тест, ни в критерий
- задача сдачи этапа 014-34 утверждала один тул
- список изменений задачи тула был неполон наполовину

## Раунд 4 — 2026-08-19, вердикт REJECTED

1 BLOCKING, 8 MAJOR, 9 MINOR. Чтение привязано к фингерпринту: HEAD `a7d6385`, отпечаток индекса
`45cc577600126140`, отпечаток незакоммиченной дельты `6558578a2dc64663`.

### BLOCKING-1 — механизм интервала заглушки не связывал

Задача 014-32b объявляла защиту через `CAPABILITY_KNOWN_GAPS`, а
`packages/mcp-server/test/eval-capability-coverage.test.ts:48`, `const accounted = new Set<string>([`
собирает множество из `CAPABILITY_TOOLS` и `CAPABILITY_EXCLUSIONS`, и эту карту не читает.
Регистрация двух заглушек оставила бы сьюту красной.

**Закрыто:** интервал заглушки объявляется в `CAPABILITY_EXCLUSIONS`, причина называет снимающую
задачу, 014-34 сверяет отсутствие обеих записей после отгрузки логики.

### Три решения, вынесенные на отдельную оценку

| Решение | Вердикт раунда 4 |
| :--- | :--- |
| Одна заглушка на оба тула | верно; класс задачи занижен — перенесена в «крупные» |
| Смягчение предиката в 014-32c, а не в 014-32a | верно, сверено по коду |
| AC-29 закрывает 014-32c | верно по замыслу, механизм не связывал — см. BLOCKING-1 |

### Инструменты

Раунд 4 установил, что `check_positional_refs.py` **существует** —
`.agent/skills/documentation-standards/scripts/check_positional_refs.py`. Прежний поиск его не нашёл,
потому что `.agent` — симлинк, а `find` без `-L` по симлинкам не идёт. Это класс, уже записанный в
`docs/backlog/wi-39-framework-skill-references-do-not-resolve.md`.

Подмена резолвера собственным чекером признана неадекватной: она осматривала 27 ссылок из 135, и три
дефекта нашлись именно в неосмотренной части.

## Раунд 5 — 2026-08-19, вердикт REJECTED

6 BLOCKING, 8 MAJOR, 11 MINOR — после дедупликации 63 переживших находок шести измерений (8 находок опровергнуты верификаторами и сняты). Чтение привязано к фингерпринту, вычисленному ведущим: HEAD `a7d6385`, отпечаток индекса `00f1482e7b22655f`, отпечаток незакоммиченной дельты `12c2578859fdd956` (раунд 4: `45cc577600126140` / `6558578a2dc64663` — дерево изменилось, что и есть предмет ревью).

**Инструмент.** В отличие от раунда 4, `check_positional_refs.py` в этом раунде **прогнан** ведущим: `python3 .agent/skills/documentation-standards/scripts/check_positional_refs.py --all docs/tasks --json` — 117 документов, 381 ссылка, 99 находок по каталогу, из них на четыре файла `014-32*` приходятся **ноль ошибок** и четыре предупреждения `DRIFT_SUSPECT` (цели правятся тем же изменением). Резолвер осматривает и цели в коде (69 находок с целями `.ts`/`.mjs`), но ссылку `32b:130` он не поймал — цитата стоит на следующей строке после координаты и не образует распознаваемой пары. Зелёный прогон резолвера **не** является доказательством корректности цитат в этих файлах.

### BLOCKING

**BLOCKING-1 — список изменений 014-32b не называет ни одного гейта, который ломает добавляемый маршрут и строка манифеста; сьюта падает на стабовом коммите.**
`docs/tasks/task-014-32b-tool-registration-stub.md:146` добавляет строку `token.pools` в манифест, `:155` — маршрут `{ capability: 'token.pools', adapterIds: ['dexscreener'] }`, а `:201`/`:209` объявляют «сьюта зелёная на заглушках». Проверено чтением кода — красными становятся минимум восемь утверждений в четырёх файлах, ни один из которых в задаче не назван (`grep` по всему файлу задачи: `capability-manifest.test`, `readme-tool-table`, `tool-response-shape`, `ttl-coverage`, `policy.test` — **ни одного вхождения**):

- `packages/core/test/policy.test.ts:733` — `expect(routes).toHaveLength(27);`
- `packages/core/test/capability-manifest.test.ts:70` — `expect(routedCapabilities).toHaveLength(26);`; `:184` — `expect(Object.keys(TTL_SECONDS_BEFORE_THE_MOVE)).toHaveLength(26);` с `toStrictEqual` на всей таблице (`:185-189`), заголовок теста `:183` — «pins all 26, so a TTL edit disguised as a migration fails here», то есть таблица заморожена намеренно; `:664` — `expect(scannedFieldCount(manifestSource)).toBe(29); // 26 deadlineMs + 3 paidLegMs`; `:1108` — `expect(blocks.size).toBe(26);`
- `packages/core/test/ttl-coverage.test.ts:219` — `toStrictEqual(routedCapabilities)`
- `packages/mcp-server/test/readme-tool-table.test.ts:620` — `expect(routed).toHaveLength(26);`; `:412` — `expect(rows.length).toBe(21); // the exact row count of §3.2's table, not a floor` с проверкой полноты §3.2 на `:414-421`
- `packages/mcp-server/test/docs-counts.test.ts:329-348` читает `docs/architectures/system-architecture-chain-normalization.md:1176` — `providers.config.ts` holds **27 routes** over 26 — против живого `routes.length`; предложение не назначено ни одной задаче этапа (`32b` упоминает этот файл только в перечне счётчиков тулов, `:65`)
- `packages/mcp-server/test/docs-counts.test.ts:356-380` (отдельный `it`, «states how many tools take a chain») против `interfaces.md:17` «Seventeen of the twenty tools take a chain» — оба новых тула принимают `chain` (`32b:169`, `32d:72`), нужен переход 17→19, в списке изменений его нет

**Следствие при исполнении:** исполнитель, идущий по списку изменений буквально, получает красную сьюту на коммите, который по контракту Stub-First обязан быть зелёным. Это класс BLOCKING раунда 3 («список изменений задачи тула был неполон наполовину») на большем знаменателе. Что автор знает верное число — доказывается его же артефактом: `docs/tasks/task-014-31-shareable-values.md:25-26` — «На момент планирования их 26; задача 014-32b добавляет 27-ю».

**BLOCKING-2 — счётчик тулов зашит вне восьми инвентарных каналов.**
`32b:73-76` строит план на «восьми инвентарных каналах» (`packages/mcp-server/test/inventory-channels.ts:36`). Вне их лежит `packages/mcp-server/test/tool-response-shape.test.ts:122` — `expect(tools).toHaveLength(20);` над живым реестром; две регистрации дают 22. Знаменатель «восемь» опровергнут записью самого репозитория: `packages/mcp-server/.AGENTS.md:1579-1587`, «Three gates outside the eight also fired», где названы `tool-response-shape.test.ts`, `readme-tool-table.test.ts` и `tools/chain-discoverability.test.ts` (последний требует, чтобы описание каждого принимающего `chain` тула называло `onchain_list_chains` — оба новых тула принимают `chain`). Задача опирается на знаменатель, который в пакете, который она правит, задокументирован как устаревший.

**BLOCKING-3 — предписанное лекарство для гейта числительных не может подействовать, а русский якорь не выражает 22 ни в одном написании.**
`32b:109-112`: «Словарь числительных гейта заканчивается на `twenty` и `двадцать` … Словарь расширяется этой же задачей», критерий приёмки `:210` — «словарь числительных гейта принимает 22». Механизм проверен: `WORDS` объявлен в `docs-counts.test.ts:43` и читается **только** в `toNumber` (`:71-74`), который вызывается на `match[1]` уже успешного регулярного выражения; шаблоны захватывают `(\w+)` (`:169`, `:172`, `:175`, `:211`, `:219`, `:223`), а `\w` — это `[A-Za-z0-9_]`, дефис и пробел не входят. «twenty-two» не захватывается — шаблон просто перестаёт совпадать, утверждение исчезает, и `expect(claims.length).toBeGreaterThanOrEqual(24)` (`:235`) падает. Расширение словаря — недостижимый код. Русская сторона хуже: `:207-210` — `/\*\*([А-Яа-яЁё]+) тулов\*\*, в порядке публикации/g`, одно кириллическое слово, и тот же якорь открывает область перечисления WI-48 на `:276` с сообщением «the region cannot be located … Re-anchor it.». Живой текст — `docs/onchain-analytics/ROADMAP.md:272` `**Двадцать тулов**, в порядке публикации:`. Ни «Двадцать два», ни «22» этот шаблон не удовлетворяют. Правка регулярных выражений и переякоривание намеренно громкой области WI-48 — проектное решение, которое план обязан принять сам; ни одна задача его не несёт.

**BLOCKING-4 — в списке гейтованных документов назван несуществующий файл, а тот, который гейт читает, не назван.**
`32b:86-88`: заголовок «Семь гейтованных документов — каждый называет оба тула», первый пункт — «`README.md` и `packages/mcp-server/README.md`». Глоб `packages/mcp-server/*.md` не даёт совпадений — файла нет. Настоящий седьмой документ — `README.ru.md`: `packages/mcp-server/test/tool-inventory-docs.test.ts:49-57` перечисляет `GATED_DOCUMENTS` как `README.md`, `README.ru.md`, `docs/ARCHITECTURE.md`, `docs/architectures/interfaces.md`, `docs/architectures/functional-architecture.md`, `docs/onchain-analytics/ROADMAP.md`, `packages/mcp-server/.AGENTS.md`, а `inventory-channels.ts:49` формулирует обязательство как «both READMEs». `README.ru` не встречается ни в одном из четырёх файлов `014-32*`. Дополнительно `readme-tool-table.test.ts:170` — `const READMES = ['README.md', 'README.ru.md'] as const;` с `:177` `expect(rows.length).toBe(toolSpecs.length);` на каждый язык и построчным сличением языков `:323-344`. Половинчатость внутри одного файла: `:104` тот же документ пишет «обоих `README`» верно. Отказ громкий и самоназывающий, но канал 3 направлен по несуществующему пути, а пункт приёмки `:202` «Восемь инвентарных каналов пройдены» ставится над красной сьютой.

**BLOCKING-5 — три состояния R-33.5 записываются в модуль, который читатель импортировать не может; носителя третьего состояния в плане нет.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:171-172`: «Матрица покрытия читает это значение: `excluded` даёт отказ, `unverified` — отказ с иным текстом». Та же задача закрывает все пути к этому чтению: `:36-38` кладёт сгенерированный модуль в `packages/core/scripts/` «рядом с единственным потребителем — `sync-chain-registry.ts`; адаптер читает `chain.vendors['dexscreener']` и модуль не импортирует», `:46` — «ничего под `src/` его не импортирует», `:141` — «**Предикат адаптера эта задача не трогает.**». Единственный оставшийся носитель — колонка реестра, и она двусостояночная по типу: `packages/core/src/chain/registry-core.ts:58` — `readonly vendors: Readonly<Record<string, string | null>>;`, с докстрингом `:56-57`, который прямо запрещает нужное: «Vendor NAMING only … Never coverage: conflating the two makes "the vendor has no such chain" indistinguishable from "we never checked" (R-58d)». Матрица — `packages/core/src/chain/coverage.ts:88`, `const computeCovered = (capability: string, chain: ChainInfo): boolean =>`, булева; текст отказа строится без ветви по статусу вендора. Ни `coverage.ts`, ни `errors.ts`, ни трёхсостояночное поле реестра не появляются в списке изменений `32a` (`:33-198`). Прецедент делает обратное: генерируемый модуль Nansen лежит под `src/` и импортируется адаптером (`packages/core/src/adapters/nansen/index.ts:9`). Требование рантаймовое, не документное: `docs/TASK.md:362-363` — «матрица покрытия и текст отказа различают три исхода зонда». TC-UNIT-08 (`32a:221-222`) и чекбокс R-33.5 (`32a:262`) не имеют артефакта, на котором могли бы прогнаться. Это блокирующий пункт раунда 3 («подпункт R-33.5 не прослеживался ни в изменение, ни в тест, ни в критерий»), закрытый по двум ногам из трёх.

**BLOCKING-6 — интервал заглушки не закрывается ничем; проверка, на которой построено закрытие BLOCKING-1 раунда 4, в 014-34 отсутствует.**
`32b:57-59` заносит `pool.info` и `token.pools` в `CAPABILITY_EXCLUSIONS` и утверждает «Записи удаляют 014-32c и 014-32d тем же коммитом»; `:61-64` — обязательный по M6 разбор маскируемого случая с митигацией «а 014-34 сверяет отсутствие обеих записей». Проверено: `grep` по `docs/tasks` даёт `CAPABILITY_EXCLUSIONS` **только** в `task-014-32b`; и `32c` (`:55`, `:142`), и `32d` (`:68`, `:149`) говорят о `CAPABILITY_KNOWN_GAPS`, а `task-014-34-acceptance.md` не содержит ни одного вхождения `CAPABILITY_EXCLUSIONS`. Хуже: `packages/mcp-server/eval/capabilities.mjs:85-93` держит в `CAPABILITY_KNOWN_GAPS` ровно два ключа — `token.metadata` и `pool.info`; строки `token.pools` там нет и ни одна задача её не создаёт, поэтому `32d:68` и чекбокс `:149` неисполнимы по построению. То есть объявленная временной запись M6 не имеет ни владельца снятия в собственных списках изменений исполняющих задач, ни обещанного бэкстопа. Именно предложение `32b:63` записано в этом файле (`:34-35`) как закрытие BLOCKING-1 раунда 4 — закрытие ложно относительно соседнего артефакта. Смягчающее (найдено верификатором): после отгрузки кейсов способность попадает в `CAPABILITY_TOOLS`, и `capabilities.mjs:124` `if (wired.has(capability) || CAPABILITY_EXCLUSIONS.has(capability)) continue;` коротит на первой ветви, так что забытая запись — устаревший текст, а не активная маска, и `run.mjs:489` печатает исключения в каждом прогоне. Блокирующим пункт признан не по рантайму, а по тому, что блокирующая находка предыдущего раунда объявлена закрытой механизмом, которого в плане нет.

### MAJOR

**MAJOR-1 — числа маршрутов и способностей, которые 014-32b велит вписать в манифест, ошибочны на три.**
`32b:150-151`: «Зашитый счёт маршрутов там же: `packages/core/src/capability-manifest.ts:210`, `All 23 routed capabilities` — становится 24 способности и 25 маршрутов». Цитата резолвится: `:212` — ` * All 23 routed capabilities (`providers.config.ts` — 24 routes, 23 distinct capabilities;`. Но она устарела: живой замер — 27 маршрутов над 26 способностями (`system-architecture-chain-normalization.md:1176`, `capability-manifest.test.ts:70`, `policy.test.ts:733`). После задачи истина — 27 способностей и 28 маршрутов, а не «24 и 25». Задача противоречит себе двумя строками выше (`:109`, «26 → 27»). Нарушено «Measure, don't eyeball». Знаменатель — два: та же устаревшая пара стоит в `capability-manifest.ts:74`, и её `32b` не называет (смягчение: `:34` датирована — «**Measured 2026-08-05, re-measured 2026-08-11**», что по доктрине R-23.6 сохраняется, а `:168` — голое настоящее время). TC-E2E-05 задачи 014-34 (`:69-75`) ловит формы `<число> rows` и `<число> of <число> capabilities` — ни `All 24 routed capabilities`, ни `25 routes, 24 distinct capabilities` под них не подходят, то есть свежевписанное неверное число не поймает никто.

**MAJOR-2 — факт «26 строк манифеста» живёт минимум в восьми местах настоящего времени; задача назначает одно.**
`32b:149` правит счёт только внутри `capability-manifest.ts`. Живыми и незакреплёнными остаются `docs/ARCHITECTURE.md:120` («capability manifest carries twenty-six rows.»), `docs/architectures/interfaces.md:618`, `:1372` («Each of the 26 rows carries an explicit value» — нормативная формулировка R-18/AC-13, которую `32b:146` и исполняет), `:1391`, `docs/architectures/system-architecture-call-budget.md:108`, `:139`, а также `packages/mcp-server/src/env.ts:64` и `.env.example:260`. Три из них — датированные замеры и по R-23.6 сохраняются; остальные становятся ложными в день коммита. Ни один гейт их не читает (`capability-manifest.test.ts:46-48` требует лишь `toBeGreaterThan(10)`), то есть расхождение тихое. Класс M3.

**MAJOR-3 — таблица оценки не сходится с собственными строками, и дни выведены из суммы, которой нет.**
`docs/PLAN.md:405-409`: 8×2–3=16–24, 25×3–5=75–125, 7×6–10=42–70, 4×2–4=8–16 → **141–235**, при заявленном итоге **139–230**. Число задач 44 верно. `:411` и `:8` дают «19–33 рабочих дня», а правило округления `:432-433` применено к неверному итогу («139 / 7 = 19,9 → 19; 230 / 7 = 32,9 → 33»); по тому же правилу верная сумма даёт 20–34. `:401` утверждает «Оценка получена сложением задач» — сложением она не получена. Согласуется с переносом 014-32b в класс «крупных» в раунде 4 и прибавкой +2 вместо пересчёта.

**MAJOR-4 — TASK.md несёт доразбиение до расщепления, ссылаясь на PLAN.md как на источник.**
`docs/TASK.md:635-638`: «**137–228 часов, 19–33 рабочих дня** … ([PLAN.md](PLAN.md) §Оценка). Разложение — 44 задачи в четырёх классах: девять стабов по 2–3 часа, двадцать пять реализаций по 3–5, шесть крупных по 6–10 и четыре обязательства и гейта по 2–4»; `:645-646` перечисляет шесть крупных без 014-32b; `:12` повторяет «137–228 часов». PLAN.md теперь говорит восемь стабов (`:405`, `:58`) и семь крупных (`:407`, `:413`, `:423`). Оговорка `TASK.md:12` про «расхождение с источником — §5.2» покрывает расхождение с ROADMAP, а не это. Четыре места, обновлено ноль — спецификация и план разошлись в разложении собственного этапа.

**MAJOR-5 — три критерия, которые 014-32b объявляет частично закрытыми, отсутствуют в карте AC.**
`32b:206-208` объявляет AC-2, AC-11 и AC-13 частично закрытыми. Карта не называет ни одного: `PLAN.md:363` (AC-1, AC-2 → 014-09), `:369` (AC-11 → 014-22), `:371` (AC-13 → 014-31). Соглашение план объявляет о себе сам — `:359`, «Критерии, закрываемые двумя задачами частично, помечены» — и применяет в других местах (`:379` AC-24, `:389` AC-39). Проекция требований при этом обновлена (`:323`, «R-18 … → 014-31, 014-32b») — то есть один и тот же факт поправлен в одной проекции из двух. Карта нагружена на приёмке: `docs/tasks/task-014-34-acceptance.md:136` — «Каждый из 53 критериев сопоставлен с закрывшей его задачей».

**MAJOR-6 — гейт перечисления имён WI-48 — отдельный читатель, и он не назван.**
`docs-counts.test.ts:260` — `it('lists the tool NAMES correctly wherever a document enumerates them (WI-48)')` с тремя ограниченными областями (`:273-303`: таблица ROADMAP, список `**The … tools today:**` в `.AGENTS.md`, узел C4 `MCP["MCP server @onchain-intel/mcp-server — \d+ tools` в `functional-architecture.md`) и сличением `toStrictEqual(actual)` на `:321-325`. `32b:86-93` трактует «называет тул» и «перечисляет тулы» как одно обязательство. Узлу C4 нужны голые имена без префикса `onchain_`, разделённые `·` (экстрактор `:295-301`) — формы, которой список изменений не описывает; обновление одного лишь `— NN tools` (`32b:104`) оставляет утверждение области красным. Смягчение: `inventory-channels.ts:53-57` — собственный объявленный канал репозитория для этого файла — тоже опускает обязательство перечисления, то есть задача добросовестно отражает неполный источник.

**MAJOR-7 — переписываемая причина в `CAPABILITY_KNOWN_GAPS` недостижима из-за записи, которую добавляет та же задача.**
`32b:126` («причина строки `pool.info` переписывается под заглушку») стоит строкой ниже `:125`, добавляющего обе способности в `CAPABILITY_EXCLUSIONS`. `capabilities.mjs:124` коротит на `CAPABILITY_EXCLUSIONS.has(capability)` до чтения `CAPABILITY_KNOWN_GAPS.get(capability)` (`:131`), поэтому исключённая способность до поиска причины не доходит и строку `no-probe` не печатает вовсе. Предписанная правка — мёртвый текст, а исключение дополнительно убирает честную строку пробела из живых отчётов eval. `accountedCapabilities()` при этом не имеет ни одного импортёра (`grep` по `packages/`).

**MAJOR-8 — запись ревью не может нести вердикт о закрытии: 35 находок из 40 в ней не сохранены.**
`docs/reviews/plan-014-review.md:3-5` формулирует собственное правило («Раунд без текста предыдущего раунда структурно не может закрыть собственный цикл»), затем `:9-20` фиксирует раунд 3 как «4 BLOCKING, 11 MAJOR, 7 MINOR. Текст отчёта в репозитории не сохранён» с четырьмя однострочными пересказами, а `:24-53` — раунд 4 как «1 BLOCKING, 8 MAJOR, 9 MINOR» с полным текстом только BLOCKING-1. Текст уцелел для 5 находок из 40; единственный след утраченной нумерации — `docs/backlog/wi-61-curated-registry-row-mechanism.md:9`, `source: 'plan-review TASK-014, MAJOR-3, 2026-08-19'`. Понижено с BLOCKING: неполнота записи запрещает **утверждать** закрытие непрочитанного, но не запрещает независимое полное перечтение текущих артефактов, что раунд 5 и сделал. Следствие — ограничение формулировки вердикта, см. раздел о закрытии.

### MINOR

1. `docs/tasks/task-014-14-principal-in-context-stub.md:119` — «Задача 014-32b добавляет `onchain_pool_info`, задача 014-32c — `onchain_token_pools`, после чего их 22»: после расщепления обе регистрации в 014-32b (`32b:11-13`, `:78-81`), а `32c:132` объявляет снимок `tools/list` неизменным. Итог 22 верен, неверна атрибуция; исправленная версия уже лежит в коде — `packages/mcp-server/test/tool-inventory-docs.test.ts:150`.
2. `docs/tasks/task-014-22-tool-input-host-gate.md:111` — «Задачи 014-32b и 014-32c добавляют тулы». Половинчатость внутри одного файла: `:108` уже поправлена («тул `pool.info` задачи 014-32b»).
3. `docs/tasks/task-014-25-failure-classes-stub.md:43-44` («включая тул, добавляемый задачей 014-32b», ед. ч.) и `docs/tasks/task-014-15-principal-interception.md:61-62` («двадцать первым тулом задачи 014-32b»). Блокирующий раунда 3 «014-34 утверждала один тул» исправлен в 014-34 (`:119-121`) и не исправлен в двух сиблингах — знаменатель 3, применено 1.
4. `docs/PLAN.md:269` — «Требования: R-21, R-34» при `:323` «R-18 … → 014-31, 014-32b» и реальной работе по R-18 в задаче (`32b:146-147`, TC-UNIT-06 `:184`).
5. `docs/PLAN.md:268` — 014-32b, файл которой называется `task-014-32b-tool-registration-stub.md` и которая по `:11-13` буквально соответствует определению `PLAN.md:57-59`, не помечена `[STUB]`; `grep` даёт ровно восемь маркеров (`:104`, `:124`, `:134`, `:152`, `:178`, `:192`, `:206`, `:216`), при том что преемники несут `[LOGIC]` (`:272`, `:276`). Добавление маркера потребует «восемь» → «девять».
6. `docs/PLAN.md:273` и `:277` — «Зависимости: 014-32a, 014-32b», тогда как `32c:19` и `32d:18` объявляют дополнительно 014-22. Ребро удовлетворяется транзитивно через 014-32b, порядок исполнения не страдает.
7. `docs/tasks/task-014-32b-tool-registration-stub.md:130-131` — цитируется `packages/mcp-server/test/eval-checks-coverage.test.ts:70`, `const invoked = new Set<string>([`; на `:70` стоит `it('checks every tool the eval actually invokes', () => {`, объявление на `:71` и в форме `const invoked = [`. Процитированный референт в файле не встречается вовсе — это форма соседней ссылки (`eval-capability-coverage.test.ts:48`, `const accounted = new Set<string>([`, процитированной в `32b:49-50` верно). Существо вывода при этом верно, и резолвер эту пару не ловит.
8. `docs/tasks/task-014-32a-dexscreener-chain-coverage.md:270` — «Число в `interfaces.md` строках 773-774» против `:185` того же файла «Строки 774-775 … `Coverage is three chains … 3 of 458`». В цели `773` пуста, `3 of 458` — на `775`. Один факт, две координаты в одном документе.
9. `docs/tasks/task-014-34-acceptance.md:104-105` — координаты `interfaces.md:600` и `:616` для литерала `13 tools`, который стоит на `:601` и `:617` (третья, `system-architecture-mcp-server-package.md:29`, верна). Смягчение: заявленный вход шага — грепаемые «вхождения `13 tools`», а координаты помечены как «замер 2026-08-13».
10. `packages/mcp-server/test/tool-inventory-docs.test.ts:149-153` — половина фразы заменена новой клаузой про 014-32b/014-32d, хвост оставлен: строка «// across every DEX on a chain or across chains. DESIGNED in …» осталась без подлежащего. Соседние записи (`:105-108`, `:115-122`) — целые предложения, то есть это остаток правки, а не стиль.
11. `docs/PLAN.md:9` — «| Редакция | 4, после третьего ревью границы фазы 2026-08-19 |» при завершённом раунде 4, чьи исправления и составляют рассматриваемую дельту; `docs/TASK.md:15` независимо пишет «план 4» о числе раундов.

### Вердикт

**REJECTED.** Шесть блокирующих переживают, и три из них — прямое воспроизведение блокирующих раундов 3 и 4 на новом знаменателе: список изменений 014-32b по-прежнему неполон, но теперь количественно измеримо — маршрут и строка манифеста, которые задача добавляет, роняют минимум восемь утверждений в четырёх тестовых файлах и двух гейтованных таблицах, ни один из которых в задаче не назван, а её собственный критерий приёмки объявляет сьюту зелёной на стабах; предписанное лекарство для гейта числительных не может подействовать, потому что шаблон захватывает `(\w+)` и до словаря дело не доходит, а русский якорь области WI-48 не выражает 22 ни в одном написании, то есть Stub-First-фаза не имеет исполнимого выхода; третье состояние R-33.5 записывается в модуль, который по прямому запрету той же задачи никто под `src/` не импортирует, при том что колонка реестра двусостояночная по типу и её докстринг (`registry-core.ts:56-57`) запрещает вкладывать в неё покрытие — значит, закрытие R-33.5 требует не исполнения, а проектного решения о том, где живёт третье состояние; и наконец, интервал заглушки, которым раунд 4 закрыл свой единственный BLOCKING, не снимается ни одной из двух названных задач и не проверяется задачей 014-34, в которой соответствующей проверки просто нет. Крупные находки по отдельности восстановимы внутри исполнения, но вместе они означают, что план ошибается о мире в измеримых величинах — итог таблицы оценки не равен сумме её строк, спецификация и план дают разное разложение этапа, а числа, которые исполнителю велено вписать в манифест, устарели на три на день написания, — то есть исполнение по этому плану строило бы не то и сверяло бы себя не с тем. Граница «планирование → исполнение» не пересекается.

### Закрытие находок раундов 3 и 4

Проверяемые (текст сохранён) — раунд 3, четыре блокирующих:

- **«Stub-First нарушен для двух новых тулов»** — **НЕ ЗАКРЫТ**. Фаза формы заведена (014-32b), но её контракт неисполним: BLOCKING-1, BLOCKING-2, BLOCKING-3.
- **«подпункт R-33.5 не прослеживался ни в изменение, ни в тест, ни в критерий»** — **ЗАКРЫТ НАПОЛОВИНУ**. Тест (TC-UNIT-08) и критерий появились; ноги «изменение» нет — BLOCKING-5.
- **«задача сдачи этапа 014-34 утверждала один тул»** — **ЗАКРЫТ** в 014-34 (`:119-121`), с остатком в двух сиблингах (MINOR-3).
- **«список изменений задачи тула был неполон наполовину»** — **НЕ ЗАКРЫТ**, BLOCKING-1 и BLOCKING-4.

Раунд 4:

- **BLOCKING-1 «механизм интервала заглушки не связывал»** — **ЗАКРЫТ НАПОЛОВИНУ**. Перенос в `CAPABILITY_EXCLUSIONS` выполнен и механически верен (`eval-capability-coverage.test.ts:48-51` собирает `accounted` именно из `CAPABILITY_TOOLS` и `CAPABILITY_EXCLUSIONS`), но записанное закрытие (`plan-014-review.md:34-35`) опирается на проверку в 014-34, которой в 014-34 нет, а снятие записей не значится ни в одном списке изменений преемников — BLOCKING-6.
- **Три вынесенных решения** — по существу подтверждаются: одна заглушка на оба тула, смягчение предиката в 014-32c, AC-29 закрывает 014-32c. Первое подорвано отсутствием маркера `[STUB]` и неполнотой списка изменений.
- **Инструментальный пункт про `check_positional_refs.py`** — **ЗАКРЫТ**: резолвер существует и в этом раунде прогнан ведущим (см. врезку «Инструмент»), подмена собственным чекером не применялась.

Не поддаются проверке, потому что их текст не был записан: **11 MAJOR и 7 MINOR раунда 3** и **8 MAJOR и 9 MINOR раунда 4** — всего 35 находок из 40. Раунд 5 о них ничего не утверждает: ни что они закрыты, ни что открыты. По правилу `plan-014-review.md:3-5` это структурное свойство записи, а не результат чтения (MAJOR-8). Настоящий раунд компенсирует его независимым полным перечтением текущих артефактов, но восстановить утраченные формулировки не может — сверка «находка N закрыта» для этих 35 невозможна ни в этом раунде, ни в любом следующем.

## Раунд 6 — 2026-08-19, вердикт REJECTED

4 BLOCKING, 5 MAJOR, 7 MINOR — после дедупликации 34 переживших находок шести измерений (10 находок опровергнуты верификаторами и сняты, две находки понижены верификаторами с BLOCKING до MAJOR). Чтение привязано к фингерпринту из брифа: HEAD `a7d6385`, незакоммиченное дерево ветки `feat/t-014-network-transport-planning`, 23 пути, отпечаток отслеживаемой дельты `00061172c729faaf`, отпечаток неотслеживаемых файлов `3b27c166f8138cbe` (раунд 5: `00f1482e7b22655f` / `12c2578859fdd956` — дерево изменилось, корректирующий проход и есть предмет этого раунда).

**Инструмент.** `check_positional_refs.py` в этом раунде **не прогнан**: ни у одного из шести измерений и ни у ведущего нет исполняющего инструмента в этой роли. Все координаты ниже проверены ручным чтением цитируемых диапазонов; для четырёх файлов `014-32*`, `014-34` и `PLAN.md` проверены **все** пары «координата + референт», и, за двумя исключениями (MINOR-3), референт буквально стоит на цитируемой строке. Арифметика, перестроенная корректирующим проходом, сходится: строки таблицы оценок дают 141–235 часов, 44 задачи в разбивке 8/25/7/4, правило округления 141/7→20 и 235/7→34 применено к заявленному итогу, `TASK.md:12`/`:636-638` и `PLAN.md:8`/`:408-414`/`:437-438` согласованы, маркеров `[STUB]` ровно девять (`PLAN.md:109,129,139,157,183,197,211,221,273`) против класса стоимости из восьми, и это расхождение объяснено на `:61-64`.

**Главный итог раунда.** Корректирующий проход закрыл 20 из 25 находок раунда 5 и **снял основное проектное препятствие**: третье состояние зонда получило носителя (сгенерированный модуль под `src/`), хук (`chainProbeStatus`), функцию-читателя (`coverageStatus`) и носителя текста (`hint`). Решение владельца по B-5 внутренне когерентно в трёх частях из четырёх. Но там, где раунд 5 требовал **полноты перечисления**, проход снова применил правку к части знаменателя: перечень «одиннадцать утверждений в шести файлах» не содержит трёх независимо выведённых утверждений, а переякоривание области WI-48 выполнено для одного якоря из двух, которые ломает предписанная самой задачей правка. Это тот же класс M3, за который раунды 3 и 5 уже отклоняли план, и он снова даёт красную сьюту на коммите, критерий приёмки которого — «сьюта зелёная на заглушках».

### BLOCKING

**BLOCKING-1 — гейт отношения ENFORCEMENT в манифесте не назван ни одной задачей; сьюта красна на стабовом коммите.**
`docs/tasks/task-014-32b-tool-registration-stub.md:353-354`: «Каждый гейт из раздела «Гейты вне восьми инвентарных каналов» зелёный — **одиннадцать утверждений в шести файлах плюс два документных числа**». Список манифестных мест закрыт явно на `:236`: «два места самого манифеста, названные выше: `:117` и `:236`».

Противоречащая улика. `packages/core/src/capability-manifest.ts:95`:
`* - **26 of 26 capabilities are therefore actually bounded by their row below.** The three added`
Это предложение читается гейтом: `packages/core/test/capability-manifest.test.ts:1174` — `it('the two ratios in the ENFORCEMENT prose are the ones this run measured', () => {`, извлечение на `:1176` — `const capabilityClaim = /\*\*(\d+) of (\d+) capabilities are therefore actually bounded/.exec(`, утверждение на `:1191-1194` — `expect([Number(capabilityClaim![1]), Number(capabilityClaim![2])]).toStrictEqual([measuredEnforced, blocks.size,]);`. `blocks.size` строится `manifestRowBlocks` по каждой строке манифеста и сегодня запинен на 26 (`:1108`, `expect(blocks.size).toBe(26);`) ⇒ с новой строкой `token.pools` становится 27 безусловно. `measuredEnforced` считает способности, все адаптеры которых читают дедлайн; `token.pools` маршрутизируется в `dexscreener`, который входит в измеренный список `aware` (`:1198-1209`) ⇒ тоже 27. Ожидается `[27, 27]` против отгруженного `[26, 26]`.

Знаменатель: `grep` по всем файлам `task-014-3*` даёт `capability-manifest.ts` только на `32b:207` (`:117`), `32b:210` (`:236`), `32b:212` (`:210`) и `32b:218` (`:74`); строка `:95` не названа нигде. Отягчающее: `docs/TASK.md:269-270` называет `:95` поимённо в R-23.7 как место, обязанное совпадать с длиной манифеста, — факт был записан выше по течению и потерян в корректирующем проходе. `TC-E2E-05` задачи 014-34 (`docs/tasks/task-014-34-acceptance.md:126-128`) ловит форму `<число> of <число> capabilities`, но 014-34 идёт **после** 014-32b и коммит 014-32b зелёным не удержит.

**Следствие при исполнении:** исполнитель, идущий по списку изменений буквально, отгружает стабовый коммит с красным `capability-manifest.test.ts` и ставит галочку, утверждающую полноту перечисления.

**BLOCKING-2 — таблица `deadlineMs`/`paidLegMs` в архитектуре — вторая проверка полноты по маршрутизируемым способностям; ни утверждение, ни правка документа не назначены.**
`docs/tasks/task-014-32b-tool-registration-stub.md:280-286` называет из `readme-tool-table.test.ts` строки `:620` (`expect(routed).toHaveLength(26);`) и `:412` (таблица §3.2), а на `:353-354` объявляет ровно **два** документных числа: `interfaces.md:17` и `system-architecture-chain-normalization.md:1176`.

Противоречащая улика. `packages/mcp-server/test/readme-tool-table.test.ts:600-604`:
```
  for (const capability of routed) {
    if (!documented.has(capability)) {
      defects.push(`${capability}: the manifest gives it a deadlineMs the table never documents`);
```
и утверждение `:622-627` — `expect(deadlineDefects(markdown, routed, capabilityManifests), …).toStrictEqual([]);`. `routed` выводится из живого реестра (`:610`, `const routed = [...new Set(routes.map((route) => route.capability))];`) ⇒ содержит `token.pools`. `documented` наполняется только из строк таблицы `docs/architectures/system-architecture-call-budget.md:320-328`; её первая строка данных `:246` перечисляет пятнадцать идентификаторов поимённо, от `token.price` до `protocol.incidents`, и `token.pools` в ней нет. Восьмой строкой это чинить запрещено: `:347` — `expect(deadlineRows(markdown, routed).length).toBe(7); // the exact row count, not a floor`. Единственная зелёная правка — расширить перечень на `:246`, и её не назначает ни одна задача плана (`grep` по четырём `014-32*` на `system-architecture` даёт `:93`, `:193` → `:108`, `:194` → `:139`, `:245` → `:1176`).

Это **другая** таблица, чем TTL-таблица §3.2, которую задача корректно назначает на `:282-284`, — тот же тестовый файл, два гейта полноты, назначен один.

**Следствие при исполнении:** красный `readme-tool-table.test.ts` со строкой дефекта, буквально называющей `token.pools`, на коммите, обязанном быть зелёным.

**BLOCKING-3 — у замороженной TTL-таблицы два читателя; именованное исключение прописано для одного.**
`docs/tasks/task-014-32b-tool-registration-stub.md:288-293` называет два утверждения — `capability-manifest.test.ts:184` и `ttl-coverage.test.ts:219` — и формулирует средство в единственном числе (`:299`, «Сравнение получает явный перечень способностей, заведённых после переезда»).

Противоречащая улика. `packages/core/test/ttl-coverage.test.ts:239` — отдельный `it` в другом блоке (`it('is unreachable for the 26 routed capabilities — enumerated, not asserted in general', () => {`), читающий тот же замороженный литерал:
```
    const fallingThrough = routedCapabilities.filter(
      (capability) => !Object.keys(TTL_FOR_BEFORE_THE_MOVE).includes(capability),
    );
    expect(fallingThrough).toStrictEqual([]);
```
(`:243-246`). `routedCapabilities` выводится из живых `routes` (`:91`) ⇒ с новым маршрутом массив становится `['token.pools']` и утверждение падает — причём под заголовком про провал в `DEFAULT_TTL_SECONDS`, то есть отказ указывает не на ту проблему. Сразу за ним `:247-249` — `expect(explicitRows.has(capability), capability).toBe(true);` — падает по той же причине. `grep` по `docs/tasks` подтверждает: `ttl-coverage.test.ts` упомянут в задачах ровно один раз, на `32b:292`.

**Следствие при исполнении:** красная сьюта; исполнитель, применивший исключение буквально там, где сказано, увидит отказ, чей текст уводит от прописанного решения.

**BLOCKING-4 — предписанное расширение числительных ломает якорь области WI-48, который задача не переякоривает: якорей два, обработан один.**
`docs/tasks/task-014-32b-tool-registration-stub.md:137-139` расширяет захват «во всех шаблонах числительных», перечисляя восемь координат `docs-counts.test.ts` — `:169`, `:170`, `:172`, `:175`, `:211`, `:219`, `:223`, `:377`. Перечисление восьми проверено и **полно и верно**: это ровно все захватывающие шаблоны счёта тулов в файле. Переякоривание прописано на `:143-145` для одной координаты — `:276` — и критерий приёмки `:364-365` стоит в единственном числе: «якорь области WI-48 переякорен».

Противоречащая улика. `packages/mcp-server/test/docs-counts.test.ts:282`:
`        from: /\*\*The \w+ tools today:\*\*/,`
— это вход второй из трёх областей перечисления имён (`:270-303`), читающий `packages/mcp-server/.AGENTS.md:35`: `**The twenty tools today:** \`onchain_ping\`, …` — **слово**, не цифра. Задача сама ставит парный **захватывающий** шаблон этого же предложения (`:211`, `...claimsWithSource('packages/mcp-server/.AGENTS.md', /\*\*The (\w+) tools today:\*\*/g),`) в список расширения, то есть намеренно ведёт это предложение к дефисной форме. `\w` — `[A-Za-z0-9_]` (задача констатирует это сама на `:151`), `twenty-two` он не покрывает, а у якоря `:282` нет группы захвата ⇒ расширение шаблонов до него не дотягивается.

Отказ громкий и проверен по коду: `docs-counts.test.ts:308-312` — `` `${region.file}: the enumeration's opening no longer matches ${String(region.from)} — the ` + `region cannot be located, so this gate would check nothing. Re-anchor it.` ``. Критерий приёмки `:366` («Три области перечисления имён содержат оба тула») при этом невыполним.

Смягчение, которое **не** снимает находку: исполнитель может написать `**The 22 tools today:**` — цифру `\w` покрывает. Но этот путь план не предписывает, он противоречит собственному решению задачи о дефисной/двухсловной форме (`:364-365`) и дому стиля (`docs/ARCHITECTURE.md:52`, `capability manifest carries twenty-six rows.`). Красное следует из следования плану.

### MAJOR

**MAJOR-1 — плечо «текст отказа» подпункта R-33.5 не имеет файла-исполнителя: единственное место построения отказа не входит ни в один список изменений.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:202-205`:
```
**Файл `packages/core/src/chain/errors.ts`:**

- Отказ по `unverified` называет отсутствие зонда, отказ по `excluded` — отсутствие сети у вендора.
  Носитель уже есть, добавлять поле не нужно: `packages/core/src/chain/errors.ts:111`, `hint?: string;`
```
Проверено: `packages/core/src/chain/errors.ts:109-111` объявляет `hint?: string;` с комментарием «Appended verbatim», а `:129` только конкатенирует его — `(details.hint ? \` ${details.hint}\` : '')`. Значит, запись в списке изменений — **правка нулевого объёма**, а выбор между `excluded` и `unverified` обязан сделать вызывающий.

Единственный вызывающий, строящий обобщённый отказ по паре (способность, сеть), — `packages/core/src/adapters/registry.ts:916` (`if (chainInfo && !this.getCoverage().isCovered(capability, chainInfo)) {`), `:922-928`:
```
      throw new CapabilityNotCoveredOnChainError({
        capability,
        chain: chainInfo.slug,
        availableChains: served.slugs,
        totalServedChains: served.total,
        availableCapabilities: coverage.capabilitiesFor(chainInfo),
      });
```
— `hint` не передаётся. `grep` по четырём файлам `014-32*` на `registry.ts` даёт только `packages/core/scripts/sync-chain-registry.ts` и `registry.data.json`; `packages/core/src/adapters/registry.ts` не назван нигде (его называют лишь 014-30 `:90` и 014-39 `:245-246`). Правка адаптера как площадка закрыта самой задачей: `32a:153-154` — «Это **единственная** правка адаптера в этой задаче».

Внутреннее напряжение: `32a:197` утверждает «ни один существующий вызывающий не меняет поведения», тогда как плечо «текст отказа» требует ровно того, чтобы существующий вызывающий изменил вывод.

**Почему MAJOR, а не BLOCKING (понижение относительно раунда 5).** Решение, на котором блокировал раунд 5, принято: носитель, хук, функция статуса и носитель текста названы. Ничего не становится красным. `TC-UNIT-08` (`32a:265-266`) и чекбокс `32a:306` заставляют исполнителя дойти до этого места, а один `grep` по `CapabilityNotCoveredOnChainError` его находит. Восстановимо внутри исполнения.

**MAJOR-2 — умолчание `excluded` для адаптера без хука утверждает вендорский факт, которого репозиторий не знает; это R-58d на двенадцати адаптерах.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:207-210`:
```
**Адаптер без хука отвечает `excluded`, и это не нарушение R-58d.** Покрытие `nansen` выведено из
вендорского OpenAPI-манифеста, который перечисляет обслуживаемое; отсутствие сети в таком перечне —
улика исключения, а не отсутствие зонда.
```
Семантика значения зафиксирована двумя строками выше — `:204`, «отказ по `excluded` — отсутствие сети у вендора». `:196-199` делает булев предикат проекцией новой трёхзначной функции ⇒ умолчание достаёт **каждого** адаптера, а не только nansen. Предикат объявляют двенадцать модулей (rpc-evm, rpc-solana, coingecko, blockscout, blockchain-info, defillama, nansen, dexscreener, dash-platform, platform-explorer, pg-history, dune), а не один.

Противоречащие улики:
- `packages/core/src/adapters/rpc-evm/index.ts:147-153` — `return (chain.family === 'evm' && chain.rpcHosts !== null && chain.nativeSymbol !== null && chain.nativeDecimals !== null);` против `packages/core/src/chain/registry-core.ts:56-57` — «Vendor NAMING only … Never coverage: conflating the two makes "the vendor has no such chain" indistinguishable from "we never checked" (R-58d)». `rpcHosts: null` — наша кураторская дыра, вендора в картине нет.
- `packages/core/src/adapters/dune/index.ts:25` — `chainSupport: (): boolean => false,` — сплошной отказ уровня адаптера в M1; под `:204` он рендерится как «у вендора нет этой сети», что ложно про каждую сеть, которую Dune реально обслуживает.
- `packages/core/src/adapters/coingecko/index.ts:107` — `chain.vendors['coingecko'] != null`, и 142 строки реестра несут `"coingecko": null`, включая те, которые генератор оставил `null` намеренно (`sync-chain-registry.ts:349-351`, `:358` — «Better no vendor id than a confidently wrong one»).
- `docs/architectures/data-model-entities.md:197` — «the absence of a probe means **`unverified`, not `unsupported`** (R-58d)», в документе, который эта же задача правит (`32a:222-226`).

Обоснование через nansen к тому же неверно про код: `packages/core/src/adapters/nansen/index.ts:160` — `if (!hasAddressValidator(chain.family)) return false;` — третий конъюнкт, отбрасывающий сети, которые манифест перечисляет; адаптер сам это записывает на `:151-153` («Dropped: `bitcoin`, `near`, `sei`, …»), а `packages/core/src/adapters/nansen/chain-coverage.ts:66` несёт `'bitcoin',` под `entity.labels`.

Пункт M6 самой задачи (`32a:212-216`) разбирает только новый ответ `unverified` и не разбирает новоутверждаемый `excluded` — а именно он делает вендорское заявление.

**Следствие при исполнении:** отказ на сети, которую мы просто не курировали, понесёт утверждение, что вендор её не обслуживает, — класс L-18, переустановленный задачей, которая его чинит. AC-51 (`32a:309-310`) ограничен dexscreener и это не поймает. MAJOR: сьюта не краснеет, средство — сузить умолчание (например, `unverified`, пока хук не сказал иного), восстановимо в исполнении.

**MAJOR-3 — «в десяти местах настоящего времени» перечисляет девять, и место, названное координатой в раунде 5, выпало.**
`docs/tasks/task-014-32b-tool-registration-stub.md:226` — «Число «26 строк манифеста» живёт в десяти местах настоящего времени, и задача назначает все». Прямой подсчёт списка: семь буллетов `:229-235` (`ARCHITECTURE.md:120`, `interfaces.md:618`, `:1372`, `:1391`, `system-architecture-call-budget.md:108`, `:139`, `session-manager.ts:120`) плюс `:196` «два места самого манифеста … `:117` и `:196`» = **девять**. Десятым не может быть `env.ts:64`: `:198-199` выводит его за скобки как датированный и не переписываемый, и критерий `:71` повторяет то же разделение.

Раунд 5 (MAJOR-2, `plan-014-review.md:96`) называл координатой `.env.example:260` — она жива: `# (замер по всем 26 строкам манифеста 2026-08-13). Значение 330 000 и ниже` — и в задаче не встречается. Второе такое место — `docs/architectures/deployment.md:214`: `- Measured 330 000 ms over all 26 manifest rows, 2026-08-13; 330 000 is a floor, not a ceiling.`

**Следствие:** чекбокс называет знаменатель, который его собственный список не обеспечивает, а предписание «рядом ставится действующее число» применено к одному датированному месту из трёх. Дрейф тихий — `capability-manifest.test.ts:46` проверяет лишь нижнюю границу, — то есть ровно класс M3, который абзац сам и называет.

**MAJOR-4 — предписанная двухсловная русская форма грамматически невыразима: оба читателя зашивают существительное `тулов`.**
`docs/tasks/task-014-32b-tool-registration-stub.md:140-142` предписывает: «Русский шаблон принимает двухсловное числительное: `docs-counts.test.ts:209`, `/\*\*([А-Яа-яЁё]+) тулов\*\*, в порядке публикации/g`», и `:147` расширяет словарь на `'двадцать два'`.

Противоречащая улика. Оба читателя предложения зашивают литерал ` тулов**`: захват `docs-counts.test.ts:209` и якорь области `:276` — `from: /\*\*[А-Яа-яЁё]+ тулов\*\*, в порядке публикации:/,`. Живой текст `docs/onchain-analytics/ROADMAP.md:272` — `**Двадцать тулов**, в порядке публикации:` — верный родительный множественного для 20; 22 управляет родительным единственного («двадцать два тула»), который после расширения не совпадает ни с одним из двух шаблонов, потому что расширение трогает числительное и никогда — существительное.

**Следствие:** план неявно предписывает в публичный гейтуемый документ безграмотное «**Двадцать два тулов**» и нигде этого не говорит; исполнитель, пишущий по-русски правильно, получает громкий отказ области с `:308-312`. Пограничная между MAJOR и MINOR: план как написан зелен ценой плохой прозы; оставлено MAJOR, потому что альтернативная ветка — красная сьюта.

**MAJOR-5 — набор гейтов выведен чтением там, где собственный прецедент репозитория для этой формы задачи требует его измерить.**
`docs/tasks/task-014-32b-tool-registration-stub.md:325-346` (тест-кейсы) и `:351-368` (критерии) не содержат шага «прогнать сьюту неисправленной и записать, что упало». Вместо этого `:353-354` пинит счётный перечень, а мутационный тест `:344-345` замораживает «одиннадцать утверждений».

Противоречащая улика — прецедент той же формы: `docs/tasks/task-013-8-tool-registration-channels.md:16-17` формулирует цель как «измерив фактическое срабатывание каждого, а не пересказав список по памяти», а `:23-31` делает прогон полной сьюты **до** правок первым пунктом списка изменений; `packages/mcp-server/test/inventory-channels.ts:19-22` фиксирует результат словами «The list below was MEASURED, not recalled».

BLOCKING-1, BLOCKING-2 и BLOCKING-3 выше доказывают этот перечень неполным как минимум на три проверенных утверждения, то есть это не предпочтение стиля: план принял метод, который его же прецедентная задача записала как источник расходящихся списков. MAJOR: восстановимо, как только сьюта напечатает промахи.

### MINOR

**MINOR-1 — `packages/core/.AGENTS.md` описывает механизм `DEXSCREENER_OBSERVED`, который 014-32a удаляет, и в списке изменений не назван.**
`packages/core/.AGENTS.md:394-397` — «`vendors.dexscreener` comes from an explicit OBSERVED list … guessing ids there would claim unverified coverage». `32a:135` буквально удаляет `packages/core/scripts/sync-chain-registry.ts:62` (`const DEXSCREENER_OBSERVED: Readonly<Record<string, string>> = {`), а `grep` по всему файлу задачи на `AGENTS` даёт ноль. Конвенция называть пакетный `.AGENTS.md` в задаче существует (`32b:102`/`:110`; 15 файлов задач по репозиторию так делают). Знаменатель — один, а не два: `.AGENTS.md:1208` остаётся верным, поскольку `chainSupport` задача не трогает (`32a:156`). Негейтуемый дрейф документации, тривиально восстановим.

**MINOR-2 — «Улики всех трёх существующих генераторов запинены» ложно и просчитано.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:231-235`. Генераторов, эмитящих модуль, под `packages/core/scripts/` четыре — `gen-nansen-coverage.ts`, `gen-defillama-dex-chains.ts`, `gen-defillama-chain-aliases.ts`, `gen-blockscout-chains.ts`. `docs/onchain-analytics/raw/nansen-openapi-2026-07-23.json` лежит на диске, но `grep` по `docs/provenance.json` даёт только blockscout и defillama и ни одной записи nansen. Документ внутренне противоречив в любом прочтении: `32a:46` называет `gen-nansen-coverage.ts` контрактным близнецом, то есть nansen — один из «трёх», чья улика не запинена. Предписанное действие (запинить новую улику) остаётся верным ⇒ ложная поддерживающая ссылка.

**MINOR-3 — координата приставлена к цитате отказа, которой на ней нет.**
`docs/tasks/task-014-32b-tool-registration-stub.md:159-160`: «гейт падает с сообщением «the region cannot be located … Re-anchor it.» (`packages/mcp-server/test/docs-counts.test.ts:276`)». На `:276` стоит `from: /\*\*[А-Яа-яЁё]+ тулов\*\*, в порядке публикации:/,`; процитированное сообщение живёт на `:310-311`. Референт не встречается в цитируемой строке — нарушение собственного контракта позиционных ссылок (WI-43). Существо окружающего утверждения верно, дефект ограничен цитатой.

**MINOR-4 — итоговый счёт недосчитывает собственный раздел: двенадцатое утверждение в седьмом файле стоит под заголовком, который он считает.**
Раздел «Гейты вне восьми инвентарных каналов» открывается на `:249`, следующий заголовок — на `:313`, то есть `:303-311` внутри него. На `:307-311` названо ещё одно ломающееся утверждение в седьмом файле — `packages/core/test/dexscreener.contract.test.ts:140`, `expect(caps.map((c) => c.id).sort()).toEqual(['pairs.active', 'pool.info']);`, — ломаемое правкой `capabilities()`, предписанной тем же разделом на `:303-305`. Критерий `:353-354` («одиннадцать утверждений в шести файлах») описывает только `:257-301`. С учётом BLOCKING-1..3 истинная цифра — не менее четырнадцати утверждений в семи файлах. Само утверждение названо ⇒ исполнение восстановимо; непригодна только проверка полноты.

**MINOR-5 — третий счёт тулов в `ARCHITECTURE.md` не читается ни одним гейтом и не назначен ни одной задаче.**
`docs/ARCHITECTURE.md` несёт три счёта в настоящем времени: `:50` («and twenty workflow-oriented MCP tools», ловится `docs-counts.test.ts:169`), `:198` («Contracts for all twenty MCP tools that are registered today», ловится `:170`) и `:51` («The server publishes twenty tools; the»), который не ловится ничем: в `it` про счёт тулов (`docs-counts.test.ts:168-231`) шаблонов по `ARCHITECTURE.md` ровно два. `32b` делегирует класс гейту (`:130-133`) и отдельно называет только §5 (`:163-166`, это предложение `:198`) ⇒ `:51` тихо становится ложным на регистрирующем коммите при критерии `:364` «Все числа тулов в настоящем времени обновлены». Смягчение: сообщение гейта называет `docs/ARCHITECTURE.md`, и исполнитель окажется в файле строкой выше. Не новое от корректирующего прохода.

**MINOR-6 — датированный замер помещён в список переписывания, а его близнец двумя буллетами ниже корректно выведен из него.**
`docs/tasks/task-014-32b-tool-registration-stub.md:235` назначает `packages/mcp-server/src/transport/session-manager.ts:120`, тогда как `session-manager.ts:119-120` — «number it is compared against was frozen when this line was written. **Measured 2026-08-12:** … `max(deadlineMs) = 60_000` over all 26 rows.» — та же датированная форма, которую `:238-239` выводит из переписывания для `env.ts:64`. Дефект чуть шире, чем в исходной формулировке: `system-architecture-network-profile.md:138-139` («measured 2026-08-12, `max(deadlineMs) = 60_000` over all 26 rows of…») тоже датирован и тоже назначен (`32b:234`). Непоследовательная обработка одной и той же формы.

**MINOR-7 — смена `shape` у `pool.info` применена к одному из двух комментариев в том же файле, которые на неё опираются.**
`32b:206` переписывает AUDIT-комментарий `pool.info` (`packages/core/src/capability-manifest.ts:291-294`), но `:331-333` (строка `protocol.list`) обосновывает собственный `shape` словами «by the same reading that classified `pool.info`: the return is a collection, not one row.» После коммита, переклассифицирующего `pool.info` в `point`, эта перекрёстная ссылка указывает на прочтение, которое тот же коммит удалил. Ни в одном файле `014-32*` не названо; `shape`-прозу гейты не читают ⇒ дрейф тихий, класс M3, который `32b:241-243` называет сам.

### Статус 25 находок раунда 5

**Закрыты, 20:** BLOCKING-2, BLOCKING-4, BLOCKING-6, MAJOR-1, MAJOR-3, MAJOR-4, MAJOR-5, MAJOR-6, MAJOR-7 и все одиннадцать MINOR (MINOR-1 … MINOR-11). Каждая проверена по коду закрывающей правки, в частности: `GATED_DOCUMENTS` в `packages/mcp-server/test/tool-inventory-docs.test.ts:49-57` совпадает со списком из семи документов в `32b`, включая `README.ru.md` (B-4); `docs/tasks/task-014-34-acceptance.md:54-67` и `:153` несут проверку отсутствия в `CAPABILITY_EXCLUSIONS` (B-6); `PLAN.md:410-414` даёт 141–235 часов и 20–34 дня (M-3); маркеров `[STUB]` девять при собственном счёте девять (MINOR о маркере); цитаты `eval-checks-coverage.test.ts:70`/`:71` стали буквальными (MINOR-7); переатрибуции `014-14:119`, `014-15:62`, `014-22:108`/`:111`, `014-25:43-44`, `tool-inventory-docs.test.ts:149-153` сходятся.

**Закрыты наполовину, 4:** BLOCKING-1 (перечисление неполно — см. BLOCKING-1/2/3 выше), BLOCKING-3 (переякорен один якорь из двух — см. BLOCKING-4), BLOCKING-5 (носитель, хук и функция появились, плечо текста отказа без исполнителя — MAJOR-1), MAJOR-2 (девять мест под заявкой о десяти, выпали `.env.example:260` и `deployment.md:214` — MAJOR-3).

**Открыта и объявлена незакрываемой, 1:** MAJOR-8 (35 находок раундов 3 и 4, чей текст не сохранён). Декларация признана честной: раздел раунда 5 полон по всем 25 находкам, и раунд 6 не обязан был повторять дефект. Раздел раунда 6 записан полностью по той же причине.

### Дефекты, внесённые корректирующим проходом раунда 5

1. Умолчание `excluded` для адаптера без хука (`32a:207-210`) — новый текст, утверждающий вендорский факт на двенадцати адаптерах вопреки R-58d (MAJOR-2). Класс L-18 внутри задачи, которая его чинит.
2. Предписание расширить `docs-counts.test.ts:211` до дефисной формы (`32b:139`) при переякоривании только `:276` — ломает якорь области `:282` и делает критерий `:366` невыполнимым (BLOCKING-4).
3. Предписание двухсловной русской формы (`32b:140-142`, `:147`) при зашитом существительном `тулов` в обоих читателях (MAJOR-4).
4. Заголовок «в десяти местах настоящего времени» (`32b:226`) над перечислением из девяти, с потерей координаты `.env.example:260`, названной раундом 5 (MAJOR-3).
5. Датированный замер `session-manager.ts:120` помещён в список переписывания рядом с абзацем, который такую же форму из него выводит (MINOR-6).
6. Координата `:276` приставлена к цитате сообщения, живущего на `:310-311` (MINOR-3).
7. Запись `errors.ts` в списке изменений `32a:202-205` — правка нулевого объёма, занимающая место файла-исполнителя (MAJOR-1).
8. Утверждение «Улики всех трёх существующих генераторов запинены» (`32a:231-235`) — генераторов четыре, улика nansen не запинена (MINOR-2).

### Вердикт и обоснование

**REJECTED.** Отклонение не инерционное: раунд 6 фиксирует реальный прогресс — 20 находок из 25 закрыты по коду, проектное решение по B-5 принято владельцем и в трёх частях из четырёх когерентно, арифметика оценок, счёт задач, маркеры `[STUB]` и позиционные ссылки в четырёх новых файлах теперь выдерживают ручную проверку, а восемь координат расширения числительных перечислены полно и верно. Но задача 014-32b объявляет своим критерием приёмки «сьюта зелёная на заглушках» (`:351`) и одновременно пинит перечень ломающихся утверждений как счётный факт (`:353-354`), а этот перечень независимо выведенным набором опровергается в четырёх местах: гейт отношения ENFORCEMENT (`capability-manifest.test.ts:1191` над `capability-manifest.ts:95`), проверка полноты таблицы дедлайнов (`readme-tool-table.test.ts:622` над `system-architecture.md:2463`), второй читатель замороженной TTL-таблицы (`ttl-coverage.test.ts:246`) и якорь области WI-48 (`docs-counts.test.ts:282`), причём последний ломает не забытая, а **предписанная самой задачей** правка. Исполнитель, идущий по списку изменений буквально, получает красную сьюту на коммите, который по контракту Stub-First обязан быть зелёным, и ставит галочку, утверждающую полноту. Это тот же класс M3 и на том же документе, за который отклоняли раунды 3 и 5, — а MAJOR-5 объясняет, почему он воспроизводится: перечень выведен чтением там, где прецедентная задача `013-8` предписывает его измерить прогоном неисправленной сьюты. Средство узкое и механическое: добавить четыре пропущенных координаты, переякорить `:242`, решить вопрос с русской грамматикой (или согласиться на цифровую форму в обоих гейтуемых предложениях), назначить `packages/core/src/adapters/registry.ts` носителем плеча текста отказа, сузить умолчание хука до `unverified` — и, чтобы разорвать цикл, заменить счётный перечень шагом «прогнать сьюту неисправленной и записать измеренный список», как это сделала 013-8. После этого граница «планирование → исполнение» может быть пересечена.

## Раунд 7 — 2026-08-20, вердикт ACCEPTED

0 BLOCKING, 5 MAJOR, 5 MINOR — после дедупликации 23 переживших находок шести измерений (17 находок опровергнуты верификаторами и сняты). Чтение привязано к фингерпринту из брифа: HEAD `a7d6385`, незакоммиченное дерево ветки `feat/t-014-network-transport-planning`, 23 пути, отпечаток отслеживаемой дельты `c6f69e29fe05e98e`, отпечаток неотслеживаемых файлов `0549228d8bdafc16` (раунд 6: `00061172c729faaf` / `3b27c166f8138cbe` — дерево изменилось, корректирующий проход раунда 6 и есть предмет этого раунда).

**Инструмент.** `check_positional_refs.py` в этом раунде **не прогнан**: ни у одного из шести измерений и ни у ведущего нет исполняющего инструмента в этой роли. Все координаты ниже проверены ручным чтением цитируемых диапазонов; сьюта не прогонялась, поэтому предписанная задачей 014-32b сверка измеренного списка отказов не выполнена и её результат этим раундом не подтверждён.

**Главный итог раунда.** Все четыре BLOCKING раунда 6 закрыты по коду: гейт отношения ENFORCEMENT (`capability-manifest.ts:95` ↔ `capability-manifest.test.ts:1176`/`:1191`), проверка полноты таблицы дедлайнов (`system-architecture.md:2463` ↔ `readme-tool-table.test.ts:619`/`:350`), второй читатель замороженной TTL-таблицы (`ttl-coverage.test.ts:246`) и второй якорь области WI-48 (`docs-counts.test.ts:282`) названы поимённо, процитированы буквально и покрыты критериями приёмки `:146`, `:157`, `:155`, `:151-152`. Главное — **структурное средство сработало, а не переехало**: `32b:291-301` предписывает первым действием прогнать неисправленную сьюту и сверять перечень с измеренным списком в обе стороны, критерии `:136-139` не отмечаются чтением, а прецедент `013-8` (`task-013-8-tool-registration-channels.md:23-31`, `:219-220`, `inventory-channels.ts:19-22`) процитирован верно. Независимый вывод красного набора из кода, выполненный двумя измерениями до чтения перечня, не нашёл ни одного гейтованного утверждения, которого в перечне нет; единственное расхождение (`wire-deadline.test.ts:193-194`) восстанавливается самим шагом измерения и потому снято верификатором.

Пережившее — целиком **негейтованный остаток**: числа, которые не читает ни один тест, и два проектных пробела в 014-32a, разрешимых внутри исполнения. Ни одно из них не делает сьюту красной и ни одно не меняет разбиение задач, зависимости или контракт Stub-First.

### MAJOR

**MAJOR-1 — знаменатель «26 строк манифеста» неполон минимум на четыре носителя, и критерий приёмки сертифицирует ложную полноту.**
`docs/tasks/task-014-32b-tool-registration-stub.md:257`: «Число «26 строк манифеста» живёт в **тринадцати местах, и задача назначает все**». Перечень на `:260-265` даёт восемь мест настоящего времени (пять буллетов плюс «три места самого манифеста … `:95`, `:117` и `:236``), на `:269-273` — пять датированных замеров; критерий `:436-437` пинит ровно это: «во всех восьми местах настоящего времени; пять датированных замеров не переписаны».

Противоречащая улика — четыре носителя того же факта, не названные ни в одном файле `014-32*` (`grep` по `docs/tasks` на `session-lifecycle`, `interfaces.md:819`, `interfaces.md:1369`, `ARCHITECTURE.md:10` не даёт ни одного попадания):
- `packages/mcp-server/test/session-lifecycle.test.ts:552` — `// Measured 2026-08-12: \`max(deadlineMs) = 60_000\` over all 26 rows of the shipped manifest.` — дословный близнец назначенного на `:270` `session-manager.ts:119-120`, в `it`, который эту же ассерцию исполняет (`:551`).
- `docs/architectures/interfaces.md:819` — `**Seven manifest keys are served by no tool today** (measured 2026-08-13, 26 keys against 20 tools):` — несёт оба устаревающих числа сразу, и именно на это предложение опирается базовая линия `014-32c:105`.
- `docs/architectures/interfaces.md:1369` — `returns 26, and the count` под заголовком `**Measured state, 2026-08-12.**` (`:1361`) — четырьмя строками выше назначенного `:1372`.
- `docs/ARCHITECTURE.md:10` — `the capability manifest carries **26** rows and the server publishes **20** tools (both measured, not quoted)` в строке `| **Updated** | 2026-08-12, **v4.11**`.

Классификация лазейку не спасает: все четыре несут датированную форму, то есть по правилу самой задачи (`:277`, «Одна форма — одно обращение») принадлежат списку датированных, где заявлено пять, а истинное число — не менее девяти. Тишина проверена: `packages/core/test/capability-manifest.test.ts:46-48` утверждает лишь `toBeGreaterThan(10)` и по `routedCapabilities.length`, и по `Object.keys(capabilityManifests).length`; из двух шаблонов по `ARCHITECTURE.md` в `docs-counts.test.ts:169`/`:170` ни один не ловит форму `:10`; подстраховка `task-014-34-acceptance.md:36` грепает `ARCHITECTURE.md` только на отсутствие `manifest of 20 rows`, `a 14th tool`, `thirteen tools` — ни одна из этих строк не живая.

Это **не** пересказ MAJOR-3 раунда 6: там были названы `.env.example:260` и `deployment.md:214`, обе теперь в списке. Это тот же класс M3 на новом, большем знаменателе — третий раунд подряд перечисление растёт при каждой независимой проверке.

**Следствие при исполнении:** исполнитель отмечает `:436-437` как выполненный, тогда как не менее четырёх мест дерева продолжают утверждать 26 строк, а два из них — ещё и 20 тулов. Дрейф тихий, сьюта зелёная.

**MAJOR-2 — счёт «27 routes» стоит в четырёх местах одного документа, назначено одно, а критерий приёмки сметает все.**
`32b:345` назначает единственную координату: `docs/architectures/system-architecture-chain-normalization.md:1126`, `holds **27 routes** over 26`. Критерий `:149` сформулирован шире: «Счёт способностей и маршрутов приведён к замеру: 27 способностей, 28 маршрутов».

Противоречащая улика — `grep` на `27 routes` по `docs/` даёт четыре живых предложения: `system-architecture-chain-normalization.md:396` (`an audit of all 27 routes`), `:443` (`| \`any\` | always \`true\` (today's implicit default) | 26 of 27 routes, which carry no policy today`), `:870` (`**13 of the 27 routes**`), `:1176` (назначенное). Гейтована только последняя: `docs-counts.test.ts:335` ловит `/holds \*\*(\d+) routes\*\*\s+over\s+\d+\s+distinct\s+capabilities/g`, чему остальные три предложения не удовлетворяют. `:443` явно настоящего времени («today») и становится «27 of 28» в день коммита; `:870` стоит внутри датированной заметки ревью (`:816`) и по R-23.6 защитим, поэтому находка на него не опирается.

**Следствие при исполнении:** предписанный прогон неисправленной сьюты (`:291-294`) вскрывает только гейтованные утверждения, поэтому эти два предложения им не выявляются, а критерий `:439` отмечается как выполненный.

**MAJOR-3 — исход зонда `verified` не имеет отображения в `CoverageStatus`; второй конъюнкт предиката dexscreener в задаче не назван.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:153` объявляет хук трёхзначным: `chainProbeStatus?(chain: ChainInfo): 'verified' | 'excluded' | 'unverified';`. Тип статуса на `:201` — `'covered' | 'excluded' | 'unverified'`. `covered` вычисляется предикатом, а не зондом (`packages/core/src/chain/coverage.ts:88`, поверх `packages/core/src/adapters/dexscreener/index.ts:147-148`: `chain.vendors['dexscreener'] != null && chain.nativeSymbol != null`), и задача прямо объявляет хук не-предикатом, не расширяющим покрытие (`:162-168`).

Противоречащая улика: пара «зонд = `verified`, покрытие = `false`» достижима и ожидается самим планом. `packages/core/src/chain/registry.data.json` несёт 23 строки с `"nativeSymbol": null`; `task-014-32c-pool-info-tool.md:129-130` строит TC-UNIT-02/03 ровно на «сети без `nativeSymbol`» для dexscreener, то есть после 014-32a такие строки могут нести непустую колонку `vendors.dexscreener`. Второй конъюнкт (`chain.nativeSymbol != null`) в 014-32a не упомянут ни разу, пересечение «65 обслуживаемых строк ∩ 23 строки без `nativeSymbol`» не измерено, а правило композиции для этой пары отсутствует: `:208-212` различает только `excluded`/`unverified`, `:225` задаёт умолчание для адаптера **без** хука, разбор M6 (`:242-246`) разбирает лишь маскировку «зонд вернул пусто». `TC-UNIT-08` (`:302-303`) эту комбинацию не наблюдает.

Это не пересказ MAJOR-2 раунда 6 (та касалась умолчания для адаптеров без хука и закрыта переводом на `unverified`) — здесь речь об адаптере **с** хуком.

**Следствие при исполнении:** для сети, подтверждённой зондом и непокрытой из-за отсутствия `nativeSymbol`, ветка `excluded` даёт вендорское утверждение на обслуживаемой сети (класс L-18 внутри задачи, которая L-18 чинит), а ветка `unverified` утверждает непроведённый зонд о зондированной сети. Восстановимо в исполнении одним замером пересечения и одним решением о композиции; ни разбиения задач, ни зависимостей это не меняет.

**MAJOR-4 — AC-51, единственный критерий R-33, не имеет тест-кейса; объявленный способ проверки не реализован ни одним из одиннадцати кейсов 014-32a.**
Чекбокс стоит на `32a:346-347`. `docs/TASK.md:565` объявляет способ проверки: «тест на улике зонда против матрицы покрытия».

Противоречащая улика: полный перечень кейсов задачи — TC-UNIT-01…09 (`:291-305`) и TC-REG-01/02 (`:309-311`). Прочитаны все: 01/02/03/05/06/07 гоняют генератор на синтетической улике; 04 сличает закоммиченный модуль с выводом генератора (улика→модуль, не улика→**матрица**); 08 сличает два текста отказа между собой; 09 — курируемое переопределение. Задача сама разводит понятия: матрица — `packages/core/src/chain/coverage.ts` (`:199-206`), модуль — карта `caip2→{chainId,status}` (`:36-37`), поэтому прочтение TC-UNIT-04 как теста «против матрицы» не проходит. `TC-REG-01` указывает на `packages/core/test/coverage.test.ts:348`, а это нижняя граница (`covered.some((slug) => slug !== 'ethereum' && slug !== 'solana')` на `:352`); `TC-REG-02` прямо запрещает пинить размер колонки числом. `grep` по `docs/tasks`: `AC-51` встречается только в 014-32a — у 014-34 подстраховки нет. Матрица действительно не является функцией улики (см. MAJOR-3).

**Следствие при исполнении:** единственный критерий R-33 закрывается глазами, регрессионного детектора L-18 после задачи не существует. Восстановимо добавлением одного кейса — но добавить его придётся вместе с решением по MAJOR-3.

**MAJOR-5 — `PLAN.md` продолжает пинить счётный перечень, который 014-32b от себя отменила, и число ложно.**
`docs/PLAN.md:63-64`: «Причина стоимости — шестнадцать инвентарных каналов и **одиннадцать утверждений** вне их». `grep` по `docs/**/*.md` на `одиннадцат` даёт эту строку и записи ревью — из текста задачи слово удалено.

Противоречащая улика: `32b:291` — «Первым действием прогоняется непочиненная сьюта, и перечень ниже — вход, а не итог», `:296` — «**Why перечень не пинится числом.**», а собственное перечисление задачи `:309-385` называет семнадцать утверждений в семи файлах. Раунд 6 независимо измерил «не менее четырнадцати» (`plan-014-review.md:294`) и там же зафиксировал, что слово «одиннадцать» пришло из удалённого ныне критерия задачи.

**Следствие при исполнении:** прямого нет — это обоснование класса стоимости, а не инструкция; исполнитель по нему ничего не делает, и истинное большее число классификацию «крупная» только усиливает. Дефект в том, что документ, оправдывающий задачу, воспроизводит метод, который задача отменила: половинчатое применение (M3) в чистом виде, на числе, которое предыдущий раунд уже опроверг поимённо.

### MINOR

**MINOR-1 — 014-32b противоречит собственному знаменателю в одном разделе.**
`32b:282-283`: «Это класс M3: факт живёт в **десяти местах**, правка одного выглядит как выполненная работа», — шестнадцатью строками ниже `:257` («в тринадцати местах») и при перечислении 8+5=13, сертифицированном на `:436`. «Десять» — остаток редакции, которую раунд 6 отклонил (`plan-014-review.md:318`): заголовок и список исправлены, предложение, называющее класс отказа, — нет. Ни одно число не читается гейтом; цена — читатель, сверяющий полноту с неверным знаменателем.

**MINOR-2 — датированный замер помещён в список переписывания вопреки собственному правилу задачи.**
`32b:264` относит `docs/architectures/system-architecture-call-budget.md:108` (`assigns it, so all 26 rows run on ADR-002 D3`) к восьми местам настоящего времени, которые «становятся ложными в день коммита». Живое предложение начинается на `:106`: «**Measured 2026-08-12:** the field is declared (`packages/core/src/capability-manifest.ts:149`, `shareable?: boolean;`) and **no** manifest row assigns it, so all 26 rows run on ADR-002 D3's `true` default» — та же датированная форма, которую две строки ниже задача направляет в обработку по R-23.6 для `session-manager.ts:119` и `system-architecture-network-profile.md:138`, и то же правило, которое она формулирует на `:235`. Третья координата, отличная от двух исправленных в раунде 6, — остаток, а не пересказ. **Следствие:** исполнитель правит `26` внутри записи о том, что было замерено в названный день, порождая замер, которого никто не делал — ровно то, что R-23.6 (`docs/TASK.md:267-268`) предотвращает.

**MINOR-3 — три позиционные ссылки, внесённые корректирующим проходом, указывают на строку выше референта.**
(1) `32b:152-153` цитирует `packages/mcp-server/test/docs-counts.test.ts:310` под референт `` `region cannot be located, so this gate would check nothing. Re-anchor it.`, ``; на `:310` стоит первая половина шаблонной строки, референт — на `:311`, и та же задача цитирует `:311` верно на `:180-181` — документ расходится сам с собой. (2) `32b:350-351` цитирует `packages/mcp-server/test/readme-tool-table.test.ts:601` под `` `${capability}: the manifest gives it a deadlineMs the table never documents` ``; `:601` — `if (!documented.has(capability)) {`, референт на `:602`. (3) `32b:339` утверждает, что `capability-manifest.ts:95` «названа поимённо в `docs/TASK.md:269`»; `:269` заканчивается на «докстринг говорит 20 строк, таблица того же», а `:95` стоит на `:270` (слабейшая из трёх: буллет R-23.7 действительно начинается на `:269`, подстрока не цитируется). Две из трёх стоят внутри перечня, который шаг сверки (`:427-428`) построчно сопоставляет с измеренным списком отказов. Существо каждого окружающего утверждения проверено и верно. Отдельно: заявка `32a:231-232` о `rpc-evm/index.ts:147` (референт `chain.rpcHosts !== null` стоит на `:150`, внутри функции, объявленной на `:147`) верификаторами оценена противоречиво и в счёт не включена.

**MINOR-4 — счётчики редакции и раундов ревью отстают на один в четырёх местах.**
`docs/PLAN.md:9` («Редакция | 5, после пятого ревью границы фазы 2026-08-19»), `docs/PLAN.md:435` («три — архитектуры, пять — плана»), `docs/TASK.md:15` («план 5»), `docs/TASK.md:650` («три архитектуры, пять плана»). Дерево уже несёт раунд 6 полностью (`plan-014-review.md:151`, «## Раунд 6 — 2026-08-19, вердикт REJECTED»), и рассматриваемая дельта — ответ на него, то есть завершённых раундов плана шесть. Конвенция не изобретена находкой: MINOR-11 раунда 5 (`plan-014-review.md:128`) отклонил ровно эту форму отставания, раунд 6 записал его закрытым (`:307`) — маркер сдвинули один раз и не заставили двигаться с каждым проходом. Факт живёт в четырёх местах, обновлено ноль (M3). Только провенанс документа, исполнение не затрагивает.

**MINOR-5 — статистика генераторов исправлена и снова неверна: их пять, а не четыре.**
`32a:267-269`: «Генераторов, эмитящих модуль, под `packages/core/scripts/` **четыре** — `gen-nansen-coverage.ts`, `gen-defillama-dex-chains.ts`, `gen-defillama-chain-aliases.ts`, `gen-blockscout-chains.ts`». Пятый удовлетворяет заявленному критерию дословно: `packages/core/scripts/generate-nansen-cost-table.mjs` читает закоммиченную улику (`:39-42`, `docs/onchain-analytics/raw/nansen-openapi-2026-07-23.json`) и пишет закоммиченный модуль (`:43`, `const outPath = path.resolve(packageRoot, 'src/adapters/nansen/cost-table.ts');`), который импортирует рантайм (`packages/core/src/adapters/nansen/cost-of.ts:2`), а его собственный заголовок `:1` самоопределяется как «same "committed-.ts-literal, regenerated by hand" pattern». Предложение, чья единственная функция — быть счётным фактом («утверждение было ложно и просчитано»), ложно второй раз по тому же критерию, который раунд 6 уже отклонил (MINOR-2). Опровергнутая подчасть: заявка «запинены улики двух: blockscout и defillama» верна — `docs/provenance.json:12`, `:36`, `:44` пинят улики ровно этих двух вендоров. Предписание «новая улика пинится» не затронуто.

### Статус находок раунда 6

**Закрыты, 9:** BLOCKING-1 (`capability-manifest.ts:95` назван на `32b:332-339`, гейт `:1176`/`:1191` проверен, целевые значения 27/27 верны, критерий `:438`), BLOCKING-2 (`system-architecture.md:2463` назван на `:57-64`, пятнадцать способностей без `token.pools` проверены, пин семи строк `readme-tool-table.test.ts:619` учтён, критерий `:157`), BLOCKING-3 (`ttl-coverage.test.ts:246` назван на `:72-77`, третье утверждение `:208` исключения не требует — `explicitRows` выводится из живого манифеста, критерий `:155`), BLOCKING-4 (оба якоря WI-48 названы на `:103-106`, критерий `:151-152` говорит «**оба** якоря»), MAJOR-1 (`registry.ts:922` — буквально `throw new CapabilityNotCoveredOnChainError({`; `errors.ts` честно объявлен нулевой правкой, `hint?: string` на `:71`, конкатенация на `:89`), MAJOR-2 (умолчание переведено на `unverified`, хук расширен до трёх значений, обоснование через nansen отозвано как ошибочное), MAJOR-4 (`32b:155-166` расширяет и числительное, и существительное в обоих читателях — `grep` подтверждает ровно два читателя ` тулов`: `docs-counts.test.ts:209` и `:236`; предписана форма «Двадцать два тула», цифровая отклонена с указанием причины), MINOR-1 (`packages/core/.AGENTS.md:396` назван), MINOR-4 (`dexscreener.contract.test.ts:140` — собственный пункт списка изменений `:381-385`), MINOR-7 (`capability-manifest.ts:333` назван на `:232-237`).

**Закрыты в задаче, но не распространены, 1:** MAJOR-5 — счётный перечень заменён измерением в 014-32b (`:291-301`, критерии `:426-429`), но `PLAN.md:63` продолжает пинить «одиннадцать утверждений» (MAJOR-5 настоящего раунда).

**Закрыты наполовину, 4:** MAJOR-3 (обе пропущенные координаты добавлены и проверены, но знаменатель по-прежнему неполон — MAJOR-1 — и `:283` противоречит `:257` — MINOR-1); MINOR-2 (исправлено с трёх на четыре, истина — пять, MINOR-5); MINOR-3 (отмеченная координата `:276` исправлена, появились три новые на строку мимо — MINOR-3); MINOR-5 (`ARCHITECTURE.md:120` назван на `:188-191`, но сопровождающая заявка «третий счёт в этом файле» неверна: `ARCHITECTURE.md:10` — четвёртый, негейтованный и неназначенный, вошёл в MAJOR-1); MINOR-6 (`session-manager.ts:119` и `system-architecture-network-profile.md:138` переведены в датированную группу, `system-architecture-call-budget.md:108` остался в переписывании — MINOR-2).

Восемь дефектов, приписанных раундом 6 корректирующему проходу раунда 5: **#1 закрыт, #2 закрыт, #3 закрыт, #7 закрыт; #4, #5, #6, #8 закрыты наполовину** (соответственно MINOR-1, MINOR-2, MINOR-3, MINOR-5 настоящего раунда).

### Дефекты, внесённые корректирующим проходом раунда 6

1. Заявка о полноте «живёт в тринадцати местах, и задача назначает все» (`32b:257`) вместе с критерием `:436-437`, пинящим «восемь … пять датированных», — новый счётный факт, опровергаемый минимум четырьмя носителями (MAJOR-1). Счётный метод, отменённый для гейтованной половины, воспроизведён для негейтованной.
2. Критерий `:439` («27 способностей, 28 маршрутов») сметает четыре места счёта маршрутов при назначенном одном (MAJOR-2).
3. Хук расширен до трёх значений (`32a:153`) с исходом `verified`, у которого нет клетки в `CoverageStatus` (`:201`), при неназванном втором конъюнкте предиката dexscreener (MAJOR-3).
4. Три новые координаты мимо референта на строку: `docs-counts.test.ts:310`, `readme-tool-table.test.ts:601`, `TASK.md:269` (MINOR-3) — две из них внутри перечня, добавленного специально в ответ на BLOCKING раунда 6.
5. Исправленная статистика генераторов («четыре») ложна второй раз по тому же критерию (MINOR-5).
6. Остаточное «в десяти местах» (`32b:283`), пережившее правку заголовка и списка в том же разделе (MINOR-1).

### Вердикт и обоснование

**ACCEPTED.** Принятие не усталостное, и оно опирается на проверяемое изменение качества, а не на счёт раундов. Во-первых, все четыре BLOCKING раунда 6 закрыты по коду, а не по декларации: каждый пропущенный гейт назван, процитирован буквально, его целевое значение вычислено и закреплено критерием приёмки. Во-вторых — и это главное — раунд 6 требовал не четырёх координат, а смены метода, и метод сменён по-настоящему: `32b:291-301` предписывает прогнать сьюту неисправленной первой, а критерии `:426-429` требуют сверки в обе стороны (не попавшее в перечень и упавшее — дописать; перечисленное и не упавшее — вычеркнуть с указанием причины), причём регрессионный кейс `:418` выводится из **измеренного** списка, а не из прозы. Исполнимость проверена по коду: `pnpm test` — это `pnpm -r test` → `vitest run` по пакету, `typecheck` — отдельный скрипт, поэтому недоделанная заглушка не срывает замер; ничего в предписанном непочиненном состоянии не бросает на импорте. Два измерения независимо вывели красный набор из кода **до** чтения перечня и не нашли ни одного гейтованного утверждения, которого задача не называет. Класс «счётное перечисление отстаёт от кода» для всего, что печатает сьюта, закрыт.

Пережившее — негейтованный остаток и два пробела в 014-32a. Ни один из пяти MAJOR не делает сьюту красной, не меняет разбиение на четыре задачи, порядок этапов, зависимости (`32c:18-19`, `32d:18-22` ↔ `PLAN.md:278`/`:282`), интервал заглушки (`32b:60-71` ↔ `32c:55-60` ↔ `32d:66-73` ↔ `014-34:54-67`) или контракт Stub-First. MAJOR-1 и MAJOR-2 дают устаревшие числа в прозе; MAJOR-5 — устаревшее число в обосновании стоимости; MAJOR-3 и MAJOR-4 требуют одного замера (пересечение «обслуживается вендором» ∩ «`nativeSymbol` = null`»), одного решения о композиции и одного тест-кейса — работы, которая по объёму и по природе принадлежит исполнению, а не планированию. Отклонять седьмой раз ради дописывания четырёх координат означало бы применить к негейтованной половине ровно тот метод, который в этом же раунде признан несостоятельным: три раунда подряд каждый независимый пересчёт находил новый носитель, и седьмой нашёл бы восемнадцатый.

Поэтому граница «планирование → исполнение» пересекается, а перечисленное ниже выполняется **первым действием исполнения** — правки механические, ни одна не затрагивает структуру плана:

1. Заменить счётные формулировки `32b:257` и критериев `:436-437`, `:439` на измеряемые по образцу `:427-428`: перечень мест получается `grep`-ом на исполнении и сверяется с текстом задачи в обе стороны. Известные недостающие носители — `session-lifecycle.test.ts:552`, `interfaces.md:819`, `interfaces.md:1369`, `ARCHITECTURE.md:10`, `system-architecture-chain-normalization.md:396`, `system-architecture-chain-normalization.md:443`.
2. Перенести `system-architecture-call-budget.md:108` в группу датированных замеров (MINOR-2) и снять остаточное «в десяти местах» на `:243` (MINOR-1).
3. Измерить пересечение `vendors.dexscreener != null` ∩ `nativeSymbol == null`, зафиксировать правило композиции для пары (зонд `verified`, покрытие `false`) и добавить кейс, закрывающий AC-51 объявленным в `TASK.md:565` способом (MAJOR-3, MAJOR-4).
4. Привести `PLAN.md:63` в соответствие с отменённым методом (MAJOR-5), исправить три координаты (MINOR-3), счёт генераторов (MINOR-5) и счётчики редакции/раундов в четырёх местах (MINOR-4).

План безопасно передавать разработчику, который следует ему буквально: при таком следовании система строится верная, сьюта на стабовом коммите зелёная, а весь остаточный риск — устаревшие числа в прозе и один недоспецифицированный текст отказа, оба обнаруживаемые и исправимые внутри исполнения.

## Раунд 8 — 2026-08-20, вердикт ACCEPTED

0 BLOCKING, 4 MAJOR, 5 MINOR — после дедупликации 17 переживших находок шести измерений (20 находок опровергнуты верификаторами и сняты). Чтение привязано к фингерпринту из брифа: HEAD `a7d6385`, незакоммиченное дерево ветки `feat/t-014-network-transport-planning`, 23 пути, отпечаток отслеживаемой дельты `0d4f8391718d9d00`, отпечаток неотслеживаемых файлов `7c26ca6d9ed4b603` (раунд 7: `c6f69e29fe05e98e` / `0549228d8bdafc16` — дерево изменилось, корректирующий проход раунда 7 и есть предмет этого раунда).

**Инструмент.** `check_positional_refs.py` в этом раунде **не прогнан**, сьюта **не прогнана**: ни у одного из шести измерений нет исполняющего инструмента в этой роли. Ведущий прогнал только предписанные задачей `grep`-и и точечные чтения; все координаты ниже проверены буквальным чтением цитируемых диапазонов. Предписанный задачей 014-32b замер неисправленной сьюты этим раундом не подтверждён.

**Главный итог раунда.** Семь из десяти находок раунда 7 закрыты по коду, включая все четыре, которые опирались на счётные перечни и координаты. Структурное средство — замена перечня носителей двумя `grep`-ами и правилом обработки по форме — **реально, а не декларативно**: оно достаёт носители, которые три редакции подряд пропускали (`session-lifecycle.test.ts:552`, `ARCHITECTURE.md:10`, `system-architecture-chain-normalization.md:396`/`:443`/`:870`/`:1176`, три файла задач этапа), и снимает класс «перечень стареет между написанием и исполнением» для всех форм, которые шаблоны покрывают. Остаток — две формы, в шаблон не попавшие, один непокрытый класс путей и одна проектная развилка в 014-32a.

### MAJOR

**MAJOR-1 — шаблоны грепа не видят три живых носителя факта «26», ради поиска которых перечень и заменён, а критерий приёмки сертифицирует полноту тем же слепым инструментом.**
`docs/tasks/task-014-32b-tool-registration-stub.md:265`, буквально:
`-E "26 (manifest )?rows|26 строк|twenty-six rows|26 of 26|manifest of 26|26 capabilit" . ;`
Критерий `:440-441`: «Греп по трём числам прогнан; каждое найденное вхождение обработано по своей форме, и повторный прогон не находит ни одного вхождения старого числа в настоящем времени».

Противоречащая улика — прогон обоих шаблонов дословно по дереву. Альтернатива `26 capabilit` **не даёт ни одного нового места**: её единственные попадания — `packages/core/src/capability-manifest.ts:95` и его цитаты в задаче и в этом файле ревью, все уже покрытые альтернативой `26 of 26`. Живая форма факта в репозитории — `26 routed capabilities`, со словом между числом и существительным:
- `packages/core/test/capability-manifest.test.ts:67`, `it('covers exactly the 26 routed capabilities, named', () => {`
- `packages/core/test/ttl-coverage.test.ts:218`, `it('covers all 26 routed capabilities, so no row escaped the comparison', () => {`
- `packages/core/test/ttl-coverage.test.ts:239`, `it('is unreachable for the 26 routed capabilities — enumerated, not asserted in general', () => {`

Ни одна не попадает ни в один шаблон. Асимметрия — подпись класса M3: соседний шаблон на следующей строке (`:267`) написан как `23 (distinct )?capabilit`, то есть автор знал класс «слово между числом и существительным» и применил его к одному числу из двух. Первые два заголовка стоят в одной и трёх строках от ассерций, которые тот же коммит переводит на 27 (`:70` назначена на `32b:329-330`, `:219` — на `:364-365`), поэтому отгружаемое дерево несёт заголовок «26» над ассерцией «27».

Та же слепота на второй форме — `returns 26` и `26 keys`:
- `docs/architectures/interfaces.md:618`, `Object.keys(capabilityManifests)` returns 26. The historical text stands unchanged; the current`
- `docs/architectures/interfaces.md:813`, `**Seven manifest keys are served by no tool today** (measured 2026-08-13, 26 keys against 20 tools):`
- `docs/architectures/interfaces.md:1363`, `**none** of the manifest rows: `Object.keys(capabilityManifests).length` returns 26, and the count`

`:813` и `:1363` — ровно те две координаты, которые раунд 7 передал автору поимённо (`docs/reviews/plan-014-review.md:345-346`), `:612` стояла в перечне, удалённом этим проходом. `:813` не имеет защиты датированного замера в смысле правила `:273`: дата в ней есть, но предложение написано в настоящем времени («served by no tool **today**»), и после 32b/32c/32d оно ложно по обоим числам сразу (27 ключей, 22 тула, пять необслуживаемых). Класс, объявленный самой задачей на `:259-260`, включает «маршрутизируемых способностей (26)» — то есть все шесть мест внутри объёма, а не вне его.

Ничто другое их не ловит: `packages/core/test/capability-manifest.test.ts:46` проверяет только нижнюю границу (`has more than 10 routes and more than 10 manifest entries`), TC-E2E-04 задачи 014-34 (`:117-123`) читает вхождения `13 tools`, TC-E2E-05 (`:124-133`) ограничена файлом `packages/core/src/capability-manifest.ts`, а буллеты `32b:123-128` назначают в `interfaces.md` только якоря §5.1.7/§5.1.8, преамбулу §5.1 и строки 829-830.

Последствие на исполнении: критерий `:441` отмечается зелёным, потому что шаблон этих строк не находил и на повторном прогоне не найдёт; дерево отгружается с шестью живыми утверждениями «26» в настоящем времени, три из них — в интерфейсном контракте, три — в заголовках тестов над ассерциями «27». Сьюта при этом зелёная (заголовки ничего не утверждают), поэтому MAJOR, не BLOCKING.

**MAJOR-2 — греп не ограничен по путям и достаёт классы, которые собственный гейт репозитория объявляет неприкосновенными; правило трёх форм не имеет для них ветви, а критерий буквально невыполним.**
`docs/tasks/task-014-32b-tool-registration-stub.md:264-275`. Греп идёт от корня (`.`), исключая только `node_modules`, `.git` и `dist`. Правило обработки: настоящее время — переписать; датированный замер — сохранить и поставить рядом действующее число; исторический контекст эпохи — **`docs/architectures/version-history.md` и changelog-поля** — не трогать.

Противоречащая улика — прогон даёт, среди прочего: `docs/reviews/task-014-review.md:63` (настоящее время внутри постоянной записи ревью), `docs/reviews/plan-014-review.md:165`, датированные замеры в `docs/tasks/task-014-13-session-lifecycle.md:119`, `task-014-23-wire-deadline.md:85`, `task-014-40-env-keys.md:79`, весь исследовательский дамп `docs/dune-query-discovery/`, `.env.example:260`, а также подстрочные ложные срабатывания на `126 строк` (`26 строк` не имеет границы слова). Единственная ветвь исключения (`:274-275`) названа перечислительно — `version-history.md` и changelog-поля — и ни одного из этих путей не достигает.

Репозиторий этот вопрос уже решил в коде: `packages/mcp-server/test/tool-inventory-docs.test.ts:123-134` исключает `docs/tasks`, `docs/plans`, `docs/reviews`, `docs/issues`, `docs/backlog`, `docs/TASK.md` и `docs/PLAN.md`, а докстринг `:31-34` называет причину: «Rewriting history to match today's inventory would be worse than no gate at all». Правило задачи прямо противоречит решению, зафиксированному в исполняемом гейте того же репозитория.

Критерий `:440-441` вдобавок невыполним буквально: сама задача 014-32b несёт старые числа в настоящем времени на `:239-240` и `:337`, а строка `:265` совпадает сама с собой — повторный прогон всегда находит вхождения. Отметить его можно только несформулированным суждением.

Последствие: исполнитель, идущий буквально, правит запись аудита (`docs/reviews/task-014-review.md`) и датированные замеры чужих задач. Ущерб откатывается по git и виден в диффе, поэтому MAJOR, не BLOCKING, — но правило не является решаемым без дополнительных суждений, чего стандарт этого раунда требует прямо.

**MAJOR-3 — отображение `verified → covered` объявлено фактом, и этот факт опровергается предикатом dexscreener; запрошенное раундом 7 правило композиции по-прежнему не написано.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:201-204`, буквально: «`verified` → `covered`, `excluded` → `excluded`, `unverified` → `unverified`. Хук трёхзначен, потому что модуль трёхзначен, но `verified` никогда не доходит до текста отказа: сеть, подтверждённая зондом, покрыта, а отказ строится только на непокрытой паре».

Противоречащая улика — покрытие есть конъюнкция, из которой зонд контролирует ровно один член:
- `packages/core/src/adapters/dexscreener/index.ts:147-148`, `chainSupport: (chain: ChainInfo): boolean =>` / `chain.vendors['dexscreener'] != null && chain.nativeSymbol != null,`
- `packages/core/src/chain/coverage.ts:88-89` строит матрицу над этим предикатом и добавляет член `!chain.deprecated`
- `packages/core/src/chain/registry.data.json` несёт 23 строки с `"nativeSymbol": null`

Пара (зонд `verified`, покрытие `false`) не просто достижима — она **предполагается тем же планом**: `docs/tasks/task-014-32c-pool-info-tool.md:129-130` строит TC-UNIT-02 и TC-UNIT-03 на «сети без `nativeSymbol`», обслуживаемой dexscreener, а ненулевой `vendors.dexscreener` у такой строки может появиться только из зонда 32a; TC-UNIT-03 при этом прямо фиксирует, что `pairs.active` конъюнкт сохраняет — то есть именно та способность, над которой квантифицирует TC-UNIT-10 (`32a:308-310`). Собственное правило генератора «два ответивших кандидата → `null`» (`32a:100-102`) и строки `deprecated` дают ту же пару независимо.

Слово `nativeSymbol` в файле 014-32a **не встречается ни разу**. Раунд 7 (`docs/reviews/plan-014-review.md:431`, пункт 3) требовал измерить пересечение и зафиксировать правило композиции; корректирующий проход поставил на это место утверждение о недостижимости.

Опровергнутые части находки, зафиксированные как таковые: ветвь «предикат расширяется хуком и покрытие уезжает на сети, где `extractFetchArgs` бросает» исполнителю недоступна — она запрещена трижды в том же файле (`32a:162-164`, «Новый хук предикатом не является: `chainSupport` он не читает и покрытие не расширяет»; разбор последствия на `:166-168`; `:206`); ветвь воспроизведения L-18 арбитрирована TC-UNIT-10, который запрещает `excluded` на подтверждённой зондом паре; переопределение булева предиката через `coverageStatus(...) === 'covered'` над существующим телом `computeCovered` (`coverage.ts:88`) — обычный рефакторинг без изменения поведения.

Переживает ровно неопределённая клетка: подтверждённая зондом, но непокрытая сеть получает текст отказа `unverified`, утверждающий, что зонд не проводился, — на сети, которая зондировалась. Ошибка пользовательская, видимая, исправимая внутри исполнения одним замером и одним решением. Сьюту красной не делает.

**MAJOR-4 — 014-32c предписывает вывод уровня комиссии через `eth_call`, но ни один список изменений не владеет модулем, который его исполняет, и ни один критерий не требует, чтобы поле заполнялось.**
`docs/tasks/task-014-32c-pool-info-tool.md:85-86`: «Пул без метода, отдающего комиссию, отвечает типизированным отказом. Вывод — один `eth_call` метода `fee()`, селектор `0xddca3f43`», и `:88-92` называет 15 сетей семейства `evm`, на которых поле выводимо.

Противоречащая улика: строка `eth_call` не встречается нигде под `packages/` (ноль попаданий); единственные методы `rpc-evm` — `eth_gasPrice` и `eth_getBalance`; список изменений 014-32c (`:30-68`) называет четыре файла — `pool-info.ts`, `dexscreener/index.ts` (только второй HTTP-шаг), `eval/capabilities.mjs`, `data-model.md`, — и ни одна задача в `docs/tasks` не называет `packages/core/src/adapters/rpc-evm/index.ts` файлом к изменению (единственное упоминание, `32a:233`, — доказательная проза). Маршрут `{ capability: 'pool.info', adapterIds: ['dexscreener'] }` ведёт только в dexscreener, чей allowlist зафиксирован на `api.dexscreener.com` (`docs/architectures/interfaces.md:762-767`), тогда как `eth_call` обязан идти в `rpcHosts` сети — межадаптерный путь без способности и без маршрута.

Требование при этом существует: `docs/TASK.md:243` — «21.1 отгружается тул для `pool.info`, принимающий адрес пула и возвращающий адреса токенов, резервы **и уровень комиссии**».

Последствие на исполнении — третья ветвь, а не первые две: критерий приёмки `:148` («Отсутствие уровня комиссии выражено типизированным отказом, не пустым значением») и TC-E2E-01/02 (`:121-122`) полностью удовлетворяются безусловным отказом, поэтому `feeTierBps` может отгрузиться никогда не заполняемым при всех отмеченных галочках, и `interfaces.md:742-745` («R-21.1's fee-tier clause is satisfied by the derivation, not by the optional marker») останется невыполненной. Это и форма M6, на которую `Примечания` задачи (`:155-156`) ссылаются, не называя, какой сломанный случай маскирует новый законный отказ. Не BLOCKING, потому что проектное решение существует вне задачи (`interfaces.md:726-729` называет, где живёт метод) — исполнителю не хватает владельца, строки в списке изменений и теста, требующего заполнения, а не замысла.

### MINOR

**MINOR-1 — счёт генераторов «четыре» ложен третий раунд подряд.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:269-271`: «Генераторов, эмитящих закоммиченный модуль, четыре — `gen-nansen-coverage.ts`, `gen-defillama-dex-chains.ts`, `gen-defillama-chain-aliases.ts`, `gen-blockscout-chains.ts`». Пятый удовлетворяет собственному критерию предложения дословно: `packages/core/scripts/generate-nansen-cost-table.mjs` читает закоммиченную улику (`:39-42`, `docs/onchain-analytics/raw/nansen-openapi-2026-07-23.json`), пишет закоммиченный модуль (`:43`, `const outPath = path.resolve(packageRoot, 'src/adapters/nansen/cost-table.ts');`), который импортирует рантайм (`packages/core/src/adapters/nansen/cost-of.ts:2`), а его заголовок `:1` самоопределяется как «same "committed-.ts-literal, regenerated by hand" pattern». Раунд 7 назвал этот файл по координате (`docs/reviews/plan-014-review.md:399-400`); корректирующий проход переписал формулировку и дописал «пятым становится генератор этой задачи», посчитав новый генератор вместо пропущенного существующего. Ничего не исполняется на этом числе; предписание «новая улика пинится» не затронуто, страдает только производный знаменатель «улики двух из них запинены».

**MINOR-2 — 014-32b утверждает, что `TC-E2E-05` не ловит форму `All <число> routed capabilities`; 014-34 определяет её именно ловящей, и та же задача противоречит себе на `:286`.**
`docs/tasks/task-014-32b-tool-registration-stub.md:253-255`: «Гейт `TC-E2E-05` задачи 014-34 ловит формы `<число> rows` и `<число> of <число> capabilities`, а форму `All <число> routed capabilities` не ловит». Противоречащая улика — `docs/tasks/task-014-34-acceptance.md:126-131`, где третья форма перечислена среди входных данных TC-E2E-05 с пометкой «**Третья форма добавлена 2026-08-19.** Прежние две не ловили строку `packages/core/src/capability-manifest.ts:210`». Та же задача 32b на `:286` относит `:210` к гейтованным «через `TC-E2E-05` задачи 014-34». Действия исполнителя это не меняет: правка `:210` предписана с измеренной целью на `:241-245` («28 маршрутов над 27 способностями») и закреплена критерием `:443`. Переживает ложное, ненесущее нагрузки утверждение о поведении планируемого гейта — что стандарт «всякое утверждение о гейте истинно про код» запрещает независимо от последствия.

**MINOR-3 — строка способностей `dexscreener` в архитектуре не назначена и не читается ни одним гейтом.**
`docs/architectures/system-architecture-chain-normalization.md:1378`: `| `dexscreener` | `pairs.active`, `pool.info` | REST (`fetch`) | none (keyless) | R-6 Must requires both; `pool.info` gains its tool under R-21.1 …` — перечислены две способности. `32b:381-383` заставляет `capabilities()` объявлять третью, и TC-UNIT-07 (`:117`) это пинит. Единственный гейт над таблицей — `packages/mcp-server/test/docs-counts.test.ts:489-495` — проверяет только присутствие идентификатора адаптера (`expect(systemArchitecture, ...).toContain(id)`), никогда колонку способностей. Файл `system-architecture.md` назван в 32b только на `:59` и `:59`, в список гейтуемых документов (`:53-62`) не входит, оба предписанных грепа чисто числовые. Тихий дрейф документа без последствий на сьюте.

**MINOR-4 — `PLAN.md` объявляет восемь исключённых из UC-1 задач и называет шесть.**
`docs/PLAN.md:354` исключает восемь номеров («все, кроме 01, 05, 08, 17, 18, 19, 28, 31»); проза `:358-360` даёт четыре категории, которые резолвятся в шесть задач: `task-014-01-principal-probe.md:1`, `task-014-05-settings-classification.md:1`, `task-014-08-admin-operations.md:1` и 17/18/19 как «ветвь лимитера без 014-20». 014-28 (`free-to-paid-escalation`) и 014-31 (`shareable-values`) не покрыты ни одной категорией; ветвью лимитера они не являются — сам `PLAN.md:317-319` определяет её как R-7 → 17, 18, 19, 39, а 014-39 не исключена. Сами строки таблицы верны (`task-014-28-…:5` и `task-014-31-…:5` объявляют только UC-2) — недосчитывает пояснительное предложение. Последствий на исполнении нет; это счётное утверждение, чьё перечисление не доходит до собственного числа.

**MINOR-5 — базовое предложение R-21.3, процитированное 014-32c как её вход, не назначено ни одной задаче.**
`docs/architectures/interfaces.md:819` и `:817` (`R-21.1 removes one of the seven. R-21.3 covers the other six: each gets a tool or a declared row`) процитированы на `32c:105-111` как базовая линия. После регистрации `onchain_pool_info` задачей 32b и шести объявленных строк задачей 32c оба предложения ложны в настоящем времени («today», «before the gate can pass»), а список изменений 32c (`:31-68`) не содержит `interfaces.md` вовсе; буллеты 32b по этому файлу (`:124-129`) называют другие места. Гейта на эту форму нет, греп 32b слеп к `26 keys` (MAJOR-1). Устаревшая контрактная проза переживает этап молча.

### Статус находок раунда 7

**Закрыты, 7.**
- **MAJOR-2** (счёт «27 routes» в четырёх местах при одном назначенном): все четыре — `system-architecture-chain-normalization.md:396`, `:443`, `:870`, `:1176` — попадают во второй предписанный шаблон (`32b:267`), способностная половина покрыта первым; критерий `:153` опирается на процедуру, которая до них доходит.
- **MAJOR-4** (у AC-51 не было тест-кейса): TC-UNIT-10 добавлен на `32a:308-310` с пометкой «(AC-51)» и написан как сверка улики с матрицей — способом, объявленным в `TASK.md:565`; TC-UNIT-11 (`:311-312`) пинит умолчание `unverified`.
- **MAJOR-5** (`PLAN.md:63` пинил «одиннадцать утверждений»): `PLAN.md:63-64` теперь называет каналы и явно говорит, что перечень измеряется прогоном, а не пинится.
- **MINOR-1** (остаточное «в десяти местах»): снято; `32b:277-282` упоминает десять/девять/тринадцать только как историю трёх отвергнутых редакций, живого знаменателя там нет.
- **MINOR-2** (`system-architecture-call-budget.md:108` в списке на переписывание): перечень удалён целиком, правило `32b:272-274` направляет датированный замер в «не переписывается; рядом ставится действующее число».
- **MINOR-3** (три ссылки мимо референта на строку): все три исправлены и проверены в коде — `docs-counts.test.ts:311`, `readme-tool-table.test.ts:602`, `TASK.md:270` несут процитированный референт буквально.
- **MINOR-4** (счётчики редакции и раундов в четырёх местах): `PLAN.md:9`, `PLAN.md:435`, `TASK.md:15`, `TASK.md:650` обновлены и взаимно согласованы (редакция 6, семь раундов ревью плана).

**Закрыты наполовину, 2.**
- **MAJOR-1** (неполный знаменатель «26 строк манифеста»): счётный перечень заменён процедурой, и процедура достаёт три из четырёх носителей, названных раундом 7. Не достаёт `interfaces.md:819` — того самого, на котором стоит базовая линия `014-32c:105`, — а также `:618`, `:1369` и трёх заголовков тестов (MAJOR-1 настоящего раунда).
- **MAJOR-3** (у исхода `verified` не было клетки, второй конъюнкт не назван): клетка добавлена, но правило композиции и замер пересечения заменены утверждением о недостижимости, которое опровергается `dexscreener/index.ts:147-148` и `014-32c:129-130`; `nativeSymbol` в 014-32a встречается ноль раз (MAJOR-3 настоящего раунда).

**Открыта, 1.**
- **MINOR-5** (счёт генераторов): по-прежнему «четыре», `generate-nansen-cost-table.mjs` по-прежнему не назван (MINOR-1 настоящего раунда).

### Дефекты, внесённые корректирующим проходом раунда 7

1. **Мёртвая альтернатива и асимметричный шаблон.** `26 capabilit` (`32b:265`) не даёт ни одного места сверх уже покрытых, тогда как соседний шаблон `23 (distinct )?capabilit` (`:267`) допускает промежуточное слово. Три живых носителя формы `26 routed capabilities` — `capability-manifest.test.ts:67`, `ttl-coverage.test.ts:218`, `:239` — стали невидимы именно этим проходом (MAJOR-1). Класс M3 в чистом виде: правило применено к одному числу из двух.
2. **Греп без ограничения по путям.** Новая процедура сметает `docs/reviews`, `docs/tasks`, `docs/PLAN.md`, `docs/TASK.md` и исследовательский корпус — классы, которые `tool-inventory-docs.test.ts:123-134` исключает решением, записанным в исполняемом коде, — а правило трёх форм не имеет для них ветви (MAJOR-2).
3. **Критерий, невыполнимый буквально.** `:440-441` требует, чтобы повторный прогон не находил ни одного вхождения в настоящем времени, при том что сама задача несёт такие вхождения на `:239-240` и `:337`, а строка шаблона совпадает сама с собой (MAJOR-2).
4. **Ложное утверждение о планируемом гейте.** `32b:253-255` отрицает форму, которую 014-34 в том же проходе добавила (`:126-131`), и которой 32b сама пользуется на `:286` (MINOR-2).
5. **Декларация вместо замера.** `32a:203-204` («сеть, подтверждённая зондом, покрыта») подставлена на место запрошенного раундом 7 измерения пересечения и опровергается вторым конъюнктом предиката (MAJOR-3).

### Вердикт и обоснование

**ACCEPTED.** Принятие не усталостное и опирается на проверяемое различие между тем, что осталось, и тем, за что отклоняли. Вопрос раунда — сломает ли репозиторий разработчик, следующий плану буквально, и построит ли он не то, — получает по коду отрицательный ответ на обеих половинах: разбиение на четыре задачи, порядок этапов, зависимости (`32c:18-19`, `32d:18-22` ↔ `PLAN.md:278`/`:282`), интервал заглушки (`32b:60-71` ↔ `32c:55-60` ↔ `32d:66-73` ↔ `014-34:54-67`) и контракт Stub-First согласованы и подтверждены; независимый вывод красного набора из кода, выполненный двумя измерениями до чтения перечня, снова не нашёл ни одного гейтованного утверждения, которого 014-32b не называет, а все четыре координатные правки прошлого прохода легли на референт буквально. Ни один из четырёх MAJOR не делает сьюту красной, не меняет ни одного требования и не отменяет ни одного решения владельца: MAJOR-1 и MAJOR-2 оставляют устаревшие числа в прозе и заголовках и требуют двух дописанных альтернатив в шаблоне плюс ограничения по путям; MAJOR-3 требует одного замера и одного решения о композиции; MAJOR-4 — одной строки владельца в списке изменений и одного теста, требующего заполнения поля. Это работа объёма и природы исполнения. Против отклонения работает и наблюдаемая динамика: три раунда подряд корректирующий проход закрывал названное и вносил соседнее, причём именно на счётных утверждениях, — восьмое отклонение ради двух альтернатив в регулярном выражении с высокой вероятностью породило бы девятый носитель вместо закрытия класса. Структурное средство при этом впервые не переехало, а сработало: замена перечня процедурой достала носители, которые три редакции пропускали.

**За чем исполнителю следить, и первым действием исполнения:**

1. **Шаблоны грепа дополнить до прогона, а не после.** В первый шаблон добавить альтернативы `26 routed capabilit`, `returns 26` и `26 keys`; известные недостающие носители — `capability-manifest.test.ts:67`, `ttl-coverage.test.ts:218`, `:239`, `interfaces.md:618`, `:819`, `:1369`. Ограничить оба грепа путями `packages/`, `docs/architectures/`, `docs/ARCHITECTURE.md`, `README*.md`, `.env.example`, исключив `docs/reviews`, `docs/tasks`, `docs/plans`, `docs/issues`, `docs/backlog`, `docs/PLAN.md`, `docs/TASK.md`, `docs/dune-query-discovery` — тем же списком, который несёт `tool-inventory-docs.test.ts:123-134`. Критерий `:443-444` переформулировать так, чтобы он был выполним (повторный прогон в ограниченном объёме, исключая цитаты внутри самой задачи).
2. **Измерить пересечение `vendors.dexscreener != null` ∩ (`nativeSymbol == null` ∨ `deprecated`) и записать правило композиции для пары (зонд `verified`, покрытие `false`)** — до реализации R-33.5. Утверждение `32a:203-204` о недостижимости этой пары снять как ложное; клетку заполнить явно (текст отказа, называющий причину непокрытия, а не отсутствие зонда), TC-UNIT-10 расширить на неё.
3. **Назначить владельца вывода уровня комиссии.** До начала 014-32c решить, каким модулем идёт `eth_call` `fee()`, внести его в список изменений и добавить тест, требующий **заполнения** `feeTierBps` хотя бы на одной из 15 названных сетей, — иначе R-21.1 отгружается неисполненным при всех отмеченных галочках.
4. **Мелочи, механически:** счёт генераторов в `32a:269-271` (пять, с `generate-nansen-cost-table.mjs`); снять ложное утверждение `32b:253-255` о `TC-E2E-05`; дописать `pairs.active`/`pool.info`/`token.pools` в `system-architecture-chain-normalization.md:1378`; привести перечисление на `PLAN.md:358-360` к восьми номерам строки `:354`; назначить кому-то `interfaces.md:819`/`:817`.

План безопасно передавать разработчику, который следует ему буквально: система строится верная, сьюта на стабовом коммите зелёная, а весь остаточный риск — устаревшие числа в прозе и заголовках тестов, одна неопределённая клетка текста отказа и одно неназначенное плечо вывода, все три обнаруживаемые и исправимые внутри исполнения.

## Раунд 9 — 2026-08-20, вердикт ACCEPTED

0 BLOCKING, 4 MAJOR, 5 MINOR — после дедупликации 22 переживших находки шести измерений (19 находок опровергнуты верификаторами и сняты). Чтение привязано к фингерпринту из брифа: HEAD `a7d6385`, незакоммиченное дерево ветки `feat/t-014-network-transport-planning`, 23 пути, отпечаток отслеживаемой дельты `1a616b228f2f11ff`, отпечаток неотслеживаемых файлов `353aadc674456b9a` (раунд 8: `0d4f8391718d9d00` / `7c26ca6d9ed4b603` — дерево изменилось, корректирующий проход раунда 8 и есть предмет этого раунда).

**Инструмент.** `check_positional_refs.py` **не прогнан**, сьюта **не прогнана**: ни у одного из шести измерений нет исполняющего инструмента в этой роли. Ведущий прогнал предписанные задачей `grep`-и дословно и точечные чтения; все координаты ниже проверены буквальным чтением цитируемых диапазонов. Предписанный задачей 014-32b замер неисправленной сьюты этим раундом не подтверждён — это условие остаётся непроверенным, а не пройденным.

**Главный итог раунда.** Пять из десяти находок раунда 7 и два из девяти пунктов раунда 8 закрыты по коду; ещё три — наполовину. Позиционная точность впервые перестала быть проблемой: около 110 пар «координата + референт» в четырёх файлах 014-32\*, в 014-34 и в `PLAN.md` проверены вручную, референт стоит на цитируемой строке буквально во всех, включая обе правки этого прохода (`dexscreener/index.ts:148`, `coverage.ts:89`). Арифметика сходится целиком: таблица оценки 141–235 при заявленном 141–235, 44 задачи в 15 этапах, девять маркеров `[STUB]` при собственном счёте девять, 53 критерия без дыр и дублей, RTM по одному пункту на 34 требования, счета тулов (20→22), способностей (26→27), маршрутов (27→28) и реестра (458 строк, 23 `nativeSymbol: null`, 19 `rpcHosts`) верны против живого кода. Остаток — один и тот же класс M3 в четырёх местах: правило сформулировано в одной точке и не донесено до типа, строки списка изменений, критерия или шаблона, из которых исполнитель реально работает.

### MAJOR

**MAJOR-1 — расширенный шаблон грепа по-прежнему слеп к формам `returns 26` и `26 keys`; критерий приёмки сертифицирует полноту слепым инструментом.**
`docs/tasks/task-014-32b-tool-registration-stub.md:266`, буквально:
`-E "26 ([a-z]+ )?(rows|capabilit)|26 строк|twenty-six rows|26 of 26|manifest of 26" \`
Критерий `:459-460` требует, чтобы повторный прогон не находил ни одного вхождения старого числа в настоящем времени.

Расширение работает наполовину и ровно на той половине, которую раунд 8 назвал первой: `([a-z]+ )?` действительно достаёт три заголовка тестов — `packages/core/test/capability-manifest.test.ts:67`, `packages/core/test/ttl-coverage.test.ts:218` и `:239`, — которых шаблон раунда 8 не видел. Три остальных носителя, переданных автору поимённо с готовым лекарством (`docs/reviews/plan-014-review.md:554`, «добавить альтернативы `26 routed capabilit`, `returns 26` и `26 keys`»), не пойманы ни одной альтернативой; прогон дословно по дереву даёт в `docs/architectures/interfaces.md` только `:1366` и `:1385`:

- `docs/architectures/interfaces.md:618`, `Object.keys(capabilityManifests)` returns 26. The historical text stands unchanged; the current`
- `docs/architectures/interfaces.md:813`, `**Seven manifest keys are served by no tool today** (measured 2026-08-13, 26 keys against 20 tools):`
- `docs/architectures/interfaces.md:1363`, `returns 26, and the count`

Из трёх альтернатив применена одна — подпись класса M3 на третьем раунде подряд. Класс, объявленный самой задачей на `:259-261`, включает маршрутизируемые способности, то есть все шесть мест внутри объёма. Ничто другое их не читает: `capability-manifest.test.ts:46` проверяет только нижнюю границу, `TC-E2E-05` задачи 014-34 (`task-014-34-acceptance.md:124-133`) ограничена файлом `packages/core/src/capability-manifest.ts`, а греп по `docs/tasks` за формой `interfaces.md:<n>` возвращает лишь `32b:206` (`:829`), `32b:342` (`:17`), `32c:102` (`:810`), `32c:110` (`:817`), `014-34:119`/`:120` — ни одна из трёх координат никому не назначена.

Тяжесть ограничена сверху и это установлено, а не предположено: `:610-612` открывается «Measured 2026-08-12», `:1361` — «**Measured state, 2026-08-12.**», поэтому по собственному правилу задачи (`:286`) и по тай-брейку (`:291-294`) эти две строки не переписываются даже будучи найденными — теряется приписанное рядом действующее число, а не снимается ложное утверждение. Несущая половина — `:813`: дата в предложении есть, но написано оно в настоящем времени («served by no tool **today**»), и после 32b/32c/32d ложно по обоим числам сразу (27 ключей, 22 тула). Последствие на исполнении: `:459-460` отмечается зелёным, потому что шаблон этих строк не находил и на повторном прогоне не найдёт. Сьюта зелёная ⇒ MAJOR, не BLOCKING.

**MAJOR-2 — область поиска названа явно, но содержит ровно те классы путей, которые исполняемый гейт репозитория исключает решением в коде, и предложение, обосновывающее область, ложно про этот гейт.**
`docs/tasks/task-014-32b-tool-registration-stub.md:267`/`:270` — аргументы обоих грепов `docs packages README.md README.ru.md .env.example` при исключениях только `node_modules`, `.git`, `dist`. `:279-281`, буквально: «`docs/architectures/version-history.md`, `packages/*/dist/` и сгенерированные `docs/dune-query-discovery/**` дают шум, который не правится. Список путей выше — тот же, что обходят документные гейты.»

Опровергается тремя уликами. Первая — сама команда: `docs/dune-query-discovery/**` названо шумом, которого область избегает, и лежит внутри `docs`; фактически исключён только `dist`. Вторая — замер: первый шаблон по объявленной области даёт **70 попаданий в 24 файлах**, из них 22 в `docs/reviews/plan-014-review.md`, 1 в `docs/reviews/task-014-review.md`, 17 в шести файлах `docs/dune-query-discovery/`, 8 в самой 32b и по одному-двум в `docs/tasks/task-014-{13,23,40}` и `docs/backlog/wi-43-…`; на документы, которые задача обязана править, приходится меньше десятой части. Третья — код: `packages/mcp-server/test/tool-inventory-docs.test.ts:123-134` исключает `/^docs\/tasks\//`, `/^docs\/plans\//`, `/^docs\/reviews\//`, `/^docs\/issues\//`, `/^docs\/backlog\//`, `/^docs\/dune-query-discovery\//`, `/^docs\/onchain-analytics\/raw\//`, `/^docs\/TASK\.md$/`, `/^docs\/PLAN\.md$/`, а причина записана в докстринге `:33-34`: «Rewriting history to match today's inventory would be worse than no gate at all». Ни один из девяти классов в область не внесён; `docs-counts.test.ts` обходит фиксированный подокументный список, который тоже не совпадает с этой областью. Это нарушение стандарта раунда «всякое утверждение о поведении гейта истинно про код», и это второй проход подряд по MAJOR-2 раунда 8, закрытый декларацией вместо ограничения (лекарство со списком исключений выдано на `:554`).

Правило трёх форм (`:283-289`) ветви для этих классов не имеет — его «не трогается вовсе» называет только `version-history.md`, changelog-поля `ARCHITECTURE.md` и «тексты, прямо названные записями прошлого», — а тай-брейк `:291-294` направляет спорное в «рядом ставится действующее число». Последствие на исполнении: буквальный исполнитель дописывает числа внутрь постоянной записи ревью (`docs/reviews/task-014-review.md:63`, настоящее время внутри завершённого аудита) и внутрь датированных замеров четырёх чужих задач. Правка аддитивная, видна в `git diff` и откатывается ⇒ MAJOR, не BLOCKING. Опровергнутая подчасть: «критерий `:459-460` буквально невыполним» — преувеличение, критерий квалифицирован «в настоящем времени», а строка шаблона и цитаты кода утверждениями настоящего времени не являются.

**MAJOR-3 — вывод уровня комиссии не имеет владельца ни в одном списке изменений и не имеет теста, требующего заполнения поля.**
`docs/tasks/task-014-32c-pool-info-tool.md:85-86`: «Вывод — один `eth_call` метода `fee()`, селектор `0xddca3f43`», `:88-92` перечисляет 15 сетей. Список изменений `:30-67` называет ровно четыре файла — `packages/mcp-server/src/tools/pool-info.ts`, `packages/core/src/adapters/dexscreener/index.ts` (только второй HTTP-шаг), `packages/mcp-server/eval/capabilities.mjs`, `docs/architectures/data-model.md`; `rpc-evm` в нём нет, `feeTierBps` в нём нет.

Противоречащая улика: `grep -rn "eth_call" packages/` даёт **ноль** попаданий — метода в коде не существует; `docs/architectures/interfaces.md:726-729` прямо назначает владельца («`eth_call` becomes a third method on `rpc-evm`»), то есть решение принято, и задача, которая его отгружает, его не несёт; `providers.config.ts` маршрутизирует `pool.info` только на `dexscreener`, чей аллоулист — `api.dexscreener.com` (`interfaces.md:762-763`), поэтому вывод пересекает адаптеры без владельца. Тесты не спасают: TC-E2E-01 (`:122`) требует только адреса токенов и резервы, TC-E2E-02 (`:122`) — только отказ, а критерий `:149` («Отсутствие уровня комиссии выражено типизированным отказом, не пустым значением») полностью удовлетворяется безусловным отказом. `docs/TASK.md:243` при этом требует «адреса токенов, резервы **и уровень комиссии**». Последствие на исполнении: 014-32c отгружается со всеми отмеченными галочками и `feeTierBps`, не заполненным ни на одной из 15 названных сетей, — положительное плечо R-21.1 остаётся неисполненным. Это дословно MAJOR-4 раунда 8; корректирующий проход его не касался, то есть пункт **не закрыт, а повторён**. Закрывается одной строкой владельца и одним тестом ⇒ MAJOR.

**MAJOR-4 — третья формулировка отказа R-33.5 донесена до правила и до тест-кейса и не донесена до строки списка изменений и до обоих критериев приёмки.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:206-210` вводит три формулировки, `:328-330` (TC-UNIT-08) требует трёх различимых текстов и поимённо называет случай «зонд `verified`, `nativeSymbol` отсутствует». Но `:236`, буквально: «Оно вызывает `coverageStatus(...)` и передаёт `hint`, различающий `excluded` и `unverified`»; критерий `:375`: «R-33.5: `unverified` отличим от `excluded` в матрице покрытия и в тексте отказа»; критерий `:386`: «Отказ строится в `adapters/registry.ts` и несёт `hint`, различающий `excluded` и `unverified`». Требование в `docs/TASK.md:362-363` различает три исхода, значит последний гейт перед «готово» сертифицирует два из трёх.

Пара, ради которой третья формулировка существует, реальна и проверена: предикат требует двух условий (`packages/core/src/adapters/dexscreener/index.ts:148`, `chain.vendors['dexscreener'] != null && chain.nativeSymbol != null,`), матрица дополнительно отбрасывает `deprecated` (`packages/core/src/chain/coverage.ts:89`), а в реестре 23 строки несут `nativeSymbol: null`. Опровергнуто и снято из формулировки два более сильных утверждения: (а) «объявленный тип не может нести третью формулировку» — `CoverageStatus` тут ни при чём, `:207` говорит, что текст выбирает ответ **хука**, а хук объявлен трёхзначным на `:153` (`chainProbeStatus?(chain): 'verified' | 'excluded' | 'unverified'`), так что реализация выразима без расширения типа; (б) «исполнитель отгрузит две формулировки с зелёными галочками» — TC-UNIT-08 в том же файле делает такую реализацию красной. Переживает то, что исполнитель, работающий по списку изменений и по списку критериев, получает контракт, которому его же тест-кейс противоречит, и вынужден задать вопрос до начала работы ⇒ MAJOR, не BLOCKING.

### MINOR

**MINOR-1 — строка адаптера `dexscreener` в архитектурной таблице несёт две способности и никому не назначена.**
`docs/architectures/system-architecture-chain-normalization.md:1378`, буквально: `| `dexscreener` | `pairs.active`, `pool.info` | REST (`fetch`) | none (keyless) | R-6 Must requires both; `pool.info` gains its tool under R-21.1 …`. При этом `32b:400-402` заставляет `capabilities()` объявлять третью способность, а TC-UNIT-07 (`:136`) её пинит. `system-architecture.md` присутствует в 32b только на `:76` (`:1176`) и `:80` (`:246`); греп по `docs/tasks` за `:1378` не даёт ничего. Единственный гейт над таблицей — `packages/mcp-server/test/docs-counts.test.ts:489-495` — проверяет только присутствие идентификатора адаптера (`expect(systemArchitecture, …).toContain(id)`), оба предписанных грепа чисто числовые. Раунд 8 назвал эту строку в пункте 4 лекарства; правка не применена. Тихий дрейф документации, сьюта зелёная ⇒ MINOR.

**MINOR-2 — пояснение к строке UC-1 объявляет восемь исключённых задач и перечисляет шесть.**
`docs/PLAN.md:354`, буквально: `| UC-1 — n8n вызывает способность по сети | все, кроме 01, 05, 08, 17, 18, 19, 28, 31 |`. `:358-360`: «Восемь исключённых задач — проба принципала, классификация настроек, админ-операции и вся ветвь лимитера без 014-20». Четыре категории резолвятся в 01, 05, 08 плюс ветвь лимитера, которую сам же файл определяет как R-7 → 014-17, 014-18, 014-19, 014-39 (`PLAN.md:317`; 014-39 при этом не исключена). 014-28 (R-28, `:338`) и 014-31 (R-18, `:328`) не покрыты ни одной категорией; сама строка таблицы верна — `docs/tasks/task-014-28-free-to-paid-escalation.md:5` объявляет только UC-2. Лекарство выдано раундом 8 на `:557` и не применено. Последствий на исполнении нет ⇒ MINOR, но это ровно тот класс счётных утверждений, к которому план себя обязывает.

**MINOR-3 — базовые предложения R-21.3, процитированные 014-32c как её вход, не принадлежат ни одной задаче.**
`docs/tasks/task-014-32c-pool-info-tool.md:102` и `:110` цитируют `docs/architectures/interfaces.md:816` и `:817` как улику, но список изменений 32c (`:31-68`) не содержит записи `interfaces.md` вовсе, а буллеты 32b (`:124-129`) покрывают только якоря §5.1.7/§5.1.8, преамбулу §5.1 и строки 829-830. После 32b и 32c предложения `:819` («Seven manifest keys are served by no tool today … 26 keys against 20 tools») и `:817` («R-21.1 removes one of the seven. R-21.3 covers the other six») ложны в настоящем времени, и ни одна задача не указывает на них исполнителю. Пункт «назначить кому-то `interfaces.md:819`/`:817`» стоял в лекарстве раунда 8 (`:562`) и не применён. Пересекается с MAJOR-1 по `:819`, но дефект другой: там слепота инструмента, здесь отсутствие владельца ⇒ MINOR.

**MINOR-4 — производный знаменатель «двух из пяти» неверен.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:295-297`: «В `docs/provenance.json` запинены улики двух: blockscout и defillama, то есть двух из пяти». Проверено по файлам: улики запинены у **трёх** генераторов — `docs/provenance.json:12` (blockscout), `:44` (defillama-dexs-allchains) и `:36` (`defillama-chain-name-vocabularies-2026-08-11.json`, читается `gen-defillama-chain-aliases.ts:47`); без улики ровно два, и оба nansen (`gen-nansen-coverage.ts:25`, `generate-nansen-cost-table.mjs:41` читают `nansen-openapi-2026-07-23.json`, которого в `provenance.json` нет). «Двух» верно про вендоров и ложно в клаузе «то есть двух из пяти», привязывающей счёт к пяти генераторам. Раунд 8 предупредил ровно об этом производном знаменателе (`:508`); проход исправил 4→5 и оставил производную. Предписание «новая улика пинится» не затронуто ⇒ MINOR.

**MINOR-5 — счётчики редакции и раундов ревью отстают на один в четырёх местах.**
`docs/PLAN.md:9` («| Редакция | 6, после седьмого ревью границы фазы 2026-08-20 |»), `docs/PLAN.md:435` («три раунда ревью спецификации, три — архитектуры, семь — плана»), `docs/TASK.md:15` («спецификация 3 · архитектура 3 · план 7»), `docs/TASK.md:650` (то же прозой). Дерево несёт раунд 8 целиком (`docs/reviews/plan-014-review.md:436`, секция `:436-559`), и рассматриваемая дельта — ответ на него: завершённых раундов плана восемь, редакция седьмая. Отклонялось раундом 5 (`:128`), повторено раундом 7 (`:396-397`), записано раундом 8 с этими же четырьмя координатами (`:531`). Факт живёт в четырёх местах, обновлено ноль ⇒ MINOR (провенанс).

### Статус находок раунда 7 по номерам

- **MAJOR-1** (знаменатель «26 строк» неполон, критерий сертифицирует ложную полноту) — **наполовину закрыт**. Перечень заменён процедурой, `([a-z]+ )?` достаёт три заголовка тестов; формы `returns 26` и `26 keys` остаются невидимы → переоткрыт как MAJOR-1 раунда 9.
- **MAJOR-2** (счёт «27 routes» в четырёх местах, назначено одно) — **закрыт**: второй шаблон `2[347] ([a-z]+ )?(routes|capabilit)` покрывает класс целиком, перечня больше нет.
- **MAJOR-3** (исход зонда `verified` не отображён в статусе; второй конъюнкт предиката не назван) — **наполовину закрыт**: правило композиции `32a:206-210`, третья формулировка и трёхзначный хук `:153` появились, второй конъюнкт назван буквально (`dexscreener/index.ts:148`); строка `:236` и критерии `:375`/`:386` остались двузначными → MAJOR-4 раунда 9.
- **MAJOR-4** (AC-51 без тест-кейса) — **закрыт**: TC-UNIT-10 на `32a:330-333` сверяет улику зонда с матрицей объявленным в `TASK.md` способом.
- **MAJOR-5** (`PLAN.md` пинит отменённый перечень) — **закрыт**: `PLAN.md:62-64` теперь говорит «перечень задача не пинит, а измеряет прогоном».
- **MINOR-1** (32b противоречит собственному знаменателю) — **закрыт**: «в десяти местах» снято, `:297` описывает историю недосчётов как довод против перечня.
- **MINOR-2** (датированный замер в списке переписывания) — **закрыт по существу**: список переписывания упразднён; `system-架`… `system-architecture-call-budget.md:108` теперь попадает под первый шаблон и обрабатывается правилом форм.
- **MINOR-3** (три координаты выше референта) — **закрыт**: `docs-counts.test.ts:311`, `readme-tool-table.test.ts:602`, `TASK.md:270` несут референт буквально.
- **MINOR-4** (счётчики редакции и раундов) — **открыт**, третий раунд подряд → MINOR-5 раунда 9.
- **MINOR-5** (генераторов пять, а не четыре) — **закрыт**: `32a:291-292` называет пять с буквальной цитатой `generate-nansen-cost-table.mjs:1`; остался производный знаменатель → MINOR-4 раунда 9.

### Дефекты, внесённые корректирующим проходом раунда 8

1. **Ложное утверждение о поведении исполняемого гейта.** Предложение `32b:281` «Список путей выше — тот же, что обходят документные гейты» внесено этим проходом и опровергается `tool-inventory-docs.test.ts:123-134` по девяти классам путей (MAJOR-2). Раунд 8 просил ограничить область по этому списку; вместо ограничения записано равенство, которого нет.
2. **Объявленное ограничение области не реализовано.** `32b:279-280` называет `docs/dune-query-discovery/**` шумом, который область исключает, а строка `:267`/`:270` выше этот же путь включает; измеренный шум — 63 попадания из 70 (MAJOR-2).
3. **Половинчатое расширение шаблона.** Из трёх альтернатив, переданных поимённо на `:554`, применена одна; три носителя, названные координатами, остались невидимы (MAJOR-1).
4. **Асимметрия правила трёх формулировок.** Правило и TC-UNIT-08 переведены на три исхода, строка списка изменений `32a:236` и оба критерия `:375`/`:386` оставлены на двух (MAJOR-4).
5. **Производная от исправленного числа не пересчитана.** Счёт генераторов исправлен 4→5, клауза «то есть двух из пяти» осталась и стала ложной (MINOR-4).

### Вердикт и обоснование

**ACCEPTED.** Вопрос раунда — сломает ли репозиторий разработчик, следующий плану буквально, и отгрузит ли он то, чего спецификация не просила, — получает по коду отрицательный ответ на обеих половинах. Разбиение на четыре задачи, порядок этапов, зависимости (`32c:18-19`, `32d:18-22` ↔ `PLAN.md:278`/`:282`), интервал заглушки (`32b:60-71` ↔ `32c:55-60` ↔ `32d:66-73` ↔ `014-34:54-67`) и контракт Stub-First проверены и согласованы; Stub-First подтверждён не декларацией, а гейтами: `tool-spec.test.ts:522-539` требует единственной `const CAPABILITY` и ограничивает `resolveCapability(` только при вызове, поэтому стабовый хендлер, отвечающий типизированным отказом, зелёный, а записи `CAPABILITY_EXCLUSIONS` удовлетворяют `eval-capability-coverage.test.ts:48-50`. Два измерения независимо вывели красный набор из кода **до** чтения перечня и снова не нашли ни одного гейтованного утверждения, которого 014-32b не называет; арифметика, RTM и карта критериев сходятся полностью; все координатные правки прошлого прохода легли на референт буквально, и впервые за пять раундов ни одно измерение не нашло ни одной несуществующей ссылки, кроме одной, которую верификаторы сняли. Ни один из четырёх MAJOR не делает сьюту красной, не меняет ни одного требования и не отменяет ни одного решения владельца: MAJOR-1 и MAJOR-2 закрываются двумя альтернативами в регулярном выражении и списком путей, MAJOR-3 — одной строкой владельца и одним тестом, MAJOR-4 — одной строкой буллета и двумя критериями. Против девятого отклонения работает и наблюдаемая динамика: три прохода подряд корректирующее изменение закрывало названное и вносило соседнее именно на счётных и обосновательных предложениях, — отклонение ради четырёх механических правок с высокой вероятностью породило бы пятую. Структурное средство при этом продолжает работать: греп-процедура достала носители, которые четыре редакции перечня пропускали, и класс «перечень стареет между написанием и исполнением» закрыт для всех форм, которые шаблон покрывает.

**За чем исполнителю следить, и первым действием исполнения:**

1. **Шаблоны и область грепа доправить до прогона, а не после.** В первый шаблон добавить `returns 26` и `26 keys` (недостающие носители: `interfaces.md:618`, `:819`, `:1369`). Область ограничить `packages/`, `docs/architectures/`, `docs/ARCHITECTURE.md`, `README*.md`, `.env.example`, исключив `docs/reviews`, `docs/tasks`, `docs/plans`, `docs/issues`, `docs/backlog`, `docs/PLAN.md`, `docs/TASK.md`, `docs/dune-query-discovery` — тем же списком, который несёт `tool-inventory-docs.test.ts:123-134`; предложение `32b:281` о равенстве области документным гейтам снять как ложное. Пока это не сделано, **не править** ничего под `docs/reviews`, `docs/tasks` и `docs/dune-query-discovery`: это записи прошлого, и решение об этом стоит в исполняемом коде.
2. **Назначить владельца вывода уровня комиссии до начала 014-32c.** Решить, каким модулем идёт `eth_call` `fee()` (`interfaces.md:726-729` называет `rpc-evm`), внести файл в список изменений и добавить тест, требующий **заполнения** `feeTierBps` хотя бы на одной из 15 сетей `32c:88-92`. Иначе R-21.1 отгружается неисполненным при всех отмеченных галочках.
3. **Привести R-33.5 к трём исходам во всех местах, а не в двух.** `32a:236` и критерии `:377`/`:388` переписать под три формулировки; текст третьей брать из `:207-210`, источник — `chainProbeStatus`, расширять `CoverageStatus` не нужно. TC-UNIT-08 — контракт, которому реализация обязана соответствовать.
4. **Мелочи, механически:** дописать `token.pools` в `system-architecture-chain-normalization.md:1378`; привести перечисление `PLAN.md:358-360` к восьми номерам строки `:354`; назначить владельца `interfaces.md:816`/`:819`/`:817`; исправить производный знаменатель на `32a:295-297` (улики запинены у трёх генераторов из пяти, без улики — два nansen-овских); поднять счётчики редакции и раундов в четырёх местах (`PLAN.md:9`, `:435`, `TASK.md:15`, `:650`).

**Замечание по самой записи, не дефект плана.** Бриф раунда 9 сообщает, что раунды 7 и 8 вернули REJECTED; заголовки `docs/reviews/plan-014-review.md:328` и `:436` записывают оба как ACCEPTED, а `:425`/`:550` несут развёрнутое обоснование принятия. Числовые части брифа с файлом совпадают дословно, расходится только слово вердикта. Файл объявляет себя единственной записью, по которой следующий раунд подтверждает закрытие (`:3-5`, `:561-564`), поэтому расхождение следует устранить в записи, а не в транскрипте.

План безопасно передавать разработчику, который следует ему буквально: система строится верная, сьюта на стабовом коммите зелёная, а весь остаточный риск — устаревшие числа в прозе, одна процедура поиска, требующая двух дописанных альтернатив и списка путей, одно неназначенное плечо вывода и один недоведённый до критериев текст отказа, все обнаруживаемые и исправимые внутри исполнения.

## Раунд 10 — 2026-08-20, вердикт ACCEPTED

**Инструмент.** `check_positional_refs.py` в этом раунде **не прогнан**, сьюта **не прогнана**, замер объёма триажа (443 строки, `32b:281`) **не воспроизведён**: ни у одного из шести измерений нет исполняющего инструмента в этой роли. Ведущий проверил ключевые несущие координаты чтением через `Bash`. Всё, что требует прогона, отмечено как непроверенное, а не как пройденное. Дерево: HEAD `a7d6385`, незакоммиченное рабочее дерево на `feat/t-014-network-transport-planning`, 23 пути, отпечатки `9194999997c36f1e` / `c62184a8b74452bc` — процитированы из брифа.

**Счёт.** BLOCKING 0 · MAJOR 3 · MINOR 5. Шестнадцать переживших находок шести измерений после дедупликации сведены к восьми: R-33.5 названа четырьмя измерениями (closure/refs/rtm/handoff), слепота грепа — четырьмя (grep/mechanism/refs/rtm), счётчики редакции — двумя, счёт «один» в `32c:123-125` — двумя. Опровергнуто верификаторами 15.

**Главный итог раунда.** Три MAJOR раунда 9, требовавшие кода, закрыты по коду: слепой греп заменён намеренно избыточным шаблоном с постфильтром и списком исключений, буквально скопированным с исполняемого гейта (`tool-inventory-docs.test.ts:123-134`); область изменений больше не противоречит гейту; вывод комиссии `eth_call` получила поимённого владельца (`packages/core/src/adapters/rpc-evm/index.ts`), кейс TC-E2E-02a и критерий, который безусловный отказ не закрывает. Четыре MINOR раунда 9 закрыты. Пережившее — один тот же половинчато применённый подпункт (четвёртый раунд подряд), один негейтованный носитель счёта тулов, который новый шаблон не видит по регистру, и один тест-кейс, который расширение `CoverageStatus` до четырёх значений сделало истинным по построению.

### MAJOR

**MAJOR-1 — третья формулировка отказа R-33.5 дошла до правила композиции, типа и TC-UNIT-08, но не дошла до строки списка изменений и ни до одного из двух критериев приёмки.**

`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:236-238` — «Оно вызывает `coverageStatus(...)` и передаёт `hint`, различающий `excluded` и `unverified`»; критерий `:377` — «R-33.5: `unverified` отличим от `excluded` в матрице покрытия и в тексте отказа»; критерий `:388` — «Отказ строится в `adapters/registry.ts` и несёт `hint`, различающий `excluded` и `unverified`». Все три строки двузначны и прочитаны буквально.

Противоречащая улика внутри того же файла: `:201` объявляет `CoverageStatus = 'covered' | 'excluded' | 'unverified' | 'vendor-serves-chain-capability-absent'` — **четыре** значения; `:204-212` задаёт три формулировки отказа и заканчивается «Три формулировки, ни одна не совпадает с другой»; TC-UNIT-08 (`:330-332`) требует «три исхода зонда дают три различимых текста отказа… сеть с зондом `verified` и без `nativeSymbol` получает третью формулировку, а не текст „зонд не выполнялся“». Само требование трёхзначно: `docs/TASK.md:362-363` — «текст отказа различают три исхода зонда: подтверждён, исключён вендором, не проверен». Пара достижима: `packages/core/src/adapters/dexscreener/index.ts:148`, `chain.vendors['dexscreener'] != null && chain.nativeSymbol != null,` плюс `packages/core/src/chain/coverage.ts:89`. Другой критерий третий исход не поглощает: AC-51 (`:380-381`) запрещает лишь утверждать, что вендор не обслуживает обслуживаемую сеть, — сборка с двумя формулировками, выдающая «зонд не выполнялся», его проходит; `:389-390` покрывает только умолчание для адаптера без хука.

Это дословно MAJOR-4 раунда 9 (`docs/reviews/plan-014-review.md:598-601`) с выписанным средством на `:649` («`32a:236` и критерии `:375`/`:386` переписать под три формулировки»). Корректирующий проход переписал абзац **непосредственно над** этими строками (`:199-212`, добавив четвёртое значение) и оставил все три координаты байт-в-байт, сдвинув их на +2 ростом соседней прозы. Знаменатель: факт живёт в пяти местах (требование, правило, тип, кейс, критерии), применён в трёх, четвёртый раунд подряд — подпись M3.

**Следствие при исполнении:** последний чек-лист перед «готово» (в приёмке 014-34 пункта по R-33 нет) сертифицирует два исхода из трёх, тогда как TC-UNIT-08 в той же задаче требует трёх. Исполнитель, работающий по списку изменений и чек-боксам, упирается в противоречие внутри одной задачи и останавливается на вопрос до того, как напишет вызов в `registry.ts`. Сьюта на стабе не краснеет, разбиение задач и зависимости не меняются — MAJOR, не BLOCKING; восстановимо одной правкой трёх строк внутри исполнения.

**MAJOR-2 — шаблон грепа регистрозависим на числительном прописью, и один живой негейтованный счёт тулов в настоящем времени внутри его же области поиска невидим и грепу, и всем гейтам.**

`docs/tasks/task-014-32b-tool-registration-stub.md:265-269` — первый `grep -RIn … -E '\b(2[0-9]|twenty[- ][a-z]+|двадцать [а-я]+)\b'`, без `-i`; `-i` стоит только на третьей ступени (`:271`), поэтому строка, отвергнутая первой ступенью, до постфильтра не доходит. `docs/architectures/functional-architecture.md:117` читается буквально: `- **Twenty registered tools**, all zod in/out and registry-routed, declared once in` — заглавная `T`, ни одной цифры на строке; не совпадает ни с одной из трёх альтернатив. За строкой идёт перечисление ровно двадцати тулов (`:119-142`).

Негейтованность подтверждена: в `packages/mcp-server/test/docs-counts.test.ts` по этому файлу ровно два шаблона — `/(\d+) adapters registered/g` (`:147-149`) и `/mcp-server — (\d+) tools/g` (`:179-182`, узел C4), а область перечисления WI-48 заякорена на узле C4 (`:292-294`); `tool-inventory-docs.test.ts` требует от файла только упоминания **имён** тулов (`inventory-channels.ts:49`). Заявка `32b:274-275` — «Он ловит любое число из двадцатых рядом с любым из релевантных существительных, на обоих языках» — про команду как написана неверна; критерий `:485` («Все числа тулов в настоящем времени обновлены») при этом сертифицирует полноту. Знаменатель показывает половинчатость, а не равномерный пропуск: ровно параллельный негейтованный случай назван вручную (`32b:188-191`, `docs/ARCHITECTURE.md:119`), а `ROADMAP.md:272` («**Двадцать тулов**», заглавная Д, тоже мимо `двадцать [а-я]+`) уцелел только потому, что назначен отдельно на `:161-162`/`:488`.

**Следствие при исполнении:** минимальная правка, зелёная по обоим гейтам этого файла, — узел C4; заголовок «Twenty registered tools» и перечисление под ним остаются на 20 при реестре из 22, критерий `:485` отмечен, сьюта зелёная. Смягчение честное и учтено: файл гейтован на **имена**, поэтому исполнитель почти наверняка правит подпункты рядом со строкой 117 и может заметить; но собственная история репозитория (`docs-counts.test.ts:243-246`) фиксирует дрейф именно этого файла именно так, поэтому «почти наверняка заметит» — не механизм. Область грепа объявлена по трём числам 26/26/27, счёт тулов ловится соседним предписанием, — поэтому MAJOR, а не BLOCKING: результат тихий устаревший текст в документе, правка аддитивна и обратима.

**MAJOR-3 — TC-UNIT-10, единственный тест-кейс AC-51, стал истинным по построению; объявленный им способ падения не роняет его.**

`32a:335-337`: «TC-UNIT-10 (AC-51): улика зонда сверяется с матрицей покрытия — для каждой пары (`pairs.active`, сеть), у которой в улике статус `verified`, `coverageStatus` не возвращает `excluded`. Падает при откате колонки к литералу из трёх сетей». Вывод против того же файла: правило `:204-212` возвращает `covered`, если предикат истинен, и при ложном предикате отображает ответ хука `verified` → `vendor-serves-chain-capability-absent`; хук определён как чтение сгенерированного модуля (`:159`, `:232`), модуль несёт исход зонда из улики (`:189-191`), а TC-UNIT-04 (`:323`) пинит равенство «модуль = вывод генератора по улике». Значит для любой пары, у которой **улика** говорит `verified`, значение `excluded` недостижимо при любом содержимом колонки — утверждение не может упасть. Объявленная мутация (откат колонки к литералу из трёх сетей) переводит эти сети из `covered` в `vendor-serves-chain-capability-absent`, снова не в `excluded`; откат вместе с модулем даёт умолчание `unverified` (`:251`) — тоже не `excluded`.

Подстраховки нет: TC-REG-01 указывает на `packages/core/test/coverage.test.ts:348` — это нижняя граница R-57 (`ethereum`, `solana`, ещё одна), не детектор покрытия dexscreener; `AC-51` в `docs/tasks` встречается только в 014-32a. Раунд 8 закрыл MAJOR-4 раунда 7 именно на этом кейсе (`plan-014-review.md:91` секции статуса), а корректирующий проход раунда 9, добавив четвёртое значение, кейс обесточил, не заметив этого.

**Следствие при исполнении:** R-33 уходит в исполнение без регрессионного детектора L-18, которого требовал раунд 7: единственный критерий закрывается кейсом, который зелен при любой реализации, включая ту, что снова начнёт утверждать вендорское исключение на обслуживаемой сети (через `excluded` по умолчанию у соседних адаптеров это перекрыто TC-UNIT-11, но по самому dexscreener — нет). Восстановимо переписыванием одного кейса: сверять надо не `!== 'excluded'` против улики, а текст отказа против пары (улика `verified`, предикат `false`) — то есть ровно тот случай, ради которого введено четвёртое значение. Работа по объёму и природе принадлежит исполнению; сьюта на стабе не краснеет.

### MINOR

**MINOR-1 — 014-32c предписывает переписать `interfaces.md:819` под счёт, который дважды опровергает та же задача.**

`docs/tasks/task-014-32c-pool-info-tool.md:123-125`: «Строка 813 — `**Seven manifest keys are served by no tool today**` — после этой задачи неверна в обоих числах и в самом утверждении: ключей не семь, а один, и он обслужен. Предложение переписывается по факту, вместе с числами `26 keys against 20 tools`». После 32b/32c необслуживаемых ключей остаётся **шесть**, и об этом сказано в том же файле дважды: `:133` («Шесть оставшихся закрывает R-21.3, и это объём этой задачи») и критерий `:171` («Шесть оставшихся необслуживаемых ключей несут строку объявленного списка с причиной»); целевой документ это уже предвосхищает — `docs/architectures/interfaces.md:823`, «R-21.1 removes one of the seven. R-21.3 covers the other six». Семёрка измерена на `32c:117-119` и совпадает с `interfaces.md:819-821` буквально. Оговорка верификатора принята: клаузула «и он обслужен» связна лишь при втором чтении, где «один» обозначает `pool.info`, а не число замены, — то есть текст неоднозначен, а не однозначно ложен. Прозу никакой гейт не читает (AC-29 сличает ключи манифеста с полями тулов, `interfaces.md:814-817`). **Следствие:** буквальное исполнение публикует свежий ложный счёт в настоящем времени в документе, ради де-дрейфа которого существует T-014; самокорректируется у внимательного исполнителя, так как истина стоит дважды в пределах пятидесяти строк.

**MINOR-2 — у `interfaces.md:819` два владельца с противоположными правилами обработки.**

Строка несёт `26`, существительное `manifest` и лежит под `docs/` вне всех исключений грепа 014-32b (`:265-272`), а её `26` — одно из трёх чисел, ради которых греп написан (`:260-261`). По собственному правилу формы 014-32b (`:300`) она **датированный замер** («в предложении стоит дата или слово `Measured`/`замер`») — «не переписывается; рядом ставится действующее число», и разрешение спорного случая (`:305-308`, «Переписанный замер невосстановим») подтверждает то же. 014-32c:125 предписывает противоположное — «Предложение переписывается по факту». Предложение удовлетворяет обоим триггерам сразу (`measured 2026-08-13` и настоящее время `today`) и становится ложным, когда приземляется 32b — за одну задачу до того, как её касается 32c. Проза негейтована ⇒ MINOR, но одна координата с двумя взаимоисключающими правилами в двух задачах одного плана — дефект плана, а не предпочтение.

**MINOR-3 — позиционная ссылка, которой 014-32a доказывает существование пары (зонд подтвердил, покрытия нет), указывает на постороннюю прозу.**

`32a:220-221` ссылается на `docs/tasks/task-014-32c-pool-info-tool.md:129` без цитаты референта, а `32c:129` — это «**Why это в списке изменений.** Оба предложения задача цитирует как свой вход, и оба они описывают…», абзац про семь ключей манифеста, где нет ни `nativeSymbol`, ни непокрытой подтверждённой сети. Нужный референт — `32c:155-156` (TC-UNIT-02/-03 про `chainSupport` без `nativeSymbol`); вставка вывода комиссии на `32c:83-108`, сделанная корректирующим проходом, сдвинула их примерно на 26 строк (155−26 = 129). Нарушен собственный стандарт плана (координата + буквальная подстрока). Исполнение не затрагивает.

**MINOR-4 — счётчики редакции и раундов ревью отстают на два в четырёх местах.**

`docs/PLAN.md:9` («| Редакция | 6, после седьмого ревью границы фазы 2026-08-20 |»), `docs/PLAN.md:435-436` («три раунда ревью спецификации, три — архитектуры, семь — плана»), `docs/TASK.md:15` («спецификация 3 · архитектура 3 · план 7»), `docs/TASK.md:649-650` (то же прозой). Дерево несёт раунд 9 полностью (`plan-014-review.md:561`), рассматриваемая дельта — ответ на него, значит завершённых раундов плана девять, редакция восьмая. Конвенция не изобретена находкой: те же четыре координаты названы раундом 5 (`:128`), раундом 7 (`:396-397`), раундом 8 (`:531`) и раундом 9 (`:617-618`). Знаменатель четыре, применено ноль, отставание выросло с одного до двух. Только провенанс, ни один гейт этих полей не читает.

**MINOR-5 — оборванная клауза в доказательной фразе 014-32a.**

`32a:256`: «`packages/core/src/adapters/rpc-evm/index.ts:150`, `chain.rpcHosts !== null &&` — предикат стоит на, а `docs/architectures/data-model-entities.md:144` читает этот `null` как нашу» — у предлога «на» нет дополнения. Обе цитируемые координаты резолвятся точно (`rpc-evm/index.ts:150` — `chain.rpcHosts !== null &&`; `data-model-entities.md:144` — строка `rpcHosts`), то есть стандарт позиционных ссылок соблюдён, дефект — только в оборванной фразе внутри абзаца, переписанного корректирующим проходом. Аргумент несут соседние клаузы (`:278-281`) и критерий `:108`; следствия при исполнении нет.

### Статус находок раунда 7

- **MAJOR-1** (неполный знаменатель «26 строк манифеста», критерий сертифицировал ложную полноту) — **закрыт**. Счётное перечисление заменено процедурой, и процедура достаёт все носители, названные раундами 7-9: `interfaces.md:618`, `:819`, `:1369`, `ARCHITECTURE.md:120`, `system-architecture-chain-normalization.md:396`/`:443`/`:1176`, `.env.example:260`. Класс «негейтованный счёт мимо шаблона» пережил на другой оси — счёт тулов прописью с заглавной (MAJOR-2 настоящего раунда).
- **MAJOR-2** (счёт «27 routes» в четырёх местах при одном назначенном) — **закрыт** раундом 8, подтверждено: все четыре носителя попадают в шаблон.
- **MAJOR-3** (у исхода зонда `verified` не было клетки в `CoverageStatus`) — **закрыт**: четвёртое значение `vendor-serves-chain-capability-absent` (`32a:201`), правило композиции `:164-172`, второй конъюнкт предиката назван и процитирован (`dexscreener/index.ts:148`).
- **MAJOR-4** (у AC-51 не было тест-кейса) — **вновь открыт**. Раунд 8 закрыл его на TC-UNIT-10; корректирующий проход раунда 9, добавив четвёртое значение, сделал кейс истинным по построению (MAJOR-3 настоящего раунда). Детектора L-18, которого требовал раунд 7, снова нет.
- **MAJOR-5** (`PLAN.md:63` пинил «одиннадцать утверждений») — **закрыт**: `PLAN.md:61-64` называет каналы и говорит, что перечень измеряется прогоном.
- **MINOR-1** (остаточное «в десяти местах») — **закрыт**: `32b:311-312` упоминает десять/девять/тринадцать только как историю трёх отвергнутых редакций.
- **MINOR-2** (`system-architecture-call-budget.md:108` в списке на переписывание) — **закрыт**: перечень удалён, правило формы `32b:299-308` направляет датированный замер в «не переписывается».
- **MINOR-3** (три ссылки мимо референта на строку) — **закрыт**: все координаты четырёх файлов задач проверены поштучно, ни одной мимо референта.
- **MINOR-4** (счётчики редакции и раундов) — **вновь открыт и углублён**: закрыт раундом 8, снова открыт раундом 9, отставание выросло до двух (MINOR-4 настоящего раунда).
- **MINOR-5** (счёт генераторов) — **закрыт**: `32a:292-300` называет пять с цитатой заголовка пятого и объявляет генератор задачи шестым; заявка о провенансе снята, а не переформулирована.

### Дефекты, внесённые корректирующим проходом раунда 9

1. Расширение `CoverageStatus` до четырёх значений обесточило TC-UNIT-10 — единственный кейс AC-51 стал истинным по построению, а объявленная мутация его не роняет (MAJOR-3).
2. Новый пункт `32c:123-125`, взявший во владение `interfaces.md:819`, предписывает счёт «один» там, где та же задача дважды говорит «шесть» (MINOR-1).
3. Тот же пункт создал вторую, противоположную политику обработки для координаты, которую правило формы 014-32b относит к датированным замерам (MINOR-2).
4. Вставка вывода комиссии `eth_call` на `32c:83-108` сдвинула строки и оставила ссылку `32a:220-221` указывающей на посторонний абзац (MINOR-3).
5. В переписанном абзаце `32a:256` осталась оборванная клауза «предикат стоит на, а» (MINOR-5).

Не внесены корректирующим проходом, а перенесены: половинчатая R-33.5 (MAJOR-1, четвёртый раунд), негейтованный `functional-architecture.md:117` (MAJOR-2, ранее не назывался ни одним раундом), счётчики редакции (MINOR-4).

### Вердикт и обоснование

**ACCEPTED.** Ни одна из трёх MAJOR не делает сьюту красной, не меняет разбиение на четыре задачи, порядок этапов, зависимости, интервал заглушки и контракт Stub-First, и ни одна не заставляет исполнителя построить не то, что просит спецификация: MAJOR-1 — противоречие **внутри** одной задачи, где тест-кейс требует трёх формулировок, а чек-лист сертифицирует две, поэтому буквальное следование останавливает работу на вопросе, а не на неверной сборке; MAJOR-2 даёт в худшем случае одну устаревшую негейтованную строку в документе, правка которой аддитивна и обратима; MAJOR-3 отнимает регрессионный детектор, но требует одного переписанного кейса — работы объёма и природы исполнения. Единственное, что могло бы стать BLOCKING, — вопрос маршрутизации к новому `eth_call` в 014-32c — опровергнут: `rpc-evm` назван владельцем, `servedCapabilities`-механика существует, а решение о способе подключения принадлежит исполнению, не планированию; та же участь у восьми других претендентов на BLOCKING, снятых верификаторами. Пять MINOR — провенанс и проза. Три задачи из четырёх передаются разработчику сегодня; 014-32a требует одного уточнения (две формулировки или три) до того, как будет написан вызов в `registry.ts`, и это уточнение помещается в один абзац. Десятый отказ означал бы применить к остатку метод, который сам план уже признал несостоятельным: каждый независимый пересчёт негейтованной прозы находит новый носитель, и одиннадцатый нашёл бы следующий.

**Что исполнителю держать в поле зрения.** (1) Начать 014-32a с решения по R-33.5: правило `:204-212` и TC-UNIT-08 требуют трёх формулировок — привести к ним `:236-238`, `:377`, `:388` **до** правки `registry.ts`, иначе чек-лист будет закрыт над реализацией, которую роняет собственный тест задачи. (2) Переписать TC-UNIT-10 так, чтобы он падал: сверять текст отказа на паре (улика `verified`, предикат `false`), а не отсутствие `excluded`, которое недостижимо по построению. (3) При прогоне грепа 014-32b помнить, что числительное прописью в шаблоне регистрозависимо: пройти отдельно `Twenty`/`Двадцать` с заглавной — как минимум `docs/architectures/functional-architecture.md:117` и перечисление `:119-142` гейт не читает. (4) `interfaces.md:819` обработать один раз и по правилу 014-32b (датированный замер: действующее число рядом, исходный текст сохранён), а не переписыванием, предписанным `32c:125`; при переписывании остаётся **шесть** необслуживаемых ключей, не один. (5) Замер объёма триажа (443 строки) и предписанный прогон неисправленной сьюты этим раундом не подтверждены — оба выполняются исполнителем как первое действие, и результат сверяется с перечнем в обе стороны, как требуют `32b:328-331` и `:474-477`.

## Раунд 11 — 2026-08-20, вердикт ACCEPTED

0 BLOCKING, 0 MAJOR, 3 MINOR — после дедупликации шести переживших находок шести измерений (16 находок опровергнуты верификаторами и сняты; четыре из шести survivors оказались одной и той же координатой, названной четырьмя измерениями независимо). Чтение привязано к фингерпринту из брифа: HEAD `a7d6385`, незакоммиченное дерево ветки `feat/t-014-network-transport-planning`, 23 пути, отпечаток отслеживаемой дельты `4c3b65b33788b8cb`, отпечаток неотслеживаемых файлов `87d933355ba011ea`. Ведущий сверил все три пережившие находки по файлам напрямую (`git status --short` подтверждает состав дельты: 20 изменённых путей, два переименования `32b→32c` и `32c→32d`, три неотслеживаемых файла).

**Инструмент.** `check_positional_refs.py` в этом раунде **не прогнан** — ни у одного из шести измерений нет исполняющего инструмента в этой роли; сьюта не прогонялась. Предписанные 014-32b прогон неисправленной сьюты и замер триажа (443 строки, `32b:300`) этим раундом **не подтверждены** — это непроверенные условия, а не пройденные; оба выполняет исполнитель первым действием. Все координаты ниже проверены буквальным чтением цитируемых диапазонов.

**Главный итог раунда.** Все восемь находок раунда 10 отработаны: шесть закрыты, одна закрыта наполовину, одна открыта; регрессий ранее закрытого нет. Впервые за девять раундов **не осталось ни одной находки уровня MAJOR**, и ни одно измерение не нашло гейтованного утверждения, которое краснеет от самой правки и не названо в 014-32b. Три ключевые проверки этого раунда — механизменная, ссылочная и передаточная — сходятся: (1) TC-UNIT-10 переписан в фальсифицируемую форму, и объявленная мутация его действительно роняет — при откате колонки к литералу из трёх сетей предикат `packages/core/src/adapters/dexscreener/index.ts:148` ложен, зонд по-прежнему отвечает `verified`, статус становится `vendor-serves-chain-capability-absent` ≠ `covered`; (2) R-33.5 доведена до трёх формулировок во всех пяти носителях — правило `32a:204-212`, тип `:201`, TC-UNIT-08 `:332-334`, строка списка изменений `:236-239` и **оба** критерия `:385-386`/`:397`, знаменатель 5 из 5; (3) `-i` стоит на обеих ступенях грепа (`32b:281` `-Ei`, `:284` `grep -Ei`), и `docs/architectures/functional-architecture.md:117` («`- **Twenty registered tools**, all zod in/out and registry-routed, declared once in`», заглавная `T`, цифр на строке нет) проходит все три ступени конвейера; сама строка и перечисление двадцати имён `:119-142` названы в `32b:193-204` с верным основанием — единственная область WI-48 этого файла закреплена на узле C4 (`packages/mcp-server/test/docs-counts.test.ts:292-294`, `from: /MCP\["MCP server @onchain-intel\/mcp-server — \d+ tools/`), поэтому перечисление не читает ни один гейт. Числовая самосогласованность плана сходится полностью: 8×2-3 + 25×3-5 + 7×6-10 + 4×2-4 = 141-235 при 44 файлах задач на диске, 141/7→20, 235/7→34; девять маркеров `[STUB]`; 53 критерия (AC-1…AC-52 плюс AC-17b) без дыр и дублей; 34 требования по одной проекции; переносимые счётчики верны по живому коду (тулы 20→22, способности 26→27, маршруты 27→28, `scannedFieldCount` 29→30, строки §3.2 21→22).

### MINOR

**MINOR-1 — позиционная ссылка, обосновывающая пару (зонд подтвердил, покрытия нет), указывает на посторонний абзац; за этот проход промах вырос.**
`docs/tasks/task-014-32a-dexscreener-chain-coverage.md:220-221` читается буквально: «Пара (зонд подтвердил, покрытия нет) — не гипотетическая: она предписана этим же планом в `docs/tasks/task-014-32c-pool-info-tool.md:129`, а в реестре 23 строки несут `nativeSymbol: null`.» Цитируемая координата `docs/tasks/task-014-32c-pool-info-tool.md:129` читается «**Почему шесть, а не один.** `pool.info` уходит из семёрки, потому что его обслуживает тул этой» — абзац арифметики ключей манифеста, в котором нет ни `nativeSymbol`, ни зонда, ни сети без покрытия. Несущий референт — `32c:40` («`chainSupport` перестаёт требовать `nativeSymbol` для способностей, адресуемых по адресу») и `32c:159` («**TC-UNIT-02:** `chainSupport` возвращает `true` для `pool.info` на сети без `nativeSymbol`»). Раунд 10 записал этот же дефект (`docs/reviews/plan-014-review.md:702-704`), назвав тогда верную цель `32c:155-156`; корректирующий проход вставил новый блок `32c:129-131` и сдвинул референт на `:159`, **не тронув ссылку**, — подпись M3 (правка применена к одному месту из двух). Подстрока рядом с координатой не процитирована, поэтому собственный стандарт плана (координата + буквальный референт) на этой строке тоже не выполнен. Последствие только провенансное: проверяющий, ищущий мотивацию четвёртого значения `CoverageStatus`, попадает в чужой абзац; само обоснование независимо подтверждено кодом на `32a:214-219` (`dexscreener/index.ts:148`, `chain/coverage.ts:89` — обе координаты сегодня резолвятся дословно). Сьюту не краснит ⇒ MINOR. Названо четырьмя измерениями из шести независимо.

**MINOR-2 — счётчик редакции отстаёт: раунд применён в четырёх местах, редакция — ни в одном.**
`docs/PLAN.md:9` читается «`| Редакция     | 7, после девятого ревью границы фазы 2026-08-20      |`»; `docs/PLAN.md:435-436` — «три раунда ревью спецификации, три — архитектуры, девять — плана»; `docs/TASK.md:15` — «план 9»; `docs/TASK.md:649-650` — «девять плана». Номер раунда, таким образом, применён 4 из 4. Но раунд 10 предписал **обе** величины одной строкой (`docs/reviews/plan-014-review.md:708`: «завершённых раундов плана девять, редакция восьмая»), а редакция осталась `7`. Запись раунда 10 присутствует в этой же дельте (`docs/reviews/plan-014-review.md:656`, «## Раунд 10 — 2026-08-20»), то есть рассматриваемое дерево — ответ на десятый раунд, и редакция после него восьмая. Пятый раунд подряд на тех же координатах, каждый раз применена ровно половина факта. Последствие только провенансное: `docs/PLAN.md` и `docs/TASK.md` исключены из документного гейта (`packages/mcp-server/test/tool-inventory-docs.test.ts:131-132`), ни один тест не краснеет ⇒ MINOR.

**MINOR-3 — суффикс «DESIGNED, not registered» переживает в четырёх местах `interfaces.md`, а 014-32b назначает только преамбулу §5.1.**
Поиск по репозиторию даёт ровно пять вхождений строки: `docs/architectures/interfaces.md:621` («`#### 5.1.7 The pool tool (T-014, R-21.1) — DESIGNED, not registered`»), `:643` («`// tier where derivable. DexScreener-backed, keyless, 0 credits. DESIGNED, not registered.`»), `:843` («`#### 5.1.8 The token-pools tool (T-014, R-34) — DESIGNED, not registered`»), `:864` («`// DESIGNED, not registered.`») и `docs/tasks/task-014-32d-token-pools-tool.md:39`, где заголовок §5.1.8 **вместе с суффиксом** процитирован как якорь контракта задачи. Список изменений 014-32b по этому файлу (`docs/tasks/task-014-32b-tool-registration-stub.md:123-128`) назначает две вещи: якорь `// Capability:` для §5.1.7/§5.1.8 (`:125`) и «Преамбула §5.1 перестаёт называть тулы спроектированными и не зарегистрированными» (`:127`) — это другое предложение, `docs/architectures/interfaces.md:12`: «**Two further tools are designed and not registered.**». Ни один другой пункт 014-32b, 014-32c, 014-32d или 014-34 к четырём вхождениям не подходит (проверено поиском по «5.1.7 / 5.1.8 / спроектирован / DESIGNED» во всех четырёх файлах): числовой обход по построению не достаёт их (`32b:281`, шаблон `\b(2[0-9]|twenty[- ][a-z]+|двадцать [а-я]+)\b` — ни на одной из четырёх строк нет числа третьего десятка), обязательство по гейтованным документам (`:94-104`) требует лишь присутствия имени тула в файле, а `docs-counts` читает числовые утверждения и якоря `// Capability:`. Знаменатель проверен: пять вхождений, назначено ноль (плюс отдельно назначенная преамбула — шестое, другое предложение). Автор демонстрирует владение этим классом заявлений на `32b:186` (для `ARCHITECTURE.md`) и на `:126` — и останавливается, снова подпись M3. Последствие в момент исполнения: после 014-32b документ, задача которого — быть источником недрейфующей истины, утверждает про два зарегистрированных тула, что они не зарегистрированы, и ни один гейт этого не ловит; если же исполнитель по своей инициативе почистит заголовки, перестанет резолвиться буквальная цитата контракта в `task-014-32d-token-pools-tool.md:39`. Правка аддитивна и обратима, работы — две строки ⇒ MINOR.

### Статус находок раунда 7 по номерам

- **MAJOR-1** (знаменатель «26 строк манифеста» неполон, критерий сертифицирует ложную полноту) — **закрыт**, подтверждено третий раунд подряд. Процедура достаёт все носители, названные раундами 7-10, включая формы, за которые отклоняли раунды 7-9 (`interfaces.md:618`/`:819`/`:1369`/`:1372`/`:1391`, три заголовка тестов `26 routed capabilities`, четыре носителя `27 routes`), и сверх перечня — `capability-manifest.ts:122` через совпадение существительного с путём файла.
- **MAJOR-2** (счёт «27 routes» в четырёх местах при одном назначенном) — **закрыт**.
- **MAJOR-3** (у исхода зонда `verified` не было клетки в `CoverageStatus`) — **закрыт**.
- **MAJOR-4** (у AC-51 нет тест-кейса) — **закрыт**. Раунд 10 переоткрыл его: расширение `CoverageStatus` сделало TC-UNIT-10 истинным по построению. Корректирующий проход переписал кейс (`32a:337-344`) в форму «`coverageStatus` возвращает `covered` для каждой сети, у которой улика зонда `verified`, `nativeSymbol` непуст и нет `deprecated`», добавив на `:342-345` объяснение, почему прежняя форма «не возвращает `excluded`» не могла упасть. Проверено против правила композиции: объявленная мутация кейс роняет. Претензия «AC-51 истинен по построению» поднята измерением RTM и **опровергнута** верификатором.
- **MAJOR-5** (`PLAN.md:63` пинил «одиннадцать утверждений») — **закрыт**.
- **MINOR-1** (остаточное «в десяти местах») — **закрыт**.
- **MINOR-2** (датированный замер в списке переписывания) — **закрыт**; `32c:123-125` теперь классифицирует `interfaces.md:819` как датированный замер (`measured 2026-08-13` присутствует на строке буквально) и предписывает приписку рядом, согласуясь с правилом формы `32b:319-320`.
- **MINOR-3** (позиционные ссылки мимо референта) — **открыт на одной координате** (MINOR-1 настоящего раунда); закрывался раундом 10 на трёх файлах задач, четвёртая координата пережила и за этот проход отдалилась от референта.
- **MINOR-4** (счётчики редакции и раундов) — **закрыт наполовину, пятый раунд подряд**: раунд применён 4 из 4, редакция не применена (MINOR-2 настоящего раунда).
- **MINOR-5** (счёт генераторов) — **закрыт**.

### Дефекты, внесённые корректирующим проходом раунда 10

1. Новый блок `docs/tasks/task-014-32c-pool-info-tool.md:129-131` («Почему шесть, а не один») сдвинул референт ссылки `32a:220-221` с `:155` на `:159`, оставив саму ссылку нетронутой, — единственный дефект, порождённый этим проходом (MINOR-1). Проход одновременно закрыл на этой же строке две другие находки раунда 10 (счёт «один» → «шесть», конфликт двух политик обработки координаты), то есть внёс провенансный дефект, устранив два содержательных.

Не внесены проходом, а перенесены: отставание счётчика редакции (MINOR-2, пятый раунд) и суффикс «DESIGNED, not registered» в четырёх местах (MINOR-3, ни одним предыдущим раундом не назывался).

### Вердикт и обоснование

**ACCEPTED, и на этот раз без оговорок уровня MAJOR.** Операциональный вопрос раунда — сломает ли разработчик, следующий плану буквально, репозиторий или соберёт не то, что просит спецификация, — получает отрицательный ответ по всем шести измерениям: ни одно механизменное утверждение вида «гейт X на `file:line` делает Y» не оказалось ложным при поштучной проверке примерно семидесяти координат кода; независимый вывод красного набора, выполненный двумя измерениями **до** чтения перечня 014-32b, не нашёл ни одного гейтованного утверждения, которого в перечне нет; контракт Stub-First подтверждён не заявкой, а кодом — стабовый модуль с `const CAPABILITY` и без вызова `resolveCapability(` проходит `packages/mcp-server/test/tool-spec.test.ts:522-541`, две строки `CAPABILITY_EXCLUSIONS` удовлетворяют `eval-capability-coverage.test.ts:48-51`, а `eval/checks.mjs` правки не требует, потому что `checks` выводится из `cases/` (`checks.mjs:38-63`), — то есть 014-32b действительно приземляется зелёной на заглушках. Три пережившие находки — провенанс и проза: неверная координата в обосновании, отставший на единицу счётчик редакции и негейтованный суффикс в четырёх строках архитектурного документа. Ни одна не делает сьюту красной, не меняет разбиение на четыре задачи, порядок этапов, зависимости, интервал заглушки, ни один счёт, по которому исполнитель действует. Одиннадцатый отказ был бы отказом по инерции: девять кандидатов в BLOCKING и MAJOR (маршрутизация `eth_call` без достижимого вызывающего, замороженные схемы публикуемых входов, конфликт TC-UNIT-06 и TC-UNIT-10, отсутствие фазы формы у 014-32a, фильтрация `docs/ARCHITECTURE.md:10` вторым шагом грепа и другие) сняты верификаторами по коду, а остаток убывает монотонно — 23, 17, 22, 16, 6.

**Что исполнителю держать в поле зрения.** (1) Первым действием 014-32b выполнить предписанное `32b:347-350`: прогнать **неисправленную** сьюту, записать измеренный список падений и сверить его с перечнем задачи **в обе стороны**; ни этот прогон, ни замер «443 строки» (`32b:300`) ни одним раундом не подтверждены — оценка ведущего измерения ≈400 строк по той же области с записанным числом согласуется, но проверенным его не делает. (2) При обходе грепом помнить о структурной особенности второй ступени: `grep -vE 'docs/(TASK|PLAN)\.md|version-history\.md'` фильтрует по **всей строке вывода**, а не по пути, поэтому `docs/ARCHITECTURE.md:10` вычёркивается из-за ссылки на changelog в собственном тексте — строка несёт «26 rows / 20 tools» в настоящем времени, её не читает ни один гейт (`docs-counts.test.ts:169`/`:242` требуют других формулировок) и не ловит приёмка этапа (`task-014-34-acceptance.md:36` проверяет три других литерала); во всей области такая строка ровно одна, и обработать её нужно вручную. (3) Заодно с преамбулой §5.1 обработать четыре оставшихся вхождения «DESIGNED, not registered» (`interfaces.md:621`, `:643`, `:843`, `:864`) — и, тронув заголовки, поправить цитату в `task-014-32d-token-pools-tool.md:39`, иначе якорь контракта перестанет резолвиться. (4) В 014-32c два вопроса решаются только выходом за текст задачи: в каком модуле живут шесть строк объявленного списка AC-29 (`32c:137-141` называет работу, но не файл; `interfaces.md:830-833` отвечает `CAPABILITY_KNOWN_GAPS` и предостерегает от заведения второго списка) и в каком файле стоит сам гейт AC-29 — принять оба решения до правки, а не в её ходе. (5) Способ подключения вывода комиссии `eth_call` в `rpc-evm` — решение исполнения, но принимать его нужно осознанно: тул доходит до адаптера только через `resolveCapability(capability, …)` (`packages/core/src/routing/registry.ts:1755-1780`), `pool.info` маршрутизируется исключительно в `dexscreener`, а одно-`CAPABILITY`-гейт `tool-spec.test.ts:535-540` закрывает очевидный обход. (6) Ссылку `32a:220-221` перед работой над третьей формулировкой R-33.5 читать как указывающую на `32c:40` и `32c:159` — по указанной координате `32c:129` нужного референта нет.

## Правило

Каждый следующий раунд дописывает свою секцию сюда. Отчёт, существующий только в транскрипте сессии,
не является записью.
