{
  "$comment": [
    "Known-failing eval rows, as DATA — adding or removing one is a config edit, never a code change.",
    "",
    "WHAT THIS IS NOT: this file does NOT affect `eval/run.mjs`, its verdicts, or its exit code.",
    "`pnpm eval` stays red while a real defect is open, because it is also a release gate and a gate",
    "that goes green over an open defect is worse than no gate. This file is read ONLY by",
    "`scripts/eval-gate.mjs` (`pnpm gate`), and only to decide whether a failing row should BLOCK a",
    "task from being called done. Acknowledgement is a property of the gate's report, not of grading.",
    "",
    "WHAT AN ENTRY MEANS: 'at most `maxFailing` of these `rows` are failing, we know why, it is filed,",
    "and a human decided it must not block unrelated work until `reviewBy`'. It does NOT mean 'hidden'.",
    "Every entry is NAMED on every run with its issue id AND its live tally, so a set sliding from one",
    "failing row toward its bound is visible long before it blocks (L-2).",
    "",
    "WHY A SET AND A BOUND, NOT A ROW AND A BOOLEAN (RF-10, task 014-43). A per-row boolean asserts a",
    "CONSTANT — this row is failing — and rejects an intermittent failure from both sides at once: the",
    "run where it fails reports it as unfiled, and the run where it passes reports the entry as stale.",
    "Every vendor failure this project has met is intermittent. A bound absorbs the rotation while",
    "still blocking growth; `reviewBy` replaces 'the row passes' as the trigger to look again.",
    "",
    "SETTING `maxFailing`: it is the number of rows measured failing WHEN THE ENTRY WAS WRITTEN, never",
    "`rows.length` and never `rows.length - 1`. One more failing row than that is a NEW fact, and the",
    "gate blocks so a human re-measures. Raising the bound is therefore an act of measurement — say so",
    "in `why`, with the date. A bound equal to `rows.length` is refused by the gate: it could never be",
    "exceeded, so it would accept the total outage the bound exists to keep visible.",
    "",
    "WHAT COUNTS AS A MEASUREMENT (owner decision 2026-08-24, after WI-65). A bound is the maximum over",
    "TWO CONSECUTIVE runs whose `link.verdict` was `stable` — the gate now measures its own egress while",
    "it runs and prints that verdict above the failures. A run whose link was `degraded` or `unknown` is",
    "an OBSERVATION, not a measurement, and no bound may be set or raised from it. This is not caution",
    "for its own sake: on 2026-08-24 one run blamed three vendors at once and the cause was a stall on",
    "our own uplink, so a bound raised from it would have bought slack the vendors never earned — after",
    "which the next real widening would have arrived inside that slack, unseen. Two runs rather than one",
    "because a flapping vendor gives a different number every time: the same capability measured 1, 2, 3,",
    "4 and 6 failing rows across five runs of that one day.",
    "",
    "WHEN THE VENDOR REALLY HAS DEGRADED (owner decision 2026-08-24). Raise the bound and carry the",
    "degradation to `reviewBy`, rather than leaving the gate blocked — a blocked gate stops unrelated",
    "work, which is the cost this file exists to avoid. Two limits on that. The bound still comes from",
    "two stable runs, so 'the vendor is bad today' is not by itself a reason to raise anything. And a",
    "NEAR-TOTAL outage is not degradation: a bound covering most of its own set accepts the outage it",
    "exists to keep visible, so that case goes back to the three exits — the vendor recovers, the",
    "capability stops being advertised, or a second adapter serves it.",
    "",
    "SETTING `reviewBy`: at most 90 days out, enforced. A date further away is a permanent exemption",
    "wearing the costume of a review. When it arrives the gate blocks and names the entry; the cure is",
    "to re-measure and then either extend the date with the new evidence in `why`, or close the issue",
    "and delete the entry. A green run in between is NOT a closure — see RF-10's own Do-not.",
    "",
    "ROWS: '<chain>/<capability>', exactly as the eval reports them. A chain-less row (the server and",
    "registry checks) uses the em-dash the eval itself prints: '—/<capability>'. A row this file names",
    "that the matrix no longer contains BLOCKS: coverage shrank, which is the RF-5 shape.",
    "",
    "`issue` MUST reference a record that exists in docs/KNOWN_ISSUES.md, and since task 014-43 the",
    "gate CHECKS it. An acknowledgement without a filed issue is just silence with extra steps."
  ],
  "acknowledged": {
    "L-12/token.holders": {
      "issue": "L-12",
      "since": "2026-08-11",
      "reviewBy": "2026-09-20",
      "rows": [
        "ethereum/token.holders",
        "base/token.holders",
        "arbitrum/token.holders",
        "polygon/token.holders",
        "gnosis/token.holders"
      ],
      "maxFailing": 1,
      "why": "Re-measured 2026-08-21 (task 014-42, scripts/probe-blockscout-holders-latency.ts). The 5s hop ceiling this entry was written against is gone: token.holders now runs at HOLDERS_TIMEOUT_MS = 60_000 under a 60_000 deadline, and ethereum, arbitrum, polygon and gnosis all answer — their rows were cleared from this file by the same task. base did NOT recover: across three probe rounds it answered once, at 45.8s, and returned HTTP 500 or nothing otherwise, on the PRO facade and on the keyless base.blockscout.com alike. Same day, /api/v2/stats on base answered in 0.99s, so this is that chain's holders index and not the host. NOT a ceiling problem any more, which is why raising it further is not the next move: L-12's remaining half is one chain's index, and the open options there are the paid route or retiring the capability on base. SET AND BOUND: the five chains the capability is served on, bound 1 — the number measured failing when this entry was rewritten on 2026-08-21, which is base alone. A second failing chain is a new fact and blocks until someone measures it. Naming only base/token.holders here would have been a bound that can never be exceeded, i.e. a permanent blindfold over that row."
    },
    "L-20/chain.transactions": {
      "issue": "L-20",
      "since": "2026-08-20",
      "reviewBy": "2026-09-20",
      "rows": [
        "ethereum/chain.transactions",
        "base/chain.transactions",
        "arbitrum/chain.transactions",
        "polygon/chain.transactions",
        "gnosis/chain.transactions"
      ],
      "maxFailing": 1,
      "why": "Blockscout answers HTTP 500 for base's /api/v2/stats — the document chain.transactions reads. Measured 2026-08-20: 500 on two attempts of three through the PRO facade and on the keyless base.blockscout.com alike, so there is no second route. Re-measured the morning of 2026-08-21: 500 on four of five attempts, worse. Hours later the same document answered in 0.98s on every chain and this row passed the gate. THIS ENTRY EXISTS WHILE THE ROW IS GREEN, and that is the whole point of task 014-43: under the old per-row boolean the entry had to be deleted the moment the vendor recovered, so the next 500 arrived as an unfiled failure and blocked. One green run is not a closure — L-20's own fix-path item 2 says so. Bound 1 over the five chains chain.transactions is served on: base is the measured one, and a SECOND chain answering 500 would be a new fact about the vendor rather than this one."
    },
    "L-24/dex.volume.history": {
      "issue": "L-24",
      "since": "2026-08-24",
      "reviewBy": "2026-09-24",
      "rows": [
        "ethereum/dex.volume.history",
        "solana/dex.volume.history",
        "base/dex.volume.history",
        "arbitrum/dex.volume.history",
        "polygon/dex.volume.history",
        "bsc/dex.volume.history",
        "avalanche/dex.volume.history",
        "berachain/dex.volume.history",
        "gnosis/dex.volume.history",
        "tron/dex.volume.history",
        "bitcoin/dex.volume.history"
      ],
      "maxFailing": 4,
      "why": "Замерено 2026-08-24 напрямую против вендора, и обе альтернативные причины отвергнуты замером, а не рассуждением. НЕ наш дедлайн и не размер документа: /overview/dexs/bsc — 9.18 с на 620 КБ, /overview/dexs/avalanche — 32.84 с на 320 КБ, /protocol/pancakeswap — ни байта за 60 с, /protocol/babylon-protocol — 1.06 с на 48 КБ. Размер этот порядок не объясняет, и те же маршруты укладываются в 15 с, когда вендору хорошо, то есть это не форма L-22, где потолком был наш собственный бюджет. НЕ наш егресс: прогон C того же дня шёл во время измеренного локального провала (пять несвязанных хостов на одинаковом полу ~1.6 с, через полторы минуты 0.39–0.53 с, WI-65), и его строки в границу НЕ засчитаны — граница взята с прогона D, чей канал измерен здоровым. ПОЧЕМУ ТОЛЬКО ЭТИ ДВЕ СПОСОБНОСТИ: это единственные маршруты defillama, которые всё ещё запрашиваются НА КАЖДЫЙ ВЫЗОВ; protocol.tvl, chain.tvl, protocol.list и protocol.incidents читают общие документы раз в TTL, поэтому один медленный запрос к источнику размазан по всем сетям и до построчного дедлайна не доходит. Граница 4 из 11 — прогоны A и D сошлись на четырёх, и это РАЗНЫЕ четыре сети (A: ethereum, polygon, bsc, avalanche; D: bsc, avalanche, berachain, bitcoin). Пятая красная строка — новый факт о вендоре, и гейт обязан на ней встать."
    },
    "L-24/protocol.tvl.history": {
      "issue": "L-24",
      "since": "2026-08-24",
      "reviewBy": "2026-09-24",
      "rows": [
        "ethereum/protocol.tvl.history",
        "solana/protocol.tvl.history",
        "base/protocol.tvl.history",
        "arbitrum/protocol.tvl.history",
        "polygon/protocol.tvl.history",
        "bsc/protocol.tvl.history",
        "avalanche/protocol.tvl.history",
        "berachain/protocol.tvl.history",
        "gnosis/protocol.tvl.history",
        "tron/protocol.tvl.history",
        "bitcoin/protocol.tvl.history"
      ],
      "maxFailing": 4,
      "why": "Замерено 2026-08-24 напрямую против вендора, и обе альтернативные причины отвергнуты замером, а не рассуждением. НЕ наш дедлайн и не размер документа: /overview/dexs/bsc — 9.18 с на 620 КБ, /overview/dexs/avalanche — 32.84 с на 320 КБ, /protocol/pancakeswap — ни байта за 60 с, /protocol/babylon-protocol — 1.06 с на 48 КБ. Размер этот порядок не объясняет, и те же маршруты укладываются в 15 с, когда вендору хорошо, то есть это не форма L-22, где потолком был наш собственный бюджет. НЕ наш егресс: прогон C того же дня шёл во время измеренного локального провала (пять несвязанных хостов на одинаковом полу ~1.6 с, через полторы минуты 0.39–0.53 с, WI-65), и его строки в границу НЕ засчитаны — граница взята с прогона D, чей канал измерен здоровым. ПОЧЕМУ ТОЛЬКО ЭТИ ДВЕ СПОСОБНОСТИ: это единственные маршруты defillama, которые всё ещё запрашиваются НА КАЖДЫЙ ВЫЗОВ; protocol.tvl, chain.tvl, protocol.list и protocol.incidents читают общие документы раз в TTL, поэтому один медленный запрос к источнику размазан по всем сетям и до построчного дедлайна не доходит. МНОЖЕСТВО ИСПРАВЛЕНО в тот же день: первая редакция этой записи перечислила 10 сетей и потеряла bitcoin, хотя матрица его обслуживает — это ошибка набора, а не поднятие границы, и она вылезла сразу: следующий прогон объявил bitcoin/protocol.tvl.history незаявленным отказом. zcash/protocol.tvl.history в множество НЕ включён по другой причине: у него нет курированного входа (`no-probe`), то есть эта строка не может упасть и только разбавляла бы знаменатель. ГРАНИЦА 4 из 11 — максимум, измеренный на прогоне E (bsc, berachain, tron, bitcoin); прогон D дал 2 (bsc, bitcoin). Условия названы, потому что читатель обязан их учесть: контрольное плечо кейса http-shared-limiter-rate на прогоне E показало 4420 мс задержки RPC против ~265 мс на прогонах B и D, то есть канал просел ПО ХОДУ прогона E. Приписать эти отказы вендору всё равно можно — прямые пробы против api.llama.fi минутами ранее, на измеренно здоровом канале, дали 32.84 с и полное отсутствие ответа за 60 с (L-24), — но 4 здесь потолок, включающий плохой канал. На review эта граница обязана ОПУСТИТЬСЯ, если вендор выправился, а не ползти вверх."
    },
    "L-26/token.price": {
      "issue": "L-26",
      "since": "2026-08-24",
      "reviewBy": "2026-10-02",
      "rows": [
        "ethereum/token.price",
        "solana/token.price",
        "base/token.price",
        "arbitrum/token.price",
        "polygon/token.price",
        "bsc/token.price",
        "avalanche/token.price",
        "berachain/token.price",
        "gnosis/token.price",
        "tron/token.price"
      ],
      "maxFailing": 2,
      "why": "ЗАМЕР 2026-09-02, граница поднята 1 → 2 — это НОВОЕ ИЗМЕРЕНИЕ, а не расширение под удобство. Два подряд прогона на измеренно стабильном канале: 20:31 UTC — 0 из 10, 20:52 UTC — 2 из 10 (ethereum и avalanche). Максимум по двум подряд равен 2, и правило владельца от 2026-08-24 требует максимума, а не среднего. НО ФАКТ ИЗМЕНИЛСЯ, и это записано в самой L-26: прежние отказы были `capability deadline exceeded` на 15 000 мс у tron, нынешние — `capability unavailable` за ~10 010 и ~10 510 мс у двух других сетей. Другой класс отказа, другое время, другие сети; общее — вендор, способность и переходный характер. Причину установить нельзя: класс `capability unavailable` доходит до вызывающего без причины, строка stderr несёт только id и имя события, а на локальном профиле хранилища диагностики нет вовсе. Прежний текст: Одна строка, один прогон: tron/token.price отдал `capability deadline exceeded` при дедлайне 15 000 мс, на прогоне, чей канал гейт сам измерил стабильным (WI-65). Вендор при этом здоров с обеих сторон: тот же маршрут, опрошенный трижды через минуты, ответил за 0.91/0.46/0.48 с по 20 659 Б, контроль "
    },
    "L-27/protocol.incidents": {
      "issue": "L-27",
      "since": "2026-08-24",
      "reviewBy": "2026-09-24",
      "rows": [
        "ethereum/protocol.incidents",
        "solana/protocol.incidents",
        "base/protocol.incidents",
        "arbitrum/protocol.incidents",
        "polygon/protocol.incidents",
        "bsc/protocol.incidents",
        "avalanche/protocol.incidents",
        "berachain/protocol.incidents",
        "gnosis/protocol.incidents",
        "tron/protocol.incidents",
        "bitcoin/protocol.incidents",
        "zcash/protocol.incidents"
      ],
      "maxFailing": 1,
      "why": "Первый вызывающий общего документа платит дедлайн, которого документ не стоит. ethereum/protocol.incidents — 15 008 мс, весь бюджет; одиннадцать сетей позади него в том же обходе взяли ответ из общего кеша за 1–98 мс. Вендор ни при чём и это измерено трижды: GET /hacks отвечает 200 за 0.46/0.49/0.49 с, 37 072 байта. Канал прогона гейт измерил сам — стабилен, 36 проб, 0 отказов, медианный connect 16–21 мс (WI-65). ПРИЧИНА НЕ УСТАНОВЛЕНА. Это не awaitSharedDocument: оба пути маршрута им обёрнуты, и с L-26 тот производитель называет фазу — отказ прочитался бы как [phase: shared-document]. Он не назвал НИКАКОЙ, и это самое информативное: отказ пришёл от производителя, которого первый заход L-26 не разметил — DeadlineWouldExceedError, отказ лимитера, который вообще не начинал ждать. Теперь размечен и он, поэтому следующий случай ответит на вопрос сам. Кандидат назван кандидатом: все способности defillama делят один ковш, и к моменту protocol.incidents на первой сети обход уже выпустил по нему шесть вызовов. Граница 1 из 12 — измеренное число; вторая красная сеть это другой факт."
    }
  }
}
