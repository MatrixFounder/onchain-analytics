# Задачник — слой «сложные запросы»: бесплатный ярус + Dune

| Поле                     | Значение                                                                                                                                                                               |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Источник**             | Исследование [dune-query-discovery](README.md), прогоны `wf_af845947-864` + `wf_86bdc4b9-bab`, дата верификации **2026-07-27**                                                         |
| **Статус трека A**       | A-1 ✅ · A-2 ✅ · A-3 ✅ · **A-4 не начата**. Трек B заблокирован T0. Подробности — «Порядок исполнения» в конце файла                                                                 |
| **⚠️ Достоверность**     | Два утверждения этого файла опровергнуты живыми зондами (A-2 про 401, A-3 про `/q/*`). Перед A-4 читать предупреждение над её разделом — [WI-22](../backlog/wi-22-dune-tasks-source-doc-refuted-twice.md) |
| **Вердикт исследования** | **NARROW-AND-GO** — полноценный Dune-слой не строим; строим узкий read-only срез, а бесплатный ярус разворачиваем немедленно и независимо                                              |
| **Тип**                  | Feature (multi-phase) — трек A конфигурация/адаптеры, трек B требует внешнего гейта                                                                                                    |
| **Зависимости**          | Трек A — нет. Трек B — **блокируется T0** (действие владельца)                                                                                                                         |
| **Разблокирует**         | Агрегирующие ончейн-вопросы, на которые бесплатный контур сегодня отвечает отказом                                                                                                     |
| **Ключевые артефакты**   | [repo-audit.md](repo-audit.md) — точки врезки · [report.md §6](report.md) — дизайн и контракты · [verification-corrections.md](verification-corrections.md) — что нельзя брать на веру |

> **Как читать этот файл.** Два независимых трека. **Трек A не зависит от Dune вообще** и может
> стартовать сегодня. **Трек B целиком висит на T0** — живом зонде, который может выполнить только
> владелец (агентам запрещено заводить аккаунты). Если T0 даст отрицательный результат, трек B
> закрывается как NO-GO, а движок всё равно получает объёмы DEX, holders и метки бесплатно.

---

## Трек A — бесплатный ярус (стартовать сразу, Dune не нужен)

### A-1. DefiLlama: агрегаты объёмов DEX

**Почему первым.** Закрывает класс вопросов, который основной прогон объявил незакрываемым, и
делает это на **уже существующей** машинерии: адаптер `defillama` есть, хост `api.llama.fi` в
SSRF-allowlist есть. Самая дешёвая интеграция во всём исследовании, строка с максимальным
fit **12/12**.

**Файлы:**

- `packages/core/src/adapters/defillama/index.ts` — новый метод/ветка `fetch` + `normalize`
- `packages/core/src/providers.config.ts:107-110` — регистрация; **`rateLimit` на `:109` пересмотреть**
- `packages/core/src/providers.config.ts` — новый маршрут рядом с `:69`
- `packages/core/src/cache/ttl.ts` — явная строка TTL для новой capability

**Шаги:**

1. Новая capability (предложение: `dex.volume.history`) + маршрут `adapterIds: ['defillama']`.
2. `GET https://api.llama.fi/overview/dexs/{chain}?excludeTotalDataChartBreakdown=true` — **без
   заголовка авторизации**. Ответ: `totalDataChart` = `[[unix_ts, usd], …]`, дневной шаг 86400 с.
3. Сжатие трафика обязательно: `?excludeTotalDataChart=true&excludeTotalDataChartBreakdown=true`
   даёт 1.6 МБ вместо 18 МБ (11.2×) — брать полный документ только когда нужен ряд.
4. `rateLimit` на `:109` (`capacity 5 / refillPerSec 1`) — это **наш** тормоз, не вендорский: живая
   проверка дала 65/65 origin-запросов без единого 429. Поднять до разумного, оставив запас.
5. Строка TTL — **обязательна**. Без явной строки capability молча провалится в
   `DEFAULT_TTL_SECONDS = 300` (`cache/ttl.ts:61`); эта авария в проекте уже случалась.

**DoD:**

- [x] Контрактный тест на записанной фикстуре: успешный путь + усечение большого документа.
- [x] Тест: за квартал приходит 92 точки **без разрывов** (`gapDays === 0`).
- [x] `onchain_list_chains` показывает новую capability ровно на поддерживаемых сетях.
- [x] `_meta.cache` даёт `miss` на первом вызове и `hit` на втором.

**Мина, которую нельзя пропустить.** Гейт у DefiLlama **по-эндпоинтный, а не по-хостовый**: на том
же бесплатном хосте `GET https://api.llama.fi/emissions` → **HTTP 402** «Upgrade to the paid API
plan». Обобщать «api.llama.fi = бесплатно» на непроверенные пути запрещено — каждый новый путь
пробовать отдельно.

---

> ### ✅ ВЫПОЛНЕНО 2026-07-27 — [TASK-007](../tasks/task-007-defillama-dex-volumes.md) `defillama-dex-volumes`
>
> Capability `dex.volume.history`, тул `onchain_dex_volume`, 8 задач, **0 кредитов**.
>
> **Три факта, которых в этом задачнике не было — их дал живой зонд перед проектированием, и каждый
> изменил дизайн.** Записаны здесь, потому что задачник писался по отчёту, а отчёт этих вещей не
> знал:
>
> 1. **Покрытие DEX ≠ покрытие TVL.** Предложенный здесь путь «адаптер уже есть, `vendors.defillama`
>    уже заполнен» вёл прямо в переобещание: колонка выведена из TVL-каталога и непуста у **всех 458**
>    сетей, а объёмы вендор отдаёт по **287** (наших — **274**). Наивный предикат обещал бы capability
>    на **184 сетях без данных** — дефект H-1 из TASK-006 слово в слово. Понадобился генерируемый
>    список `DEFILLAMA_DEX_CHAINS` из живого `allChains`.
> 2. **Незнакомая сеть не даёт честной ошибки.** `/overview/dexs/dash` → **HTTP 500** (не 404), а
>    `/overview/dexs/litecoin` → **HTTP 200 с нулями и у́же набором ключей**. Отсюда два требования,
>    которых в задачнике нет: гейт покрытия **до** вызова и сверка эхо-поля `chain` в ответе.
> 3. **Кап размера ответа на этом хосте не работал вовсе.** `api.llama.fi` не шлёт `Content-Length`,
>    а `safeFetch` при отсутствующем заголовке делал ранний `return`. Пункт DoD «усечение большого
>    документа» без починки проверял бы пустоту. Закрыт **R-47(1)** (потоковый счётчик байт) — это
>    расширение области, зафиксированное как решение, а не протащенное молча.
>
> **Побочно:** `rateLimit` `defillama` поднят `5/1 → 10/5` (замер: 40/40 конкурентных origin-запросов
> без единого 429; на старом значении десятисетевой прогон спал ~5 с, широкий — упирался в
> `MAX_WAIT_MS` и **бросал**).
>
> Не тронуто, как и планировалось: A-2, A-3, A-4, весь трек B, эндпоинты `/overview/fees`,
> `/overview/options`, `/summary/dexs/{p}` (гейт по-эндпоинтный), `pro-api.llama.fi`.

---

### A-2. Blockscout: оживить `token.holders` и снять `entity.labels` с платного Nansen

**Почему.** `token.holders` сегодня мёртвая заглушка, обслуживающая ноль сетей
(`adapters/dune/index.ts:25`), а `entity.labels` идёт на платный Nansen
(`providers.config.ts:74`). Blockscout отдаёт оба класса бесплатно.

**Файлы:**

- `packages/core/src/adapters/blockscout/index.ts` — новый адаптер (простой REST, не MCP-клиент)
- `packages/core/src/providers.config.ts` — регистрация + перенацеливание двух маршрутов
- `packages/mcp-server/src/env.ts` — `BLOCKSCOUT_PRO_API_KEY`
- `.env.example` — новая переменная с комментарием

**Шаги:**

1. Регистрация: `hosts: ['mcp.blockscout.com']`, `rateLimit: { capacity: 5, refillPerSec: 5 }` под
   документированные 5 RPS, `requiresEnv: ['BLOCKSCOUT_PRO_API_KEY']`.
2. Вызовы через REST-фасад: `GET https://mcp.blockscout.com/v1/<tool>` — MCP-клиент не нужен.
   `token.holders` → `/v1/direct_api_call?chain_id=1&endpoint_path=/api/v2/tokens/<addr>/holders`;
   `entity.labels` → `/v1/get_address_info`.
3. **Ключ `proapi_*` завести сразу**, даже пока хост отвечает без него: issue #425 объявил
   принуждение с 2026-07-20 (дата прошла, механизм вмёржен PR #431, переменная на деплое не
   выставлена). Это grace-период, а не контракт — сломается молча в любой день.
4. `chainSupport` строить из живого `get_chains_list`, **не** хардкодить: покрытие 100 сетей, из них
   47 тестнетов, мейннетов 53, **не-EVM = 0** (Bitcoin и Solana отсутствуют). Иначе матрица
   покрытия начнёт рекламировать несуществующее.
5. ~~Fallback на `rpc-evm` при HTTP 429 и при HTTP 401 (битый ключ отдаёт 401 **без** отката на
   серверный — проверено живьём).~~ **ОПРОВЕРГНУТО зондом 2026-07-28** — см. блок ниже: фасад
   отвечает **200** на заведомо мусорный ключ, а `rpc-evm` не участвует ни в одном из двух маршрутов.
   Читать как «следующий адаптер в цепочке, если он есть».
6. Читать `value_truncated` и поле `notes` в ответе: сервер сам режет метаданные, усечённое
   значение нельзя принимать за полное.

**DoD:**

- [x] Контрактный тест на фикстуре для обоих маршрутов.
- [x] `entity.labels` резолвится на Blockscout, Nansen остаётся в цепочке фолбэком.
- [x] Тест: 401 и 429 уводят на `rpc-evm`, а не роняют вызов. — с поправкой: **не на `rpc-evm`**,
      а на следующий адаптер, если он есть (см. блок ниже).
- [x] `chainSupport` выведен из реестра сетей, а не из литерала.

**Реальный потолок ниже наивного:** `get_address_info` фанится в 3 апстрима (20 + 120 + 20 ≈ 160
кредитов) ⇒ ≈625 вызовов/сутки из 100K credits/day, а не ≈5 000.

**Юридический риск, вынести владельцу.** Лицензия `LicenseRef-Blockscout` — не OSI, `gh api` даёт
`NOASSERTION`, §4(a) запрещает монетизированное/SaaS-использование **Software** без коммерческой
лицензии, §10(b) допускает отзыв. Применимость к _потреблению хостируемого сервиса_ —
**наша трактовка, юридически не подтверждённая**. Отдельного ToS у PRO API найти не удалось.
Если движок пойдёт в продукт — это вопрос к юристу до, а не после.

---

> ### ✅ ВЫПОЛНЕНО 2026-07-28…29 — [TASK-008](../tasks/task-008-blockscout-free-tier.md) `blockscout-free-tier`
>
> Адаптер `blockscout`, `token.holders` снят с мёртвого `dune`, `entity.labels` поставлена **перед**
> платным Nansen, тул `onchain_token_holders`. **0 кредитов.** Блок дописан задним числом
> в ходе актуализации 2026-07-30: сама TASK-008 этот файл не обновила.
>
> **Что разошлось с текстом задачника:**
>
> 1. **🔴 «Битый ключ отдаёт 401» невоспроизводимо.** Фасад `mcp.blockscout.com` возвращает **200**
>    и на `Authorization: Bearer proapi_deadbeef…`, и на `x-api-key` — аутентификацию он не
>    принуждает вовсе. 401/402 принадлежат **другому** хосту (`api.blockscout.com`), который
>    задачник вызывать не предписывает. Ветка 401 в коде есть (принуждение объявлено и вмёржено), но
>    проверяется **только фикстурой** — живого теста на неё сегодня построить нельзя.
> 2. **`rpc-evm` не участвует ни в одном из двух маршрутов.** У `entity.labels` следующий —
>    `nansen`; у `token.holders` маршрут из одного адаптера, поэтому 429 там честный отказ, а не
>    переход.
> 3. **Хостов два, а не один**, и ключ уходит **в URL** (`?apikey=`), а не заголовком — это задевает
>    D10, поэтому полный URL вырезан из сообщений об ошибках, логов и кеш-ключа.
> 4. **`rateLimit` 5/5 → 5/2** (решение владельца): предписанные 5 RPS разрешали 432 000 вызовов в
>    сутки против измеренного потолка ≈625 — вся суточная квота за ~125 секунд. Значение
>    **оборонительное, не измеренное**: вендор не шлёт `RateLimit-*`, калиброваться не по чему.
> 5. **🔴 Ответ несёт `instructions[]` — императивы, адресованные модели**, с подставленным адресом
>    из запроса. Отбрасываются **рекурсивно**, не обрезаются: усечённая инструкция остаётся
>    инструкцией.
> 6. **Метки лежат в `data.metadata.tags[]`**, а не в `basic_info.metadata` — та `null` даже у
>    Binance Hot Wallet.
> 7. **Одного порядка адаптеров оказалось мало:** пустой ответ Blockscout обрывал цепочку и заслонял
>    Nansen на весь TTL. Добавлен `CapabilityRoute.isSatisfying` — политика лежит **данными в
>    маршруте**, а не кодом в провайдере (провайдер, знающий о том, кто за ним стоит, не может
>    разворачиваться отдельно). Постоянное место этой политики — **OQ-4**, к перепроектированию.
>
> **Открыто:** значение метки по-прежнему уходит модели (ограничено 256 символами); учёта расхода
> кредитов нет. Юридический вопрос закрыт владельцем: личное использование, §4(a) не задевается.

---

### A-3. Независимая сверка BTC-чисел

**Файлы:** `packages/core/src/adapters/mempool-space/`, при необходимости `blockchain-info/`.

- `mempool.space` REST — keyless, вердикт adopt (`/api/v1/mining/*`, difficulty, fees, tip height).
- ~~`blockchain.info/q/*` — keyless, «одно число одним вызовом» для эмиссии.~~
  **ОПРОВЕРГНУТО живым зондом 2026-07-29 — см. блок ниже.** `/q/totalbc` отдаёт **не эмиссию**, а
  реально заявленное предложение; формульная величина лежит в `/stats.totalbc`. Строка оставлена
  зачёркнутой, а не переписана: ошибка задачника — сама по себе факт, важный для A-4.
- **AWS Public Blockchain Data (анонимный S3, `--no-sign-request`)** — не адаптер, а
  `steal-pattern`: офлайн-пересчёт, когда число надо доказать самому.

**DoD:**

- [x] Проба в `packages/mcp-server/eval/probes.json` — новый источник добавляется **данными**, не кодом.
- [x] Кросс-чек в `eval/checks.mjs`: эмиссия BTC из двух независимых источников сходится в пределах
      заданной дельты (эталон замера: расхождение circulation против расчётной эмиссии 32.08 BTC =
      **0.00016%**).

---

---

> ### ✅ ВЫПОЛНЕНО 2026-07-29 — [TASK-009](../TASK.md) `btc-supply-independent-verification`
>
> _(ссылка ведёт в `docs/TASK.md`, пока задача текущая; после ротации —_
> `docs/tasks/task-009-btc-supply-independent-verification.md`_. Именно так протухла ссылка на
> TASK-007 в блоке A-1 выше — она указывала на переезжающий слот, а не на архив.)_
>
> Capability `chain.supply`, адаптер `blockchain-info`, тул `onchain_chain_supply`, референсная ось
> eval, 8 задач, **0 кредитов**. Скоуп сужен решением владельца 2026-07-29: **адаптер
> `mempool-space` не писался** (п. 4 ниже), широкая поверхность вендора — hashrate,
> difficulty-adjustment, recommended fees, mining pools — потребителя не имеет, а capability без
> потребителя это дефект «объявлено везде, обслужено нигде», который чинила A-2.
>
> **Три факта, которых в этом задачнике не было, и один, который здесь написан неверно.** Все
> получены живым keyless-зондом перед проектированием; evidence запиннена в `docs/provenance.json`.
>
> 1. **🔴 `/q/totalbc` — не эмиссия, и это меняет контракт.** Решающий тест: сколько ЦЕЛЫХ субсидий
>    укладывается между значением и границей халвинга (блок 840 000). `/stats.totalbc` → **120102,
>    целое** ⇒ это сама формула халвингов. `/q/totalbc` → **120092.8, дробное** ⇒ формулой быть не
>    может и устаревшей копией формулы тоже (та дала бы целое смещение) ⇒ это реально **заявленное**
>    предложение. Разница ≈29–32 BTC — невостребованная майнерами coinbase-субсидия, ровно та
>    природа, что у «Δ 32.08 BTC» из отчёта. Отдать одно под именем другого — фабрикация с
>    погрешностью **0.00016%**, которую ниже по течению не поймает никто. Движок отдаёт **обе**
>    величины разными полями.
> 2. **🔴 Сверка supply с формулой — тавтология, а не проверка.** Вендор считает `totalbc` той же
>    формулой: совпало **бит-в-бит на обеих зондированных высотах** и будет совпадать всегда.
>    Информацию несёт только сверка **высоты** с независимым источником, а формула уже разносит
>    расхождение в supply. Поэтому кросс-чек сравнивает высоты, а не supply — иначе он вечно зелёный.
> 3. **Дельту нельзя мерить в процентах.** Один блок = 3.125 BTC = **0.000016%**; суточное
>    отставание вендора (144 блока) = 0.0023%. На любой процентной шкале, которую выберет человек,
>    и off-by-one, и полные сутки протухания читаются как ноль. Порог живёт данными
>    (`maxDeltaBlocks`, по умолчанию 6 ≈ час) и измеряется **в блоках субсидии**. Замеренный разброс
>    источников высоты — **−2…0 блока**; живой прогон дал дельту 0.
> 4. **`mempool.space` намеренно НЕ стал адаптером** — и это не экономия, а условие честности:
>    источник, которым движок сам отдаёт число, не может быть его независимой проверкой. Он входит
>    только как эталон eval и не значится ни в одном маршруте и ни в одном SSRF-allowlist.
>
> **Побочно:** `blockchain.info` и `api.blockchain.info` взаимозаменяемы для `/q/*` и `/stats`
> (в allowlist нужен один хост); заголовков `RateLimit-*`/`Retry-After` не шлёт ни blockchain.info,
> ни mempool.space — лимитер только оборонительный, цитировать его как потолок нельзя.
> У `bitcoin` в реестре `nativeDecimals: null`, поэтому `decimals: 8` берётся из консенсуса, а не
> из реестра.
>
> Не тронуто, как и планировалось: A-4, весь трек B, `/charts/*` (исторические ряды BTC),
> AWS Public Blockchain Data.

---

> ### ⚠️ Прежде чем брать A-4: этот файл ошибся дважды подряд
>
> Оба утверждения ниже были написаны как проверенные и оба опровергнуты живым зондом — по одному на
> каждую задачу, которая на них опиралась. Ведётся как [WI-22](../backlog/wi-22-dune-tasks-source-doc-refuted-twice.md).
>
> | задача         | утверждение файла                                              | что измерено                                                                                                            |
> | -------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
> | A-2 (TASK-008) | «битый ключ отдаёт 401 **без** отката — проверено живьём»       | Фасад вернул **200** на заведомо мусорный ключ; аутентификация не принуждалась вовсе. 401 — у **другого** хоста          |
> | A-3 (TASK-009) | «`/q/*` — одно число одним вызовом **для эмиссии**»            | `/q/totalbc` — не эмиссия (дробное число субсидий), а заявленное предложение; формула — в `/stats.totalbc`               |
>
> У обеих ошибок одна форма: назван настоящий эндпоинт и описан **почти** верно, а разница слишком
> мала, чтобы не пережить ревью. Файл писался по исследовательскому отчёту, и это не его дефект —
> но три утверждения A-4 про адаптер `dune` того же класса и **той же давности**.
>
> **Что делать:** зондировать до проектирования и записывать дельту в §1.1 собственной задачи, как
> сделали TASK-008 и TASK-009. Ссылки вида `файл:строка` ниже тоже не считать текущими — они были
> верны на момент написания, а файлы с тех пор двигались (`token.holders` уже уехал на `blockscout`,
> так что первый и третий пункты A-4 надо перечитать заново).

### A-4. Три дефекта, найденных попутно (не связаны с Dune)

- [ ] **Стаб не выполняет собственное обещание.** Докстринг `adapters/dune/index.ts:4-13` говорит,
      что стаб существует ради внятной причины недоступности, но при переданной сети первым
      срабатывает coverage-гейт (`adapters/registry.ts:213-226`) и вызывающий получает
      `CapabilityNotCoveredOnChainError`, а не обещанную причину. Обещание доставляется только на
      пути без сети.
- [ ] **`costOf: () => ({ credits: 0 })`** у кредитно-метрируемого вендора (`dune/index.ts:28`) —
      спящая инверсия fail-closed правила R-37 (`nansen/cost-of.ts:71`). Пока `isAvailable()`
      безусловно false это ловушка, а не баг; в момент оживления адаптера станет дырой в бюджете.
- [ ] **Dune нигде не заведён как работа** — ни в `docs/BACKLOG.md`, ни в `docs/KNOWN_ISSUES.md`,
      при том что `ROADMAP.md:99` числит «Dune Query API (free 2,500cr/мес)» среди адаптеров M1, а
      `ROADMAP.md:83` переносит его в M2. Расхождение роадмапа с реальностью не отслеживается.

---

## Трек B — Dune. Заблокирован до T0

### T0. Живой зонд (владелец, до первой строки кода)

**Это гейт всего трека.** Агентам заводить аккаунты запрещено — одна добивка исследования была
остановлена классификатором безопасности ровно на этом.

**Шаги:** завести бесплатный `DUNE_API_KEY`, выполнить три вызова на одном известном публичном
`query_id`:

1. `GET /api/v1/query/{id}/results` — читается ли **чужой публичный** результат на Free?
2. `POST /api/v1/sql/execute` — что отвечает, и что говорит поле `isFreeformAllowed`?
3. `POST /api/v1/usage` до и после — **фактическая** дельта кредитов.

**Зачем именно живой вызов.** Первоисточник Dune противоречит сам себе по четырём решающим
вопросам ([verification-corrections.md](verification-corrections.md) §В-2), из них два бьют прямо
сюда: `pricing-faqs.md:254` — «the API is included in all Dune plans… including those in the Free
plan», против `:135` — «paid plans offer significantly lower per-credit rates and **additional
features like API access**». Пока это не разрешено замером, весь слой A трека B — гипотеза.

**Решение по результату:**

- Зонд подтвердил чтение на Free → идём в T1…T7.
- Зонд отказал → **NO-GO**, трек B закрывается, результат вписывается в
  [verification-corrections.md](verification-corrections.md) «Что осталось UNVERIFIED».

---

### Архитектура трека B: гибрид, три слоя

Развилка «проксировать官 MCP / свой REST-адаптер / гибрид» разрешена в пользу **гибрида**, с
жёстким разделением по признаку «тратит ли это кредиты автономно»:

- **Слой A — данные. Наш REST-адаптер `dune`, включён всегда.** Только чтение результата
  закреплённого в каталоге запроса. Идёт через `registry.resolve()` → `safeFetch` → token-bucket →
  `BudgetStore` → кэш. Даёт то, чего официальный MCP дать не может: дешёвое чтение уже посчитанного
  (`GET /query/{id}/results` не триггерит исполнение) плюс наш кэш поверх.
- **Слой B — авторинг. Официальный MCP, по умолчанию ВЫКЛЮЧЕН.**
  `claude mcp add --scope user --transport http dune https://api.dune.com/mcp/v1 --header "x-dune-api-key: <key>"`
  Включает человек на сессию «сочинить/починить запрос», выключает после. Причина изоляции одна и
  она не про вкус: **этот путь физически невозможно загейтить `BudgetStore`**, поэтому он не должен
  быть достижим из автономного цикла.
- **Слой C — каталог. Файл-реестр в репозитории.** Замена несуществующего шага «найти чужой
  запрос»: программного поиска чужой аналитики в API **нет как класса** — в официальном MCP тула
  поиска нет, в OpenAPI 39 путей и единственный search (`POST /v1/datasets/search`) ищет **таблицы**,
  а `GET /v1/queries` возвращает **свои** запросы.

---

### T1. Каталог + два бесплатных тула

Ноль сети, ноль кредитов, проверяемо за вечер.

**Файлы:** `packages/core/src/adapters/dune/catalog.json`, `catalog/*.sql`, `catalog.ts`
(zod + sha256 + фильтр по `adapterRegistrations`); `packages/mcp-server/src/tools/analytics-catalog.ts`,
`analytics-explain.ts`; регистрация в `packages/mcp-server/src/server.ts:67-79`.

**DoD:**

- [ ] `pnpm test` зелёный на офлайн-тесте каталога: валидность схемы, существование и хэш каждого
      `.sql`, обязательные поля у записей `active`, провайдер зарегистрирован.
- [ ] Оба тула отвечают по stdio **без единого сетевого вызова**.
- [ ] Три примера записей лежат в файле (три разных состояния: `draft`/`active`/`stale`).

**Поля записи каталога** (минимум): `queryId` · `provider` · `sourceUrl` · `owner` · `purpose` ·
`expectedColumns` · `sqlSha256` · `maxStalenessDays` · `sourceTables[]` с классом доверия ·
`crossCheckSource` · `allowedDelta` · `costObserved` · `verifiedAt` · `status`.

> Поле `provider` — не украшение: каталог провайдер-агностичен намеренно. Тот же вендор погасил
> Dune Sim 2026-08-01, закрыв регистрации 2026-05-18. Запись можно перенацелить, не меняя контракт тула.

---

### T2. Канонический тип «таблица» — блокирующая развилка

**Это главный архитектурный блокер.** Канонические zod-типы закрыты, а результат чужого запроса —
произвольная таблица. `grep -rn 'z.record|passthrough()|z.unknown()|z.any()'` по `packages/core/src`
и `packages/mcp-server/src` даёт сегодня **ноль** попаданий, и это состояние надо сохранить.

**Файлы:** `packages/core/src/types/query-table.ts`; барррель `packages/core/src/types/index.ts:4-13`
(9 схем → 10).

**Решение:** колоночный конверт с закрытым `CellSchema` — та же выразительность, но `.strict()`
сохраняется и `registerTool(… .shape)` продолжает работать (образец — `tools/chain-tvl.ts:76-77`,
где `.shape` требует `z.object` со статически известной формой).

Переиспользовать готовую доктрину обрезки недоверенного вендорского текста:
`adapters/nansen/normalize.ts:18` (`MAX_VENDOR_ROWS = 200`), `:21` (`MAX_VENDOR_STRING_ITEMS = 64`),
`:23` (`MAX_VENDOR_STRING_LENGTH = 256`), `:40` (`truncateString`).

**DoD:**

- [ ] Юнит-тесты: закрытое `CellSchema`; `.strict()` конверта; усечение с выставленным флагом
      `truncated`; отбраковка ячейки с управляющим символом и сверхдлинной строкой.
- [ ] Тест: `QueryTableSchema.shape` пригоден для `registerTool`.
- [ ] `grep` на `z.record|passthrough|z.any` по обоим пакетам по-прежнему даёт ноль.

---

### T3. Обобщение бюджета

Всё ещё ноль сети. **Хорошая новость:** `BudgetStore` уже провайдер-агностичен
(`packages/core/src/cache/budget-store.ts:46-93` — каждый метод принимает `provider: string`), а
`dune` уже числится платным (`cache/sqlite-store.ts:48`). Обобщать надо только витрину.

**Файлы:** `packages/mcp-server/src/tools/budget-meta.ts:9` и `:38` (параметризовать литерал
`'nansen'` закрытым объединением); `packages/core/src/adapters/dune/budget-gate.ts`; проброс
постоянного отказа по образцу `adapters/registry.ts:290`.

**DoD:**

- [ ] `checkAndReserve('dune', …)` отказывает выше выведенного дневного потолка.
- [ ] `maxCalls` отказывает вызову ценой 0 кредитов.
- [ ] `recordDelta` сводит знаковую дельту в тот же бакет и окно, что и резерв.
- [ ] `_meta.budget` показывает **обоих** провайдеров.
- [ ] Бюджетный отказ не превращается в совет «повторите позже».

---

### T4. Настоящие `fetch` / `normalize`, на фикстуре

**Файл:** `packages/core/src/adapters/dune/index.ts` — заменяются ровно четыре места:

| Строка   | Было                                       | Станет                                                          |
| -------- | ------------------------------------------ | --------------------------------------------------------------- |
| `:25`    | `chainSupport: () => false`                | предикат из каталога                                            |
| `:28`    | `costOf: () => ({ credits: 0 })`           | цена из записи; **fail-closed `+Infinity`** на неизвестном slug |
| `:31-36` | бросают `NotImplementedInM1Error`          | реальная реализация                                             |
| `:39`    | безусловный `isAvailable() === {ok:false}` | проверка наличия `DUNE_API_KEY`                                 |

`.env.example:54,57` — раскомментировать и переписать.

**Две мины транспорта:**

1. **Кап размера ответа не работает на форме, которую отдаёт Dune.** При отсутствующем
   `Content-Length` (chunked) `net/safe-fetch.ts:147-154` делает ранний `return` вовсе без проверки.
   Уже заведено как R-47 (`docs/BACKLOG.md:19`) — здесь оно становится блокирующим.
2. **Зарегистрированный rate limit враждебен poll-циклу.** `providers.config.ts:116`
   (`capacity 2 / refillPerSec 0.1`) = 1 токен / 10 с; шестой токен превышает `MAX_WAIT_MS = 30_000`
   (`net/rate-limit.ts:32`) и бросает. Poll-цикл умрёт примерно на пятом опросе — пересмотреть
   вместе с реализацией.

**DoD (сети в CI нет — всё на записанной фикстуре):**

- [ ] Успешный путь.
- [ ] Усечение по `maxRows` и по `maxBytes` **при отсутствующем `Content-Length`**.
- [ ] Исчерпание опроса.
- [ ] Санитизация ячейки.
- [ ] `isAvailable()` возвращает причину про отсутствующий ключ, а не «deferred to M2».

---

### T5. Роут, capability, тул

**Файлы:** `providers.config.ts` — `{ capability: 'analytics.query', adapterIds: ['dune'] }` рядом
с `:69`; `cache/ttl.ts` — строка `'analytics.query': 3600`; `packages/mcp-server/src/tools/analytics-query.ts`

- регистрация в `server.ts:67-79` (10 тулов → 13).
  `packages/mcp-server/src/index.ts:91` уже содержит `['dune', createDuneAdapter()]` — правка не нужна.

**DoD:**

- [ ] Сквозной тест обработчика на фикстуре.
- [ ] `onchain_list_chains` показывает `analytics.query` ровно на сетях каталога и ни на одной
      больше (`chain/coverage.ts:88-89`).
- [ ] `_meta.cache`: `miss` на первом вызове, `hit` на втором.

---

### T6. Одно настоящее платное исполнение, руками

Шаг, который превращает `unverified` в число.

**DoD:**

- [ ] Ровно **одна** запись каталога переходит `draft → active`.
- [ ] Человек проставляет `queryId`, `sourceUrl`, `owner`, пиннит SQL и `sqlSha256`.
- [ ] `costObserved` заполняется **замеренной** дельтой леджера, `verifiedAt` — датой.

---

### T7. Eval-зонд, opt-in

**Файлы:** `packages/mcp-server/eval/probes.json`, `checks.mjs`, `run.mjs` (флаг
`ONCHAIN_EVAL_DUNE`, по умолчанию **выключен**), `eval/README.md`.

**DoD:**

- [ ] С выключенным флагом прогон не меняется вообще.
- [ ] С включённым — искусственно испорченный `expectedColumns` даёт `degraded`, а не `ok`.
- [ ] README называет цену прогона в кредитах.

---

## Чего НЕ делать (это тоже результат исследования)

| Анти-решение                                                                  | Почему нет                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Подключить официальный Dune MCP **дефолтным** сервером рядом с нашим          | 26 схем тулов в контексте каждого хода, конкуренция с нашими 10 (`server.ts:67-79`), и главное — каждый вызов минует `BudgetStore`, кэш, allowlist и `_meta`. Квота 2 500 кредитов/мес **не переносится и не докупается**; автономный цикл сожжёт её без единого гейта         |
| Реализовать произвольный SQL (`POST /v1/sql/execute`)                         | Единственная точка, где модель могла бы диктовать вендору код. Её не должно существовать в кодовой базе                                                                                                                                                                        |
| Тянуть чужой SQL в рантайме через `getDuneQuery`                              | Гейтится планом (Analyst — или Plus, первоисточник противоречит сам себе), на Free не работает; и автор может отредактировать запрос в любой момент — наши числа поменяют смысл молча                                                                                          |
| Отдавать `token.holders` через Dune                                           | Потребителя у capability не осталось (`token.risk` уехал на nansen), а Blockscout отдаёт то же даром. Тратить кредиты на бесплатное — строго худший размен                                                                                                                     |
| Заводить materialized views на Free                                           | «On the free tier you can only create a 1MB materialized view»; интервал cron от 15 минут; каждый refresh — исполнение, то есть кредиты. Авто-обновляемый MV = постоянная утечка против непереносимой квоты                                                                    |
| Строить визуализации и дашборды из движка                                     | Visualization (5) + Dashboard (4) = 9 из 26 тулов обслуживают оформление **своих** артефактов. Для дата-движка ценность нулевая — это работа человека в слое B                                                                                                                 |
| Класть ключ в `?api_key=`                                                     | Вендор допускает, мы нет: `net/args-hash.ts:38-42` запрещает секреты в `args`, а URL утекает в логи прокси, access-логи и Referer                                                                                                                                              |
| Добавить `z.record` / `passthrough()` / `z.any()` ради «произвольной таблицы» | Сегодня их в обоих пакетах **ноль**. Колоночный конверт (T2) даёт ту же выразительность, сохраняя `.strict()`                                                                                                                                                                  |
| Кэшировать `execution_id` вместо результата                                   | Перечитывание результата не бесплатно — это был бы механизм повторной оплаты, замаскированный под кэш                                                                                                                                                                          |
| Сделать обход кэша параметром в `args`                                        | Раздробит `argsHash` (`net/args-hash.ts:44-47`) и превратит кэш в переключатель биллинга. Свежесть регулируется `ttlSeconds` записи каталога                                                                                                                                   |
| Строить продовый путь на `GET /v1/dashboards/by-slug/…`                       | Эндпоинт есть в OpenAPI, но **отсутствует во всём опубликованном справочнике** (`llms.txt`: 0 совпадений на `api-reference/dashboards`), требует второго хопа через `/v1/visualizations/{id}`, плановый гейт неизвестен. Это принятие риска молчаливой поломки без уведомления |
| Считать Dune единственным источником сложных агрегатов                        | Тот же вендор погасил Dune Sim 2026-08-01. Каталог поэтому провайдер-агностичен                                                                                                                                                                                                |

---

## Риск-лог: что должно быть решено до первого рубля

1. **Доступ к API на Free противоречив в самом первоисточнике** — `pricing-faqs.md:254` против
   `:135`. Разрешается **только** T0.
2. **Analyst или Plus для Query-эндпоинтов** — из первоисточника не выводится (3 страницы против 1).
   На практический вывод не влияет (на Free не работает ни то, ни другое), на смету апгрейда — да.
3. **Собственные лимиты `/mcp/v1`** (rate limit, размер ответа, серверный таймаут) вендором не
   задекларированы — `unverified`. Единственное документированное ограничение чужое и клиентское:
   таймаут MCP-клиента 60 с, обход — `tool_timeout_sec = 300`.
4. **Цена конкретного исполнения непредсказуема по определению вендора** — дословно: «we don't share
   a single, per-query formula because credit usage depends on real-time factors». Поэтому
   `costObserved` заполняется только замером, а `reserveCredits` ставит человек и защищает
   `checkAndReserve`.
5. **Свежесть на Free не контролируется вообще.** «The Free query engine is limited to manual query
   editor executions only; automated executions (API, scheduling, refreshing) are not available».
   Протухший результат чужого запроса **нельзя обновить через API ни за какие кредиты** — свежесть
   целиком во власти владельца запроса. Это ограничение продукта, а не нашей реализации, и оно
   должно быть явно отражено в контракте тула (`staleAsOf` в ответе).
6. **Лицензия Blockscout** (A-2) — юридический вопрос, не инженерный.

---

## Порядок исполнения

```
A-1 ✅ ──▶ A-2 ✅ ──▶ A-3 ✅ ──▶ A-4 ⬜        (бесплатный ярус, ни от чего не зависит)
                │
ВЛАДЕЛЕЦ ──▶ T0 зонд ──┬── отказ ──▶ NO-GO: трек B закрыт, движок уже работает на A
                       └── успех ──▶ T1 ─▶ T2 ─▶ T3 ─▶ T4 ─▶ T5 ─▶ T6 ─▶ T7
```

| задача | статус | что закрыло                                                                              |
| ------ | ------ | ------------------------------------------------------------------------------------------ |
| A-1    | ✅     | TASK-007 `defillama-dex-volumes` — `dex.volume.history`, тул `onchain_dex_volume`         |
| A-2    | ✅     | TASK-008 `blockscout-free-tier` — `token.holders` + `entity.labels`, тул `onchain_token_holders` |
| A-3    | ✅     | TASK-009 `btc-supply-independent-verification` — `chain.supply`, тул `onchain_chain_supply`, референсная ось eval |
| A-4    | ⬜     | не начата — **сперва перечитать предупреждение над разделом A-4**                         |
| T0…T7  | ⬜     | трек B по-прежнему заблокирован T0 (действие владельца)                                   |

T1–T3 не требуют сети и могут идти параллельно с треком A. T4 и далее — только после T0.

**Перепроверка исследования:** квартал (до **2026-10-27**) либо немедленно при изменении тарифов
или планов Dune. Команда: `Workflow dune-query-discovery { date:"YYYY-MM-DD", … }` — см.
[README.md](README.md).
