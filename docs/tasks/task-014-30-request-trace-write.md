# Задача 014.30: запись следа на четырёх путях `[LOGIC]`

## Связь со сценариями
- UC-1 — n8n вызывает способность по сети

<!-- contract:goal -->

## Цель задачи

Писать след запроса на попадании в кеш, на ожидании чужого вендорского вызова, на живом вызове и на
отказе уровня исполнения тула. Класс исхода записывать тремя значениями `ADR-003` D4, включая ответ,
выданный за потолком дедлайна (R-27.6).

**Why все четыре.** Владелец объявил: платят оба клиента, даже если второй получил ответ из кеша.
Клиентская цена и наш вендорский расход — разные величины, и T-015 посчитает первую только если
T-014 записал вторую пообъектно.

<!-- contract:changes -->

## Описание изменений

### Изменения в существующих файлах

**Файл `packages/mcp-server/src/tools/registry.ts`:**
- Писать строку следа в обёртке `defineTool` (строка 289), в точке перехвата принципала задачи
  014-15
- Одна запись, при завершении вызова
- Объявить `ToolOutcome.coalesced?: true` (`:80`); `toCallToolResult` (`:201`) это поле не публикует

**Файл `packages/mcp-server/src/engine/request-trace-store.ts`:**
- Заменить стаб реализацией поверх репозитория 014-03

**Why пакет `mcp-server`.** `security.md` §7.5.1 и `deployment.md` §10.2.1 держат SQL по
`onchain.request_trace` вне `packages/core/src`. Задачи 014-02 и 014-03 называют тот же путь.

**Файл `packages/core/src/adapters/nansen/singleflight.ts`:**
- Добавить необязательный четвёртый параметр `onCoalesced?: () => void`, вызываемый в ветви ведомого
  (`:65`, `const existing = inFlight.get(key);`) до возврата общего промиса

**Файл `packages/core/src/adapters/types.ts`:**
- Добавить `AdapterPort.fetch` необязательный четвёртый параметр `onCoalesced?: () => void` (`:52`)

**Файл `packages/core/src/adapters/nansen/index.ts`:**
- Передать полученный `onCoalesced` в `singleflight` (`:628`)

**Файл `packages/core/src/adapters/registry.ts`:**
- Завести признак ведомого рядом с `attempted` и передавать колбэк в `adapter.fetch` (`:1224`,
  `:1580`)
- `withDiagnostics` (`:877`) ставит `coalesced: true` тем же правилом, что и `deadlineOverrunMs`
- Объявить `CapabilityResolution.coalesced?: true` (`:330`)

**Файл `packages/mcp-server/src/tools/resolve-capability.ts`:**
- Объявить `ResolveSuccess.coalesced?: true` и передавать значение дословно (`:63`)

### Четыре пути

| Путь | `served_from` | `vendor_credits`, `vendor_calls` | `vendor_provider`, `vendor_day`, `vendor_window_start` |
| :--- | :--- | :--- | :--- |
| попадание в кеш | `cache` | пусто | пусто |
| ожидание чужого вызова | `coalesced` | пусто | координаты корзин ведущего |
| промах, ведущий живой вызов | `vendor` | вклад этого запроса | координаты своих корзин |
| ни кеш, ни вендор не обслуживали | `none` | пусто | пусто |

На пути `cache` заполняется `cache_age_ms`. На путях `cache` и `none` вендорского вызова не было,
поэтому все пять вендорских колонок пусты.

**Why `vendor_provider` записан и на строке ведомого.** Координата корзины — пара: `usage` ключуется
`(provider, day)`, `usage_window` — `(provider, window_start)`. Без имени вендора обе координаты
неполны.

**Why `none` покрывает два случая.** `served_from` называет источник ответа, а не исход. При отказе
уровня исполнения тула заполняется `refusal_class` и `outcome` принимает значение `refusal`. При
ответе без обращения к кешу и вендору `outcome` принимает значение `answer`, а `refusal_class` пуст.

**Why `coalesced` — отдельная строка, а не свёртка в соседа.** Один вендорский вызов обслужил два
списания. Строка ведомого несёт координаты корзин ведущего, поэтому T-015 связывает списание с
расходом, который его обслужил.

**Why суммирование не даёт двойного счёта.** `SUM` пропускает пустые значения в обоих движках,
поэтому строка ведомого не добавляет ничего ни к сумме кредитов, ни к сумме вызовов (R-27.3).

### Признак ведомого запроса доходит до места записи

Коалесцирование существует в одном месте — `packages/core/src/adapters/nansen/singleflight.ts` —
внутри адаптера и без сигнала наружу. Текущая сигнатура (`:24`):

```ts
export function createSingleflight(): <T>(
  key: string,
  fn: () => Promise<T>,
  deadlineAtMs?: number,
) => Promise<T>;
```

Ведомый распознаётся одной строкой — `const existing = inFlight.get(key);` (`:65`). Изменение:
четвёртый необязательный параметр `onCoalesced?: () => void`, вызываемый синхронно в этой ветви.

Маршрут признака до места записи:

| Шаг | Где | Что несёт |
| :--- | :--- | :--- |
| 1 | `singleflight.ts:65` | вызывает `onCoalesced()` в ветви ведомого |
| 2 | `nansen/index.ts:628` | передаёт колбэк, полученный `fetch` четвёртым параметром |
| 3 | `adapters/registry.ts:1224`, `:1580` | пишет признак в локальную переменную обхода |
| 4 | `adapters/registry.ts:877` — `withDiagnostics` | ставит `CapabilityResolution.coalesced` |
| 5 | `resolve-capability.ts:63` | передаёт `ResolveSuccess.coalesced` дословно |
| 6 | `mcp-server/src/tools/registry.ts:289` | обёртка пишет `served_from = 'coalesced'` |

**Why наружу выходит колбэк, а не поле результата.** Ведущий и ведомый получают один и тот же промис
и одно и то же значение. Признак различает вызывающих, а не ответы, поэтому в результате он ехать не
может.

**Why четвёртый параметр `fetch`, а не новый механизм.** `deadlineAtMs` добавлен тем же способом —
необязательно и аддитивно (D4/R-140, `packages/core/src/adapters/types.ts:52`). Адаптеры, не
объявляющие коалесцирования, параметр не читают.

**Why `withDiagnostics`, а не место конструирования резолюции.** Тем же правилом там уже ставится
`deadlineOverrunMs`: факт известен только в момент возврата, и третий такой факт не требует правки
трёх `return` обхода.

**Why признак не публикуется в `_meta`.** `_meta.cache.status` сохраняет два значения, и ведомый
сообщает `miss` (`interfaces.md` §5.4.4). Третье значение переопределило бы поле на каждом туле
реестра.

**Why без этого TC-UNIT-03 невыполним.** Обёртка `defineTool`, где пишется след, о ведомом запросе
не знает. Строка `coalesced` не возникает ни на одном входе, и оба конкурентных запроса пишутся
одинаково.

**Why без признака T-015 не посчитает.** Владелец объявил: платят оба клиента (OQ-6). Строка
`coalesced` — это оплаченный запрос, не сделавший вендорского вызова. Без признака ведомого число
списаний, порождённых одним вендорским вызовом, из леджера не восстанавливается, а именно его T-015
сверяет с `usage` (R-27.3).

Сегодня коалесцирование объявляет только адаптер `nansen`: `createSingleflight` импортирован в одном
файле — `packages/core/src/adapters/nansen/index.ts:45`. На остальных адаптерах значение `coalesced`
не возникает.

### Третий класс исхода — `partial_deadline`

`outcome` несёт три класса `ADR-003` D4 и ничего больше: `answer`, `refusal`, `partial_deadline`
(`data-model.md` §4.5.7, R-27.6). Третий пишется, когда обход перешёл собственный потолок и всё же
вернул ответ.

Такой обход даёт `CapabilityResolution.deadlineOverrunMs`
(`packages/core/src/adapters/registry.ts:379`), который ставит `withDiagnostics` (`:877`) при
положительном превышении. Дальше он идёт как `ResolveSuccess.timing`
(`packages/mcp-server/src/tools/resolve-capability.ts:58`) и `ToolOutcome.timing`
(`packages/mcp-server/src/tools/registry.ts:88`), а клиенту публикуется
как `_meta.timing.overrunMs`. Обёртка читает то же поле и пишет `outcome = 'partial_deadline'`.

**Why ветвь возникает.** Все источники были опрошены, все ответили, последний вышел за потолок:
ничего не отменено и ничего не предотвращено, поэтому ответ возвращается, а не отбрасывается
(`reliability.md` §9.1, `open-questions.md` OQ-T012-6, решение владельца 2026-08-05).

**Why признак берётся из `timing`, а не измеряется в обёртке.** `effectiveDeadlineAtMs` вычисляется
внутри `resolve()` как `min(Date.now() + manifest.deadlineMs, requestedDeadlineAtMs ?? Infinity)`.
Обёртке эта величина не видна.

**Why отменённый дедлайном ответ сюда не попадает.** Истёкший дедлайн, не давший ответа, бросает
`CapabilityDeadlineExceededError`; это `outcome = 'refusal'` с заполненным `refusal_class`.
Частичный результат вместо ответа не возвращается (`reliability.md` §9.1, R-156).

**Why `served_from` при этом не меняется.** `served_from` называет источник ответа, `outcome` —
класс исхода. Превышение отмечается на той ветви обхода, которая вернула ответ, поэтому строка
`partial_deadline` может нести любое из четырёх значений `served_from`.

**Why класс не сворачивается ни в одного из соседей.** По `ADR-003` D4 такой ответ тарифицируется
полной ценой, а отказ клиенту возвращается. Свёртка в `refusal` вернула бы деньги за выданный ответ,
свёртка в `answer` сделала бы превышение потолка ненаблюдаемым в леджере.

### Координаты расхода вместо ссылки на строку

Живой вызов соотносится с вендорским расходом пятью колонками: `vendor_provider`, `vendor_credits`,
`vendor_calls`, `vendor_day`, `vendor_window_start`.

**Why координата, а не ссылка на строку.** `usage` ключуется `(provider, day)`, `usage_window` —
`(provider, window_start)`. Обе величины — счётчики по окнам, и построчной записи, на которую можно
указать, не существует.

### `capability` пуст в двух случаях, а не в одном

`request_trace.tool` объявлен `NOT NULL`, `request_trace.capability` допускает пустое значение.
Строка пишется на каждый допущенный вызов тула. Оба случая пустого значения названы в
`data-model.md` §4.5.7a:

| Случай | `outcome` | Пример |
| :--- | :--- | :--- |
| запрос упал до резолва | `refusal` | насыщенный лимитер отказывает до выбора маршрута |
| тул не резолвит способности вовсе | `answer` | `onchain_ping`, `onchain_list_chains` |

**Why второй случай существует.** След пишется в обёртке `defineTool` (`system-architecture.md`
§3.4.3) — выше слоя способностей. Два тула из двадцати отвечают, не вызывая `resolveCapability`, и
схема, требующая способность на каждой успешной строке, их бы не записала.

**Why это важно T-015.** Строка с `outcome = 'answer'` и пустым `capability` — это тарифицируемый
запрос, не потративший вендорского вызова. Чтение пустого значения как признака сбоя выбросило бы
её из счёта.

**Why это проверяемо на `onchain_ping`.** `packages/mcp-server/src/tools/ping.ts` не импортирует
`resolveCapability` и отвечает синхронно. Вызов `onchain_ping` по HTTP обязан оставить строку с
заполненным `tool` и пустым `capability`. Второй такой обработчик — `list-chains.ts`.

**Why запись в обёртке, а не в `resolve-capability.ts`.** Из 26 файлов в
`packages/mcp-server/src/tools/` `resolveCapability` вызывают 18. Обработчики `ping.ts` и
`list-chains.ts` в их число не входят, и запись из этого файла их пропустила бы.

### Протокольный отказ строки не оставляет

Запрос, отвергнутый на шаге 1 или 2, принципала не имеет, а `request_trace.principal_id` объявлен
`NOT NULL`. Такие отказы наблюдаемы в канале диагностики событиями `auth.rejected`,
`perimeter.rejected`, `session.limit_reached`, `session.evicted`.

### Недоставленный ответ записан как ответ

Платный вызов, дорезанный после разрыва соединения (задача 014-24), даёт строку с
`outcome = 'answer'` и `served_from = 'vendor'`. От строки доставленного ответа она не отличается.

**Why не отличается.** `outcome` называет класс исхода по `ADR-003` D4, `served_from` — источник
ответа. Доставку не выражает ни один из двух словарей, и оба закрыты ограничением `CHECK`.

**Why расход при этом не теряется.** Вендорские колонки несут вклад этого запроса, поэтому сверка
T-015 с `usage` (R-27.3) сходится и на недоставленном ответе.

**Why запись выполнима после разрыва.** Задача 014-24 отвязывает завершение платного вызова от жизни
соединения, а строку пишет обёртка `defineTool` по завершении, а не путь доставки.

### Что запись не делает

Списания с клиентского баланса нет: это T-015. Запись фиксирует факт обслуживания, а не цену.

Значение `escalated_to_paid` эта задача оставляет умолчанием `0`; единицу ставит задача 014-28.

### Кто был первым, значения не имеет

Владелец заявил это прямо. Запись не пытается определить, чей вызов наполнил кеш.

**Why это упрощает задачу.** Атрибуция первого потребовала бы хранить владельца записи кеша, то
есть принципала рядом с кешем, — что запрещено R-5.

<!-- contract:tests -->

## Тест-кейсы

### Сквозные тесты
1. **TC-E2E-01:** на каждый запрос есть строка с принципалом, тулом, временем, классом исхода и
   источником ответа
   - Входные данные: четыре вызова — попадание, ожидание чужого вызова, промах, отказ
   - Ожидаемый результат: четыре строки со значениями `served_from` `cache`, `coalesced`, `vendor`,
     `none` (AC-39)
2. **TC-E2E-02:** `onchain_ping` по HTTP оставляет строку
   - Входные данные: один вызов `onchain_ping` с валидным токеном
   - Ожидаемый результат: одна строка, `tool = 'onchain_ping'`, `capability` пусто,
     `outcome = 'answer'`, `served_from = 'none'`
3. **TC-E2E-03:** протокольный отказ строки следа не оставляет
   - Входные данные: запрос без заголовка `Authorization`
   - Ожидаемый результат: число строк в `request_trace` не изменилось; в диагностике есть событие
     `auth.rejected`

### Модульные тесты
1. **TC-UNIT-01:** попадание в кеш даёт `served_from = 'cache'`, заполненный `cache_age_ms` и пять
   пустых вендорских колонок
2. **TC-UNIT-02:** ведущий живой вызов даёт `served_from = 'vendor'` и координаты, совпадающие с
   корзинами `usage` и `usage_window`, в которые лёг расход
   - Входные данные: один промах на платном адаптере
   - Ожидаемый результат: `vendor_provider`, `vendor_day` и `vendor_window_start` равны ключам
     обновлённых корзин
3. **TC-UNIT-03:** два конкурентных запроса на один ключ дают строку `vendor` и строку `coalesced`
   - Входные данные: два вызова способности адаптера `nansen` с одним `deriveArgsHash`, второй
     начат до завершения первого
   - Ожидаемый результат: `vendor_provider`, `vendor_day` и `vendor_window_start` у обеих строк
     совпадают; `vendor_credits` и `vendor_calls` заполнены только у ведущего
4. **TC-UNIT-04:** отказ уровня исполнения тула даёт `served_from = 'none'`, `outcome = 'refusal'` и
   заполненный `refusal_class`
5. **TC-UNIT-05:** сумма `vendor_credits` за сутки совпадает со счётчиком `usage` за те же сутки
   - Входные данные: серия из попаданий, ведомых и ведущих вызовов
   - Ожидаемый результат: две величины равны
6. **TC-UNIT-06:** строка пишется один раз, при завершении
   - Входные данные: запрос, остановленный до точки записи, — процесс не дошёл до завершения вызова
   - Ожидаемый результат: строки следа нет; строки диагностики, записанные до остановки, несут
     `trace_id`, которому строка следа не отвечает
7. **TC-UNIT-07:** запись переживает перезапуск процесса
8. **TC-UNIT-08:** ответ, выданный за потолком, даёт `outcome = 'partial_deadline'`
   - Входные данные: обход, где все источники опрошены и все ответили, а последний вышел за
     `effectiveDeadlineAtMs` — форма `TC-OQ6-a` и `TC-OQ6-b`
     (`packages/core/test/registry.deadline.test.ts:1145`)
   - Ожидаемый результат: одна строка, `outcome = 'partial_deadline'`, `refusal_class` пуст;
     в ответе присутствует `_meta.timing.overrunMs`
9. **TC-UNIT-09:** истёкший дедлайн, не давший ответа, даёт `outcome = 'refusal'`
   - Входные данные: обход, где источник не ответил до `effectiveDeadlineAtMs`
   - Ожидаемый результат: `refusal_class` несёт имя `CapabilityDeadlineExceededError`
10. **TC-UNIT-10:** ведомый вызов `singleflight` вызывает `onCoalesced`, ведущий — нет
    - Входные данные: два конкурентных вызова с одним ключом и один одиночный
    - Ожидаемый результат: колбэк вызван один раз, у второго вызывающего
11. **TC-UNIT-11:** разрыв соединения после принятия вызова вендором оставляет строку
    - Входные данные: платный вызов, дорезанный по правилу задачи 014-24; ответ клиенту не доставлен
    - Ожидаемый результат: одна строка, `outcome = 'answer'`, `served_from = 'vendor'`,
      `vendor_credits` и `vendor_calls` заполнены и равны вкладу в `usage`

### Регрессионные тесты
- Медиана длительности `onchain_ping` измерена до и после введения записи; оба числа внесены в
  примечания задачи
- Снимок `tools/list` не изменился

<!-- contract:acceptance -->

## Критерии приёмки
- [ ] AC-39: четыре теста — попадание, ожидание чужого вызова, промах, отказ — зелёные
- [ ] Живой вызов несёт координаты корзин `usage` и `usage_window`; ссылки на строку в записи нет
- [ ] Строка `coalesced` несёт координаты ведущего и пустые `vendor_credits`, `vendor_calls`
- [ ] Признак ведомого доходит от `singleflight` до обёртки `defineTool` шестью шагами таблицы
- [ ] Ответ за потолком записан классом `partial_deadline`; отменённый дедлайном ответ — классом
      `refusal` с заполненным `refusal_class`
- [ ] `onchain_ping` по HTTP оставляет строку с заполненным `tool` и пустым `capability`
- [ ] Протокольный отказ следа не оставляет и наблюдаем в диагностике
- [ ] Запись переживает перезапуск

## Примечания

Зависит от 014-15: след пишется в той же обёртке `defineTool`
(`packages/mcp-server/src/tools/registry.ts:289`), где перехватывается принципал. Порядок здесь —
порядок кода, а не удобства.

Прежняя редакция задачи называла три пути и соотносила живой вызов со строкой `usage` через
`usage_ref`. Такой строки не существует: `usage` и `usage_window` — счётчики по окнам.

Расхождение по комментарию колонки `capability` закрыто в архитектуре: `data-model.md` §4.5.7a
называет два случая пустого значения — падение до резолва и успешный ответ тула, не резолвящего
способности. Комментарий колонки ссылается на этот раздел. Долга здесь нет.

Имя класса `partial_deadline` восходит к `ADR-002` D4 п.5 («ответил хотя бы один → частичный
результат»). Решение владельца 2026-08-03 этот возврат отменило (R-156, `reliability.md` §9.1):
истёкший дедлайн бросает исключение. Ветвь, которую класс покрывает сегодня, — полный ответ,
выданный за потолком (OQ-T012-6). Тарификация третьего класса не меняется: `ADR-003` D4 списывает
полную цену.
