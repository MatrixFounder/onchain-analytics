# Provider Matrix (raw: 20 записей рана 2026-06-30 + 3 в addendum 2026-07-20)

> Flat scan of every provider entry returned by the research agents. For the curated/ranked view see `research-digest.md`.
> **Addendum 2026-07-20:** +3 записи (privacy/Dash Platform) — см. секцию внизу; сырые данные: [raw/providers-addendum-2026-07-20.json](raw/providers-addendum-2026-07-20.json).

| Provider | Category | API/auth | MCP | Pricing | Confidence |
|---|---|---|---|---|---|
| Nansen | smart-money intelligence / on-chain analytics (entity-labeled wallet + token + smart-money data) | REST (HTTPS, all endpoints POST with JSON bodies) / API key (header `apikey: YOUR_API_KEY`); key generated in-app at app.nansen.ai/auth/agent-setup. No OAuth. MCP server uses a separate header `NANSEN-API-KEY`. | official | Subscription simplified on 2025-09-25 to two plans: Free ($0/mo; 100 trial API c | high |
| Surf (AskSurf.ai) — Surf API / Surf Skill (developer data API for AI agents) | Aggregated crypto data API + analyst-LLM layer for AI agents (smart-money labels, on-chain SQL, market/social/prediction-market data). NOT a single raw-data source — it is a unifier that 'replaces 60+ APIs'. | REST (OpenAPI 3 spec published); includes a raw on-chain SQL endpoint over ClickHouse / API key as Bearer token (Authorization: Bearer $SURF_API_KEY). 30 free credits/day anonymously with NO key; full access requires signup at agents.asksurf.ai and pay-as-you-go credits. Separate billing from the consumer Chat/Studio subscription. | official | Developer/API side: 30 free credits/day with no key; then pay-as-you-go credits  | high |
| Dune Analytics — Query / Execution API | querying-platform (custom SQL over decoded on-chain data + community dashboards) | REST. api.dune.com/api/v1: POST /query/{query_id}/execute -> execution_id; GET /execution/{execution_id}/status; GET /execution/{execution_id}/results (JSON) & /results/csv; GET /query/{query_id}/results (latest cached); POST /execution/{execution_id}/cancel. Supports filters on cached results + dynamic query params. Plus Trino connector (SQL warehouse) and dbt connector. / API key (X-Dune-API-Key header). Included on all plans, including Free (subject to credit spend limit). | official | Credit-based, 4 tiers: Free ($0, 2,500 credits/mo), Analyst, Plus, Enterprise (c | high |
| Dune Sim APIs (sim.dune.com) | raw EVM/SVM data — realtime multichain wallet/transaction data (DeFi/market data adjacent) | REST. EVM: /v1/evm/balances/{address}, /evm/activity, /evm/transactions, /evm/token-info, /evm/token-holders, /evm/collectibles, /evm/subscriptions (webhooks). SVM (beta): /beta/svm/balances, /beta/svm/transactions (param chains=solana|eclipse|all). JSON responses, HTTPS only. / API key via X-Sim-Api-Key header; HTTPS-only (plain HTTP fails). Keys generated in Sim dashboard at sim.dune.com. Single key works across all supported chains. | unknown | Usage-based (Compute Units / CUs), scales free -> enterprise. Free/developer tie | medium |
| Arkham Intelligence | smart-money intelligence / entity & wallet de-anonymization (address attribution + fund-flow tracking) | REST + WebSocket (real-time transfer streaming) / API key (sent in 'API-Key' header). Access granted via an application/request form at intel.arkm.com/api; no self-serve signup visible. | community | Not public. Operates on a freemium model for the web platform (free tier + paid  | high |
| Glassnode | macro on-chain & market-data analytics (statistical on-chain indicators / Studio API) | REST (JSON). Base URL https://api.glassnode.com/v1/ . Also CLI tool and Excel add-in. / API key, passed either as 'X-Api-Key' header or 'api_key' query parameter. Key generated at studio.glassnode.com/settings/api. Requests consume data credits tied to subscription tier. | official | Per live studio.glassnode.com/pricing (2026): ADVANCED $49/mo billed annually —  | high |
| Allium | Enterprise on-chain data warehouse / indexed blockchain data platform (raw + decoded + analytics) | mixed (REST Realtime APIs + SQL via Explorer/data-warehouse + streaming: Kafka, GCP Pub/Sub, AWS SNS, WebSocket) / API key (X-API-KEY header; keys generated at app.allium.so/settings/api-keys). Also supports machine/agent payments (x402 EIP-712 USDC-on-Base, Tempo MPP) to pay-per-call without signup. | official | Not public for enterprise — quote-based, contact sales. Web sources indicate ent | high |
| Bitquery | Indexed multi-chain blockchain data API (GraphQL) + real-time streaming; strong DEX/trading data | mixed — primary unified GraphQL (V1 40+ chains, V2); plus real-time streaming via WebSocket (GraphQL subscriptions), Kafka, and Solana/EVM gRPC (CoreCast); SQL/ClickHouse access and bulk cloud datashares (Snowflake/BigQuery/S3/Azure) / API key (OAuth-style access token / Bearer) for GraphQL & streams; MCP uses OAuth 2.1 browser sign-in or query-token. Free tier requires signup; Commercial via sales. | official | Points-based, usage-driven (revised model rolled out June 2024). Official pricin | high |
| The Graph | Decentralized indexing protocol — custom subgraphs (GraphQL) + Token API (REST) + Substreams streaming; querying platform | mixed — GraphQL (custom Subgraphs via Gateway) + REST (Token API, OpenAPI spec, powered by Pinax) + Substreams (gRPC firehose streaming into custom sinks); newer 'Amp' database / Gateway API key from Subgraph Studio for subgraph GraphQL queries; JWT/Bearer access token (thegraph.market) for Token API REST. Billing on Arbitrum (GRT) or credit card. | official | Subgraph queries: Free Plan = 100,000 queries/month free; beyond that $2 per 100 | high |
| Covalent / GoldRush (goldrush.dev) | unified multi-chain raw + DeFi/market on-chain data API (balances, transactions, tokens, NFTs, DEX, pricing) with AI-agent tooling | REST (also JSON-RPC passthrough + WebSocket Streaming API in beta; first-party TS/JS, Python, Rust SDKs) / API key — Bearer token (Authorization: Bearer KEY); also Basic-auth username or ?key= query param. Plus keyless x402 pay-per-request (agent pays per call, no account/key). | official | Free Trial: $0, 25,000 credits, 4 RPS, 14 days. Vibe Coding: $10/mo, 10,000 cred | high |
| Moralis (moralis.io / moralis.com) | unified multi-chain Web3 Data API (balances, transactions, tokens, NFTs, DeFi, prices) + AI-native data layer (Cortex/MCP) | REST (Web3 Data API, EVM + Solana; also Streams webhooks and RPC nodes). Official JS/Python SDKs. / API key — X-API-Key request header (base URL https://deep-index.moralis.io/api/v2.2). MCP server reads MORALIS_API_KEY env var. | official | Free/Starter: $0, 40,000 Compute Units (CU) per day, 40 RPS, 2 RPC nodes, attrib | high |
| DeFiLlama | DeFi/market data (TVL, yields, stablecoins, fees/revenue) — aggregated protocol intelligence | REST / none for free API; API key for Pro (key embedded in URL path) | community | Free public API (api.llama.fi): no key, 31 endpoint groups, generous but unspeci | high |
| DexScreener | Raw DEX data (pairs, prices, liquidity, new/boosted tokens) — multi-chain DEX terminal | REST / none (keyless, no signup required) | community | Free, no API key. Rate limits stated in docs: 60 requests/minute for token-profi | high |
| GeckoTerminal | Raw on-chain DEX data (pools, OHLCV, trending/new pools) — DEX-native market terminal | REST / none for the free public API (no key); same data also served via CoinGecko API /onchain endpoints with a CoinGecko key | official | Free public API, no API key, currently in Beta. Public rate limit ~30 calls/minu | high |
| CoinGecko | DeFi/market data (aggregated CEX+DEX prices, market cap, OHLC) + on-chain via GeckoTerminal /onchain | REST / Demo API key for free tier; Pro API key for paid tiers (MCP public endpoint needs no key) | official | Demo (free): requires free Demo key, ~30 calls/min stable, 10,000 calls/month (s | high |
| Birdeye (Birdeye Data Services, birdeye.so) | Raw on-chain + DEX/market data (Solana-first, multi-chain) — token analytics & trading data | REST + WebSocket / API key (x-api-key header); free Standard key available | official | CU (compute-unit) based. Free Standard: 30,000 CU/month, 1 request/sec, limited  | high |
| Cielo Finance | smart-money / wallet-intelligence | REST + WebSocket / API key (obtained via build.cielo.finance portal) | none | Credit-based. Free: 5,000 credits/mo, 10 credits/s, access to 1 endpoint only. B | high |
| DeBank (DeBank Cloud API) | DeFi / portfolio data | REST (OpenAPI / Pro API) / API key — register at cloud.debank.com, pass header `AccessKey: <key>` | community | Unit-based billing — buy 'units' via the cloud.debank.com dashboard; each endpoi | high |
| Helius | raw Solana data / RPC + enhanced APIs + webhooks | REST + JSON-RPC + WebSocket (LaserStream WSS) + gRPC + Webhooks / API key (per-key, set via dashboard; HELIUS_API_KEY for tooling/MCP) | official | Free: $0/mo, 1M credits, 10 req/s (1 sendTx/s), webhooks + LaserStream WSS stand | high |
| Hyperliquid | DeFi / perps market & positions data | REST (info endpoint, POST/JSON) + WebSocket (also EVM RPC for HyperEVM) / none for public info/market data; signed wallet actions only required for the trading/exchange endpoint (placing orders) | community | Free / not metered by API key. Public info & market data has no paid tier and no | high |

## Addendum 2026-07-20 — privacy / Dash Platform (post-run)

> Добавлено ПОСЛЕ основного рана 2026-06-30 по результатам верификации coin-analytics-диалога
> (run `wf_f294ed8b-f82`, вердикты: [../../reference/coin-analytics-dialog-verification.md](../../reference/coin-analytics-dialog-verification.md)).
> Закрывает пробел: privacy-метрики (shielded pool, shield/unshield) не покрыты ни одним из 20 провайдеров выше.
> Источник истины строк: [raw/providers-addendum-2026-07-20.json](raw/providers-addendum-2026-07-20.json). Числа as-of 2026-07-19.

| Provider | Category | API/auth | MCP | Pricing | Confidence |
|---|---|---|---|---|---|
| Dash Platform (DAPI) | L2 platform-native data API — identities, contracts, documents, credits, shielded pool (Orchard) | gRPC (platform.proto) + rs-sdk/js-evo-sdk / none (публичные evonode-эндпоинты) | unknown | Free; но 6 shielded-эндпоинтов (v4.0.0) «not yet available on public nodes» (as-of 2026-07-19); истории НЕТ — только current state | high |
| Dash Platform Explorer (pshenmic) | community explorer + keyless REST API над Platform L2 — единственный готовый источник ИСТОРИИ Platform-метрик | REST platform-explorer.pshenmic.dev / none | unknown | Free, open-source (v2.4.0 от 2026-07-13); community-сервис без SLA | high |
| ZecHub (open data) | исторические on-chain ряды Zcash (shielded supply 2017–2026) — калибровка privacy-adoption порогов | статический JSON на raw.githubusercontent.com / none | none | Free, open-source; каденс обновлений не гарантирован | high |
