{
 "question_coverage": [
  "Q1: partial — есть решающий негатив (в Discovery официального MCP НЕТ тула поиска по чужим запросам: searchDocs/searchTables/listBlockchains/searchTablesByContractAddress/getTableSize — orchestrator-crosscheck.md §1; в OpenAPI 39 путей и единственный search — POST /v1/datasets/search, т.е. поиск ТАБЛИЦ, а GET /v1/queries — список СВОИХ). НЕ ХВАТАЕТ: (а) таблицы «поверхность → официальная/нет → free/нет» — она нигде не собрана; (б) профиля-и-реджекта неофициальных поверхностей: внутренний search дынного веб-приложения (core-api.dune.com — в scratchpad ни одного обращения, не зондировался) и Google `site:dune.com/queries`; (в) GET /v1/dashboards/by-slug/{owner_handle}/{slug} — единственный документированный мост «человеческий URL дашборда → query_id», его нет ни в строках, ни в below-line; (г) фиксации того, что UI-фильтры `code:\"from dex.trades\"`, `author:`, `tags:` (docs/web-app/search) существуют ТОЛЬКО в UI и потому непригодны headless-агенту по фильтру §5 брифа",
  "Q2: partial — доковая часть закрыта сильно: GET /v1/query/{id}/results работает для ЛЮБОГО публичного запроса («must either be public or a query you have ownership of», api-reference/executions/endpoint/get-query-result:17), НЕ триггерит исполнение и списывает по размеру результата (Free = 20 кредитов/МБ, api-reference/overview/billing «Data Export»); у POST /v1/query/{id}/execute задокументирован 403 «Not allowed to execute query… not enough permissions». НЕ ХВАТАЕТ: живого ответа на ЦЕНТРАЛЬНЫЙ вопрос — исполняется ли ЧУЖОЙ публичный запрос на Free (нет ключа → ни одного вызова); нет минимального заряда за чтение результата и класса rate-limit для /query/{id}/results (в таблицах low/high этот эндпоинт вообще не перечислен, api-reference/overview/rate-limits)",
  "Q3: partial — «нет на Free» доказано документом («To access Query endpoints, an Analyst plan or higher is required» — queries/endpoint/create:15 и queries/endpoint/read:17), но ответ разваливается на трёх местах: (а) первоисточник САМ СЕБЕ противоречит — overview/billing:65-67 «Queries Endpoint — Available on Plus and Enterprise plans» (Analyst vs Plus), противоречие нигде не зафиксировано; (б) обходы не сведены в один ответ: POST /v1/sql/execute (freeform SQL без сохранённого запроса, scope Read, планового гейта в доке НЕТ) и `select * from query_<id>` (Query Views с пробросом параметров) — второе ушло НИЖЕ ЛИНИИ и не профилировалось; (в) флаг isFreeformAllowed в query-object намекает на аккаунт-гейт freeform SQL и не проверен",
  "Q4: partial — жёсткие числа собраны из первоисточника (Free 2 500 кредитов/мес и $5.00/100 extra — resources/credits-billing/how-credits-work; экспорт Free 20 кредитов/МБ, запись 3 кредита/ГБ, storage 100 МБ, webhooks 1 — overview/billing; 15 rpm low + 40 rpm high, комбинированно 55 — overview/rate-limits). НЕ ХВАТАЕТ главного множителя: Dune ДОСЛОВНО отказывается публиковать формулу («we don't share a single, per-query formula because credit usage depends on real-time factors») ⇒ член «кредитов за исполнение» принципиально недоступен из доки, единственный измеритель — execution_cost_credits на /v1/execution/{id}/status и дельта POST /v1/usage, а ключа нет. Также не сведены: поведение при исчерпании (402 + тумблер extra-credit limit — ниже линии), max_credits_per_request / ignore_max_credits_per_request как рычаг, и альтернативная кривая цены MPP (живой 402: amount 10710 при decimals 6 ≈ $0.0107/запрос)",
  "Q5: partial — материал есть (Spellbook-дерево выкачано: 14 805 путей, 34 bitcoin-модели; поля is_private/params/owner из query-object; независимая сверка отработана на примере AWS S3 ↔ mempool.space), но чек-лист как последовательность проверяемых шагов не собран, и главное — ПЕРВЫЙ шаг «прочитать SQL чужого запроса» на Free невыполним по API (GET /v1/query/{id} гейтится Analyst/Plus, см. Q3-противоречие), а через UI = скрапинг, который бриф §5 запрещает. Не зафиксировано: какое поле даёт свежесть последнего исполнения, как отличать Spellbook-таблицу от сырой/чужой upload-таблицы в чужом SQL, как читаются дефолты параметров без чтения SQL",
  "Q6: partial — независимая сверка закрыта хорошо и живо (verification_log.txt: keyless DuckDB по AWS Public Blockchain Data, btc/blocks 2026-07-26 = 146 блоков/758 094 tx за 3.3 с; h=959763 tx_count 4590 совпал с mempool.space точно; Blockchair circulation 20 062 011.67 BTC против расчётной эмиссии, Δ 32.08 BTC = 0.00016%). НЕ ЗАКРЫТА именно DUNE-сторона мотивирующего примера: какие bitcoin-таблицы доступны (сырые bitcoin.* против spellbook metrics_bitcoin_transactions_daily / prices_bitcoin_tokens), точный SQL суммы coinbase-выходов, его стоимость в кредитах и почему это дороже бесплатной альтернативы. Второй сложный in-chain пример (дневной объём DEX по сетям ИЛИ распределение холдеров) не доведён ни на одном источнике",
  "Q7: partial — фактбаза по репозиторию есть (repo-audit-raw.json: constraints с якорями ARCHITECTURE.md:67, ARCHITECTURE.md:69-74 про n8n-контур и асинхронность execute→poll→results; проверено, что .mcp.json с plaintext JWT покрыт .gitignore:53). НЕ ХВАТАЕТ самого дизайна с file:line: имена capability и тулов, форма адаптера, TTL по классам ответов, врезка кредитов в существующий checkAndReserve/usage-леджер, запись api.dune.com в SSRF-allowlist, и контур ограничения модель-сгенерированного SQL (а он становится обязательным, если ответом станет /v1/sql/execute). Отдельно не учтён новый класс риска: MPP тащит в контур агента кошелёк и on-chain escrow — существующий budget-guard такого не моделирует",
  "Q8: partial (самое слабое место по покрытию) — семейство alt-free зажато квотой 6 строк (workflow dune-query-discovery.js:424), и как минимум 2 из них съел near-дубликат Blockscout, ещё одну — Blockchair со статусом REFUTED. Из сидов брифа §4 (Flipside SQL API, BigQuery public crypto datasets, Coin Metrics community, mempool.space, blockchain.com charts, growthepie, Artemis, Token Terminal, Bitquery free, The Graph) нет свидетельств профилирования ни одного. Правило маршрутизации «когда не жечь кредиты» невозможно построить: нет матрицы «класс агрегирующего вопроса → бесплатный источник → чем проверяется»",
  "Q9: covered по главному, но с висящим противоречием — sunset Sim подтверждён первоисточником дословно в этом прогоне (PRIMARY-sunsetting-sim.md: «sunsetting sim.dune.com on August 1, 2026», новые регистрации отключены с 2026-05-18, миграционные партнёры Zerion/Codex/Mobula), при этом ОДНА ИЗ СТРОК таблицы утверждает, что https://dune.com/blog/sunsetting-sim по-прежнему отдаёт краулеру 404 и держит статус UNVERIFIED. Не хватает: статуса официального Dune MCP (beta/GA) из первоисточника, датированного перехода кредитной модели (dune.com/blog/credits-changing) и построчного «что ломается у нас» по каждому сценарию — до отключения Sim осталось 4 дня от args.date",
  "Q10: missing — сравнения «курируемый реестр проверенных query_id против живого поиска» по цене/задержке/хрупкости/проверяемости нет ни в одном артефакте прогона, рекомендации нет. Хуже: сам примитив, на котором держится реестр (GET /query/{id}/results по чужому публичному id + дешёвый зонд-«схема результата») лежит НИЖЕ ЛИНИИ и не профилирован, а живой поиск по Q1 официальной поверхности не имеет — то есть развилка сейчас не между двумя рабочими вариантами, а между одним рабочим и одним несуществующим, и это должно быть сказано прямо",
  "Q11: partial — инвентарь снят с первоисточника поимённо и пересчитан оркестратором: 26 тулов = Discovery 5 + Query Lifecycle 5 + Materialized Views 6 + Visualization 5 + Dashboard 4 + Account 1 (docs.dune.com/api-reference/agents/mcp), обе авторизации подтверждены (заголовок x-dune-api-key / ?api_key, плюс OAuth 2.0 + PKCE S256, scope mcp:dune:full, Dynamic Client Registration — живой oauth-meta.json и prm.json), зафиксирован ключевой негатив: в Discovery нет поиска по чужим запросам. НЕ ХВАТАЕТ ровно того, что требовал вопрос: вход (input schema) и плановый гейт и кредиты ПО КАЖДОМУ тулу — дока даёт только имя+описание, а живой tools/list к api.dune.com/mcp/v1 без ключа не сделан (в tools.json лежит инвентарь Blockscout, не Dune); нет разницы «MCP против REST» (в MCP нет /v1/sql/execute, CSV-выгрузки, cancel-execution, uploads, pipelines, dashboards/by-slug — сравнение не сведено); нет разрешённой развилки «проксировать / свой адаптер / гибрид» с ценой каждого варианта и нет карты максимально продвинутого коннектора"
 ],
 "followups": [
  "Получить бесплатный DUNE_API_KEY и прогнать одну зонд-матрицу из 6 вызовов, записывая HTTP-код, тело ошибки, execution_cost_credits и дельту POST /v1/usage: (a) POST /v1/sql/execute {\"sql\":\"select 1\"} — разрешён ли freeform SQL на Free (это решает GO против NARROW-AND-GO); (b) GET /v1/query/21693 — читается ли SQL ЧУЖОГО публичного запроса и каким планом гейтится на самом деле; (c) POST /v1/query/21693/execute — исполняется ли чужой публичный запрос (документирован 403 «not enough permissions»); (d) GET /v1/query/21693/results — списание по 20 кредитов/МБ и есть ли минимальный заряд; (e) POST /v1/query — подтвердить отказ authoring на Free; (f) GET /v1/dashboards/by-slug/{owner}/{slug}. Этим же прогоном закрыть противоречие первоисточника: docs.dune.com/api-reference/queries/endpoint/create:15 и .../read:17 говорят «Analyst plan or higher», а .../overview/billing:65-67 — «Queries Endpoint: Available on Plus and Enterprise plans».",
  "Профилировать Dune MPP (Machine Payment Protocol) как отдельную строку-кандидата: docs.dune.com/docs/agents/mpp даёт keyless-доступ «no API keys, no user account, no credit card» ровно к POST /v1/sql/execute, GET /v1/execution/:id/results и /csv через HTTP-402-челлендж; в прогоне уже пойман живой челлендж (mpp-challenge.json: amount 10710, decimals 6, currency 0x20c0000000000000000000b9537d11c60e8b50, chainId 4217, escrow 0x33b901018174DDabE4841042ab76ba85D4e24f25). Ответить: сколько стоит один SQL-вызов в долларах, обходит ли MPP плановый гейт Analyst/Plus, какие эндпоинты НЕ покрыты, и как кошелёк + on-chain escrow ложатся (или не ложатся) на существующий budget-guard onchain-intel. Источники: docs.dune.com/docs/agents/mpp, tempo.xyz/SKILL.md, paymentauth.org/draft-httpauth-payment-00.txt.",
  "Закрыть Q1 дисквалификатором, а не умолчанием: доказать, что официальной программной поверхности поиска по ЧУЖИМ запросам/дашбордам не существует (доказательная база уже в прогоне: dune_openapi.json — 39 путей, единственный search это POST /v1/datasets/search по таблицам, а GET /v1/queries — только свои; инвентарь MCP Discovery из docs.dune.com/api-reference/agents/mcp), и отдельно профилировать-и-отклонить неофициальные поверхности с доказательством: внутренний поисковый API веб-приложения dune.com (домен core-api.dune.com — в прогоне ни разу не зондировался) и Google `site:dune.com/queries` — с цитатой из ToS Dune (в scratchpad лежат tos.html/apitos.html, но вывод из них не сделан). В той же добивке проверить GET /v1/dashboards/by-slug/{owner_handle}/{slug}: превращает ли он найденный человеком URL дашборда в список query_id, и на каком плане."
 ],
 "missing_candidates": [
  "POST /v1/sql/execute — исполнение произвольного SQL БЕЗ сохранённого запроса, минимальный scope ключа Read, планового гейта в доке и в OpenAPI нет («usage-based, consumes credits»); полностью обходит Analyst-гейт authoring и делает необязательной всю гипотезу «переиспользуем чужие запросы». В списке below-line отсутствует — либо это строка таблицы, либо системный пропуск",
  "Dune MPP (Machine Payment Protocol, HTTP 402 + Tempo payment channel) — «no API keys, no user account, no credit card», покрывает POST /v1/sql/execute и GET /v1/execution/:id/results(+/csv); живой челлендж пойман (amount 10710, decimals 6, chainId 4217, escrow 0x33b901018174DDabE4841042ab76ba85D4e24f25). Меняет одновременно плановый гейт (Q2/Q3), экономику (Q4) и модель риска (Q7)",
  "GET /v1/dashboards/by-slug/{owner_handle}/{slug} — единственный документированный программный мост «URL дашборда с dune.com → его состав и query_id»; без него связка «человек нашёл дашборд → агент исполняет» не имеет официальной реализации (Q1/Q10)",
  "Внутренний поисковый API веб-приложения dune.com (core-api.dune.com) и Google `site:dune.com/queries` как поисковая поверхность по 700K публичных запросов — ни одного обращения в прогоне; даже если вердикт reject, §4 требует reject-лог с доказательством, а сейчас поверхность просто отсутствует",
  "Dune CLI + официальные Dune Agent Skills (docs.dune.com/docs/agents/cli-and-skills: `curl -sSfL https://dune.com/cli/install.sh | sh`, `npx skills add`, JSON-вывод всех команд) — первопартийная агентская поверхность, параллельная MCP; напрямую влияет на развилку Q11 «проксировать MCP / свой адаптер / гибрид»",
  "DuneSQL Query Views `select * from query_<id>` с пробросом параметров (`query_3256410(blockchain='ethereum')`) — позволяет ПЕРЕИСПОЛЬЗОВАТЬ чужой запрос, не читая и не форкая его (то есть в обход Analyst-гейта на read/create); сейчас ниже линии и не профилирован, хотя это ядро «продвинутого коннектора»",
  "Custom Endpoints `GET /v1/endpoints/{handle}/{endpoint_slug}/results` и Pipelines (`POST /v1/pipelines/execute`, `GET /v1/pipelines/executions/{id}/status`) — присутствуют в официальном OpenAPI (dune_openapi.json), но не встречаются ни в строках, ни в below-line; это готовые механизмы доставки и цепочки исполнений, релевантные Q7/Q10"
 ],
 "unverified_critical": [
  "Разрешён ли POST /v1/sql/execute на Free-плане — в доке и в OpenAPI планового гейта нет, но в query-object есть флаг isFreeformAllowed, намекающий на аккаунт-гейт. От этой одной ячейки зависит вердикт прогона (GO против NARROW-AND-GO): при «да» агент пишет свой SQL и чужие запросы не нужны, при «нет» free-путь схлопывается до чтения кешированных результатов публичных запросов",
  "Плановый гейт Query-эндпоинтов противоречив в самом первоисточнике: «To access Query endpoints, an Analyst plan or higher is required» (api-reference/queries/endpoint/create:15 и .../read:17) против «Queries Endpoint — Available on Plus and Enterprise plans» (api-reference/overview/billing:65-67). На этом висят Q3 (форк) и Q5 (чтение чужого SQL как первый шаг проверки корректности)",
  "Исполняется ли ЧУЖОЙ публичный запрос через POST /v1/query/{id}/execute на Free — прямого утверждения в доке нет, есть только 403 «Not allowed to execute query. Query is archived, unsaved or not enough permissions». Это буквально гипотеза владельца; живой проверки не было",
  "Стоимость одного исполнения в кредитах — вендор дословно отказывается публиковать формулу («At the moment, we don't share a single, per-query formula…», resources/credits-billing/how-credits-work). Любая цифра «агентских вопросов в месяц» из 2 500 кредитов — модельная оценка, а не верифицированное число; единственные измерители (execution_cost_credits на /v1/execution/{id}/status, POST /v1/usage) требуют ключа",
  "Тарификация чтения результата: применяется ли Free-ставка 20 кредитов/МБ (overview/billing «Data Export») именно к GET /v1/query/{id}/results, есть ли минимальный заряд за запрос и к какому классу rate-limit он относится — в таблицах low/high (overview/rate-limits) этот эндпоинт не перечислен вовсе",
  "Реальная цена MPP в долларах и доступность MPP-эндпоинтов без аккаунта: живой 402 дал amount 10710 при decimals 6, но валютный токен 0x20c0000000000000000000b9537d11c60e8b50 не идентифицирован, поэтому «≈$0.0107 за запрос» — предположение о стейблкоине, а не факт",
  "Поштучные плановые гейты и стоимость в кредитах для 26 тулов официального MCP — дока даёт только имя и описание, страница MCP говорит лишь про «tracking credit usage»; живой tools/list к api.dune.com/mcp/v1 не выполнялся (файл tools.json содержит инвентарь Blockscout, не Dune)",
  "Все плановые числа взяты с docs.dune.com, потому что сама dune.com/pricing отдаёт краулеру только навигацию и loading-state (JS-рендер, зафиксировано в orchestrator-crosscheck.md §3) — то есть канонический прайс как первоисточник недоступен, и расхождение docs против pricing не может быть обнаружено этим прогоном"
 ],
 "unresolved": [
  "Два near-дубликата одного продукта: «Blockscout MCP Server (hosted mcp.blockscout.com/mcp)» и «Blockscout MCP Server (хостируемый: https://mcp.blockscout.com/mcp + REST /v1/*)» — одинаковый fit=11, обе CONFIRMED, но ВЕРДИКТЫ РАЗНЫЕ: adopt против reference. Ключ дедупа lower(vendor|name) кластер пропустил (это залогировано самим прогоном), мастер-таблица удвоит строку, а рекомендация станет самопротиворечивой",
  "Blockchair: статус REFUTED (главный тезис «рабочий keyless-источник» убит — живьём ровно 2 keyless-запроса, дальше устойчивый HTTP 430; вендор дословно «we had to enforce blocking all API requests that didn't use an API key»), вердикт понижен до reference — но fit остался 11, вровень с CONFIRMED-adopt строкой. Убийство не отразилось ни на free_fit, ни на сумме: скор и доказательства расходятся",
  "Sim sunset: оркестратор доставил первоисточник целиком (PRIMARY-sunsetting-sim.md — «Dune is sunsetting sim.dune.com on August 1, 2026», signups отключены с 2026-05-18), а строка таблицы в это же время утверждает, что https://dune.com/blog/sunsetting-sim отдаёт краулеру 404, и держит claim как UNVERIFIED. Один и тот же URL в одном прогоне имеет два взаимоисключающих статуса доставки",
  "Арифметика воронки не сходится и не соответствует каноничной строке брифа: 272 − 134 = 138 после доменного гейта, но curated = 35; разница в 103 кандидата отрезана КВОТАМИ семейств, а не гейтом, и в строке воронки этот срез и поле lost не показаны. Плюс бриф §8 объявляет порог «top-K, K = maxCandidates = 16», тогда как профилировано 35 (workflow:424 раздаёт по-семейственные квоты 16/4/3/3/3/6) — либо строка воронки, либо бриф устарел",
  "Состав того, что дошло до критика, противоречит предмету исследования: все показанные строки — alt-free эксплореры (Blockchair, Blockscout ×2), тогда как решающие Dune-пути (чтение кешированного результата чужого запроса, Query Views `select * from query_N`, result-shaping как рычаг цены, /results/csv, execution status, extra-credits overage) оказались ниже линии. Ранжирование определено квотой семейства, а не близостью к решению"
 ],
 "biases": [
  "Нет DUNE_API_KEY — ни один Dune-эндпоинт не проверен живым вызовом; все числа по Dune доковые. Единственные живые Dune-артефакты прогона — неаутентифицированный 402-челлендж MPP и OAuth-метаданные (oauth-meta.json, prm.json). Следствие: каждая ячейка про плановый гейт и кредиты — доверие к прозе доки, а дока уже поймана на самопротиворечии (Analyst против Plus)",
  "Критик судит о полноте по ~3 строкам из 35: массив строк обрезан до 26 000 символов (workflow dune-query-discovery.js:711), список below-line — до 2 500 символов (там же:709, обрыв на «Google Sheets IMPORTDATA-мост — =i»). То есть охват оценивается менее чем по 10% таблицы, а перечень потерь виден частично",
  "Форма ответа задана квотами семейств ДО доказательств: dune-access 16, dune-discovery 4, dune-authoring 3, dune-catalog 3, dune-sdk 3, alt-free 6 (workflow:424). Поверхность не может выиграть слот, которого ей не выделили; при этом два near-дубликата Blockscout съели треть слотов alt-free, из-за чего ярус бесплатных альтернатив (Flipside, BigQuery public datasets, Coin Metrics, mempool.space, blockchain.com, growthepie, Bitquery, The Graph) остался непокрытым — а это ровно то, на чём строится ответ Q8",
  "Перекос в пользу того, что отвечает без ключа: Blockscout, Blockchair и AWS Public Blockchain Data набрали густую живую доказательную базу (probes, DuckDB-замеры, сверка с mempool.space), а сам предмет исследования — Dune — доступен только для чтения доки. Под-скор verifiability и, через него, fit систематически завышены у не-Dune кандидатов",
  "Источниковый перекос: docs.dune.com (Mintlify) краулится и потому вычитан почти целиком (десятки страниц в scratchpad), а dune.com/pricing отдаёт только JS-каркас. Реальность тарифов недосэмплирована относительно прозы API-референса, и расхождение между ними в принципе не может быть замечено этим прогоном",
  "Экономика структурно неверифицируема не по вине прогона: Dune дословно отказывается публиковать формулу кредитов за запрос. Любое «N агентских вопросов в месяц» — модель-сгенерированная оценка; её обязательно помечать как непроверенную, иначе отчёт выдаёт производную от неизвестного множителя за факт",
  "Перекос по свежести: as-of 2026-07-27, до отключения Sim остаётся 4 дня (2026-08-01), а кредитная модель менялась недавно (dune.com/blog/credits-changing) — любой вторичный источник старше нескольких месяцев систематически неверен, и наоборот, часть проверенных сегодня фактов протухнет в течение недели"
 ]
}